---
phase: 14-gogcli-google-suite-cli-install-gogcli-wire-gog-gmail-and-go
plan: 02
type: execute
wave: 2
depends_on: ["14-01"]
files_modified:
  - scripts/email-triage.sh
  - scripts/openclaw-env.sh
autonomous: true
requirements: [CHAN-03]
must_haves:
  truths:
    - "scripts/email-triage.sh exits 0 and outputs {\"ok\":true,...} JSON to stdout"
    - "auth failure produces {\"ok\":false,\"error\":\"gog-auth-failed\",...} not a hang"
    - "OPENCLAW_GMAIL_ACCOUNT is exported in openclaw-env.sh"
  artifacts:
    - path: "scripts/email-triage.sh"
      provides: "Shell replacement for gmail-triage.js"
      contains: "json_ok"
    - path: "scripts/openclaw-env.sh"
      provides: "OPENCLAW_GMAIL_ACCOUNT env var"
  key_links:
    - from: "scripts/email-triage.sh"
      to: "/opt/homebrew/bin/gog"
      via: "GOG=/opt/homebrew/bin/gog explicit path (D-143)"
      pattern: "GOG=/opt/homebrew/bin/gog"
    - from: "scripts/email-triage.sh"
      to: "scripts/lib/json-response.sh"
      via: "source statement"
      pattern: "source.*json-response\\.sh"
---

<objective>
Create scripts/email-triage.sh — a zsh shell script that replaces gmail-triage.js for the Email Triage agent — using gog gmail search, gog gmail get, and gog gmail mark-read. Add OPENCLAW_GMAIL_ACCOUNT to openclaw-env.sh.

Purpose: Eliminates the Node.js googleapis layer; email triage becomes a single-binary shell script with built-in token refresh.
Output: scripts/email-triage.sh (executable, JSON stdout, logs stderr); OPENCLAW_GMAIL_ACCOUNT in openclaw-env.sh; gmail-triage.js untouched (per D-148).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/trilogy/Documents/agentic-setup/.planning/phases/14-gogcli-google-suite-cli-install-gogcli-wire-gog-gmail-and-go/14-CONTEXT.md
@/Users/trilogy/Documents/agentic-setup/.planning/phases/14-gogcli-google-suite-cli-install-gogcli-wire-gog-gmail-and-go/14-RESEARCH.md
@/Users/trilogy/Documents/agentic-setup/scripts/lib/json-response.sh

<interfaces>
From scripts/lib/json-response.sh (existing shared lib):
  json_ok  '{"key":"value"}'   — emits {"ok":true,"data":{...}} to stdout, exits 0
  json_fail "error-code" "human message" — emits {"ok":false,"error":"..."} to stdout, exits 1

From .openclaw/agents/email-triage/TOOLS.md:
  Current invocation: /opt/homebrew/opt/node@24/bin/node .../gmail-triage.js
  Pattern to replace: exec tool calls gmail-triage.js
  Keep: gmail-triage.js untouched until email-triage.sh is verified (D-148)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add OPENCLAW_GMAIL_ACCOUNT to openclaw-env.sh</name>
  <files>/Users/trilogy/Documents/agentic-setup/scripts/openclaw-env.sh</files>
  <action>
Read scripts/openclaw-env.sh. Add the following line in the exports section (this is a plain config value, not a secret — openclaw-env.sh only, per D-147):

```
export OPENCLAW_GMAIL_ACCOUNT="echo.sys.bot@gmail.com"
```

Add a comment above it: `# gogcli default account (not a secret — plain config, D-147)`.

Do NOT add this to openclaw-secrets.sh or secrets.sh.
  </action>
  <verify>
    <automated>`grep -c 'OPENCLAW_GMAIL_ACCOUNT' /Users/trilogy/Documents/agentic-setup/scripts/openclaw-env.sh`</automated>
  </verify>
  <done>OPENCLAW_GMAIL_ACCOUNT=echo.sys.bot@gmail.com present in openclaw-env.sh; not in secrets files</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create scripts/email-triage.sh</name>
  <files>/Users/trilogy/Documents/agentic-setup/scripts/email-triage.sh</files>
  <behavior>
    - When gog binary missing at /opt/homebrew/bin/gog: outputs {"ok":false,"error":"gog-not-found",...} and exits 1
    - When gog auth doctor --check fails: outputs {"ok":false,"error":"gog-auth-failed",...} with actionable message and exits 1
    - When auth passes and search returns threads: outputs {"ok":true,"data":{"threads":[...],"count":N}} to stdout
    - All human-readable progress messages go to stderr only
    - gog invocations always include --no-input --non-interactive (D-142)
    - --json envelope extracted with jq '.results // []' (D-146)
  </behavior>
  <action>
Create scripts/email-triage.sh with the following structure. Use `#!/usr/bin/env zsh` shebang and `set -euo pipefail` (CLAUDE.md mandate).

Source `$(dirname "$0")/lib/json-response.sh` for json_ok/json_fail.

Declare constants at the top:
- `GOG=/opt/homebrew/bin/gog` (explicit path per D-143)
- `JQ=/opt/homebrew/bin/jq`
- `ACCOUNT="${OPENCLAW_GMAIL_ACCOUNT:-echo.sys.bot@gmail.com}"`
- `MAX="${GMAIL_TRIAGE_MAX:-20}"`

Guard section (fail fast, actionable errors):
- `[[ -x "$GOG" ]] || json_fail "gog-not-found" "gog not at $GOG — run: brew install gogcli"`
- `[[ -x "$JQ"  ]] || json_fail "jq-not-found"  "jq not at $JQ — run: brew install jq"`
- Run `$GOG auth doctor --check --no-input --account "$ACCOUNT" >/dev/null 2>&1` in a subshell; on non-zero exit call `json_fail "gog-auth-failed" "gog auth check failed for $ACCOUNT — run: gog auth add $ACCOUNT --services gmail,calendar"`

Gmail search section:
- Run: `$GOG gmail search 'is:unread newer_than:1d' --account "$ACCOUNT" --max "$MAX" --json --no-input --non-interactive 2>/dev/null`
- Capture in RAW variable. Extract threads: `THREADS=$(printf '%s' "$RAW" | $JQ '.results // []')`
- Count: `COUNT=$(printf '%s' "$THREADS" | $JQ 'length')`
- Log to stderr: `print "Found $COUNT unread threads for $ACCOUNT" >&2`

Output: `json_ok "{\"threads\": $THREADS, \"count\": $COUNT}"`

After creating the file, run `chmod +x scripts/email-triage.sh`.

Do NOT delete or modify `~/.openclaw/agents/email-triage/scripts/gmail-triage.js` (D-148).
  </action>
  <verify>
    <automated>
source /Users/trilogy/Documents/agentic-setup/scripts/openclaw-env.sh && /Users/trilogy/Documents/agentic-setup/scripts/email-triage.sh | /opt/homebrew/bin/jq '.ok'
    </automated>
  </verify>
  <done>scripts/email-triage.sh is executable; outputs {"ok":true,...} with threads array; auth failure path outputs {"ok":false,...} without hanging; all gog calls include --no-input --non-interactive</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| gog stdout → jq | Gmail search results parsed from JSON — not shell-interpolated |
| agent exec → email-triage.sh | email-triage agent calls the script via exec tool |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-03 | Tampering | Shell injection via email content | mitigate | gog gmail get --sanitize-content flag sanitizes content; never interpolate email body into shell variables unquoted; use --body flag (not variable expansion) for send operations |
| T-14-04 | Information Disclosure | OPENCLAW_GMAIL_ACCOUNT in stdout | accept | It's a non-secret email address (D-147); acceptable in logs |
| T-14-05 | Spoofing | Accidental send during read-only triage | mitigate | email-triage.sh only calls gmail search/get/mark-read — no send operations; send capability in separate future script |
</threat_model>

<verification>
- `scripts/email-triage.sh` exists and is executable (`-x`)
- `scripts/email-triage.sh | jq '.ok'` returns `true`
- `grep -c 'no-input' scripts/email-triage.sh` returns at least 1
- `grep -c 'non-interactive' scripts/email-triage.sh` returns at least 1
- `grep -c 'OPENCLAW_GMAIL_ACCOUNT' scripts/openclaw-env.sh` returns at least 1
- `gmail-triage.js` still exists at `~/.openclaw/agents/email-triage/scripts/gmail-triage.js`
</verification>

<success_criteria>
email-triage.sh outputs valid JSON (ok:true with threads array) when auth is present; outputs ok:false with actionable error when gog not found or not authorized; all gog calls include --no-input --non-interactive; OPENCLAW_GMAIL_ACCOUNT in openclaw-env.sh; gmail-triage.js untouched.
</success_criteria>

<output>
Create `/Users/trilogy/Documents/agentic-setup/.planning/phases/14-gogcli-google-suite-cli-install-gogcli-wire-gog-gmail-and-go/14-02-SUMMARY.md` when done
</output>

---
phase: 14-gogcli-google-suite-cli-install-gogcli-wire-gog-gmail-and-go
plan: 05
type: execute
wave: 4
depends_on: ["14-04"]
files_modified:
  - scripts/verify-phase-14.sh
autonomous: true
requirements: [CHAN-03, CHAN-04]
must_haves:
  truths:
    - "verify-phase-14.sh exits 0 (all checks pass or auto-skip with clear message)"
    - "gog binary exists at /opt/homebrew/bin/gog"
    - "email-triage.sh exists and outputs valid JSON"
    - "standup-brief.sh output contains calendar_events and overnight_email keys"
    - "TOOLS.md primary invocation is email-triage.sh"
  artifacts:
    - path: "scripts/verify-phase-14.sh"
      provides: "Phase 14 verification gate"
      contains: "PASS"
  key_links:
    - from: "scripts/verify-phase-14.sh"
      to: "scripts/email-triage.sh"
      via: "script execution check"
      pattern: "email-triage\\.sh"
---

<objective>
Create and run scripts/verify-phase-14.sh — a zsh verification script that checks all Phase 14 deliverables: gogcli installed, authentication present, email-triage.sh works, standup-brief.sh has new fields, TOOLS.md updated. Checks that depend on gog auth auto-skip with a clear TODO message (not failure) if gog auth is not yet done.

Purpose: Provides a repeatable phase gate that can be re-run after the user completes the auth checkpoint.
Output: scripts/verify-phase-14.sh; run output showing PASS/SKIP for each check.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/trilogy/Documents/agentic-setup/.planning/phases/14-gogcli-google-suite-cli-install-gogcli-wire-gog-gmail-and-go/14-CONTEXT.md
@/Users/trilogy/Documents/agentic-setup/.planning/phases/14-gogcli-google-suite-cli-install-gogcli-wire-gog-gmail-and-go/14-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create and run scripts/verify-phase-14.sh</name>
  <files>/Users/trilogy/Documents/agentic-setup/scripts/verify-phase-14.sh</files>
  <action>
Create scripts/verify-phase-14.sh with `#!/usr/bin/env zsh` and `set -euo pipefail`. Use the same pass/fail/skip counter pattern as verify-phase-13.sh (read that file to confirm the exact pattern).

Implement the following 9 checks:

**Check 1 — gog binary installed:**
`[[ -x /opt/homebrew/bin/gog ]]`
PASS: "gog binary present at /opt/homebrew/bin/gog"
FAIL: "gog not installed — run: brew install gogcli"

**Check 2 — gog version:**
`/opt/homebrew/bin/gog --version 2>&1 | grep -q '0\.17'`
PASS: "gog version is 0.17.x"
FAIL: "gog version unexpected"

**Check 3 — gog auth check (auto-skip if auth not done):**
If gog binary missing: SKIP "gog not installed — skipping auth check"
Run `gog auth doctor --check --account echo.sys.bot@gmail.com --no-input >/dev/null 2>&1`
PASS: "gog auth valid for echo.sys.bot@gmail.com"
SKIP (non-zero exit): "TODO: run 'gog auth add echo.sys.bot@gmail.com --services gmail,calendar' — auth not yet completed"

Note: use SKIP (not FAIL) for auth check — this is the human checkpoint from Plan 14-01.

**Check 4 — email-triage.sh exists and is executable:**
`[[ -x /Users/trilogy/Documents/agentic-setup/scripts/email-triage.sh ]]`
PASS: "email-triage.sh exists and is executable"
FAIL: "email-triage.sh missing — Plan 14-02 not complete"

**Check 5 — email-triage.sh uses --no-input --non-interactive:**
`grep -q 'no-input' /Users/trilogy/Documents/agentic-setup/scripts/email-triage.sh && grep -q 'non-interactive' /Users/trilogy/Documents/agentic-setup/scripts/email-triage.sh`
PASS: "email-triage.sh includes --no-input --non-interactive flags"
FAIL: "email-triage.sh missing agent-safe flags"

**Check 6 — standup-brief.sh has calendar_events:**
`grep -q 'calendar_events' /Users/trilogy/Documents/agentic-setup/scripts/standup-brief.sh`
PASS: "standup-brief.sh contains calendar_events field"
FAIL: "standup-brief.sh missing calendar_events — Plan 14-03 not complete"

**Check 7 — standup-brief.sh has overnight_email:**
`grep -q 'overnight_email' /Users/trilogy/Documents/agentic-setup/scripts/standup-brief.sh`
PASS: "standup-brief.sh contains overnight_email field"
FAIL: "standup-brief.sh missing overnight_email — Plan 14-03 not complete"

**Check 8 — TOOLS.md updated to reference email-triage.sh:**
`grep -q 'email-triage.sh' /Users/trilogy/Documents/agentic-setup/.openclaw/agents/email-triage/TOOLS.md`
PASS: "TOOLS.md references email-triage.sh as primary invocation"
FAIL: "TOOLS.md not updated — Plan 14-04 not complete"

**Check 9 — .gitignore has gogcli credentials guard:**
`grep -q '\.config/gogcli' /Users/trilogy/Documents/agentic-setup/.gitignore`
PASS: ".gitignore contains .config/gogcli guard"
FAIL: ".gitignore missing gogcli guard — Plan 14-01 not complete"

**Final summary:**
```
echo ""
echo "=== Phase 14 Verification ==="
echo "PASSED: $PASS_COUNT"
echo "SKIPPED: $SKIP_COUNT (require human auth step)"
echo "FAILED: $FAIL_COUNT"
if [[ "$FAIL_COUNT" -eq 0 ]]; then
  echo "RESULT: PHASE 14 READY (${SKIP_COUNT} auth checks pending human action)"
else
  echo "RESULT: PHASE 14 INCOMPLETE — fix $FAIL_COUNT failing checks above"
  exit 1
fi
```

After creating the file, run `chmod +x scripts/verify-phase-14.sh` then run it:
`zsh /Users/trilogy/Documents/agentic-setup/scripts/verify-phase-14.sh`

Print the full output. Commit both the script and any remaining uncommitted phase files with message: `docs(14): create verify-phase-14.sh and finalize phase plans`.
  </action>
  <verify>
    <automated>zsh /Users/trilogy/Documents/agentic-setup/scripts/verify-phase-14.sh; echo "exit code: $?"</automated>
  </verify>
  <done>verify-phase-14.sh runs to completion; exits 0 (FAIL_COUNT=0); all structural checks PASS; auth check SKIPs with clear TODO; script committed</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| verify script → gog binary | Verification only — no write operations, no destructive calls |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-09 | Repudiation | Phase gate skipped | mitigate | verify-phase-14.sh is committed and re-runnable; SKIP vs FAIL distinction makes auth state transparent |
</threat_model>

<verification>
- `verify-phase-14.sh` is executable
- Script exits 0 with FAIL_COUNT=0
- Check 3 (auth) shows SKIP with TODO message if auth not yet done
- All 8 structural checks (1,2,4,5,6,7,8,9) show PASS
</verification>

<success_criteria>
verify-phase-14.sh exists, is executable, exits 0 with FAIL_COUNT=0; all structural checks pass; auth check produces actionable SKIP message; script committed to git.
</success_criteria>

<output>
Create `/Users/trilogy/Documents/agentic-setup/.planning/phases/14-gogcli-google-suite-cli-install-gogcli-wire-gog-gmail-and-go/14-05-SUMMARY.md` when done
</output>

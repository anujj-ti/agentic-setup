---
phase: 14-gogcli-google-suite-cli-install-gogcli-wire-gog-gmail-and-go
plan: 04
type: execute
wave: 3
depends_on: ["14-02", "14-03"]
files_modified:
  - .openclaw/agents/email-triage/TOOLS.md
  - .openclaw/agents/email-triage/AGENTS.md
autonomous: true
requirements: [CHAN-03]
must_haves:
  truths:
    - "TOOLS.md directs agent to call email-triage.sh (not gmail-triage.js)"
    - "TOOLS.md documents gog gmail commands with --no-input --non-interactive flags"
    - "TOOLS.md retains OAuth2 Re-Auth Runbook as historical reference (not deleted)"
    - "TOOLS.md adds a gogcli Re-Auth Runbook section for gog auth add"
    - "AGENTS.md references email-triage.sh as the primary script"
  artifacts:
    - path: ".openclaw/agents/email-triage/TOOLS.md"
      provides: "Updated agent tool documentation"
      contains: "email-triage.sh"
    - path: ".openclaw/agents/email-triage/AGENTS.md"
      provides: "Updated agent invocation reference"
      contains: "email-triage.sh"
---

<objective>
Update the Email Triage agent's TOOLS.md to document gog gmail commands as the primary invocation path and add a gogcli Re-Auth Runbook. Update AGENTS.md to reference email-triage.sh. Retain the existing OAuth2 Re-Auth Runbook as a legacy reference (per D-148 — gmail-triage.js is kept until verified).

Purpose: The agent reads TOOLS.md to decide how to invoke scripts. Without this update the agent will continue calling gmail-triage.js instead of the new email-triage.sh.
Output: TOOLS.md and AGENTS.md updated; both files committed.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/trilogy/Documents/agentic-setup/.planning/phases/14-gogcli-google-suite-cli-install-gogcli-wire-gog-gmail-and-go/14-CONTEXT.md
@/Users/trilogy/Documents/agentic-setup/.planning/phases/14-gogcli-google-suite-cli-install-gogcli-wire-gog-gmail-and-go/14-RESEARCH.md
@/Users/trilogy/Documents/agentic-setup/.openclaw/agents/email-triage/TOOLS.md
@/Users/trilogy/Documents/agentic-setup/.openclaw/agents/email-triage/AGENTS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update TOOLS.md with gog gmail primary invocation and Re-Auth Runbook</name>
  <files>/Users/trilogy/Documents/agentic-setup/.openclaw/agents/email-triage/TOOLS.md</files>
  <action>
Read .openclaw/agents/email-triage/TOOLS.md (current content already in context above).

Make the following changes:

**1. Replace the "Available Tools" section** — change the exec description from gmail-triage.js to email-triage.sh:
```
- **exec**: for `scripts/email-triage.sh` invocation ONLY — do not exec arbitrary system commands
```

**2. Replace the "Tool Policy" first paragraph:**
```
exec is granted only for Gmail script execution — specifically, calling `scripts/email-triage.sh` to fetch unread mail via gogcli. No other exec usage is permitted.
```

**3. Replace the "Gmail Script Invocation" section** with a new primary invocation block:

```markdown
## Email Triage Script Invocation (Primary — Phase 14+)

Call `email-triage.sh` from the exec tool as follows:
```
zsh /Users/trilogy/Documents/agentic-setup/scripts/email-triage.sh
```

**Before calling:** Confirm `OPENCLAW_GMAIL_ACCOUNT` is set in the environment (default: echo.sys.bot@gmail.com). If the script returns `{"ok":false,"error":"gog-auth-failed"}`, follow the gogcli Re-Auth Runbook below.

Expected output: `{"ok":true,"data":{"threads":[...],"count":N}}`

### gog gmail command reference

All commands require `--no-input --non-interactive` (D-142 — prevents TTY hang in agent context).

| Operation | Command |
|-----------|---------|
| Search unread (24h) | `gog gmail search 'is:unread newer_than:1d' --account echo.sys.bot@gmail.com --max 20 --json --no-input --non-interactive` |
| Get message body | `gog gmail get <messageId> --sanitize-content --json --no-input --non-interactive` |
| Mark as read (by query) | `gog gmail mark-read --account echo.sys.bot@gmail.com --query 'label:triaged is:unread' --no-input --non-interactive` |
| Send reply | `gog gmail send --account echo.sys.bot@gmail.com --to "addr" --subject "Subj" --body "Body" --no-input --non-interactive --json` |

JSON output note: `--json` returns `{"results":[...]}` envelope — extract array with `jq '.results // []'` (D-146).
```

**4. Add a new gogcli Re-Auth Runbook section** before the existing "OAuth2 Re-Auth Runbook" section:

```markdown
## gogcli Re-Auth Runbook (Phase 14+)

Run this when `gog auth doctor --check --account echo.sys.bot@gmail.com` exits non-zero.

### A: Download Desktop OAuth client JSON

1. Go to https://console.cloud.google.com
2. APIs & Services → Credentials → find "gogcli-agent-hub" Desktop app credential
3. Download the JSON file

### B: Store credentials and re-authorize

```zsh
/opt/homebrew/bin/gog auth credentials ~/Downloads/client_secret_*.json
/opt/homebrew/bin/gog auth add echo.sys.bot@gmail.com --services gmail,calendar
```

A browser window opens — sign in as echo.sys.bot@gmail.com and approve Gmail + Calendar scopes.

### C: Verify

```zsh
/opt/homebrew/bin/gog auth doctor --check --account echo.sys.bot@gmail.com
# Exits 0 = success
```

### D: Token expiry note

Tokens expire after 7 days if the OAuth consent screen app is in "Testing" mode.
Fix: Google Cloud Console → APIs & Services → OAuth consent screen → Publish App → In production.
```

**5. Add a deprecation notice** at the top of the existing "OAuth2 Re-Auth Runbook" section (do NOT delete it — D-148):

```markdown
## OAuth2 Re-Auth Runbook (Legacy — gmail-triage.js)

> **Superseded by Phase 14.** The primary invocation is now `email-triage.sh` via gogcli. This runbook remains valid only for re-enabling `gmail-triage.js` if gogcli authorization fails during transition.
```

**6. Update the "Keychain Key Reference" table** — add a new row for gogcli:

| `token:default:echo.sys.bot@gmail.com` | (gogcli internal) | gogcli OAuth2 refresh token — managed by gog auth, not openclaw secrets pipeline |

Leave all other existing content (Environment section, Tool Policy specifics, Synapse section) untouched.
  </action>
  <verify>
    <automated>grep -c 'email-triage.sh' /Users/trilogy/Documents/agentic-setup/.openclaw/agents/email-triage/TOOLS.md</automated>
  </verify>
  <done>TOOLS.md primary invocation is email-triage.sh; gog command reference table present; gogcli Re-Auth Runbook added; legacy OAuth2 runbook marked superseded but not deleted; gogcli Keychain entry in key reference table</done>
</task>

<task type="auto">
  <name>Task 2: Update AGENTS.md to reference email-triage.sh</name>
  <files>/Users/trilogy/Documents/agentic-setup/.openclaw/agents/email-triage/AGENTS.md</files>
  <action>
Read .openclaw/agents/email-triage/AGENTS.md.

Find any reference to `gmail-triage.js` as the primary script. Add a note alongside each reference (do NOT delete the references — D-148):

Format: add `(superseded by email-triage.sh — Phase 14)` immediately after each `gmail-triage.js` mention.

If AGENTS.md has a "Scripts" or "Invocation" section, add an entry for email-triage.sh as the current primary script:
```
- **email-triage.sh** (primary, Phase 14+): zsh script using gogcli; outputs {"ok":true,"data":{"threads":[...],"count":N}}
```

Commit both TOOLS.md and AGENTS.md together with message: `docs(14): update email-triage agent to use email-triage.sh via gogcli`.
  </action>
  <verify>
    <automated>grep -c 'email-triage.sh' /Users/trilogy/Documents/agentic-setup/.openclaw/agents/email-triage/AGENTS.md</automated>
  </verify>
  <done>AGENTS.md references email-triage.sh as primary; gmail-triage.js references marked superseded; both files committed</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Agent reads TOOLS.md → exec decision | Agent uses TOOLS.md to decide which script to call |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-08 | Tampering | Agent calls wrong script after TOOLS.md update | mitigate | TOOLS.md exec policy is explicit: email-triage.sh ONLY; gmail-triage.js listed as legacy |
</threat_model>

<verification>
- `grep -c 'email-triage.sh' .openclaw/agents/email-triage/TOOLS.md` returns at least 3
- `grep -c 'gogcli Re-Auth' .openclaw/agents/email-triage/TOOLS.md` returns 1
- `grep -c 'Superseded' .openclaw/agents/email-triage/TOOLS.md` returns 1
- `grep -c 'email-triage.sh' .openclaw/agents/email-triage/AGENTS.md` returns at least 1
- `gmail-triage.js` still exists at `~/.openclaw/agents/email-triage/scripts/gmail-triage.js`
</verification>

<success_criteria>
TOOLS.md directs agent to call email-triage.sh; gog command reference table with --no-input --non-interactive present; gogcli Re-Auth Runbook added; legacy OAuth2 runbook retained with superseded notice; AGENTS.md updated; both files committed.
</success_criteria>

<output>
Create `/Users/trilogy/Documents/agentic-setup/.planning/phases/14-gogcli-google-suite-cli-install-gogcli-wire-gog-gmail-and-go/14-04-SUMMARY.md` when done
</output>

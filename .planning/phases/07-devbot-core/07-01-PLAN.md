---
phase: 07-devbot-core
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .openclaw/agents/devbot/SOUL.md
  - .openclaw/agents/devbot/IDENTITY.md
  - .openclaw/agents/devbot/USER.md
  - .openclaw/agents/devbot/AGENTS.md
  - .openclaw/agents/devbot/TOOLS.md
  - .openclaw/agents/devbot/SECURITY.md
  - .openclaw/openclaw.json
  - .openclaw/agents/task-orchestrator/SOUL.md
autonomous: false
requirements:
  - DEV-01
  - DEV-06

must_haves:
  truths:
    - "DevBot agent entry exists in openclaw.json agents.list with correct workspace, agentDir, model, and tools.alsoAllow exec"
    - "Task Orchestrator openclaw.json entry has allowAgents: [devbot] and sessions_spawn in tools.alsoAllow"
    - "gh CLI is version 2.92.0 (per D-70)"
    - "gh auth has project scope — gh auth status output includes 'project' (per D-71)"
    - "DevBot 6 directive files exist in .openclaw/agents/devbot/"
    - "DevBot workspace and repos/ directory exist at ~/.openclaw/workspace-devbot/repos/"
    - "Task Orchestrator SOUL.md includes DevBot routing hint for GitHub operations"
  artifacts:
    - path: ".openclaw/agents/devbot/SOUL.md"
      provides: "DevBot identity, GitHub ops scope, per-repo context rules, no-Telegram boundary"
    - path: ".openclaw/agents/devbot/IDENTITY.md"
      provides: "name, role, model, emoji"
    - path: ".openclaw/agents/devbot/USER.md"
      provides: "Anuj's GitHub preferences, default repo conventions"
    - path: ".openclaw/agents/devbot/AGENTS.md"
      provides: "Startup checklist, context load sequence, workspace hygiene"
    - path: ".openclaw/agents/devbot/TOOLS.md"
      provides: "gh CLI reference, project scope auth runbook, context file format"
    - path: ".openclaw/agents/devbot/SECURITY.md"
      provides: "Credential rules, no-merge gate, duplicate-check rule"
    - path: ".openclaw/openclaw.json"
      provides: "devbot agent registered; task-orchestrator updated with allowAgents + sessions_spawn"
  key_links:
    - from: "task-orchestrator (openclaw.json)"
      to: "devbot (openclaw.json)"
      via: "subagents.allowAgents: [devbot]"
      pattern: '"allowAgents".*"devbot"'
    - from: ".openclaw/agents/devbot/SOUL.md"
      to: "sessions_spawn"
      via: "No direct Telegram channel rule"
      pattern: "sessions_spawn"
---

<objective>
Scaffold DevBot as a Task Orchestrator sub-agent and upgrade gh to 2.92.0 with project OAuth scope.

Purpose: DevBot cannot be registered or called until (a) its 6 directive files define its identity and constraints, (b) openclaw.json registers it as a sub-agent reachable from Task Orchestrator, and (c) gh has the project scope required for issue-to-board assignment.

Output: DevBot agent fully registered, Task Orchestrator wired to delegate GitHub work to it, gh at 2.92.0 with project scope, workspace directories created.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/trilogy/Documents/agentic-setup/.planning/PROJECT.md
@/Users/trilogy/Documents/agentic-setup/.planning/ROADMAP.md
@/Users/trilogy/Documents/agentic-setup/.planning/STATE.md
@/Users/trilogy/Documents/agentic-setup/.planning/phases/07-devbot-core/07-CONTEXT.md
@/Users/trilogy/Documents/agentic-setup/.planning/phases/07-devbot-core/07-RESEARCH.md

<interfaces>
<!-- Existing openclaw.json agent list shape (from .openclaw/openclaw.json) -->
<!-- Agent entries use this exact schema — use literal /Users/trilogy/... paths, never ~ -->

Existing task-orchestrator entry:
```json
{
  "id": "task-orchestrator",
  "name": "Task Orchestrator",
  "workspace": "/Users/trilogy/.openclaw/workspace-task-orchestrator",
  "agentDir": "/Users/trilogy/.openclaw/agents/task-orchestrator",
  "model": { "primary": "anthropic/claude-sonnet-4-6" },
  "subagents": { "delegationMode": "prefer" }
}
```

DevBot entry to add (new):
```json
{
  "id": "devbot",
  "name": "DevBot",
  "workspace": "/Users/trilogy/.openclaw/workspace-devbot",
  "agentDir": "/Users/trilogy/.openclaw/agents/devbot",
  "model": { "primary": "anthropic/claude-sonnet-4-6" },
  "tools": { "alsoAllow": ["exec"] }
}
```

Task Orchestrator updated entry (adds allowAgents + sessions_spawn):
```json
{
  "id": "task-orchestrator",
  ...
  "subagents": {
    "allowAgents": ["devbot"],
    "delegationMode": "prefer"
  },
  "tools": { "alsoAllow": ["sessions_spawn"] }
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Upgrade gh + add project scope + create workspace dirs</name>
  <files>
    (shell commands only — no repo files modified)
  </files>
  <action>
    Run in sequence:

    1. Upgrade gh CLI per D-70: `brew upgrade gh`. Verify the result is 2.92.0 or newer: `/opt/homebrew/bin/gh --version`. If already at 2.92.0+, skip upgrade but still verify.

    2. Create DevBot workspace and repos directory per D-73:
       `mkdir -p /Users/trilogy/.openclaw/workspace-devbot/repos`

    3. Create DevBot agent directory structure:
       `mkdir -p /Users/trilogy/.openclaw/agents/devbot/scripts/lib`
       `mkdir -p /Users/trilogy/.openclaw/agents/devbot/memory/archives`

    Note: The project scope OAuth step (D-71) requires a browser interaction and is handled in the checkpoint task below.
  </action>
  <verify>
    <automated>
      /opt/homebrew/bin/gh --version | grep -E "^gh version [2-9]\." &&
      test -d /Users/trilogy/.openclaw/workspace-devbot/repos &&
      test -d /Users/trilogy/.openclaw/agents/devbot/scripts/lib &&
      test -d /Users/trilogy/.openclaw/agents/devbot/memory/archives
    </automated>
  </verify>
  <done>gh is 2.92.0+, workspace-devbot/repos/ exists, devbot agent dirs exist with scripts/lib/ and memory/archives/</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <what-built>gh CLI upgraded to 2.92.0, devbot directories created</what-built>
  <how-to-verify>
    Run this command in your terminal (requires browser auth interaction):

    /opt/homebrew/bin/gh auth refresh -s project

    A browser window will open. Log in and authorize the additional "project" scope.
    After completing, verify the scope was added:

    /opt/homebrew/bin/gh auth status 2>&1 | grep -i "token scopes"

    Expected output should contain "project" in the scopes list.
  </how-to-verify>
  <resume-signal>Type "scope added" after /opt/homebrew/bin/gh auth status confirms "project" in scopes, or describe any auth error</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Write DevBot 6 directive files + update openclaw.json</name>
  <files>
    .openclaw/agents/devbot/SOUL.md
    .openclaw/agents/devbot/IDENTITY.md
    .openclaw/agents/devbot/USER.md
    .openclaw/agents/devbot/AGENTS.md
    .openclaw/agents/devbot/TOOLS.md
    .openclaw/agents/devbot/SECURITY.md
    .openclaw/openclaw.json
    .openclaw/agents/task-orchestrator/SOUL.md
  </files>
  <action>
    Write all six DevBot directive files to `.openclaw/agents/devbot/` and update the two JSON/SOUL files.

    **SOUL.md** — Use the exact template from RESEARCH.md Pattern 1. Key required content:
    - Identity: DevBot, GitHub operations specialist, receives work via sessions_spawn from Task Orchestrator
    - Responsibilities: issue creation, PR queue flagging, per-repo context, JSON evidence results
    - Operational Rules: NEVER start without loading per-repo CONTEXT.md; always `--repo OWNER/REPO` explicitly; gh binary at `/opt/homebrew/bin/gh`; check duplicates before creating issues via `gh issue list --search`
    - Boundaries: No direct Telegram channel; no PR merges in Phase 7; gh CLI only (no curl); must check for duplicates
    - BLOCKED protocol: describe exactly what is missing, return control
    - Model: `anthropic/claude-sonnet-4-6`

    **IDENTITY.md** — Write:
    ```
    name: DevBot
    role: GitHub Operations Specialist
    model: anthropic/claude-sonnet-4-6
    emoji: 🤖
    tier: execution
    parent: task-orchestrator
    channel: none
    ```

    **USER.md** — Write Anuj's GitHub preferences:
    - GitHub username: anujj-ti
    - Default gh binary: /opt/homebrew/bin/gh
    - Project scope: required for board assignment (run `gh auth refresh -s project` if missing)
    - JSON output always: all gh calls piped through jq; stdout is JSON only
    - Issue labels to use: bug, feature, docs, infra (check with `gh label list --repo OWNER/REPO` before assigning)
    - Default milestone: check existing milestones via `gh api repos/OWNER/REPO/milestones` before assigning
    - PR review threshold: 24 hours (configurable via STALE_HOURS in scripts)

    **AGENTS.md** — Write startup sequence:
    ```
    ## Session Startup
    1. Read SOUL.md
    2. Read MEMORY.md (if present in memory/)
    3. If task specifies a repo: read /Users/trilogy/.openclaw/workspace-devbot/repos/<owner>-<repo>/CONTEXT.md
       - If CONTEXT.md absent: acknowledge gap, proceed with defaults, create a stub after first interaction
    4. Confirm gh auth has project scope: /opt/homebrew/bin/gh auth status 2>&1 | grep project
    5. Begin work — status prefix: STARTED

    ## Workspace Hygiene
    - Per-repo context files live in: /Users/trilogy/.openclaw/workspace-devbot/repos/<owner>-<repo>/CONTEXT.md
    - Never write credentials or tokens to CONTEXT.md files
    - Update CONTEXT.md after each repo interaction (issue created, PR reviewed)
    - Memory archives: memory/archives/ (nightly dream routine — Phase 8+)
    ```

    **TOOLS.md** — Write gh CLI reference:
    ```
    ## gh CLI Reference

    Binary: /opt/homebrew/bin/gh
    Required scope: project (for board assignment)
    Auth check: /opt/homebrew/bin/gh auth status 2>&1 | grep "token scopes"
    Add project scope: /opt/homebrew/bin/gh auth refresh -s project

    ### Issue Creation (DEV-01)
    /opt/homebrew/bin/gh issue create \
      --repo OWNER/REPO \
      --title "..." \
      --body "..." \
      --label "feature" \
      --milestone "v1.0" \
      --project "Board Title" \
      --assignee "@me"
    # Returns: https://github.com/OWNER/REPO/issues/NN

    ### Retroactive board assignment
    /opt/homebrew/bin/gh project item-add PROJECT_NUMBER \
      --owner "@me" \
      --url ISSUE_URL

    ### PR Review Queue (DEV-02)
    /opt/homebrew/bin/gh pr list \
      --repo OWNER/REPO \
      --state open \
      --json number,title,createdAt,updatedAt,author,reviewDecision,reviewRequests,latestReviews,statusCheckRollup,url \
      --limit 50

    ### Per-PR CI drill-down
    /opt/homebrew/bin/gh pr checks PR_NUMBER \
      --repo OWNER/REPO \
      --json name,state,startedAt,completedAt,link

    ### Duplicate check before issue creation
    /opt/homebrew/bin/gh issue list \
      --repo OWNER/REPO \
      --search "keywords from title" \
      --json number,title,state

    ## Script Locations
    All scripts: /Users/trilogy/.openclaw/agents/devbot/scripts/
    json-response lib: scripts/lib/json-response.sh
    Issue creation: scripts/devbot-issue-create.sh
    PR queue: scripts/devbot-pr-queue.sh
    Verify: scripts/devbot-verify.sh

    ## Per-Repo Context File Format
    Location: /Users/trilogy/.openclaw/workspace-devbot/repos/<owner>-<repo>/CONTEXT.md
    Template: /Users/trilogy/.openclaw/workspace-devbot/repos/CONTEXT-TEMPLATE.md
    ```

    **SECURITY.md** — Write:
    ```
    ## DevBot Security Rules

    1. NEVER store tokens, API keys, or credentials in CONTEXT.md files — Keychain only
    2. NEVER merge PRs in Phase 7 — merges require Notion pre-log gate (Phase 10)
       Return BLOCKED with message: "PR merge requires Phase 10 Notion pre-log gate — not available in Phase 7"
    3. NEVER create an issue without first running: gh issue list --search '<title keywords>' --repo OWNER/REPO
       If a likely duplicate is found, return BLOCKED with the duplicate issue URL
    4. NEVER use gh without explicit --repo OWNER/REPO flag — cwd-based detection is unreliable in agent context
    5. NEVER call GitHub API directly via curl — use gh CLI exclusively
    6. gh token is managed by gh's own keychain integration — never echo, log, or write the token

    ## Allowed GitHub Operations (Phase 7)
    - gh issue create, gh issue list, gh issue view
    - gh pr list, gh pr view, gh pr checks
    - gh project item-add
    - gh label list, gh milestone list (via gh api)
    - gh auth status

    ## Blocked GitHub Operations (Phase 7 — future phases)
    - gh pr merge (Phase 10)
    - gh pr review --approve (Phase 10)
    - gh workflow run (Phase 8)
    ```

    **Update openclaw.json:**
    Edit the task-orchestrator agent entry to add:
    - `"subagents": { "allowAgents": ["devbot"], "delegationMode": "prefer" }`
    - `"tools": { "alsoAllow": ["sessions_spawn"] }`

    Add devbot agent entry (after task-orchestrator in agents.list):
    ```json
    {
      "id": "devbot",
      "name": "DevBot",
      "workspace": "/Users/trilogy/.openclaw/workspace-devbot",
      "agentDir": "/Users/trilogy/.openclaw/agents/devbot",
      "model": { "primary": "anthropic/claude-sonnet-4-6" },
      "tools": { "alsoAllow": ["exec"] }
    }
    ```

    Also add devbot QMD paths to the memory.qmd.paths array:
    ```json
    { "path": "/Users/trilogy/.openclaw/agents/devbot/memory", "name": "devbot-memory", "pattern": "**/*.md" },
    { "path": "/Users/trilogy/.openclaw/agents/devbot", "name": "devbot-docs", "pattern": "*.md" }
    ```

    **Update Task Orchestrator SOUL.md:**
    Add a new section after "## Boundaries":
    ```markdown
    ## Sub-Agent Routing

    When delegating GitHub operations, route to DevBot:
    - GitHub issue creation → DevBot
    - PR review queue / CI status → DevBot
    - Per-repo context queries → DevBot
    - Any "GitHub" or "repo" or "PR" or "CI" task → DevBot

    DevBot receives work via sessions_spawn. The only instruction to DevBot is:
    "Your tasks are in Beads. Run `bd ready --json` to start." (per Beads execution contract)
    ```

    Run stow after all files are written:
    `zsh /Users/trilogy/Documents/agentic-setup/scripts/stow-deploy.sh`

    Restart the gateway:
    `launchctl kickstart -k gui/$(id -u)/ai.openclaw.gateway`
  </action>
  <verify>
    <automated>
      test -f /Users/trilogy/.openclaw/agents/devbot/SOUL.md &&
      test -f /Users/trilogy/.openclaw/agents/devbot/IDENTITY.md &&
      test -f /Users/trilogy/.openclaw/agents/devbot/USER.md &&
      test -f /Users/trilogy/.openclaw/agents/devbot/AGENTS.md &&
      test -f /Users/trilogy/.openclaw/agents/devbot/TOOLS.md &&
      test -f /Users/trilogy/.openclaw/agents/devbot/SECURITY.md &&
      grep -q '"id": "devbot"' /Users/trilogy/.openclaw/openclaw.json &&
      grep -q '"allowAgents"' /Users/trilogy/.openclaw/openclaw.json &&
      grep -q '"sessions_spawn"' /Users/trilogy/.openclaw/openclaw.json &&
      grep -q "GitHub.*DevBot\|DevBot.*GitHub" /Users/trilogy/.openclaw/agents/task-orchestrator/SOUL.md
    </automated>
  </verify>
  <done>
    All 6 DevBot directive files exist at ~/.openclaw/agents/devbot/. openclaw.json has devbot in agents.list and task-orchestrator has allowAgents: ["devbot"] and sessions_spawn tool. Task Orchestrator SOUL.md has DevBot routing section. stow-deploy.sh ran successfully.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Task Orchestrator → DevBot | sessions_spawn task descriptions cross here — treated as structured input from internal orchestrator |
| DevBot → GitHub API | All gh CLI calls — DevBot has write access to GitHub repos |
| DevBot scripts → shell | exec tool allows arbitrary shell execution within agent context |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-01 | Tampering | gh issue create (wrong repo targeting) | mitigate | SOUL.md rule: always pass `--repo OWNER/REPO` explicitly; never cwd-based detection |
| T-07-02 | Tampering | Issue creation spam via prompt injection from Task Orchestrator | mitigate | SOUL.md duplicate-check rule: `gh issue list --search` before every create; BLOCKED if duplicate found |
| T-07-03 | Elevation of Privilege | DevBot attempting PR merge in Phase 7 | mitigate | SECURITY.md explicit block; SOUL.md Boundaries; return BLOCKED with Phase 10 message |
| T-07-04 | Information Disclosure | Credentials written to CONTEXT.md files | mitigate | SECURITY.md rule 1: CONTEXT.md files contain only non-sensitive operational state |
| T-07-05 | Spoofing | External Telegram message triggering DevBot directly | mitigate | No Telegram binding on DevBot; only sessions_spawn from task-orchestrator is accepted |
| T-07-SC | Tampering | npm/pip/cargo installs | accept | Phase 7 installs no packages — all tooling is gh CLI + jq via Homebrew from prior phases |
</threat_model>

<verification>
After Task 2 and stow+restart:

1. DevBot agent dirs exist:
   `ls /Users/trilogy/.openclaw/agents/devbot/`
   Expected: SOUL.md IDENTITY.md USER.md AGENTS.md TOOLS.md SECURITY.md scripts/ memory/

2. openclaw.json is valid JSON with devbot registered:
   `python3 -c "import json; json.load(open('/Users/trilogy/.openclaw/openclaw.json'))" && echo "valid JSON"`

3. task-orchestrator has allowAgents and sessions_spawn:
   `grep -A5 '"task-orchestrator"' /Users/trilogy/.openclaw/openclaw.json | grep -E "allowAgents|sessions_spawn"`

4. gh version is 2.92.0+:
   `/opt/homebrew/bin/gh --version`

5. gh has project scope (after human checkpoint):
   `/opt/homebrew/bin/gh auth status 2>&1 | grep -i "project"`
</verification>

<success_criteria>
- DevBot registered in openclaw.json with correct workspace, agentDir, model, exec tool
- Task Orchestrator openclaw.json entry has allowAgents: ["devbot"] and sessions_spawn
- gh at 2.92.0 or newer
- gh auth status shows project scope
- All 6 DevBot directive files exist at ~/.openclaw/agents/devbot/
- workspace-devbot/repos/ directory exists at ~/.openclaw/
- Task Orchestrator SOUL.md has DevBot GitHub routing section
- stow-deploy.sh ran successfully after all file writes
</success_criteria>

<output>
Create `.planning/phases/07-devbot-core/07-01-SUMMARY.md` when done
</output>

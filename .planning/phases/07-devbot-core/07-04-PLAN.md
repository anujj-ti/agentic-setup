---
phase: 07-devbot-core
plan: 04
type: execute
wave: 3
depends_on:
  - 07-01
  - 07-03
files_modified:
  - .openclaw/agents/devbot/scripts/devbot-verify.sh
autonomous: true
requirements:
  - DEV-01
  - DEV-02
  - DEV-06

must_haves:
  truths:
    - "CONTEXT-TEMPLATE.md exists at ~/.openclaw/workspace-devbot/repos/CONTEXT-TEMPLATE.md"
    - "devbot-verify.sh runs all 7 smoke checks from RESEARCH.md Validation Architecture"
    - "devbot-verify.sh exits 0 only when all checks pass"
    - "devbot agent appears in openclaw.json agents.list (confirmed by grep)"
    - "gh auth status shows project scope"
    - "A test issue is created on anujj-ti/agentic-setup and immediately closed to confirm DEV-01 end-to-end"
    - "devbot-pr-queue.sh produces valid JSON against anujj-ti/agentic-setup (read-only DEV-02 check)"
    - "repos/ directory exists in devbot workspace (DEV-06 file store)"
  artifacts:
    - path: ".openclaw/agents/devbot/scripts/devbot-verify.sh"
      provides: "Phase 7 smoke test suite covering DEV-01, DEV-02, DEV-06"
      contains: "devbot"
  key_links:
    - from: "devbot-verify.sh"
      to: "devbot-issue-create.sh"
      via: "smoke test: zsh -n syntax check"
      pattern: "zsh -n.*create-issue\|zsh -n.*devbot-issue"
    - from: "devbot-verify.sh"
      to: "devbot-pr-queue.sh"
      via: "live read-only run against anujj-ti/agentic-setup"
      pattern: "pr-queue.*agentic-setup\|agentic-setup.*pr-queue"
    - from: "CONTEXT-TEMPLATE.md"
      to: "~/.openclaw/workspace-devbot/repos/<owner>-<repo>/CONTEXT.md"
      via: "DevBot copies template on first repo contact"
      pattern: "CONTEXT-TEMPLATE"
---

<objective>
Create the per-repo context template (DEV-06) and the full phase verification script, then run an end-to-end smoke test including a test issue create+close on anujj-ti/agentic-setup.

Purpose: This is the phase gate. DEV-06 is satisfied by the context store infrastructure. The verification script confirms DEV-01, DEV-02, and DEV-06 are all working before the phase is marked complete. The test issue create+close proves the gh project scope is working end-to-end, not just syntactically.

Output: CONTEXT-TEMPLATE.md at the repos/ directory, devbot-verify.sh covering all 7 required smoke checks, end-to-end test confirming all three requirements are live.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/trilogy/Documents/agentic-setup/.planning/ROADMAP.md
@/Users/trilogy/Documents/agentic-setup/.planning/phases/07-devbot-core/07-CONTEXT.md
@/Users/trilogy/Documents/agentic-setup/.planning/phases/07-devbot-core/07-RESEARCH.md

<!-- Depends on outputs from: -->
@/Users/trilogy/Documents/agentic-setup/.planning/phases/07-devbot-core/07-01-SUMMARY.md
@/Users/trilogy/Documents/agentic-setup/.planning/phases/07-devbot-core/07-03-SUMMARY.md

<interfaces>
<!-- 7 required smoke checks from RESEARCH.md Validation Architecture: -->

1. DEV-01: devbot registered in openclaw.json
   grep -q '"id": "devbot"' /Users/trilogy/.openclaw/openclaw.json

2. DEV-01: create-issue script is syntax-valid
   zsh -n /Users/trilogy/.openclaw/agents/devbot/scripts/devbot-issue-create.sh

3. DEV-01: gh auth has project scope
   /opt/homebrew/bin/gh auth status 2>&1 | grep -q project

4. DEV-02: pr-queue script is syntax-valid
   zsh -n /Users/trilogy/.openclaw/agents/devbot/scripts/devbot-pr-queue.sh

5. DEV-02: pr-queue outputs valid JSON on live run
   zsh /Users/trilogy/.openclaw/agents/devbot/scripts/devbot-pr-queue.sh anujj-ti/agentic-setup | jq -e '.ok == true'

6. DEV-06: repos/ directory exists
   test -d /Users/trilogy/.openclaw/workspace-devbot/repos

7. DEV-06: AGENTS.md includes context load step
   grep -q "CONTEXT.md" /Users/trilogy/.openclaw/agents/devbot/AGENTS.md

<!-- CONTEXT-TEMPLATE.md format from RESEARCH.md Pattern 4 -->
Location: /Users/trilogy/.openclaw/workspace-devbot/repos/CONTEXT-TEMPLATE.md
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create per-repo CONTEXT-TEMPLATE.md (DEV-06)</name>
  <files>
    (runtime file — ~/.openclaw/workspace-devbot/repos/CONTEXT-TEMPLATE.md — not in git repo)
  </files>
  <action>
    Write `/Users/trilogy/.openclaw/workspace-devbot/repos/CONTEXT-TEMPLATE.md` using the exact format from RESEARCH.md Pattern 4.

    The template must contain these sections with placeholder values:
    ```markdown
    # Context: <owner>/<repo>

    **Last updated:** YYYY-MM-DD by DevBot
    **Loaded by:** DevBot — read at task start before any GitHub operations

    ## Stack
    - Language: (e.g., TypeScript, Python, Go)
    - Framework: (e.g., Next.js 15, FastAPI, none)
    - Package manager: (e.g., npm, pip, cargo)
    - Node version: (if applicable)

    ## Conventions
    - Branch naming: feat/<issue-number>-<slug>
    - PR title format: [#<issue>] Title
    - Labels: (list from `gh label list --repo OWNER/REPO`)
    - Milestones: (list from `gh api repos/OWNER/REPO/milestones`)

    ## Open Work
    - (Populate on first interaction: gh issue list --repo OWNER/REPO --state open --assignee "@me" --json number,title)

    ## Project Boards
    - Board: (name from `gh project list --owner "@me" --format json | jq '.[].title'`)
    - Project number: (needed for `gh project item-add PROJECT_NUMBER --owner "@me"`)

    ## Notes
    - Main branch: main (verify with `gh api repos/OWNER/REPO | jq .default_branch`)
    - CI: (check .github/workflows/ in repo)
    - Protected branches: (check with `gh api repos/OWNER/REPO/branches/main | jq .protected`)
    ```

    Also verify the repos/ directory exists (should be created from Plan 07-01):
    `test -d /Users/trilogy/.openclaw/workspace-devbot/repos || mkdir -p /Users/trilogy/.openclaw/workspace-devbot/repos`

    Write a stub context for the agentic-setup repo itself (DevBot's home repo) as the first real CONTEXT.md:
    File: `/Users/trilogy/.openclaw/workspace-devbot/repos/anujj-ti-agentic-setup/CONTEXT.md`
    Content: Fill in known values from the project:
    - Language: Shell (zsh), JSON5 (openclaw.json)
    - Framework: OpenClaw 2026.5.18
    - Package manager: none (shell-only, Homebrew for system tools)
    - Branch naming: feat/<number>-<slug>
    - Main branch: main
    - CI: none yet (Phase 8)
    - Project board: fill with "TBD — run `gh project list --owner '@me'` to populate"
    - Open Work: "Phase 7 DevBot Core (in progress), Phase 6 Email + Morning Standup (not started)"
  </action>
  <verify>
    <automated>
      test -f /Users/trilogy/.openclaw/workspace-devbot/repos/CONTEXT-TEMPLATE.md &&
      grep -q "## Stack" /Users/trilogy/.openclaw/workspace-devbot/repos/CONTEXT-TEMPLATE.md &&
      grep -q "## Open Work" /Users/trilogy/.openclaw/workspace-devbot/repos/CONTEXT-TEMPLATE.md &&
      test -f /Users/trilogy/.openclaw/workspace-devbot/repos/anujj-ti-agentic-setup/CONTEXT.md
    </automated>
  </verify>
  <done>CONTEXT-TEMPLATE.md exists with all sections; anujj-ti-agentic-setup/CONTEXT.md stub created.</done>
</task>

<task type="auto">
  <name>Task 2: Write devbot-verify.sh + run full smoke suite + test issue create/close</name>
  <files>
    .openclaw/agents/devbot/scripts/devbot-verify.sh
  </files>
  <action>
    Write `.openclaw/agents/devbot/scripts/devbot-verify.sh` implementing all 7 smoke checks from RESEARCH.md Validation Architecture. Then run it and fix any failures.

    Script structure:
    1. Shebang: `#!/usr/bin/env zsh`
    2. Strict mode: `set -euo pipefail`
    3. GH path: `GH=/opt/homebrew/bin/gh`
    4. PASS/FAIL counter pattern:
       ```zsh
       PASS=0; FAIL=0
       check() {
         local desc="$1"; shift
         if "$@" >/dev/null 2>&1; then
           echo "PASS: $desc" >&2; ((PASS++))
         else
           echo "FAIL: $desc" >&2; ((FAIL++))
         fi
       }
       ```
    5. Run all 7 checks using `check "description" <command>`:
       - Check 1 (DEV-01): `check "devbot registered in openclaw.json" grep -q '"id": "devbot"' /Users/trilogy/.openclaw/openclaw.json`
       - Check 2 (DEV-01): `check "devbot-issue-create.sh syntax valid" zsh -n /Users/trilogy/.openclaw/agents/devbot/scripts/devbot-issue-create.sh`
       - Check 3 (DEV-01): `check "gh auth has project scope" bash -c '/opt/homebrew/bin/gh auth status 2>&1 | grep -q project'`
       - Check 4 (DEV-02): `check "devbot-pr-queue.sh syntax valid" zsh -n /Users/trilogy/.openclaw/agents/devbot/scripts/devbot-pr-queue.sh`
       - Check 5 (DEV-02): `check "pr-queue outputs valid JSON" bash -c 'zsh /Users/trilogy/.openclaw/agents/devbot/scripts/devbot-pr-queue.sh anujj-ti/agentic-setup 2>/dev/null | python3 -c "import json,sys; d=json.load(sys.stdin); exit(0 if d[\"ok\"] else 1)"'`
       - Check 6 (DEV-06): `check "workspace-devbot/repos dir exists" test -d /Users/trilogy/.openclaw/workspace-devbot/repos`
       - Check 7 (DEV-06): `check "AGENTS.md includes context load step" grep -q "CONTEXT.md" /Users/trilogy/.openclaw/agents/devbot/AGENTS.md`

    6. End-to-end DEV-01 test (test issue create + immediate close):
       ```zsh
       echo "Running DEV-01 end-to-end: creating test issue on anujj-ti/agentic-setup..." >&2
       TEST_URL=$(zsh /Users/trilogy/.openclaw/agents/devbot/scripts/devbot-issue-create.sh \
         --repo anujj-ti/agentic-setup \
         --title "[devbot-verify] Phase 7 smoke test issue — close immediately" \
         --body "Automated test issue created by devbot-verify.sh to confirm DEV-01 end-to-end. Close this issue immediately." \
         --label "infra" \
         2>/dev/null | python3 -c "import json,sys; d=json.load(sys.stdin); print(d['data']['issue_url'])")
       TEST_NUM=$(basename "$TEST_URL")
       echo "Test issue created: $TEST_URL" >&2
       $GH issue close "$TEST_NUM" --repo anujj-ti/agentic-setup 2>/dev/null && \
         { echo "PASS: DEV-01 end-to-end (issue created and closed: $TEST_URL)" >&2; ((PASS++)); } || \
         { echo "FAIL: DEV-01 end-to-end (issue close failed)" >&2; ((FAIL++)); }
       ```

    7. Summary:
       ```zsh
       echo "" >&2
       echo "Smoke test complete: $PASS passed, $FAIL failed" >&2
       if [[ "$FAIL" -gt 0 ]]; then
         json_err "devbot-verify: $FAIL check(s) failed — see stderr for details"
       else
         json_ok "{\"checks_passed\": $PASS, \"checks_failed\": $FAIL, \"phase\": \"07-devbot-core\"}"
       fi
       ```

    Note: Script must source json-response.sh for json_ok/json_err output:
    Add after strict mode: `source "$(dirname "$0")/lib/json-response.sh"`

    After writing the script:
    - `chmod +x .openclaw/agents/devbot/scripts/devbot-verify.sh`
    - Run it: `zsh .openclaw/agents/devbot/scripts/devbot-verify.sh`
    - If any checks fail, fix the underlying issue (e.g., missing file, auth gap) and re-run until all 7+1 pass
  </action>
  <verify>
    <automated>
      zsh -n /Users/trilogy/Documents/agentic-setup/.openclaw/agents/devbot/scripts/devbot-verify.sh &&
      test -x /Users/trilogy/Documents/agentic-setup/.openclaw/agents/devbot/scripts/devbot-verify.sh &&
      zsh /Users/trilogy/Documents/agentic-setup/.openclaw/agents/devbot/scripts/devbot-verify.sh 2>/dev/null | python3 -c "import json,sys; d=json.load(sys.stdin); assert d['ok']==True, f'verify failed: {d}'; print(f'All checks passed: {d[\"data\"][\"checks_passed\"]}')"
    </automated>
  </verify>
  <done>
    devbot-verify.sh exits 0 with {"ok":true,"data":{"checks_passed":8,"checks_failed":0,"phase":"07-devbot-core"}}. Test issue was created on anujj-ti/agentic-setup and immediately closed. All 7 structural checks passed.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| devbot-verify.sh → GitHub API | Creates and closes a real test issue — write side effect, limited to anujj-ti/agentic-setup |
| CONTEXT-TEMPLATE.md → agent context | File is loaded into DevBot's context window at session start — must contain no secrets |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-04-01 | Information Disclosure | CONTEXT.md files contain secrets | mitigate | Template explicitly lists forbidden content; SECURITY.md rule 1; template sections reference Keychain-only credential storage |
| T-07-04-02 | Tampering | Test issue not closed (left open, polluting issue tracker) | mitigate | verify.sh immediately closes the test issue after creating it; issue title prefixed with "[devbot-verify]" for easy manual identification |
| T-07-04-03 | Denial of Service | devbot-verify.sh runs on wrong repo | accept | Hardcoded to anujj-ti/agentic-setup for Phase 7; verify script is not a general-purpose tool |
| T-07-SC | Tampering | npm/pip/cargo installs | accept | No packages installed — pure zsh script |
</threat_model>

<verification>
Phase 7 is complete when all of these are true:

1. devbot-verify.sh exits 0:
   `zsh .openclaw/agents/devbot/scripts/devbot-verify.sh 2>/dev/null | python3 -c "import json,sys; d=json.load(sys.stdin); print('PHASE 7 GATE:', 'PASS' if d['ok'] else 'FAIL')"`

2. ROADMAP Phase 7 success criteria met:
   - SC1: DevBot creates issue with label/milestone/project (confirmed by end-to-end test in verify.sh)
   - SC2: PR review queue surfaces stale PRs and failing CI (confirmed by check 5 live run)
   - SC3: Per-repo context file exists and AGENTS.md loads it at session start (confirmed by checks 6+7)
   - SC4: All gh operations use /opt/homebrew/bin/gh 2.92.0 with JSON stdout (confirmed by checks 1-4)

3. CONTEXT-TEMPLATE.md exists:
   `test -f /Users/trilogy/.openclaw/workspace-devbot/repos/CONTEXT-TEMPLATE.md && echo "template exists"`

4. agentic-setup CONTEXT.md stub created:
   `test -f /Users/trilogy/.openclaw/workspace-devbot/repos/anujj-ti-agentic-setup/CONTEXT.md && echo "stub exists"`

5. Test issue (created by verify.sh) is closed:
   `gh issue list --repo anujj-ti/agentic-setup --state open --search "[devbot-verify]" | grep -c "." || echo "no open devbot-verify issues (good)"`
</verification>

<success_criteria>
- devbot-verify.sh passes all 8 checks (7 structural + 1 end-to-end) with {"ok":true}
- CONTEXT-TEMPLATE.md exists at ~/.openclaw/workspace-devbot/repos/CONTEXT-TEMPLATE.md with all required sections
- anujj-ti-agentic-setup/CONTEXT.md stub exists with known stack values
- Test issue created on anujj-ti/agentic-setup and closed within the same verify run
- Phase 7 ROADMAP success criteria 1-4 all met
</success_criteria>

<output>
Create `.planning/phases/07-devbot-core/07-04-SUMMARY.md` when done
</output>

---
phase: 12-self-evolution
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - ~/.openclaw/agents/task-orchestrator/MEMORY.md
  - ~/.openclaw/agents/task-orchestrator/DREAM-ROUTINE.md
autonomous: true
requirements:
  - EVOL-02

must_haves:
  truths:
    - "MEMORY.md contains a Pattern Counter section with the PRESERVE comment marker (per D-121)"
    - "The PRESERVE marker is in the exact format: <!-- PRESERVE: pattern_counter — do not distill -->"
    - "The pattern counter table has the correct columns: Pattern, Count, Last Seen, Trigger Fired"
    - "DREAM-ROUTINE.md contains an explicit instruction to preserve the Pattern Counter section verbatim"
    - "DREAM-ROUTINE.md instruction uses the phrase PRESERVE VERBATIM to match the MEMORY.md marker"
  artifacts:
    - path: "~/.openclaw/agents/task-orchestrator/MEMORY.md"
      provides: "Pattern counter section with PRESERVE marker and empty initial table"
      contains: "PRESERVE"
    - path: "~/.openclaw/agents/task-orchestrator/DREAM-ROUTINE.md"
      provides: "Updated distillation instruction: preserve Pattern Counter section"
  key_links:
    - from: "MEMORY.md Pattern Counter section"
      to: "DREAM-ROUTINE.md preservation instruction"
      via: "PRESERVE comment marker"
      pattern: "PRESERVE.*pattern_counter"
---

<objective>
Add the pattern counter infrastructure to the Task Orchestrator's operational memory. Update MEMORY.md with the Pattern Counter section (with PRESERVE comment per D-121) and update DREAM-ROUTINE.md to explicitly preserve this section during nightly distillation. Without both changes, pattern counters will be lost on the first nightly run.

Purpose: EVOL-02 requires pattern counts to survive session boundaries and nightly distillation. The PRESERVE marker is the mechanism; the DREAM-ROUTINE.md instruction is the enforcement.
Output: MEMORY.md with Pattern Counter section, DREAM-ROUTINE.md updated with preservation instruction.
</objective>

<execution_context>
@/Users/trilogy/Documents/agentic-setup/.claude/get-shit-done/workflows/execute-plan.md
@/Users/trilogy/Documents/agentic-setup/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/trilogy/Documents/agentic-setup/.planning/PROJECT.md
@/Users/trilogy/Documents/agentic-setup/.planning/ROADMAP.md
@/Users/trilogy/Documents/agentic-setup/.planning/phases/12-self-evolution/12-RESEARCH.md

D-121: Pattern counter in MEMORY.md with <!-- PRESERVE --> marker.

Pattern Counter structure (research Pattern 1):
  ## Pattern Counter
  <!-- PRESERVE: pattern_counter — do not distill -->

  | Pattern | Count | Last Seen | Trigger Fired? |
  |---------|-------|-----------|----------------|
  <!-- No entries initially — Task Orchestrator adds rows as patterns are observed -->

  <!-- Threshold: count >= 2 triggers Skill Creation agent session -->
  <!-- Naming convention: lowercase, hyphens, 2-4 words, procedure level (not parameters) -->

Two safeguards for dream routine preservation (research Pitfall 4):
  1. MEMORY.md: PRESERVE comment around the counter table
  2. DREAM-ROUTINE.md: explicit instruction "Section ## Pattern Counter: preserve verbatim — do not summarize"

MEMORY.md location: ~/.openclaw/agents/task-orchestrator/MEMORY.md (created in Phase 5)
DREAM-ROUTINE.md location: ~/.openclaw/agents/task-orchestrator/DREAM-ROUTINE.md (created in Phase 5)

Both files exist from Phase 5. This plan appends to them — does NOT overwrite.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Pattern Counter section to MEMORY.md and update DREAM-ROUTINE.md</name>
  <files>
    ~/.openclaw/agents/task-orchestrator/MEMORY.md
    ~/.openclaw/agents/task-orchestrator/DREAM-ROUTINE.md
  </files>
  <action>
Read the existing ~/.openclaw/agents/task-orchestrator/MEMORY.md to understand its current structure. Check whether a Pattern Counter section already exists — if it does, do not add a duplicate.

If no Pattern Counter section exists, append the following to the end of MEMORY.md (after any existing content):

---

## Pattern Counter
<!-- PRESERVE: pattern_counter — do not distill -->

| Pattern | Count | Last Seen | Trigger Fired? |
|---------|-------|-----------|----------------|

<!-- END: pattern_counter -->

<!-- Rules:
     - Threshold: count >= 2 triggers Skill Creation agent session
     - Naming: lowercase, hyphens, 2-4 words, procedure level (not parameters)
     - Before adding a new row: check for semantic equivalents first; increment existing row if match
     - Trigger Fired: set to "yes" after Skill Creation session is spawned for that pattern
-->

---

Then read the existing ~/.openclaw/agents/task-orchestrator/DREAM-ROUTINE.md.

Find the section that describes how MEMORY.md is processed during distillation (look for references to "distill", "compress", "MEMORY.md", or similar). Append or insert (before the closing of that section) the following explicit preservation instruction:

## Pattern Counter Preservation (EVOL-02 — MANDATORY)
The ## Pattern Counter section in MEMORY.md MUST be preserved verbatim during every distillation run.
- Do NOT summarize this section
- Do NOT compress the table rows
- Do NOT omit any row even if the count is 1
- Copy the entire section — from "## Pattern Counter" through "<!-- END: pattern_counter -->" — into the distilled MEMORY.md output unchanged
- This section is protected by the <!-- PRESERVE: pattern_counter --> marker

If the dream routine token budget would be exceeded by preserving this section: compress OTHER sections first. The Pattern Counter is the last section to be compressed.

After both files are updated, verify by reading back the relevant sections.
  </action>
  <verify>
    <automated>grep -c "PRESERVE.*pattern_counter" /Users/trilogy/.openclaw/agents/task-orchestrator/MEMORY.md && grep -c "PRESERVE VERBATIM\|preserve.*verbatim\|preserved verbatim" /Users/trilogy/.openclaw/agents/task-orchestrator/DREAM-ROUTINE.md</automated>
  </verify>
  <done>
    MEMORY.md contains the Pattern Counter section with <!-- PRESERVE: pattern_counter --> marker and the empty table with correct columns. DREAM-ROUTINE.md contains an explicit instruction to preserve the Pattern Counter section verbatim, with the section delimiters (## Pattern Counter through <!-- END: pattern_counter -->).
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Dream routine → MEMORY.md Pattern Counter | PRESERVE marker is the only protection against distillation loss |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-07 | Tampering | Dream routine ignoring PRESERVE marker | mitigate | Two-layer protection: MEMORY.md PRESERVE comment + DREAM-ROUTINE.md explicit instruction; both reference the same section delimiters |
| T-12-08 | Spoofing | Pattern names drifting across sessions (defeating threshold) | mitigate | SOUL.md (Plan 12-01) contains naming convention rule; MEMORY.md has inline naming rule comment |
| T-12-09 | Denial of Service | Pattern Counter growing beyond distillation budget | accept | DREAM-ROUTINE.md instruction specifies: compress OTHER sections first; Pattern Counter is last to compress |
</threat_model>

<verification>
1. grep -c "PRESERVE.*pattern_counter" ~/.openclaw/agents/task-orchestrator/MEMORY.md returns >= 1
2. grep -c "Pattern Counter" ~/.openclaw/agents/task-orchestrator/MEMORY.md returns >= 2 (section heading + preservation rules)
3. grep -c "Count.*Last Seen.*Trigger" ~/.openclaw/agents/task-orchestrator/MEMORY.md returns >= 1 (table header row present)
4. grep -ci "preserve.*verbatim\|verbatim.*preserve" ~/.openclaw/agents/task-orchestrator/DREAM-ROUTINE.md returns >= 1
5. grep -c "pattern_counter\|Pattern Counter" ~/.openclaw/agents/task-orchestrator/DREAM-ROUTINE.md returns >= 1
</verification>

<success_criteria>
- MEMORY.md: Pattern Counter section with PRESERVE comment, empty table (correct columns), naming rules as inline comments
- DREAM-ROUTINE.md: explicit verbatim preservation instruction for Pattern Counter section
- DREAM-ROUTINE.md: specifies "compress other sections first" priority
- Both files updated without overwriting existing content
</success_criteria>

<output>
Create .planning/phases/12-self-evolution/12-03-SUMMARY.md when done
</output>

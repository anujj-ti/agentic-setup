---
phase: 12-self-evolution
plan: 05
type: execute
wave: 3
depends_on:
  - 12-01
  - 12-02
  - 12-03
  - 12-04
files_modified:
  - /Users/trilogy/Documents/agentic-setup/scripts/verify-phase-12.sh
autonomous: true
requirements:
  - EVOL-01
  - EVOL-02
  - EVOL-03

must_haves:
  truths:
    - "verify-phase-12.sh confirms SOUL.md contains all three EVOL rules sections"
    - "verify-phase-12.sh confirms the /openclaw-new-agent enforcement gate: manually created agent dir is NOT in openclaw.json"
    - "verify-phase-12.sh confirms MEMORY.md has Pattern Counter section with PRESERVE marker"
    - "verify-phase-12.sh confirms DREAM-ROUTINE.md has verbatim preservation instruction"
    - "verify-phase-12.sh confirms experiment scripts exist, pass syntax/import checks, and validate correctly"
    - "All test artifacts created in /tmp (never in ~/.openclaw/ per research Pitfall 5)"
    - "verify-phase-12.sh exits 0"
  artifacts:
    - path: "/Users/trilogy/Documents/agentic-setup/scripts/verify-phase-12.sh"
      provides: "Phase 12 gate verification covering EVOL-01 (positive + negative), EVOL-02, EVOL-03"
---

<objective>
Create and run scripts/verify-phase-12.sh — the phase gate and milestone completion verification for Phase 12 (the final phase). The script runs all structural checks for EVOL-01, EVOL-02, and EVOL-03, plus the critical negative test for EVOL-01 enforcement: manually creating an agent directory does NOT register it in openclaw.json.

Purpose: Verify the complete self-evolution capability is correctly configured. Phase 12 being the final phase means this script also serves as a milestone checkpoint.
Output: verify-phase-12.sh that exits 0 with all checks passing.
</objective>

<execution_context>
@/Users/trilogy/Documents/agentic-setup/.claude/get-shit-done/workflows/execute-plan.md
@/Users/trilogy/Documents/agentic-setup/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/trilogy/Documents/agentic-setup/.planning/PROJECT.md
@/Users/trilogy/Documents/agentic-setup/.planning/ROADMAP.md
@/Users/trilogy/Documents/agentic-setup/.planning/phases/12-self-evolution/12-RESEARCH.md

EVOL-01 enforcement gate test (research Pattern 6 and D-122):
  Create a temp agent directory in /tmp (NOT in ~/.openclaw/ — Pitfall 5)
  Write a SOUL.md to it manually
  Check that this agent does NOT appear in ~/.openclaw/openclaw.json
  This proves: without /openclaw-new-agent (which runs stow and updates openclaw.json), manual creation is inert
  Cleanup with rm -rf /tmp/test-agent-* at the end

EVOL-02 checks:
  MEMORY.md contains Pattern Counter section with correct table columns
  MEMORY.md contains <!-- PRESERVE: pattern_counter --> marker
  DREAM-ROUTINE.md contains verbatim preservation instruction

EVOL-03 checks:
  propose-experiment.js exists and exits 1 without required args
  create-experiment-page.js exists and exits 1 without OPENCLAW_NOTION_EXPERIMENTS_DB_ID
  @notionhq/client importable in task-orchestrator scripts/
  check-agent-domain.sh exists and is executable

Milestone summary:
  Phase 12 is the final phase — the verify script should print a milestone completion summary at the end.

Research Pitfall 5: test artifacts MUST be in /tmp, never in ~/.openclaw/ or ~/Documents/agentic-setup/
  Use trap for cleanup to ensure temp files are always removed on exit
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create and run verify-phase-12.sh</name>
  <files>
    /Users/trilogy/Documents/agentic-setup/scripts/verify-phase-12.sh
  </files>
  <action>
Create scripts/verify-phase-12.sh with #!/usr/bin/env zsh and set -euo pipefail.

Set up cleanup: TMPDIR_CREATED="/tmp/verify-phase-12-$$" and trap 'rm -rf "$TMPDIR_CREATED"' EXIT INT TERM
mkdir -p "$TMPDIR_CREATED"

Declare FAILURES=() array. Define helper: check() { local desc="$1" result="$2"; [[ "$result" == "0" ]] && echo "PASS: $desc" || { echo "FAIL: $desc"; FAILURES+=("$desc") } }

SECTION 1 — EVOL-01 SOUL.md rules:
  grep -qc "EVOL-01" in Task Orchestrator SOUL.md (count >= 1)
  grep -q "NEVER create agent directive" in SOUL.md
  grep -q "openclaw-new-agent.*ONLY\|ONLY.*openclaw-new-agent" in SOUL.md
  grep -q "Decision Reviewer" in SOUL.md EVOL-01 section
  grep -q "Agent Routing" in SOUL.md (routing update step)

SECTION 2 — EVOL-01 enforcement gate (negative test per D-122):
  Create test dir: FAKE_AGENT_DIR="$TMPDIR_CREATED/test-agent-violation"
  mkdir -p "$FAKE_AGENT_DIR"
  Create: echo "# Test SOUL.md — created without /openclaw-new-agent" > "$FAKE_AGENT_DIR/SOUL.md"
  Check that this agent ID ("test-agent-violation" or the dir basename) does NOT appear in ~/.openclaw/openclaw.json:
    NOT_IN_CONFIG=$(jq '.agents.list[] | select(.id == "test-agent-violation")' "$HOME/.openclaw/openclaw.json" 2>/dev/null || echo "")
    [[ -z "$NOT_IN_CONFIG" ]] means PASS (manually created agent is inert — not in config)
  Use check function with result=$([[ -z "$NOT_IN_CONFIG" ]] && echo "0" || echo "1")
  Print explanation: "PASS: manually created agent dir is inert — not registered in openclaw.json (D-122)"

SECTION 3 — EVOL-02 pattern counter:
  grep -q "PRESERVE.*pattern_counter\|pattern_counter.*PRESERVE" "$HOME/.openclaw/agents/task-orchestrator/MEMORY.md"
  grep -q "Pattern Counter" "$HOME/.openclaw/agents/task-orchestrator/MEMORY.md"
  grep -q "Count.*Last Seen\|Last Seen.*Count" "$HOME/.openclaw/agents/task-orchestrator/MEMORY.md" (table header)
  grep -qi "preserve.*verbatim\|verbatim.*preserve" "$HOME/.openclaw/agents/task-orchestrator/DREAM-ROUTINE.md"

SECTION 4 — EVOL-01 agent proposal workflow:
  test -f "$HOME/.openclaw/agents/task-orchestrator/scripts/check-agent-domain.sh"
  zsh -n "$HOME/.openclaw/agents/task-orchestrator/scripts/check-agent-domain.sh" (syntax)
  ( zsh "$HOME/.openclaw/agents/task-orchestrator/scripts/check-agent-domain.sh" "devbot" 2>/dev/null | jq -r '.ok' | grep -q "false" ) — existing agent returns ok:false
  grep -q "New Agent Proposal\|Agent Proposal" "$HOME/.openclaw/agents/task-orchestrator/TOOLS.md"

SECTION 5 — EVOL-03 experiment scripts:
  test -f "$HOME/.openclaw/agents/task-orchestrator/scripts/propose-experiment.js"
  test -f "$HOME/.openclaw/agents/task-orchestrator/scripts/create-experiment-page.js"
  /opt/homebrew/opt/node@24/bin/node -e "require('@notionhq/client')" (from task-orchestrator scripts dir, using --experimental-specifier-resolution=none if needed)
  ( cd "$HOME/.openclaw/agents/task-orchestrator/scripts" && /opt/homebrew/opt/node@24/bin/node propose-experiment.js 2>/dev/null ); EXIT_CODE=$?; [[ $EXIT_CODE -ne 0 ]] means validation works
  grep -q "OPENCLAW_NOTION_EXPERIMENTS_DB_ID" "$HOME/.openclaw/agents/task-orchestrator/TOOLS.md"
  grep -q "Stage 1\|stage 1\|experiment.*Draft\|Draft.*experiment" "$HOME/.openclaw/agents/task-orchestrator/SOUL.md" (experiment page created before agents)

SECTION 6 — Cross-check: all five quality pipeline agents still present (Phase 11 dependency):
  For each of: code-reviewer, document-reviewer, decision-reviewer, skill-reviewer, skill-creation
    jq -e ".agents.list[] | select(.id == \"<id>\")" "$HOME/.openclaw/openclaw.json" > /dev/null

Final summary:
  Count_PASSED=$((TOTAL_CHECKS - ${#FAILURES[@]}))
  if [[ ${#FAILURES[@]} -eq 0 ]]; then
    echo ""
    echo "=== Phase 12 PASSED (all checks) ==="
    echo ""
    echo "=== MILESTONE: All 12 Phases Complete ==="
    echo "Personal AI Operations Hub — self-evolving agent fleet is fully operational."
    echo "Capabilities delivered:"
    echo "  - Infrastructure: OpenClaw + cc-openclaw + secrets pipeline + stow deploy"
    echo "  - Channels: Telegram (WhatsApp deferred)"
    echo "  - Orchestration: User Orchestrator + Task Orchestrator + Beads + dream routines"
    echo "  - Email: Gmail triage + morning standup"
    echo "  - DevBot: GitHub issues, PR queue, CI monitor, autonomous dev"
    echo "  - Notion: Decision log, reversibility, experiment logging"
    echo "  - Merge: Notion-gated autonomous squash merge with revert workflow"
    echo "  - Quality: 5-agent review pipeline (Code, Document, Decision, Skill, Creation)"
    echo "  - Self-Evolution: Agent proposals, pattern-triggered skill creation, experiment framework"
    exit 0
  else
    echo "=== Phase 12 FAILED: ${FAILURES[*]} ==="
    exit 1
  fi

chmod +x the script. Run: zsh scripts/verify-phase-12.sh
  </action>
  <verify>
    <automated>cd /Users/trilogy/Documents/agentic-setup && zsh scripts/verify-phase-12.sh</automated>
  </verify>
  <done>
    verify-phase-12.sh exits 0 with "Phase 12 PASSED" and "MILESTONE: All 12 Phases Complete" output. The EVOL-01 enforcement gate (negative test) confirms that manually created agent directories are not registered in openclaw.json. All test artifacts were created in /tmp and cleaned up by trap.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| verify script → ~/.openclaw and /tmp | Read-only for ~/.openclaw; creates/cleans test artifacts only in /tmp |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-13 | Tampering | verify script creating test agent in ~/.openclaw/ | mitigate | All test artifacts in $TMPDIR_CREATED (a /tmp subdir); trap cleans up on any exit |
| T-12-14 | Denial of Service | trap failure leaving /tmp artifacts | accept | /tmp is cleaned on system restart; /tmp/verify-phase-12-$$ is unique per PID; no lasting damage |
| T-12-15 | Repudiation | Milestone marker inaccurate | accept | Milestone summary is printed only when all checks pass; it accurately reflects what was verified |
</threat_model>

<verification>
zsh scripts/verify-phase-12.sh exits 0 with:
- "Phase 12 PASSED" in output
- "MILESTONE: All 12 Phases Complete" in output
- EVOL-01 enforcement gate: "manually created agent dir is inert" check passes
- All /tmp test artifacts cleaned up (trap fires on exit)
</verification>

<success_criteria>
- All structural checks pass for EVOL-01, EVOL-02, EVOL-03
- EVOL-01 negative gate test: manually created agent directory is NOT in openclaw.json (stow was never run)
- EVOL-02: MEMORY.md PRESERVE marker present, correct table columns, DREAM-ROUTINE.md verbatim instruction
- EVOL-03: both experiment scripts exist, fail correctly without required inputs, TOOLS.md documents full lifecycle
- Phase 11 agent dependency check: all 5 quality pipeline agents still in openclaw.json
- Milestone summary printed on success
- All /tmp test artifacts cleaned up on script exit
</success_criteria>

<output>
Create .planning/phases/12-self-evolution/12-05-SUMMARY.md when done
</output>

---
phase: 12-self-evolution
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - ~/.openclaw/agents/task-orchestrator/SOUL.md
autonomous: true
requirements:
  - EVOL-01
  - EVOL-02
  - EVOL-03

must_haves:
  truths:
    - "Task Orchestrator SOUL.md contains Self-Evolution Rules section with all three EVOL behaviors"
    - "EVOL-01 rule: NEVER create agent directive files manually; /openclaw-new-agent is the only permitted path"
    - "EVOL-02 rule: pattern repeat count >= 2 triggers Skill Creation; count 1 means 'watch this pattern'"
    - "EVOL-03 rule: experiment Notion page MUST be written as Draft before any agent is spawned"
    - "Pattern counter naming convention (lowercase, hyphens, 2-4 words) is in SOUL.md"
    - "Routing hint update requirement after new agent creation is in SOUL.md"
  artifacts:
    - path: "~/.openclaw/agents/task-orchestrator/SOUL.md"
      provides: "Self-evolution behavioral rules for all 3 EVOL capabilities"
      contains: "EVOL"
  key_links:
    - from: "SOUL.md EVOL-01 rule"
      to: "/openclaw-new-agent skill"
      via: "only permitted scaffolding path"
      pattern: "openclaw-new-agent"
    - from: "SOUL.md EVOL-02 rule"
      to: "MEMORY.md pattern_counter"
      via: "pattern count check on every task completion"
      pattern: "pattern_counter\|Pattern Counter"
---

<objective>
Add the Self-Evolution Rules section to the Task Orchestrator SOUL.md. This section encodes all three EVOL behavioral rules as mandatory directives: (1) agent creation only via /openclaw-new-agent after Decision Reviewer approval, (2) pattern-repeat threshold of 2 triggers Skill Creation, (3) experiment Notion page created before any agents are spawned.

Purpose: Per D-120, Phase 12 is entirely SOUL.md rule additions — no new infrastructure. This plan delivers the behavioral foundation for all three EVOL capabilities.
Output: Task Orchestrator SOUL.md with complete Self-Evolution Rules section.
</objective>

<execution_context>
@/Users/trilogy/Documents/agentic-setup/.claude/get-shit-done/workflows/execute-plan.md
@/Users/trilogy/Documents/agentic-setup/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/trilogy/Documents/agentic-setup/.planning/PROJECT.md
@/Users/trilogy/Documents/agentic-setup/.planning/ROADMAP.md
@/Users/trilogy/Documents/agentic-setup/.planning/phases/12-self-evolution/12-RESEARCH.md

D-120: Phase 12 is entirely SOUL.md rule additions to Task Orchestrator (no new infrastructure).

Key rules from research Pattern 4 (SOUL.md Additions for EVOL Rules):

EVOL-01 — Agent Creation:
  - NEVER create agent directive files manually (writing SOUL.md, IDENTITY.md etc. directly)
  - /openclaw-new-agent is the ONLY permitted path for agent scaffolding — no exceptions
  - Before invoking /openclaw-new-agent: (a) verify no existing agent covers the domain via jq on openclaw.json; (b) author a proposal document; (c) send to Decision Reviewer; (d) only invoke after {"verdict":"pass"}
  - After /openclaw-new-agent succeeds: update Task Orchestrator SOUL.md Agent Routing section with new agent ID and domain

EVOL-02 — Pattern Repeat:
  - After every task completion: identify the procedural pattern (2-4 word name, lowercase, hyphens)
  - Check MEMORY.md pattern_counter table for that pattern name
  - If count == 0 or absent: add with count = 1, last_seen = today, trigger_fired = no
  - If count == 1: increment to 2, set last_seen = today; add Beads subtask "trigger skill creation for pattern: <name>"
  - If count >= 2 and trigger_fired == yes: no action (Skill Creation already triggered)
  - Pattern naming convention: procedure level, not parameter level. "create-github-issue" and "create-labeled-github-issue" are the SAME pattern: "github-issue-create". Look up semantically equivalent entries before creating new rows.

EVOL-03 — Experiment Framework:
  - Experiment lifecycle has 4 mandatory stages IN ORDER:
    1. Write Notion experiment page (Draft) BEFORE spawning any agents
    2. Create Beads epic + subtasks for experiment execution
    3. Sub-agents execute; close tasks with factual evidence strings
    4. Write results to Notion page → send to Document Reviewer → if pass: mark Final
  - NEVER mark experiment page Final without Document Reviewer {"verdict":"pass"}
  - If experiment agents fail mid-run: experiment page remains Draft with partial results noted; do NOT mark Final

Pitfall 1 from research: pattern names drift across sessions → SOUL.md must instruct "check for semantically equivalent entry first; if exists, increment that row verbatim."

Pitfall 3 from research: new agent created but SOUL.md routing hint not updated → explicit rule required.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Append Self-Evolution Rules to Task Orchestrator SOUL.md</name>
  <files>
    ~/.openclaw/agents/task-orchestrator/SOUL.md
  </files>
  <action>
Read the existing ~/.openclaw/agents/task-orchestrator/SOUL.md to understand its current structure and any existing sections.

Append a new section "## Self-Evolution Rules (Phase 12 — EVOL-01, EVOL-02, EVOL-03)" containing the following three subsections, verbatim as behavioral directives:

### Agent Creation (EVOL-01)
- NEVER create agent directive files manually. Writing SOUL.md, IDENTITY.md, USER.md, AGENTS.md, TOOLS.md, or SECURITY.md for a new agent directly is prohibited.
- `/openclaw-new-agent` is the ONLY permitted path for agent scaffolding — no exceptions.
- Before invoking `/openclaw-new-agent`:
  (a) Verify no existing agent covers the domain: run jq -r '.agents.list[].id' "$HOME/.openclaw/openclaw.json" and check for semantic match
  (b) Author a proposal document (see TOOLS.md for proposal template)
  (c) Send proposal to Decision Reviewer via sessions_spawn: sessions_spawn("decision-reviewer", proposal_text)
  (d) Only invoke `/openclaw-new-agent` after Decision Reviewer returns {"verdict":"pass"}
- After `/openclaw-new-agent` succeeds: update THIS SOUL.md (Task Orchestrator SOUL.md) Agent Routing section with the new agent ID and its domain keywords. Without this update, the new agent will never receive delegations.

### Pattern Repeat (EVOL-02)
- After every task completion: identify the procedural pattern (2-4 words, lowercase, hyphens, e.g., "github-issue-create", "pr-description-format")
- Pattern granularity is PROCEDURE level, not PARAMETERS level. "create github issue" and "create labeled github issue" are the SAME pattern. When in doubt, use the broader name.
- Check MEMORY.md section "## Pattern Counter" for a row matching the pattern name.
- Pattern naming rule: BEFORE creating a new row, scan existing rows for semantic equivalents. If an equivalent exists, increment THAT row's count using the EXISTING row name verbatim. Do NOT create a new row.
- Update procedure:
  - If pattern absent: add row with Count=1, Last Seen=today (YYYY-MM-DD), Trigger Fired=no
  - If Count==1: increment to Count=2, Last Seen=today; add a Beads subtask to the current epic: "trigger skill creation for pattern: <pattern-name>"
  - If Count>=2 and Trigger Fired==yes: no action
- After triggering Skill Creation: update the MEMORY.md row to set Trigger Fired=yes

### Experiment Framework (EVOL-03)
- Experiment lifecycle MUST follow these 4 stages in this exact order:
  1. Write Notion experiment page with Status=Draft BEFORE spawning any agents. Required fields: Hypothesis (falsifiable statement), Method (enumerated steps), Success Criteria (measurable outcomes), Started (ISO8601 timestamp). Capture the Notion page ID.
  2. Create Beads epic for experiment execution. Embed the Notion page ID in the epic description.
  3. Sub-agents execute steps and close their tasks with factual evidence strings.
  4. After Beads epic closes: write Results section to the Notion experiment page. Send page content to Document Reviewer via sessions_spawn. If verdict pass: update Status to Final. If verdict reject: revise Results section and resubmit (max 3 cycles then BLOCKED).
- NEVER mark an experiment page Final without Document Reviewer {"verdict":"pass"}.
- If agents fail mid-experiment: write partial results to the Notion page; leave Status as Draft; log failure reason.
- Experiment page ID must be preserved in Task Orchestrator memory for the duration of the experiment (include in Beads epic metadata).
  </action>
  <verify>
    <automated>grep -c "EVOL-01\|EVOL-02\|EVOL-03" /Users/trilogy/.openclaw/agents/task-orchestrator/SOUL.md</automated>
  </verify>
  <done>
    SOUL.md contains all three EVOL rule sections. grep for EVOL-01, EVOL-02, EVOL-03 returns >= 3. The "NEVER" directive for manual agent file creation is present. The pattern naming convention rule is present. The 4-stage experiment lifecycle is present.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| SOUL.md rule → LLM agent behavior | Behavioral rules constrain Task Orchestrator reasoning; rules must be explicit enough to eliminate ambiguity |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-01 | Elevation of Privilege | Task Orchestrator creating agents without Decision Reviewer gate | mitigate | SOUL.md NEVER + explicit 4-step pre-requisite sequence before /openclaw-new-agent invocation |
| T-12-02 | Repudiation | Experiment page created after execution | mitigate | SOUL.md stage 1 is explicit: page MUST exist before any agent spawns |
| T-12-03 | Spoofing | Pattern name drift defeating count threshold | mitigate | SOUL.md naming rule: check for semantic equivalents first; use existing row name verbatim |
</threat_model>

<verification>
1. grep -c "EVOL-01" ~/.openclaw/agents/task-orchestrator/SOUL.md >= 1
2. grep -c "NEVER create agent directive" ~/.openclaw/agents/task-orchestrator/SOUL.md >= 1
3. grep -c "pattern_counter\|Pattern Counter\|pattern counter" ~/.openclaw/agents/task-orchestrator/SOUL.md >= 1
4. grep -c "openclaw-new-agent" ~/.openclaw/agents/task-orchestrator/SOUL.md >= 2 (mentioned in EVOL-01 twice)
5. grep -c "Draft" ~/.openclaw/agents/task-orchestrator/SOUL.md >= 1 (experiment page status rule)
</verification>

<success_criteria>
- SOUL.md Self-Evolution Rules section present with all three EVOL subsections
- EVOL-01: NEVER manual, /openclaw-new-agent only, Decision Reviewer gate, SOUL.md routing update after creation
- EVOL-02: threshold = 2, procedure-level naming, semantic equivalence check before new row, Beads subtask trigger
- EVOL-03: 4-stage experiment lifecycle in order, Document Reviewer gate before Final, partial-failure handling
</success_criteria>

<output>
Create .planning/phases/12-self-evolution/12-01-SUMMARY.md when done
</output>

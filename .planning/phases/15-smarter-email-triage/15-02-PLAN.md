---
phase: 15-smarter-email-triage
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - .openclaw/agents/email-triage/memory/noise-senders.md
  - .openclaw/agents/email-triage/memory/processed-ids.jsonl
autonomous: true
requirements:
  - TRIAGE-02
  - TRIAGE-04

must_haves:
  truths:
    - "memory/noise-senders.md exists with seeded common-noise patterns"
    - "memory/processed-ids.jsonl exists as a valid empty JSONL file (zero entries)"
    - "noise-senders.md format is one address or domain per line, no comments required"
  artifacts:
    - path: ".openclaw/agents/email-triage/memory/noise-senders.md"
      provides: "Known-noise sender list editable without stow redeploy"
      contains: "noreply@"
    - path: ".openclaw/agents/email-triage/memory/processed-ids.jsonl"
      provides: "Idempotency guard — processed message IDs log"
      min_lines: 0
  key_links:
    - from: "memory/noise-senders.md"
      to: "SOUL.md noise suppression rule"
      via: "Agent reads file at startup per AGENTS.md startup checklist (Plan 15-03)"
      pattern: "noise-senders.md"
    - from: "memory/processed-ids.jsonl"
      to: "SOUL.md idempotency guard"
      via: "Agent checks file before processing each messageId"
      pattern: "processed-ids.jsonl"
---

<objective>
Create the two memory seed files that Plans 15-01, 15-03, and 15-04 reference. These are pure file-creation tasks with no agent logic changes — they must exist before AGENTS.md startup checklist references them.

Purpose: D-155 requires noise-senders.md to be editable without stow redeploy — it lives in memory/, not in the agent directive files. D-162 requires processed-ids.jsonl to exist so the startup guard has a file to check even on first run.

Output:
- `.openclaw/agents/email-triage/memory/noise-senders.md` — seeded with common noise patterns from existing triage conventions
- `.openclaw/agents/email-triage/memory/processed-ids.jsonl` — valid empty file (zero JSON lines)
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/15-smarter-email-triage/15-CONTEXT.md
@.openclaw/agents/email-triage/SOUL.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create memory/noise-senders.md with seeded noise patterns</name>
  <files>.openclaw/agents/email-triage/memory/noise-senders.md</files>
  <action>
Create `.openclaw/agents/email-triage/memory/noise-senders.md` (D-155).

File format rules (encode these as a header comment in the file itself):
- One email address or domain suffix per line
- Lines starting with `#` are comments (ignored by agent)
- Domain suffix format: `@domain.com` or `@subdomain.domain.com`
- Full address format: `exact@address.com`
- Empty lines are ignored

Seed the file with these patterns derived from common noise sources in a developer's inbox:

```
# noise-senders.md — Known-noise sender list
# Format: one address or domain per line. Lines starting with # are ignored.
# Domain suffix: @domain.com  |  Full address: exact@address.com
# Edit this file directly — no stow redeploy needed (D-155)

# GitHub automated notifications
@notifications.github.com
noreply@github.com
notifications@github.com

# CI/CD systems
noreply@circleci.com
builds@heroku.com
no-reply@travis-ci.com
noreply@semaphoreci.com

# Generic no-reply patterns
noreply@
no-reply@
donotreply@
do-not-reply@

# Marketing and newsletters (common SaaS)
newsletter@
digest@
updates@
marketing@

# Monitoring and alerting systems
alerts@
pagerduty.com
opsgenie.com
statuspage.io

# Package registries and dependency bots
npm@npmjs.com
dependabot@github.com
renovate@whitesourcesoftware.com
```

Note: The domain patterns like `noreply@` (no domain) are prefix-match patterns — the agent should match any address starting with that string. This must be consistent with how SOUL.md instructs matching.
  </action>
  <verify>
    <automated>test -f /Users/trilogy/Documents/agentic-setup/.openclaw/agents/email-triage/memory/noise-senders.md && grep -c "noreply@" /Users/trilogy/Documents/agentic-setup/.openclaw/agents/email-triage/memory/noise-senders.md</automated>
  </verify>
  <done>File exists at the correct path; contains at least 5 seeded noise patterns; has the format comment header explaining the one-per-line rule</done>
</task>

<task type="auto">
  <name>Task 2: Create memory/processed-ids.jsonl as empty guard file</name>
  <files>.openclaw/agents/email-triage/memory/processed-ids.jsonl</files>
  <action>
Create `.openclaw/agents/email-triage/memory/processed-ids.jsonl` as an empty file (zero bytes or zero JSON lines) (D-162).

This is the idempotency log. The format when populated is one JSON object per line:
`{"id":"<messageId>","processedAt":"<ISO8601 timestamp>"}`

Example future entry (do NOT write this to the file — the file starts empty):
`{"id":"18f3a2b1c4d5e6f7","processedAt":"2026-05-22T04:00:00.000Z"}`

Rules encoded in SOUL.md (Plan 15-01) govern:
- How the agent checks this file before processing a messageId
- How the agent appends after successful processing
- Trim to last 500 entries per run (D-163)

The file itself is just an empty file. Create it with zero bytes. Do not write any content — an empty JSONL file is valid (zero records).

Also create the `memory/drafts/` directory by placing a `.gitkeep` file inside it so the directory is tracked by git.
  </action>
  <verify>
    <automated>test -f /Users/trilogy/Documents/agentic-setup/.openclaw/agents/email-triage/memory/processed-ids.jsonl && test -d /Users/trilogy/Documents/agentic-setup/.openclaw/agents/email-triage/memory/drafts && echo "OK"</automated>
  </verify>
  <done>processed-ids.jsonl exists at the correct path (zero bytes is correct); memory/drafts/ directory exists; both can be read without error by the agent startup checklist</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| memory/noise-senders.md → agent suppression logic | User-editable file; overly broad entries suppress legitimate email |
| memory/processed-ids.jsonl → idempotency guard | File must not be corrupted — invalid JSON lines cause guard to fail open |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-04 | Tampering | noise-senders.md content | accept | File is user-controlled on local disk; no network-facing exposure; worst case = suppress email the user wanted, which is recoverable by editing the file |
| T-15-05 | Tampering | processed-ids.jsonl corruption | mitigate | AGENTS.md (Plan 15-03) will add: on JSON parse error, log warning and treat file as empty (fail open = reprocess, not fail closed = skip) |
| T-15-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this plan — file creation only |
</threat_model>

<verification>
test -f /Users/trilogy/Documents/agentic-setup/.openclaw/agents/email-triage/memory/noise-senders.md
test -f /Users/trilogy/Documents/agentic-setup/.openclaw/agents/email-triage/memory/processed-ids.jsonl
test -d /Users/trilogy/Documents/agentic-setup/.openclaw/agents/email-triage/memory/drafts
grep -v "^#" /Users/trilogy/Documents/agentic-setup/.openclaw/agents/email-triage/memory/noise-senders.md | grep -v "^$" | wc -l
# Should show >= 5 non-comment, non-empty lines
</verification>

<success_criteria>
Both memory seed files exist at the correct paths under .openclaw/agents/email-triage/memory/. noise-senders.md has a format header and at least 5 seeded patterns covering GitHub notifications, CI systems, and generic noreply patterns. processed-ids.jsonl is valid (zero bytes). memory/drafts/ directory exists.
</success_criteria>

<output>
Create `.planning/phases/15-smarter-email-triage/15-02-SUMMARY.md` when done
</output>

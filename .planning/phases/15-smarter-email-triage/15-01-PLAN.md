---
phase: 15-smarter-email-triage
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .openclaw/agents/email-triage/SOUL.md
autonomous: true
requirements:
  - TRIAGE-01
  - TRIAGE-02
  - TRIAGE-03

must_haves:
  truths:
    - "Every triage run assigns a priority score 1-5 to each email"
    - "Score 5 = urgent+VIP or P0; score 1 = automated noise or known-noise sender"
    - "Action Required count is capped at 20% of total emails per run"
    - "Demotion from Action Required to FYI uses lowest priority_score as tiebreaker"
    - "pct_action_required metric is logged per run in triage-YYYY-MM-DD.md"
    - "Agent never calls gog gmail send from within the triage flow"
  artifacts:
    - path: ".openclaw/agents/email-triage/SOUL.md"
      provides: "Priority scoring rules, 20% cap rule, no-send rule"
      contains: "priority_score"
  key_links:
    - from: "SOUL.md priority scoring section"
      to: "memory/triage-YYYY-MM-DD.md table rows"
      via: "LLM categorization pass outputs score alongside category"
      pattern: "priority_score"
    - from: "SOUL.md 20% cap rule"
      to: "pct_action_required metric in triage log"
      via: "post-categorization demote loop"
      pattern: "pct_action_required"
---

<objective>
Add three sections to SOUL.md for the email-triage agent:
1. Priority Scoring Rules (D-151 through D-154) — score 1-5 with five weighted signals
2. Noise Suppression + 20% Cap Rule (D-155, D-156, D-157) — cap enforcement and logging
3. Draft No-Send Rule (D-160) — hard rule against auto-sending

Purpose: All three are LLM-prompt additions to the existing categorization pass. Zero new infrastructure. The agent already runs a categorization pass; scoring and cap enforcement run in the same pass.

Output: Updated SOUL.md with three new rule sections appended after existing Operational Rules.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/15-smarter-email-triage/15-CONTEXT.md
@.openclaw/agents/email-triage/SOUL.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Priority Scoring Rules to SOUL.md</name>
  <files>.openclaw/agents/email-triage/SOUL.md</files>
  <action>
Append a new section "## Priority Scoring (TRIAGE-01)" to SOUL.md after the existing "## Escalation Protocol" section.

The section must contain:

**Score assignment rules (D-151, D-152, D-153):**

Signals in descending weight order (apply top-down, first match wins the anchor, lower signals can only raise):
1. Sender is on VIP list in `memory/vip-senders.md` (if file exists; miss = skip)
2. Subject contains urgency keywords: urgent, ASAP, P0, "deadline today", critical
3. Email is a direct reply to Anuj (not automated — no X-Mailer, no List-Unsubscribe header present, reply-to is a human address)
4. Thread depth > 2 (escalating conversation)
5. Email body contains a direct question addressed to Anuj

Score mapping:
- Score 5: (signal 1 AND signal 2) OR subject/body contains "P0" — urgent + VIP or P0 incident
- Score 4: category is Action Required AND (signal 2 OR signal 4) — deadline or escalating thread
- Score 3: category is Action Required, no deadline signal — default for actionable items
- Score 2: category is FYI or informational with no question
- Score 1: category is Automated-Noise, Newsletter, or sender matches noise-senders.md

**Output requirement (D-154):**

In `memory/triage-YYYY-MM-DD.md`, every email entry is a table row with columns:
`| priority_score | category | sender | subject | summary |`

The table header line is:
`| priority_score | category | sender | subject | summary |`
`|---|---|---|---|---|`

Add this rule as a mandatory output format — SOUL.md must explicitly say "Every processed email MUST produce one table row. Missing rows are an error."
  </action>
  <verify>
    <automated>grep -c "priority_score" /Users/trilogy/Documents/agentic-setup/.openclaw/agents/email-triage/SOUL.md</automated>
  </verify>
  <done>SOUL.md contains "priority_score" in at least 3 locations (section header, score mapping, output format); score 1-5 mapping is unambiguous; five signals are listed in descending weight order</done>
</task>

<task type="auto">
  <name>Task 2: Add 20% Action Required Cap Rule and Draft No-Send Rule</name>
  <files>.openclaw/agents/email-triage/SOUL.md</files>
  <action>
Append two more sections to SOUL.md immediately after the Priority Scoring section added in Task 1.

**Section 1: "## Noise Suppression + 20% Cap (TRIAGE-02)" (D-155, D-156, D-157):**

Rule text (use this verbatim):

"After categorizing and scoring all emails in a run, apply the following post-categorization enforcement:

1. Skip any email whose sender matches an entry in `memory/noise-senders.md` (full address OR domain suffix). These emails are not logged in the triage table — they are silently suppressed. Count them separately as `suppressed_count`.

2. 20% Action Required cap: If (action_required_count / total_emails_after_suppression) > 0.20, demote the lowest-priority Action Required items (sorted by priority_score ascending, then by arrival time ascending as tiebreaker) to FYI category until the ratio is at or below 20%.

3. Log the following metrics at the bottom of `memory/triage-YYYY-MM-DD.md`:
   ```
   pct_action_required: N%
   suppressed_count: N
   demoted_count: N
   ```
   These three fields MUST always be present even if values are zero."

**Section 2: "## Draft Reply Rule (TRIAGE-03)" (D-160):**

Rule text (use this verbatim):

"NEVER call `gog gmail send` from within the triage flow. The triage agent's output for reply generation is a draft file only. Sending is always user-initiated.

For every email classified as Action Required (after cap enforcement), write a draft reply file to `memory/drafts/YYYY-MM-DD-<messageId>.md`. The file MUST start with `[DRAFT — NOT SENT]` on line 1. File body contains: To, Subject, and Body suggestion fields.

The `memory/triage-YYYY-MM-DD.md` summary MUST include a `drafts:` list section listing each draft filename created in that run. If no Action Required emails exist, emit `drafts: []`."
  </action>
  <verify>
    <automated>grep -c "pct_action_required" /Users/trilogy/Documents/agentic-setup/.openclaw/agents/email-triage/SOUL.md</automated>
  </verify>
  <done>SOUL.md contains "pct_action_required", "NEVER call", "[DRAFT — NOT SENT]", and "drafts:" — all four strings present; 20% threshold is explicit; no-send rule is an unconditional NEVER</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| email body → SOUL.md rules | Email content is untrusted — SOUL.md already has prompt injection guardrail; scoring rules treat email body as data, not instructions |
| noise-senders.md → agent behavior | File is user-editable; malformed entries (e.g., wildcard abuse) could suppress legitimate email — mitigated by domain-suffix matching only (no regex) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-01 | Tampering | email body via priority scoring | mitigate | Existing prompt injection guardrail in SOUL.md covers score signal extraction — body content never executed |
| T-15-02 | Information Disclosure | draft files in memory/drafts/ | accept | Files live on local disk under ~/.openclaw/; no network exposure; user owns the machine |
| T-15-03 | Denial of Service | noise-senders.md wildcard entries suppressing all mail | mitigate | Matching rule: domain suffix only (e.g., `@example.com`) — no regex, no glob; documents must say "one address or domain per line" |
| T-15-SC | Tampering | npm/pip/cargo installs | accept | No new package installs in this plan — SOUL.md edits only |
</threat_model>

<verification>
grep -c "priority_score" /Users/trilogy/Documents/agentic-setup/.openclaw/agents/email-triage/SOUL.md
grep -c "pct_action_required" /Users/trilogy/Documents/agentic-setup/.openclaw/agents/email-triage/SOUL.md
grep -c "NEVER call" /Users/trilogy/Documents/agentic-setup/.openclaw/agents/email-triage/SOUL.md
grep -c "\[DRAFT — NOT SENT\]" /Users/trilogy/Documents/agentic-setup/.openclaw/agents/email-triage/SOUL.md
grep -c "drafts:" /Users/trilogy/Documents/agentic-setup/.openclaw/agents/email-triage/SOUL.md
</verification>

<success_criteria>
SOUL.md contains all five new rule sections: priority scoring (1-5 mapping + 5 signals), noise-suppression cap (20% threshold, demotion logic), metrics logging (pct_action_required/suppressed_count/demoted_count), draft file rule ([DRAFT — NOT SENT] on line 1), and no-send NEVER rule. All D-151 through D-157 and D-160 decisions are fully encoded in prompt text with no ambiguity.
</success_criteria>

<output>
Create `.planning/phases/15-smarter-email-triage/15-01-SUMMARY.md` when done
</output>

---
phase: 04-beads-task-orchestrator
plan: "04"
type: execute
wave: 4
depends_on:
  - "04-03"
files_modified: []
autonomous: true
requirements:
  - ORCH-03
  - ORCH-04

must_haves:
  truths:
    - "A test Beads epic exists in the shared DB with at least 2 sequential subtasks"
    - "Sub-agent simulation: a subtask was claimed via bd update --claim"
    - "Sub-agent simulation: the claimed subtask was closed with a factual evidence string via bd close --reason"
    - "bd list --json confirms the closed subtask has status=closed"
    - "The second subtask was unblocked after the first was closed (bd ready --json shows it)"
    - "All 6 verify-phase-04.sh checks pass"
  artifacts:
    - path: ".planning/phases/04-beads-task-orchestrator/04-04-SUMMARY.md"
      provides: "Documented claim/close cycle output including task IDs and bd list --json result"
      min_lines: 50
  key_links:
    - from: "tskorch-<hash> (test epic)"
      to: "tskorch-<hash>.1 → tskorch-<hash>.2 (sequential subtasks)"
      via: "bd dep add or --deps flag"
      pattern: "tskorch-"
---

<objective>
Run the end-to-end Beads claim/close cycle: create a test epic with sequential subtasks, simulate the sub-agent claim/execute/close loop, verify dependency enforcement, and confirm all 6 smoke test checks pass.

Purpose: This is the phase gate. The prior plans wired the infrastructure; this plan proves it works as a system. The test epic demonstrates the exact workflow sub-agents will follow in Phase 5+. The SUMMARY captures task IDs and bd output as a concrete reference for future phases.

Output: Test epic closed in Beads DB; all 6 verify-phase-04.sh checks green; SUMMARY with full command output captured.
</objective>

<execution_context>
@/Users/trilogy/.claude/get-shit-done/workflows/execute-plan.md
@/Users/trilogy/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/trilogy/Documents/agentic-setup/.planning/PROJECT.md
@/Users/trilogy/Documents/agentic-setup/.planning/ROADMAP.md
@/Users/trilogy/Documents/agentic-setup/.planning/STATE.md
@/Users/trilogy/Documents/agentic-setup/.planning/phases/04-beads-task-orchestrator/04-CONTEXT.md
@/Users/trilogy/Documents/agentic-setup/.planning/phases/04-beads-task-orchestrator/04-RESEARCH.md
@/Users/trilogy/Documents/agentic-setup/.planning/phases/04-beads-task-orchestrator/04-03-SUMMARY.md

<interfaces>
<!-- bd CLI facts confirmed by RESEARCH.md §Pattern 3 and 4 live tests -->

Epic creation:
  BEADS_DIR="$HOME/.openclaw/beads" /opt/homebrew/opt/node@24/bin/bd create "<title>" -t epic -p 1 --json
  Returns: {"id":"tskorch-<hash>","title":"...","type":"epic",...}
  Capture ID: EPIC_ID=$(... | jq -r '.id')

Subtask creation with inline dependency:
  T1=$(BEADS_DIR=... bd create "Step 1" --parent "$EPIC_ID" --json | jq -r '.id')
  T2=$(BEADS_DIR=... bd create "Step 2" --parent "$EPIC_ID" --deps "$T1" --json | jq -r '.id')
  ID format: tskorch-<hash>.1, tskorch-<hash>.2

Dependency tree verification:
  BEADS_DIR=... bd dep tree "$EPIC_ID"
  Shows linear chain: epic → T1 → T2

Claim a task (sub-agent action):
  BEADS_DIR=... bd update "$T1" --claim
  Sets status to in_progress

Close with evidence (sub-agent action):
  BEADS_DIR=... bd close "$T1" --reason "Phase 4 verification: bd claim/close cycle confirmed; all 6 smoke tests passing"
  Sets status to closed; stores reason string

After T1 closed, T2 becomes ready:
  BEADS_DIR=... bd ready --json   # returns T2 (previously blocked)

List all tasks (status confirmation):
  BEADS_DIR=... bd list --json   # T1 shows status=closed, T2 shows status=open

BEADS_DIR canonical value (D-50): $HOME/.openclaw/beads
bd canonical path (D-51): /opt/homebrew/opt/node@24/bin/bd
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run end-to-end claim/close cycle with test epic</name>
  <files>none (Beads DB operations only)</files>
  <action>
Run the full claim/close cycle in the shared Beads DB, capturing all output for the SUMMARY. Use the BD and BEADS_DIR variables throughout:

  BD="/opt/homebrew/opt/node@24/bin/bd"
  export BEADS_DIR="$HOME/.openclaw/beads"

Step 1 — Create test epic:
  EPIC_ID=$(BEADS_DIR="$HOME/.openclaw/beads" "$BD" create "Phase 4 verification: claim/close cycle test" -t epic -p 1 --json | jq -r '.id')
  echo "Epic ID: $EPIC_ID"

Step 2 — Create two sequential subtasks:
  T1=$(BEADS_DIR="$HOME/.openclaw/beads" "$BD" create "Simulate sub-agent task 1: verify bd commands work" --parent "$EPIC_ID" --json | jq -r '.id')
  T2=$(BEADS_DIR="$HOME/.openclaw/beads" "$BD" create "Simulate sub-agent task 2: verify dependency unblocking" --parent "$EPIC_ID" --deps "$T1" --json | jq -r '.id')
  echo "T1: $T1  T2: $T2"

Step 3 — Verify dependency tree (orchestrator pre-spawn check):
  BEADS_DIR="$HOME/.openclaw/beads" "$BD" dep tree "$EPIC_ID"
  Expected: shows T2 blocked by T1

Step 4 — Verify only T1 is ready (critical pre-spawn assertion):
  BEADS_DIR="$HOME/.openclaw/beads" "$BD" ready --json
  Expected: array containing T1 only (T2 must NOT appear — it is blocked)
  If T2 appears: the dependency was not set correctly — do not proceed; re-create T2 with --deps "$T1"

Step 5 — Simulate sub-agent claim (bd update --claim):
  BEADS_DIR="$HOME/.openclaw/beads" "$BD" update "$T1" --claim
  Expected: T1 status changes to in_progress

Step 6 — Simulate sub-agent close with evidence (bd close --reason):
  BEADS_DIR="$HOME/.openclaw/beads" "$BD" close "$T1" \
    --reason "Phase 4 verification complete: dolt 2.0.x installed, bd 1.0.4 at /opt/homebrew/opt/node@24/bin/bd, BEADS_DIR=$HOME/.openclaw/beads initialized with embeddeddolt/, all 6 smoke checks passing"
  Expected: T1 status changes to closed

Step 7 — Verify T2 is now unblocked (dependency enforcement confirmation):
  BEADS_DIR="$HOME/.openclaw/beads" "$BD" ready --json
  Expected: array containing T2 (T1 is now closed, T2 is unblocked)
  If T2 does NOT appear: dependency resolution failed — check `bd dep tree "$EPIC_ID"` and report in SUMMARY

Step 8 — Confirm final state via bd list:
  BEADS_DIR="$HOME/.openclaw/beads" "$BD" list --json
  Expected: T1 has status=closed with reason string; T2 has status=open

Capture ALL command output (steps 1-8) verbatim in the SUMMARY. Include the actual epic ID and task IDs so future plans can reference the first real test run.

Note per D-58: do NOT delete the test epic. It serves as a reference example.
  </action>
  <verify>
    <automated>BEADS_DIR="$HOME/.openclaw/beads" /opt/homebrew/opt/node@24/bin/bd list --json | jq '[.[] | select(.status == "closed")] | length > 0'</automated>
  </verify>
  <done>
    - Test epic exists in the Beads DB (tskorch-<hash>)
    - T1 has status=closed with a factual evidence string in --reason
    - T2 has status=open and appears in `bd ready --json` (dependency unblocking confirmed)
    - All bd commands ran against BEADS_DIR=$HOME/.openclaw/beads without "database not found" errors
  </done>
</task>

<task type="auto">
  <name>Task 2: Run all 6 smoke checks and write phase SUMMARY</name>
  <files>none (smoke test + SUMMARY output)</files>
  <action>
Run the full verify-phase-04.sh smoke test and document results in the SUMMARY.

Step 1 — Run all 6 checks:
  zsh /Users/trilogy/Documents/agentic-setup/scripts/verify-phase-04.sh

Expected output (all 6 passing):
  [PASS] dolt-installed
  [PASS] bd-version
  [PASS] beads-dir-initialized
  [PASS] beads-dir-in-secrets-sh
  [PASS] bd-ready-works
  [PASS] soul-has-beads-rule
  Results: 6 passed, 0 failed, 0 warnings (of 6 total checks)
  {"ok":true,"data":{"checks_passed":6,"checks_total":6}}

If any check fails:
- "dolt-installed": run `brew install dolt` and retry
- "bd-version": the bd binary is not at /opt/homebrew/opt/node@24/bin/bd — run Plan 04-01 Task 1 again
- "beads-dir-initialized": BEADS_DIR directory missing embeddeddolt/ — run Plan 04-02 Task 1 again
- "beads-dir-in-secrets-sh": grep can't find BEADS_DIR in secrets.sh — check that Plan 04-02 Task 2 completed
- "bd-ready-works": BEADS_DIR env not set or bd errors — check gateway env with `grep BEADS_DIR ~/.openclaw/service-env/ai.openclaw.gateway.env`
- "soul-has-beads-rule": SOUL.md grep pattern not matching — check Plan 04-03 Task 1 completed

Step 2 — Write SUMMARY (04-04-SUMMARY.md) to the phase directory. The SUMMARY must include:
  - All command outputs from Task 1 (verbatim, including task IDs)
  - bd dep tree output showing the T1→T2 dependency
  - bd ready --json output at step 4 (T1 only) and step 7 (T2 only)
  - bd list --json showing T1 closed with reason string
  - verify-phase-04.sh output (all 6 checks)
  - ROADMAP Phase 4 Success Criteria checklist (4 items) with VERIFIED/PENDING status for each
  - Manual test note: ORCH-03 and ORCH-04 are structurally verified by the claim/close cycle above; no live sub-agent session is required to prove the Beads contract
  - Next phase pointer: Phase 5 — Dream Routines
  </action>
  <verify>
    <automated>zsh /Users/trilogy/Documents/agentic-setup/scripts/verify-phase-04.sh 2>/dev/null | jq '.ok'</automated>
  </verify>
  <done>
    - verify-phase-04.sh exits 0 with {"ok":true,"data":{"checks_passed":6,"checks_total":6}}
    - All 6 checks pass (no [FAIL] lines in stderr output)
    - 04-04-SUMMARY.md exists with verbatim bd command outputs, task IDs, and the ROADMAP success criteria checklist
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| orchestrator bd commands → shared Beads DB | All agents write and read the same BEADS_DIR database |
| sub-agent close reason → Beads DB | Evidence strings are stored as audit records, not executed |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-09 | Spoofing | Agent skips bd ready pre-spawn check | mitigate | SOUL.md mandates `bd ready --json` returns only T1 before spawn; test in this plan validates the constraint works |
| T-04-10 | Tampering | Wrong BEADS_DIR at runtime causes wrong DB | mitigate | Step 4 assertion: if T2 appears in `bd ready --json`, dependency was not set — plan halts and reports; gateway.env propagation verified in Plan 04-02 |
| T-04-11 | Repudiation | Vague close reasons lose handoff context | mitigate | Step 6 uses a specific multi-field reason string as a positive example; SOUL.md + TOOLS.md rules reinforce this |
| T-04-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this plan |
</threat_model>

<verification>
After Plan 04-04 completes:

```bash
# Confirm claim/close cycle recorded in DB
BEADS_DIR="$HOME/.openclaw/beads" /opt/homebrew/opt/node@24/bin/bd list --json \
  | jq '[.[] | select(.status == "closed")] | length'
# Expected: 1 (or more if test was re-run)

# Confirm T2 unblocked and waiting
BEADS_DIR="$HOME/.openclaw/beads" /opt/homebrew/opt/node@24/bin/bd ready --json
# Expected: array with T2 (Phase 4 verification task 2)

# Full smoke test — all 6 checks pass
zsh /Users/trilogy/Documents/agentic-setup/scripts/verify-phase-04.sh
# Expected: {"ok":true,"data":{"checks_passed":6,"checks_total":6}}
```
</verification>

<success_criteria>
- Full claim/close cycle demonstrated: epic created → T1 claimed → T1 closed with evidence → T2 unblocked
- `bd ready --json` returned T1 before T1 was closed and T2 after T1 was closed (dependency enforcement confirmed)
- All 6 verify-phase-04.sh checks pass (exit 0, ok: true, checks_passed: 6)
- ROADMAP Phase 4 success criteria SC#1 through SC#4 all marked VERIFIED in SUMMARY
- SUMMARY contains verbatim bd output including actual task IDs (tskorch-<hash>) for future reference
</success_criteria>

<output>
Create `/Users/trilogy/Documents/agentic-setup/.planning/phases/04-beads-task-orchestrator/04-04-SUMMARY.md` when done.
</output>

---
phase: 04-beads-task-orchestrator
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/verify-phase-04.sh
autonomous: true
requirements:
  - INFRA-05

must_haves:
  truths:
    - "dolt 2.0.4 (or later) is installed and on PATH via Homebrew"
    - "bd 1.0.4 is installed under /opt/homebrew/opt/node@24/bin/bd"
    - "Running bd --version from the Homebrew node@24 explicit path returns 1.0.4"
    - "scripts/verify-phase-04.sh exists and is executable"
  artifacts:
    - path: "scripts/verify-phase-04.sh"
      provides: "6-check Phase 4 smoke test covering INFRA-05, ORCH-03, ORCH-04"
      min_lines: 40
  key_links:
    - from: "/opt/homebrew/opt/node@24/bin/npm"
      to: "/opt/homebrew/opt/node@24/bin/bd"
      via: "npm install -g @beads/bd@1.0.4"
      pattern: "bd.*1\\.0\\.4"
---

<objective>
Install dolt (Homebrew) and bd 1.0.4 (under Homebrew node@24), then create the Phase 4 smoke test script.

Purpose: Establish the two CLI tools Phase 4 depends on. dolt must be present before bd init; bd must install under Homebrew node@24 (not nvm node@22) to match the gateway's PATH. The verify script created here is used in Plan 04-04's end-to-end check.

Output: dolt binary on PATH via Homebrew; bd 1.0.4 at /opt/homebrew/opt/node@24/bin/bd; scripts/verify-phase-04.sh committed.
</objective>

<execution_context>
@/Users/trilogy/.claude/get-shit-done/workflows/execute-plan.md
@/Users/trilogy/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/trilogy/Documents/agentic-setup/.planning/PROJECT.md
@/Users/trilogy/Documents/agentic-setup/.planning/ROADMAP.md
@/Users/trilogy/Documents/agentic-setup/.planning/STATE.md
@/Users/trilogy/Documents/agentic-setup/.planning/phases/04-beads-task-orchestrator/04-CONTEXT.md
@/Users/trilogy/Documents/agentic-setup/.planning/phases/04-beads-task-orchestrator/04-RESEARCH.md

<interfaces>
<!-- Key paths and conventions from Phase 3 that this plan builds on. -->

From scripts/stow-deploy.sh (Phase 2 pattern):
  - zsh scripts/stow-deploy.sh  — only valid deploy entry point; rm -f jobs.json before stow

From .openclaw/scripts/openclaw-secrets.sh (current state):
  - export PATH="/opt/homebrew/opt/node@24/bin:$PATH"  — already present; node@24 is the gateway runtime

From Phase 3 TOOLS.md conventions:
  - Explicit binary path: /opt/homebrew/opt/node@24/bin/bd  (never rely on $PATH for bd in scripts)
  - Shell scripts: #!/usr/bin/env zsh + set -euo pipefail
  - JSON response shape: { "ok": true, "data": {...} } or { "ok": false, "error": "..." }

Node@24 npm path (per D-51):
  /opt/homebrew/opt/node@24/bin/npm  — use for all npm install operations in this phase
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install dolt via Homebrew and bd 1.0.4 under node@24</name>
  <files>none (tool installs, no repo files changed)</files>
  <action>
Run the following two install commands in sequence. dolt MUST complete before bd install.

Step 1 — Install dolt:
  brew install dolt

Verify dolt installed:
  dolt version
  Expected output contains "dolt version 2.0" or later.

Step 2 — Install bd 1.0.4 under Homebrew node@24 (per D-51 and D-52):
  /opt/homebrew/opt/node@24/bin/npm install -g @beads/bd@1.0.4

The postinstall script downloads a native binary from github.com/gastownhall/beads releases. This is expected — wait for it to complete before proceeding.

Verify bd installed at the correct path:
  /opt/homebrew/opt/node@24/bin/bd --version
  Expected output: "bd version 1.0.4" (or bd 1.0.4 — exact format from the CLI).

Do NOT use `npm install -g @beads/bd` without the explicit npm path — nvm node@22's npm would install to the wrong location.
Do NOT uninstall bd 1.0.3 from nvm — it is harmless and removing it could break nvm-based sessions.
  </action>
  <verify>
    <automated>brew list dolt &>/dev/null && echo "dolt: OK" && /opt/homebrew/opt/node@24/bin/bd --version 2>&1 | grep -q "1.0.4" && echo "bd 1.0.4: OK"</automated>
  </verify>
  <done>
    - `dolt version` exits 0 with version 2.0.x or later
    - `/opt/homebrew/opt/node@24/bin/bd --version` returns a string containing "1.0.4"
    - `which bd` in an nvm shell may still show the nvm path — this is acceptable; what matters is the explicit path works
  </done>
</task>

<task type="auto">
  <name>Task 2: Create scripts/verify-phase-04.sh (6-check smoke test)</name>
  <files>scripts/verify-phase-04.sh</files>
  <action>
Create scripts/verify-phase-04.sh. This script runs 6 checks covering INFRA-05, ORCH-03, and ORCH-04. It follows the same structure as scripts/verify-phase-03.sh (JSON stdout, human-readable stderr, set -euo pipefail).

The script must:
1. Define BD="/opt/homebrew/opt/node@24/bin/bd" and BEADS_DIR_PATH="$HOME/.openclaw/beads" at the top (per D-50, D-51).
2. Define a check() helper function that prints [PASS] or [FAIL] to stderr and accumulates counts.
3. Run these 6 checks in order:
   - "dolt-installed": `brew list dolt &>/dev/null`
   - "bd-version": `"$BD" --version 2>&1 | grep -q "1.0.4"`
   - "beads-dir-initialized": `[[ -d "$BEADS_DIR_PATH/embeddeddolt" ]]`
   - "beads-dir-in-secrets-sh": `grep -v '^#' "$HOME/.openclaw/scripts/openclaw-secrets.sh" | grep -q "BEADS_DIR"`
   - "bd-ready-works": `BEADS_DIR="$BEADS_DIR_PATH" "$BD" ready --json >/dev/null 2>&1`
   - "soul-has-beads-rule": `grep -q "BEFORE.*sessions_spawn\|sessions_spawn.*MUST\|sessions_spawn.*Beads\|Beads.*sessions_spawn" "$HOME/.openclaw/agents/task-orchestrator/SOUL.md"`

4. Print a results summary line (e.g., "Results: N passed, M failed, 0 warnings (of 6 total checks)").
5. Output JSON to stdout: `{"ok":true,"data":{"checks_passed":N,"checks_total":6}}` or `{"ok":false,...}` with non-zero exit on any failure.

Checks 3-6 will fail in Wave 1 (BEADS_DIR not yet initialized, secrets not yet updated, SOUL.md not yet updated) — this is expected. The script is created now so it exists for Plans 04-02 and 04-04 to run progressively.

Make the file executable: `chmod +x scripts/verify-phase-04.sh`

Shebang: `#!/usr/bin/env zsh`
Strict mode: `set -euo pipefail`
  </action>
  <verify>
    <automated>zsh /Users/trilogy/Documents/agentic-setup/scripts/verify-phase-04.sh; echo "Exit: $?"</automated>
  </verify>
  <done>
    - scripts/verify-phase-04.sh exists and is executable (chmod +x applied)
    - Script runs without zsh syntax errors
    - Checks 1-2 (dolt-installed, bd-version) pass
    - Checks 3-6 fail gracefully (not crash) with [FAIL] label — expected at this wave
    - Script outputs valid JSON to stdout (parseable by `jq .ok`)
    - Script exits non-zero if any check fails (will be 0 only after all 6 checks pass in Plan 04-04)
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| npm registry → local machine | @beads/bd postinstall downloads native binary from GitHub releases |
| Homebrew → local machine | dolt formula downloads from dolthub.com distribution |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01 | Tampering | @beads/bd npm postinstall | accept | Package is CLAUDE.md-mandated; confirmed on npm registry as github.com/gastownhall/beads; postinstall downloads from tagged release URL — not arbitrary code injection |
| T-04-02 | Tampering | dolt Homebrew formula | accept | DoltHub is Apache-2.0, 17k+ GitHub stars, referenced in CLAUDE.md; Homebrew verifies SHA256 of downloads |
| T-04-SC | Tampering | npm/pip/cargo installs | mitigate | Package Legitimacy Audit in 04-RESEARCH.md shows both packages as [ASSUMED] with manual approval — CLAUDE.md authority supersedes slopcheck for @beads/bd; slopcheck output reviewed before install |
</threat_model>

<verification>
After Plan 04-01 completes:

```bash
# Confirm dolt installed
brew list dolt

# Confirm bd 1.0.4 at the correct explicit path
/opt/homebrew/opt/node@24/bin/bd --version

# Run the smoke test (checks 1-2 should pass, 3-6 expected to fail)
zsh /Users/trilogy/Documents/agentic-setup/scripts/verify-phase-04.sh
```
</verification>

<success_criteria>
- dolt is installed via Homebrew (`brew list dolt` exits 0)
- `/opt/homebrew/opt/node@24/bin/bd --version` outputs a string containing "1.0.4"
- `scripts/verify-phase-04.sh` exists, is executable, runs without syntax errors, and checks 1-2 pass
- No bd binary was installed under nvm or any path other than /opt/homebrew/opt/node@24/bin/
</success_criteria>

<output>
Create `/Users/trilogy/Documents/agentic-setup/.planning/phases/04-beads-task-orchestrator/04-01-SUMMARY.md` when done.
</output>

---
phase: 04-beads-task-orchestrator
plan: "03"
type: execute
wave: 3
depends_on:
  - "04-02"
files_modified:
  - .openclaw/agents/task-orchestrator/SOUL.md
  - .openclaw/agents/task-orchestrator/TOOLS.md
autonomous: true
requirements:
  - ORCH-03
  - ORCH-04

must_haves:
  truths:
    - "Task Orchestrator SOUL.md contains the Beads-Enforced Execution Contract section (mandatory epic-before-spawn rule)"
    - "SOUL.md no longer contains the Phase 3 Beads-free stub language ('do NOT attempt bd or beads commands')"
    - "SOUL.md specifies the explicit bd binary path /opt/homebrew/opt/node@24/bin/bd"
    - "SOUL.md specifies BEADS_DIR as $HOME/.openclaw/beads"
    - "TOOLS.md contains the Beads Task Tracker section with orchestrator and sub-agent command reference"
    - "TOOLS.md removes the Phase 3 NOT Available exclusions for sessions_spawn and bd"
  artifacts:
    - path: ".openclaw/agents/task-orchestrator/SOUL.md"
      provides: "Beads-enforced execution contract for Task Orchestrator"
      contains: "sessions_spawn"
    - path: ".openclaw/agents/task-orchestrator/TOOLS.md"
      provides: "bd command reference for orchestrator and sub-agents"
      contains: "bd ready --json"
  key_links:
    - from: ".openclaw/agents/task-orchestrator/SOUL.md"
      to: "/opt/homebrew/opt/node@24/bin/bd"
      via: "agent executes bd commands per SOUL.md rules"
      pattern: "/opt/homebrew/opt/node@24/bin/bd"
---

<objective>
Replace the Task Orchestrator's Phase 3 stub SOUL.md with the full Beads-enforced execution contract, and update TOOLS.md with the bd command reference.

Purpose: The Phase 3 stub explicitly prohibits bd commands. Phase 4 inverts this — the epic-before-spawn rule is now MANDATORY with no exceptions. Without this update, the Task Orchestrator would attempt to spawn sub-agents with free-text instructions instead of Beads task graphs, defeating the structural enforcement that Beads provides.

Output: SOUL.md with Beads contract replacing Phase 3 stub; TOOLS.md with bd section added and Phase 3 exclusions removed.
</objective>

<execution_context>
@/Users/trilogy/.claude/get-shit-done/workflows/execute-plan.md
@/Users/trilogy/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/trilogy/Documents/agentic-setup/.planning/PROJECT.md
@/Users/trilogy/Documents/agentic-setup/.planning/ROADMAP.md
@/Users/trilogy/Documents/agentic-setup/.planning/STATE.md
@/Users/trilogy/Documents/agentic-setup/.planning/phases/04-beads-task-orchestrator/04-CONTEXT.md
@/Users/trilogy/Documents/agentic-setup/.planning/phases/04-beads-task-orchestrator/04-RESEARCH.md
@/Users/trilogy/Documents/agentic-setup/.planning/phases/04-beads-task-orchestrator/04-02-SUMMARY.md

<interfaces>
<!-- Current state of the files being replaced — read these before writing -->

.openclaw/agents/task-orchestrator/SOUL.md (current, Phase 3 stub):
  - Contains "Phase 3 Scope (Beads not yet installed)" section
  - "do NOT attempt bd or beads commands"
  - "Do not spawn sub-agents in Phase 3"
  - Model policy: anthropic/claude-sonnet-4-6 — PRESERVE this

.openclaw/agents/task-orchestrator/TOOLS.md (current):
  - "NOT Available in Phase 3" section listing sessions_spawn and bd as excluded
  - exec, read/write, GitHub CLI tools listed
  - Binary paths: /opt/homebrew/bin/gh, /opt/homebrew/opt/node@24/bin/node
  - OpenClaw gateway URL: http://localhost:18789 — PRESERVE this

RESEARCH.md §Code Examples provides:
  - Full SOUL.md replacement template (## Beads-Enforced Execution Contract section)
  - TOOLS.md Beads section template
  Both templates are the authoritative source — use them as the basis for the writes.

BEADS_DIR for Phase 4 is $HOME/.openclaw/beads (D-50), NOT the RESEARCH.md example path.
All SOUL.md/TOOLS.md references to BEADS_DIR must use: $HOME/.openclaw/beads
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace Task Orchestrator SOUL.md with Beads execution contract</name>
  <files>.openclaw/agents/task-orchestrator/SOUL.md</files>
  <action>
Write a complete replacement for .openclaw/agents/task-orchestrator/SOUL.md. The file must be written in full — do not attempt to patch the Phase 3 stub.

The replacement SOUL.md must contain these sections in this order:

1. `# SOUL.md — Task Orchestrator`

2. `## Identity`
   Content: "You are the Task Orchestrator for Anuj's Personal AI Operations Hub. You receive delegated tasks from the User Orchestrator and decompose them into Beads task graphs before spawning execution-tier sub-agents."
   (Remove the Phase 3 "execute them using available tools" framing — the Phase 4 framing is decompose-first.)

3. `## Beads-Enforced Execution Contract (MANDATORY — NO EXCEPTIONS)`
   This is the critical section. Content must include:
   - "Before spawning any sub-agent via sessions_spawn, you MUST:" followed by numbered steps:
     1. Create a Beads epic: `BEADS_DIR="$HOME/.openclaw/beads" /opt/homebrew/opt/node@24/bin/bd create "<description>" -t epic -p 1 --json`
     2. Create all subtasks under the epic with `--parent <epic-id>` and inline `--deps` for sequential ordering
     3. Verify with `BEADS_DIR="$HOME/.openclaw/beads" /opt/homebrew/opt/node@24/bin/bd dep tree <epic-id>`
     4. Confirm only the first task is ready: `BEADS_DIR="$HOME/.openclaw/beads" /opt/homebrew/opt/node@24/bin/bd ready --json` must return exactly subtask .1
   - "Only after the complete graph is committed to Beads may you run sessions_spawn."
   - "The sub-agent's only instruction is: 'Your tasks are in Beads. Run `bd ready --json` to start.'"
   - "Do NOT give sub-agents free-text task descriptions as a substitute for Beads task graphs."

4. `## Decomposition Templates`
   Feature implementation (5 subtasks): Design proposal → Implementation (blocked by 1) → Self-review (blocked by 2) → QA evidence (blocked by 3) → Open PR (blocked by 4)
   Bug fix (4 subtasks): Reproduce with evidence → Fix (blocked by 1) → Verify fix (blocked by 2) → Open PR (blocked by 3)

5. `## Progress Monitoring`
   Content: Monitor via graph queries, NOT by spawning status-check sessions. Include the three commands: `bd list --status in_progress --json`, `bd ready --json`, `bd dep tree <epic-id>`. Include the >30 minute rule: if in_progress for >30 minutes without a close, investigate — do NOT poll the agent.

6. `## Responsibilities`
   - Receive delegated tasks from User Orchestrator via sessions_spawn
   - Decompose every task into a Beads epic + subtasks before ANY execution starts
   - Spawn sub-agents only after the complete graph is committed to Beads
   - Monitor progress via Beads graph queries on heartbeat cycle
   - Report completion to User Orchestrator when epic is fully closed

7. `## Operational Rules`
   - NEVER start executing without first stating the decomposition plan
   - NEVER spawn a sub-agent without a complete, dependency-ordered Beads graph
   - Use deterministic scripts (set -euo pipefail, JSON stdout) for all tool operations
   - Log every autonomous action before executing it (Notion logging is Phase 9)
   - On BLOCKED: update task status, describe the blocker, return control

8. `## Boundaries`
   - No direct Telegram channel — receive and respond only via agent session
   - No user-facing messages — output goes to User Orchestrator, not directly to Anuj
   - BEADS_DIR is always $HOME/.openclaw/beads
   - Use explicit bd path: /opt/homebrew/opt/node@24/bin/bd

9. `## Tone`
   - Structured and factual — output is parsed by the User Orchestrator
   - Report results as factual evidence strings, not narrative summaries
   - No preamble — status first, then facts

10. `## Model Policy`
    - Primary: anthropic/claude-sonnet-4-6
    - Never change model without Anuj's explicit instruction

Note: The "Phase 3 Scope (Beads not yet installed)" section is intentionally omitted. Do not include it. Per D-56, it is fully replaced by section 3 above.
  </action>
  <verify>
    <automated>grep -q "sessions_spawn" /Users/trilogy/Documents/agentic-setup/.openclaw/agents/task-orchestrator/SOUL.md && grep -q "/opt/homebrew/opt/node@24/bin/bd" /Users/trilogy/Documents/agentic-setup/.openclaw/agents/task-orchestrator/SOUL.md && grep -qv "Phase 3 Scope" /Users/trilogy/Documents/agentic-setup/.openclaw/agents/task-orchestrator/SOUL.md && echo "SOUL.md: OK"</automated>
  </verify>
  <done>
    - SOUL.md contains "Beads-Enforced Execution Contract" section (or equivalent heading)
    - SOUL.md contains sessions_spawn in the context of "before spawning"
    - SOUL.md contains the explicit bd path /opt/homebrew/opt/node@24/bin/bd
    - SOUL.md contains BEADS_DIR "$HOME/.openclaw/beads"
    - SOUL.md does NOT contain "do NOT attempt bd" or "Phase 3 Scope" language
    - SOUL.md preserves "anthropic/claude-sonnet-4-6" model policy
  </done>
</task>

<task type="auto">
  <name>Task 2: Update TOOLS.md with Beads command reference</name>
  <files>.openclaw/agents/task-orchestrator/TOOLS.md</files>
  <action>
Write a complete replacement for .openclaw/agents/task-orchestrator/TOOLS.md. The Phase 3 "NOT Available" exclusions for sessions_spawn and bd must be removed; the Beads command reference must be added.

The replacement TOOLS.md must contain:

1. `# TOOLS.md — Task Orchestrator`

2. `## Available Tools`
   - exec: run shell commands (use for bd, gh CLI, git, scripts)
   - read/write: file operations within workspace
   - sessions_spawn: spawn execution-tier sub-agents (Phase 4+)
   - GitHub CLI (gh 2.92.0): issue/PR operations — use gh, not curl
   - Beads CLI (bd 1.0.4): task graph creation and monitoring

3. `## Tool Policy`
   - All shell commands: #!/usr/bin/env zsh + set -euo pipefail
   - stdout = JSON only for deterministic scripts; stderr = human logs
   - Use /opt/homebrew/bin/gh for GitHub operations (explicit path)
   - Use /opt/homebrew/opt/node@24/bin/node for Node.js (explicit path, nvm shadowing)
   - Use /opt/homebrew/opt/node@24/bin/bd for Beads (explicit path, nvm shadowing)
   - Always set BEADS_DIR="$HOME/.openclaw/beads" before any bd command (or rely on gateway env inheritance)

4. `## Beads Task Tracker (Phase 4+)`
   This is the new section. Per D-57, include:

   Subsection `### Configuration`:
   - BEADS_DIR: $HOME/.openclaw/beads
   - bd binary: /opt/homebrew/opt/node@24/bin/bd

   Subsection `### Task Orchestrator Commands`:
   Under heading `#### Create epic + subtasks`:
     - Create epic: `EPIC=$(BEADS_DIR="$HOME/.openclaw/beads" /opt/homebrew/opt/node@24/bin/bd create "<description>" -t epic -p 1 --json | jq -r '.id')`
     - Create subtask 1: `T1=$(BEADS_DIR="$HOME/.openclaw/beads" /opt/homebrew/opt/node@24/bin/bd create "Step 1" --parent "$EPIC" --json | jq -r '.id')`
     - Create subtask 2 with dep: `T2=$(BEADS_DIR="$HOME/.openclaw/beads" /opt/homebrew/opt/node@24/bin/bd create "Step 2" --parent "$EPIC" --deps "$T1" --json | jq -r '.id')`
     - Verify graph: `BEADS_DIR="$HOME/.openclaw/beads" /opt/homebrew/opt/node@24/bin/bd dep tree "$EPIC"`
     - Confirm first task ready: `BEADS_DIR="$HOME/.openclaw/beads" /opt/homebrew/opt/node@24/bin/bd ready --json   # Should show only T1`

   Under heading `#### Monitor progress`:
     - `BEADS_DIR="$HOME/.openclaw/beads" /opt/homebrew/opt/node@24/bin/bd list --status in_progress --json`
     - `BEADS_DIR="$HOME/.openclaw/beads" /opt/homebrew/opt/node@24/bin/bd ready --json`
     - `BEADS_DIR="$HOME/.openclaw/beads" /opt/homebrew/opt/node@24/bin/bd dep tree <epic-id>`

   Subsection `### Sub-Agent Commands` (for sub-agent TOOLS.md reference):
   Note: When creating sub-agent directive files, their TOOLS.md must include these commands.
     - Find work: `BEADS_DIR="$HOME/.openclaw/beads" /opt/homebrew/opt/node@24/bin/bd ready --json`
     - Claim task: `BEADS_DIR="$HOME/.openclaw/beads" /opt/homebrew/opt/node@24/bin/bd update <task-id> --claim`
     - Close with evidence: `BEADS_DIR="$HOME/.openclaw/beads" /opt/homebrew/opt/node@24/bin/bd close <task-id> --reason "Dev server on port 3000; migrations: 42 applied"`

   Subsection `### Rules`:
   - NEVER use `bd list --status open` to find work — use `bd ready --json`
   - NEVER close without completing. The reason string is proof of work.
   - NEVER use vague reasons. Be specific: ports, filenames, counts, test results.
   - If BLOCKED: update status, describe the exact missing piece, do not invent workarounds.

5. `## Environment`
   - OpenClaw gateway: http://localhost:18789
   - Binary: /opt/homebrew/bin/openclaw
   (Preserve from current TOOLS.md)

Do NOT include the "NOT Available in Phase 3" section. It is fully replaced by the Beads section. Do NOT re-add it under a different name.
  </action>
  <verify>
    <automated>grep -q "bd ready --json" /Users/trilogy/Documents/agentic-setup/.openclaw/agents/task-orchestrator/TOOLS.md && grep -q "sessions_spawn" /Users/trilogy/Documents/agentic-setup/.openclaw/agents/task-orchestrator/TOOLS.md && grep -qv "NOT Available in Phase 3" /Users/trilogy/Documents/agentic-setup/.openclaw/agents/task-orchestrator/TOOLS.md && echo "TOOLS.md: OK"</automated>
  </verify>
  <done>
    - TOOLS.md contains "Beads Task Tracker" section with bd commands
    - TOOLS.md lists sessions_spawn as available (not excluded)
    - TOOLS.md does NOT contain "NOT Available in Phase 3" text
    - TOOLS.md contains the explicit BEADS_DIR path $HOME/.openclaw/beads
    - TOOLS.md preserves existing tool policy, gh path, and environment section
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| SOUL.md → agent runtime | Agent reads SOUL.md as persona/rules on session start |
| SOUL.md bd rules → Beads DB | Agent executes bd commands that mutate the shared task graph |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-06 | Spoofing | SOUL.md epic-before-spawn rule bypassed | mitigate | Rule is phrased as MANDATORY NO EXCEPTIONS with explicit pre-spawn verification steps; agent cannot claim a graph is built without running `bd dep tree` and `bd ready --json` |
| T-04-07 | Tampering | Agent uses `bd list --status open` instead of `bd ready` | mitigate | TOOLS.md Rules section explicitly prohibits `bd list --status open`; SOUL.md enforces `bd ready --json` as the confirmation step before spawn |
| T-04-08 | Information Disclosure | Vague bd close reasons leak no handoff context | mitigate | SOUL.md Tone + TOOLS.md Rules both mandate specific evidence strings; "done" is called out as anti-pattern |
| T-04-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this plan |
</threat_model>

<verification>
After Plan 04-03 completes:

```bash
# Confirm Phase 3 stub language is gone
grep "Phase 3 Scope\|do NOT attempt bd" /Users/trilogy/Documents/agentic-setup/.openclaw/agents/task-orchestrator/SOUL.md
# Expected: no output (grep finds nothing)

# Confirm Beads contract is present
grep "sessions_spawn\|BEADS_DIR\|/opt/homebrew/opt/node@24/bin/bd" \
  /Users/trilogy/Documents/agentic-setup/.openclaw/agents/task-orchestrator/SOUL.md

# Confirm TOOLS.md updated
grep "bd ready\|sessions_spawn" \
  /Users/trilogy/Documents/agentic-setup/.openclaw/agents/task-orchestrator/TOOLS.md

# Run smoke test — check 6 (soul-has-beads-rule) should now pass
zsh /Users/trilogy/Documents/agentic-setup/scripts/verify-phase-04.sh
```
</verification>

<success_criteria>
- SOUL.md contains the full Beads-Enforced Execution Contract with mandatory pre-spawn steps
- SOUL.md has no Phase 3 stub language ("do NOT attempt bd", "Phase 3 Scope", "sub-agents in Phase 3")
- TOOLS.md has Beads Task Tracker section with orchestrator + sub-agent command reference
- TOOLS.md has no "NOT Available in Phase 3" exclusions
- verify-phase-04.sh check 6 (soul-has-beads-rule) passes
- All 6 checks in verify-phase-04.sh pass (checks 1-5 passing from prior plans)
</success_criteria>

<output>
Create `/Users/trilogy/Documents/agentic-setup/.planning/phases/04-beads-task-orchestrator/04-03-SUMMARY.md` when done.
</output>

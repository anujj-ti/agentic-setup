---
phase: 19-devbot-autonomous-issue-pickup-devbot-polls-for-automation-s
plan: 05
type: execute
wave: 3
depends_on:
  - 19-04
files_modified:
  - scripts/verify-phase-19.sh
autonomous: true
requirements:
  - DEV-07
  - DEV-08
  - DEV-09
  - DEV-10

must_haves:
  truths:
    - "All 4 new scripts exist and pass zsh syntax check"
    - "jobs.json has 6 entries including both new DevBot cron jobs"
    - "DevBot cron entries have correct expressions (*/5 and 0 * * * *)"
    - "SOUL.md contains Autonomous Issue Pickup section"
    - "AGENTS.md contains pickup-queue check in startup steps"
    - "All 7 required labels exist in anujj-ti/agentic-setup repo"
    - "Smoke test confirms issue monitor dry-run produces valid JSON output"
  artifacts:
    - path: "scripts/verify-phase-19.sh"
      provides: "10-check automated verification script for Phase 19"
      exports: []
  key_links:
    - from: "verify-phase-19.sh"
      to: "devbot-issue-monitor.sh, devbot-stale-claim-guard.sh, devbot-setup-labels.sh"
      via: "zsh -n syntax checks + grep gates"
      pattern: "zsh -n.*devbot"
---

<objective>
Create and run scripts/verify-phase-19.sh — the automated end-to-end verification that proves all Phase 19 deliverables are in place before the phase is marked complete.

Purpose: Every phase has a verification script. Phase 19 introduces autonomous behavior that cannot be re-verified manually each session; the script makes future regression checking trivial.
Output: verify-phase-19.sh with 10 checks. All checks must pass.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md

Verification pattern from prior phases (e.g., verify-phase-14.sh):
- #!/usr/bin/env zsh + set -euo pipefail
- PASS/FAIL counters
- Each check is numbered: CHECK N — description
- Print green CHECK N: OK or red CHECK N: FAIL
- Final summary: PASS count / total
- Exit 0 if all pass, exit 1 if any fail

Phase 19 DEV requirements to verify:
  DEV-07: devbot-issue-monitor.sh polls every 5 min (cron entry + script exists)
  DEV-08: gh issue develop branch creation + claim logic in script
  DEV-09: gh pr create --fill + gh pr merge --auto in script
  DEV-10: devbot-stale-claim-guard.sh exists + cron entry at 60 min

Checks to implement (10 total):
  1. devbot-setup-labels.sh exists + syntax OK
  2. devbot-issue-monitor.sh exists + syntax OK
  3. devbot-stale-claim-guard.sh exists + syntax OK
  4. All three scripts are executable
  5. jobs.json has DevBot Issue Monitor entry (*/5 * * * *)
  6. jobs.json has DevBot Stale Claim Guard entry (0 * * * *)
  7. SOUL.md contains Autonomous Issue Pickup section
  8. AGENTS.md contains pickup-queue check
  9. devbot-issue-monitor.sh contains required decision implementations (D-204, D-207, D-208, D-209, D-210)
  10. devbot-issue-monitor.sh dry-run: parse logic check (jq available, GH_TOKEN can be loaded from Keychain)
</context>

<interfaces>
From existing verify-phase-14.sh pattern (inferred from ROADMAP references):
  #!/usr/bin/env zsh
  set -euo pipefail
  PASS=0; FAIL=0
  check() { local n="$1" desc="$2" result="$3"
    if [[ "$result" == "0" ]]; then
      print "CHECK $n: OK — $desc"; PASS=$((PASS+1))
    else
      print "CHECK $n: FAIL — $desc"; FAIL=$((FAIL+1))
    fi
  }
  ... run checks ...
  print "--- $PASS/$((PASS+FAIL)) checks passed ---"
  exit $([[ $FAIL -eq 0 ]] && print 0 || print 1)

devbot scripts location: /Users/trilogy/Documents/agentic-setup/.openclaw/agents/devbot/scripts/
jobs.json location: /Users/trilogy/Documents/agentic-setup/.openclaw/cron/jobs.json
</interfaces>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Create verify-phase-19.sh and run it</name>
  <files>scripts/verify-phase-19.sh</files>
  <action>
Create scripts/verify-phase-19.sh with:

Shebang: #!/usr/bin/env zsh
Strict mode: set -euo pipefail

Print header: "Phase 19 — DevBot Autonomous Issue Pickup: Verification"

Constants:
  REPO_DIR="/Users/trilogy/Documents/agentic-setup"
  DEVBOT_SCRIPTS="$REPO_DIR/.openclaw/agents/devbot/scripts"
  JOBS_JSON="$REPO_DIR/.openclaw/cron/jobs.json"
  SOUL_MD="$REPO_DIR/.openclaw/agents/devbot/SOUL.md"
  AGENTS_MD="$REPO_DIR/.openclaw/agents/devbot/AGENTS.md"
  PASS=0
  FAIL=0

Define a check function:
  check() {
    local n="$1" desc="$2" result="$3"
    if [[ "$result" == "0" ]]; then
      print "  CHECK $n: OK — $desc"
      PASS=$((PASS+1))
    else
      print "  CHECK $n: FAIL — $desc"
      FAIL=$((FAIL+1))
    fi
  }

CHECK 1 — devbot-setup-labels.sh exists and passes syntax check:
  [[ -f "$DEVBOT_SCRIPTS/devbot-setup-labels.sh" ]] && zsh -n "$DEVBOT_SCRIPTS/devbot-setup-labels.sh" 2>/dev/null
  check 1 "devbot-setup-labels.sh exists + syntax OK (D-212)" $([[ $? -eq 0 ]] && print 0 || print 1)

CHECK 2 — devbot-issue-monitor.sh exists and passes syntax check:
  [[ -f "$DEVBOT_SCRIPTS/devbot-issue-monitor.sh" ]] && zsh -n "$DEVBOT_SCRIPTS/devbot-issue-monitor.sh" 2>/dev/null
  check 2 "devbot-issue-monitor.sh exists + syntax OK (D-201)" $([[ $? -eq 0 ]] && print 0 || print 1)

CHECK 3 — devbot-stale-claim-guard.sh exists and passes syntax check:
  [[ -f "$DEVBOT_SCRIPTS/devbot-stale-claim-guard.sh" ]] && zsh -n "$DEVBOT_SCRIPTS/devbot-stale-claim-guard.sh" 2>/dev/null
  check 3 "devbot-stale-claim-guard.sh exists + syntax OK (D-205)" $([[ $? -eq 0 ]] && print 0 || print 1)

CHECK 4 — all three new scripts are executable:
  [[ -x "$DEVBOT_SCRIPTS/devbot-setup-labels.sh" && \
     -x "$DEVBOT_SCRIPTS/devbot-issue-monitor.sh" && \
     -x "$DEVBOT_SCRIPTS/devbot-stale-claim-guard.sh" ]]
  check 4 "all three scripts are executable" $([[ $? -eq 0 ]] && print 0 || print 1)

CHECK 5 — DevBot Issue Monitor cron job in jobs.json with */5 expression:
  MONITOR_EXPR=$(python3 -c "
import json, sys
d = json.load(open('$JOBS_JSON'))
j = next((x for x in d['jobs'] if x.get('name') == 'DevBot Issue Monitor'), None)
print(j['schedule']['expr'] if j else '')
" 2>/dev/null)
  [[ "$MONITOR_EXPR" == "*/5 * * * *" ]]
  check 5 "DevBot Issue Monitor cron: */5 * * * * (D-201)" $([[ $? -eq 0 ]] && print 0 || print 1)

CHECK 6 — DevBot Stale Claim Guard cron job in jobs.json with 0 * * * * expression:
  GUARD_EXPR=$(python3 -c "
import json, sys
d = json.load(open('$JOBS_JSON'))
j = next((x for x in d['jobs'] if x.get('name') == 'DevBot Stale Claim Guard'), None)
print(j['schedule']['expr'] if j else '')
" 2>/dev/null)
  [[ "$GUARD_EXPR" == "0 * * * *" ]]
  check 6 "DevBot Stale Claim Guard cron: 0 * * * * (D-205)" $([[ $? -eq 0 ]] && print 0 || print 1)

CHECK 7 — SOUL.md contains Autonomous Issue Pickup section:
  grep -q 'Autonomous Issue Pickup' "$SOUL_MD" 2>/dev/null
  check 7 "SOUL.md has Autonomous Issue Pickup section" $([[ $? -eq 0 ]] && print 0 || print 1)

CHECK 8 — AGENTS.md contains pickup-queue check:
  grep -q 'pickup-queue' "$AGENTS_MD" 2>/dev/null
  check 8 "AGENTS.md has pickup-queue startup check" $([[ $? -eq 0 ]] && print 0 || print 1)

CHECK 9 — issue monitor contains all required decision implementations:
  Verify D-204 (claim), D-207 (branch), D-208 (PR), D-209 (auto-merge), D-210 (Resolves #) are all present:
  MONITOR_SCRIPT="$DEVBOT_SCRIPTS/devbot-issue-monitor.sh"
  REQUIRED_PATTERNS=(
    "add-assignee"
    "issue develop"
    "pr create"
    "--auto"
    "Resolves"
    "automation:hold"
    "last-issue-timestamp"
  )
  ALL_PRESENT=0
  for pattern in "${REQUIRED_PATTERNS[@]}"; do
    if ! grep -q "$pattern" "$MONITOR_SCRIPT" 2>/dev/null; then
      print "  MISSING pattern: $pattern" >&2
      ALL_PRESENT=1
    fi
  done
  check 9 "issue monitor contains all D-204/D-207/D-208/D-209/D-210 patterns" "$ALL_PRESENT"

CHECK 10 — GH_TOKEN Keychain entry readable (connectivity check — skip if not available):
  GH_TOKEN_VAL=$(security find-generic-password -s 'openclaw.github-bot-token' -a 'trilogy' -w 2>/dev/null || print "")
  [[ -n "$GH_TOKEN_VAL" ]]
  check 10 "openclaw.github-bot-token readable from Keychain" $([[ $? -eq 0 ]] && print 0 || print 1)

Final summary:
  print ""
  print "--- Phase 19 Verification: $PASS/$((PASS+FAIL)) checks passed ---"
  if [[ $FAIL -gt 0 ]]; then
    print "FAIL: $FAIL check(s) failed — review output above"
    exit 1
  else
    print "ALL CHECKS PASSED"
    exit 0
  fi

Make executable: chmod +x scripts/verify-phase-19.sh

Then RUN the script:
  bash /Users/trilogy/Documents/agentic-setup/scripts/verify-phase-19.sh

Report the output. If any checks fail, note which ones and what needs fixing (but do not fix in this plan — this plan's job is to produce the verification evidence).
  </action>
  <verify>
    <automated>cd /Users/trilogy/Documents/agentic-setup && zsh -n scripts/verify-phase-19.sh && echo "syntax OK" && bash scripts/verify-phase-19.sh</automated>
  </verify>
  <done>
    - verify-phase-19.sh exists, passes zsh -n, is executable
    - Script runs and reports results for all 10 checks
    - All 10 checks pass (or failures are documented with specific reasons)
    - Phase 19 verification evidence recorded in SUMMARY.md
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| verify script → Keychain | Reads GH_TOKEN to confirm it exists — does not use it |
| verify script → jobs.json | Read-only access to validate cron config |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-19-13 | Information Disclosure | GH_TOKEN in verify script | mitigate | Token checked for non-empty only — value never echoed to stdout |
| T-19-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this plan |
</threat_model>

<verification>
Run the verification script:
  bash /Users/trilogy/Documents/agentic-setup/scripts/verify-phase-19.sh

Expected output: 10/10 checks passed

Manual check for Phase 19 success criteria:
  1. DevBot autonomously polls every 5 min (DEV-07): verify-phase-19.sh CHECK 5
  2. DevBot claims + branches via gh issue develop (DEV-08): CHECK 9 (add-assignee + issue develop patterns)
  3. DevBot opens PR + auto-merge (DEV-09): CHECK 9 (pr create + --auto + Resolves patterns)
  4. Stale guard unclaims idle >2h (DEV-10): CHECKs 3, 4, 6

Phase 19 is complete when verify-phase-19.sh exits 0.
</verification>

<success_criteria>
- verify-phase-19.sh exists, is executable, passes zsh -n
- All 10 checks pass when script is run
- Phase 19 DEV-07, DEV-08, DEV-09, DEV-10 requirements all have corresponding passing checks
</success_criteria>

<output>
Create .planning/phases/19-devbot-autonomous-issue-pickup-devbot-polls-for-automation-s/19-05-SUMMARY.md when done
</output>

---
phase: 19-devbot-autonomous-issue-pickup-devbot-polls-for-automation-s
plan: 04
type: execute
wave: 2
depends_on:
  - 19-01
  - 19-02
  - 19-03
files_modified:
  - .openclaw/cron/jobs.json
  - .openclaw/agents/devbot/SOUL.md
  - .openclaw/agents/devbot/AGENTS.md
autonomous: true
requirements:
  - DEV-07
  - DEV-08
  - DEV-09
  - DEV-10

must_haves:
  truths:
    - "devbot-issue-monitor cron job appears in jobs.json firing every 5 minutes"
    - "devbot-stale-claim-guard cron job appears in jobs.json firing every 60 minutes"
    - "DevBot SOUL.md contains an Autonomous Issue Pickup section documenting the full loop"
    - "DevBot AGENTS.md startup checklist includes a check for the pickup-queue.txt file"
    - "Cron jobs use silent delivery (no Telegram noise on every poll)"
  artifacts:
    - path: ".openclaw/cron/jobs.json"
      provides: "Two new cron jobs: issue-monitor (5min) and stale-claim-guard (60min)"
      contains: "devbot-issue-monitor"
    - path: ".openclaw/agents/devbot/SOUL.md"
      provides: "Autonomous Issue Pickup section with full loop documentation"
      contains: "Autonomous Issue Pickup"
    - path: ".openclaw/agents/devbot/AGENTS.md"
      provides: "Startup check for pickup-queue.txt"
      contains: "pickup-queue"
  key_links:
    - from: "jobs.json cron entry"
      to: "devbot-issue-monitor.sh"
      via: "agentId: devbot + exec scripts/devbot-issue-monitor.sh"
      pattern: "devbot-issue-monitor"
    - from: "jobs.json cron entry"
      to: "devbot-stale-claim-guard.sh"
      via: "agentId: devbot + exec scripts/devbot-stale-claim-guard.sh"
      pattern: "devbot-stale-claim-guard"
---

<objective>
Wire the two new cron jobs into jobs.json and document the autonomous issue pickup behavior in DevBot's SOUL.md and AGENTS.md. This plan makes the scripts from Wave 1 actually run on schedule.

Purpose: Scripts created in Plans 01-03 are inert until registered with OpenClaw's cron system. This plan activates them. The SOUL.md/AGENTS.md updates ensure DevBot knows its new autonomous responsibilities.
Output: Updated jobs.json (2 new entries), updated SOUL.md (new section), updated AGENTS.md (new startup step). Then stow+restart to deploy.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md

Existing jobs.json is at:
  .openclaw/cron/jobs.json
It already has 4 entries: User Orchestrator Dream, Task Orchestrator Dream, Morning Standup, CI Monitor Poll.
The CI Monitor Poll entry is the direct template for the issue-monitor cron:
  - agentId: "ci-monitor" → "devbot"
  - expr: "*/4 * * * *" → "*/5 * * * *" (every 5 min per D-201)
  - delivery.mode: "silent" (no Telegram for each poll — same as CI Monitor)
  - timeoutSeconds: 90 (same as CI Monitor — script is lightweight)

Stale-claim guard follows same pattern with expr "0 * * * *" (every 60 min at :00).

stow-deploy.sh location: scripts/stow-deploy.sh (canonical entry per Phase 1 D-48 pattern)
Gateway restart: launchctl kickstart -k gui/$(id -u)/ai.openclaw.gateway

openclaw.json devbot entry already has exec in tools.alsoAllow — confirmed in file read above.

IMPORTANT: UUIDs for jobs.json entries must be unique. Generate with:
  python3 -c "import uuid; print(uuid.uuid4())"

jobs.json schema (from existing entries):
  {
    "id": "<uuid>",
    "agentId": "<agent-id>",
    "name": "<display name>",
    "enabled": true,
    "createdAtMs": <unix ms>,
    "schedule": { "kind": "cron", "expr": "<cron expr>", "tz": "Asia/Kolkata" },
    "sessionTarget": "isolated",
    "wakeMode": "now",
    "payload": {
      "kind": "agentTurn",
      "message": "<instruction>",
      "model": "anthropic/claude-sonnet-4-6",
      "timeoutSeconds": <seconds>
    },
    "delivery": { "mode": "silent" }
  }
</context>

<interfaces>
From .openclaw/cron/jobs.json (CI Monitor entry to use as template):
  {
    "id": "17f4643a-c03f-4363-ae35-076792323ca0",
    "agentId": "ci-monitor",
    "name": "CI Monitor Poll",
    "enabled": true,
    "schedule": { "kind": "cron", "expr": "*/4 * * * *", "tz": "Asia/Kolkata" },
    "sessionTarget": "isolated",
    "wakeMode": "now",
    "payload": {
      "kind": "agentTurn",
      "message": "Run your CI polling routine. Execute scripts/poll-ci.sh and report the result...",
      "model": "anthropic/claude-sonnet-4-6",
      "timeoutSeconds": 90
    },
    "delivery": { "mode": "silent" }
  }

From .openclaw/agents/devbot/SOUL.md (existing sections to preserve):
  - Identity, Core Responsibilities, Operational Rules, BLOCKED Protocol,
    Communication Boundaries, Phase 7 Capability Boundaries, Model Policy, Tone,
    Autonomous Merge Protocol, Autonomous Development Workflow
  ADD AFTER existing sections: ## Autonomous Issue Pickup (DEV-07/DEV-08/DEV-09)

From .openclaw/agents/devbot/AGENTS.md (existing startup checklist):
  Steps 1-5 must be preserved. ADD step for pickup-queue check.
</interfaces>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Add two cron jobs to jobs.json</name>
  <files>.openclaw/cron/jobs.json</files>
  <action>
Read .openclaw/cron/jobs.json (currently 4 entries).

Generate two new UUIDs by running:
  python3 -c "import uuid; print(uuid.uuid4()); print(uuid.uuid4())"

Get current Unix timestamp in ms:
  python3 -c "import time; print(int(time.time() * 1000))"

Add two new entries to the "jobs" array:

Entry 1 — DevBot Issue Monitor (D-201: every 5 min):
  {
    "id": "<generated-uuid-1>",
    "agentId": "devbot",
    "name": "DevBot Issue Monitor",
    "enabled": true,
    "createdAtMs": <timestamp>,
    "schedule": {
      "kind": "cron",
      "expr": "*/5 * * * *",
      "tz": "Asia/Kolkata"
    },
    "sessionTarget": "isolated",
    "wakeMode": "now",
    "payload": {
      "kind": "agentTurn",
      "message": "Run the autonomous issue monitor. Execute scripts/devbot-issue-monitor.sh anujj-ti/agentic-setup and report the result. If new issues were picked up, log the pickup count. If the script exits non-zero, report the error.",
      "model": "anthropic/claude-sonnet-4-6",
      "timeoutSeconds": 120
    },
    "delivery": {
      "mode": "silent"
    }
  }

Entry 2 — DevBot Stale Claim Guard (D-205: every 60 min):
  {
    "id": "<generated-uuid-2>",
    "agentId": "devbot",
    "name": "DevBot Stale Claim Guard",
    "enabled": true,
    "createdAtMs": <timestamp>,
    "schedule": {
      "kind": "cron",
      "expr": "0 * * * *",
      "tz": "Asia/Kolkata"
    },
    "sessionTarget": "isolated",
    "wakeMode": "now",
    "payload": {
      "kind": "agentTurn",
      "message": "Run the stale claim guard. Execute scripts/devbot-stale-claim-guard.sh anujj-ti/agentic-setup and report the result. If any stale claims were unclaimed, include the count in your response.",
      "model": "anthropic/claude-sonnet-4-6",
      "timeoutSeconds": 60
    },
    "delivery": {
      "mode": "silent"
    }
  }

Write the updated jobs.json with all 6 entries (4 existing + 2 new). Preserve all existing entry fields exactly.

After writing jobs.json, run stow+restart to deploy:
  bash /Users/trilogy/Documents/agentic-setup/scripts/stow-deploy.sh
  launchctl kickstart -k gui/$(id -u)/ai.openclaw.gateway
  </action>
  <verify>
    <automated>cd /Users/trilogy/Documents/agentic-setup && python3 -c "import json; d=json.load(open('.openclaw/cron/jobs.json')); jobs=[j['name'] for j in d['jobs']]; print(jobs); assert 'DevBot Issue Monitor' in jobs and 'DevBot Stale Claim Guard' in jobs, 'missing jobs'; assert len(d['jobs'])==6, f'expected 6 jobs, got {len(d[\"jobs\"])}'; print('OK')"</automated>
  </verify>
  <done>
    - jobs.json has 6 entries total
    - "DevBot Issue Monitor" entry present with expr "*/5 * * * *"
    - "DevBot Stale Claim Guard" entry present with expr "0 * * * *"
    - Both use agentId "devbot" and delivery mode "silent"
    - stow-deploy.sh run + gateway restarted
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Update DevBot SOUL.md and AGENTS.md with autonomous pickup documentation</name>
  <files>
    .openclaw/agents/devbot/SOUL.md
    .openclaw/agents/devbot/AGENTS.md
  </files>
  <action>
READ .openclaw/agents/devbot/SOUL.md first to understand the current last section.

APPEND the following new section to the END of .openclaw/agents/devbot/SOUL.md (after the existing "Autonomous Development Workflow" section). Do not modify any existing content:

---

## Autonomous Issue Pickup (DEV-07, DEV-08, DEV-09)

DevBot now runs autonomously every 5 minutes via cron. This section documents the pickup loop.

### Loop Overview

1. **Poll** — `devbot-issue-monitor.sh` runs every 5 min; lists open issues labeled `automation:safe` with no assignee.
2. **Filter** — Issues with `automation:hold` are skipped (kill switch per D-206).
3. **Dedup** — Issues older than `state/last-issue-timestamp` are skipped (D-202).
4. **Notion pre-log** — Log the pickup decision to Notion BEFORE any GitHub mutation (SOUL.md mandate).
5. **Claim** — `gh issue edit N --add-assignee echosysbot --add-label status:in-progress` (D-204).
6. **Branch** — `gh issue develop N --checkout` creates and checks out the linked branch (D-207).
7. **Draft PR** — `gh pr create --fill --body "Resolves #N" --draft` opens the PR (D-208, D-210).
8. **Auto-merge** — `gh pr merge --auto --squash --delete-branch` enables auto-merge when CI passes (D-209).
9. **Stale guard** — Hourly cron unassigns issues idle >2h (D-205).

### CRITICAL RULES for Autonomous Pickup

- **ALWAYS check for automation:hold** before claiming — this is the user's kill switch.
- **NEVER bypass the Notion pre-log** — the pickup decision must exist in Notion before the claim call.
- **NEVER manually process issues** that are already assigned (`--assignee @none` filter enforces this).
- **PR body MUST include "Resolves #N"** for GitHub to auto-close the issue when the PR merges (D-210).
- If any step fails: log the failure to stderr, do NOT unassign — the stale guard will recover in ≤2h.

### State Files

| File | Purpose |
|------|---------|
| `state/last-issue-timestamp` | Last processed issue timestamp — prevents reprocessing (D-202) |
| `state/pending-issues/N.json` | Issue JSON snapshot written at pickup time |
| `state/pickup-queue.txt` | Issue numbers queued for Beads execution cycle |
| `logs/issue-monitor-YYYY-MM-DD.log` | Daily stderr log from issue monitor |

### Label State Machine

| Label | Meaning | Set By |
|-------|---------|--------|
| `automation:safe` | Eligible for autonomous pickup | Human |
| `automation:hold` | Kill switch — blocks pickup | Human |
| `status:in-progress` | DevBot has claimed this issue | DevBot (devbot-issue-monitor.sh) |
| `e1/e2/e3` | Effort estimate | Human |
| `agent:echosysbot` | Optional — for filtering bot-touched issues | DevBot (optional) |

---

READ .openclaw/agents/devbot/AGENTS.md next.

ADD the following step to the Session Startup checklist AFTER step 5 (the "Emit status" step). Insert it as step 6:

6. Check for pending issue pickup queue:
   `ls /Users/trilogy/.openclaw/agents/devbot/state/pickup-queue.txt 2>/dev/null && cat /Users/trilogy/.openclaw/agents/devbot/state/pickup-queue.txt`
   If pickup-queue.txt is non-empty: the cron-spawned session should process each queued issue number via the Beads execution cycle (devbot-execute-cycle.sh). After processing, clear the queue:
   `> /Users/trilogy/.openclaw/agents/devbot/state/pickup-queue.txt`
  </action>
  <verify>
    <automated>cd /Users/trilogy/Documents/agentic-setup && grep -c 'Autonomous Issue Pickup' .openclaw/agents/devbot/SOUL.md && grep -c 'pickup-queue' .openclaw/agents/devbot/AGENTS.md && echo "SOUL.md and AGENTS.md updated OK"</automated>
  </verify>
  <done>
    - SOUL.md contains "## Autonomous Issue Pickup" section
    - SOUL.md contains label state machine table
    - SOUL.md contains the 9-step loop overview
    - AGENTS.md contains pickup-queue.txt check in startup steps
    - All existing SOUL.md and AGENTS.md content preserved
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| jobs.json → OpenClaw cron | Cron message is plain text instruction to devbot agent — no secrets in payload |
| devbot agent → exec scripts | exec tool in tools.alsoAllow already present — no new permissions added |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-19-11 | Tampering | jobs.json cron payload message | accept | Message is a plain instruction string; no dynamic injection; devbot validates at runtime |
| T-19-12 | Denial of service | 5-min cron firing DevBot frequently | mitigate | Each invocation is "isolated" session — no state leaks between runs; 120s timeout kills runaway sessions |
| T-19-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this plan |
</threat_model>

<verification>
Confirm job entries in jobs.json:
  python3 -c "
import json
d = json.load(open('.openclaw/cron/jobs.json'))
names = [j['name'] for j in d['jobs']]
print('Jobs:', names)
assert 'DevBot Issue Monitor' in names
assert 'DevBot Stale Claim Guard' in names
print('OK')
"

Confirm cron expressions:
  python3 -c "
import json
d = json.load(open('.openclaw/cron/jobs.json'))
monitor = next(j for j in d['jobs'] if j['name'] == 'DevBot Issue Monitor')
guard = next(j for j in d['jobs'] if j['name'] == 'DevBot Stale Claim Guard')
print('Monitor expr:', monitor['schedule']['expr'])
print('Guard expr:', guard['schedule']['expr'])
assert monitor['schedule']['expr'] == '*/5 * * * *'
assert guard['schedule']['expr'] == '0 * * * *'
print('OK')
"

Confirm SOUL.md and AGENTS.md updated:
  grep -c 'Autonomous Issue Pickup' .openclaw/agents/devbot/SOUL.md
  grep -c 'pickup-queue' .openclaw/agents/devbot/AGENTS.md
</verification>

<success_criteria>
- jobs.json has 6 total entries (4 original + 2 new)
- DevBot Issue Monitor: agentId devbot, expr */5 * * * *, silent delivery
- DevBot Stale Claim Guard: agentId devbot, expr 0 * * * *, silent delivery
- SOUL.md Autonomous Issue Pickup section added with 9-step loop and label state machine
- AGENTS.md startup step 6 added for pickup-queue.txt check
- stow-deploy.sh run and gateway restarted to activate cron jobs
</success_criteria>

<output>
Create .planning/phases/19-devbot-autonomous-issue-pickup-devbot-polls-for-automation-s/19-04-SUMMARY.md when done
</output>

---
phase: 01-infrastructure
plan: 05
type: execute
wave: 4
depends_on:
  - 01-01
  - 01-02
  - 01-03
  - 01-04
files_modified: []
autonomous: false
requirements:
  - INFRA-06
tags:
  - infrastructure
  - verification
  - cron
  - openclaw-status

must_haves:
  truths:
    - "/openclaw-status reports green across gateway, channels, cron jobs, and agents (channels/agents may be empty but the skill must not error)"
    - "scripts/infra-verify.sh exits 0 (all smoke tests pass)"
    - "A test cron job exists in /openclaw-status output with a non-UTC tz field (local timezone)"
    - "The OpenClaw gateway is running (runtime status is not 'unknown'); the LaunchAgent is loaded"
    - "Phase 1 acceptance is achieved — Phase 2 can begin building on top of this infrastructure"
  artifacts:
    - path: ".planning/phases/01-infrastructure/01-05-SUMMARY.md"
      provides: "Phase 1 sign-off summary with /openclaw-status output capture and test-cron-job confirmation"
      contains: "openclaw-status"
  key_links:
    - from: "scripts/infra-verify.sh"
      to: "openclaw gateway status (live health endpoint)"
      via: "/openclaw-status skill invocation in Task 1"
      pattern: "gateway status"
    - from: "/openclaw-add-cron skill"
      to: "~/.openclaw/cron/jobs.json (gateway runtime file)"
      via: "Test cron job created with explicit non-UTC tz"
      pattern: "tz"
---

<objective>
Verify the full Phase 1 infrastructure end-to-end: run `/openclaw-status` and `scripts/infra-verify.sh`, confirm both report green, then create a test cron job via `/openclaw-add-cron` with a local-timezone tz field to prove INFRA-06 in the runtime path. After this plan, all 5 ROADMAP success criteria for Phase 1 are satisfied and Phase 2 can begin.

Purpose: This is the phase gate. Every previous plan asserted its own narrow slice; this plan asserts the whole works together — including the live gateway daemon and the cron timezone field that previous plans deferred.

Output: No new code files. Outputs: (a) human-confirmed `/openclaw-status` green report, (b) infra-verify.sh JSON green, (c) a test cron job visible in `/openclaw-status` with non-UTC tz field, (d) 01-05-SUMMARY.md sign-off document.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/phases/01-infrastructure/01-CONTEXT.md
@.planning/phases/01-infrastructure/01-RESEARCH.md
@.planning/phases/01-infrastructure/01-PATTERNS.md
@.planning/phases/01-infrastructure/01-VALIDATION.md
@.planning/phases/01-infrastructure/SKELETON.md
@scripts/infra-verify.sh
@cc-openclaw/.claude/skills/openclaw-status/SKILL.md
@cc-openclaw/.claude/skills/openclaw-add-cron/SKILL.md

<interfaces>
INFRA-06 acceptance (ROADMAP Success Criteria #1 and #5):
- /openclaw-status returns green status across gateway, channels, cron jobs, and agents
- A test cron job created via /openclaw-add-cron appears in /openclaw-status output with the correct LOCAL timezone field (not UTC)

RESEARCH.md Pitfall 7: /openclaw-status checks require BOTH stow AND a running daemon. By this plan all infrastructure is in place — but if the LaunchAgent was loaded by Plan 01-01 BEFORE stow ran, the gateway may need a kick to re-read the now-symlinked openclaw.json. Use `/openclaw-restart` if needed.

Cron job structure (RESEARCH.md "Code Examples → Test Cron Job"):
- schedule.tz field is REQUIRED — omitting defaults to UTC, which fails this plan's acceptance
- Use a recognizable local timezone like America/New_York, America/Los_Angeles, Asia/Kolkata — whatever matches the user's machine

Manual-only verifications (01-VALIDATION.md "Manual-Only Verifications"):
- Confirming the tz field is local (not UTC) requires human inspection of /openclaw-status output
- This plan is therefore autonomous: false with human-verify checkpoints
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Run /openclaw-status and scripts/infra-verify.sh — confirm both green</name>
  <what-built>All Phase 1 infrastructure is installed and stow-deployed. The cc-openclaw skills are discoverable, OPENCLAW_REPO detection works, and the test secret is propagated. Now we need a live health check that the gateway daemon is reading the symlinked config and reporting healthy status.</what-built>
  <how-to-verify>
    Step 1 — Run scripts/infra-verify.sh from the repo root:
      `bash /Users/trilogy/Documents/agentic-setup/scripts/infra-verify.sh`
      Expect stdout: JSON with `.ok == true` and `.data.passed >= 8` and `.data.failed == 0`.
      If any check fails, the failure label appears in `.data.failures` — STOP and investigate.

    Step 2 — If the LaunchAgent was loaded BEFORE the first stow (likely, given Plan 01-01's ordering), kick the gateway so it re-reads the symlinked config:
      `launchctl kickstart -k "gui/$(id -u)/ai.openclaw.gateway"`
      `sleep 5`
      `openclaw gateway status --json`
      Expect: a JSON object whose `.service.runtime.status` is NOT the string `unknown` (per RESEARCH.md Pitfall 7).

    Step 3 — Run the /openclaw-status skill in Claude Code (invoke it via the slash command). Confirm the output reports:
      - Gateway: green / running / healthy (skill-specific wording from cc-openclaw/.claude/skills/openclaw-status/SKILL.md)
      - Channels section: present (empty list is acceptable for Phase 1 — channels are added in Phase 2)
      - Cron section: present (no jobs yet — added in Task 2)
      - Agents section: present (no agents yet — added in Phase 3+)

    Step 4 — Capture and paste back:
      - The stdout JSON from `bash scripts/infra-verify.sh`
      - The output of `openclaw gateway status --json`
      - The /openclaw-status output (or a clear "green" confirmation)

    If anything is red, stop here and resolve before Task 2.
  </how-to-verify>
  <resume-signal>Reply with the three captured outputs (infra-verify JSON, gateway status JSON, /openclaw-status output) and confirm gateway is green.</resume-signal>
  <files>(no files modified — verification only)</files>
  <action>This is a blocking human-verify checkpoint. Claude runs `bash scripts/infra-verify.sh` and reports the JSON result. If the LaunchAgent was loaded before the first stow (likely given the Plan 01-01 ordering — the daemon may still be holding the pre-stow plain-file config in memory), Claude additionally runs `launchctl kickstart -k "gui/$(id -u)/ai.openclaw.gateway" && sleep 5 && openclaw gateway status --json` to refresh the daemon (per RESEARCH.md Pitfall 7). Claude then asks the user to invoke the `/openclaw-status` skill in Claude Code and paste the output. Claude resumes when the user confirms gateway/channels/cron/agents are all green.</action>
  <verify>
    <automated>bash scripts/infra-verify.sh | jq -e '.ok == true and .data.passed >= 8 and .data.failed == 0' && openclaw gateway status --json 2>/dev/null | jq -e '.service.runtime.status != "unknown"'</automated>
  </verify>
  <done>scripts/infra-verify.sh returns ok:true with passed>=8 and failed:0; the gateway runtime status is not "unknown"; /openclaw-status output shows green across gateway/channels/cron/agents.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Create a test cron job via /openclaw-add-cron with a local timezone and confirm it appears in /openclaw-status</name>
  <what-built>Infrastructure is green per Task 1. The remaining ROADMAP success criterion #5 requires a test cron job created via /openclaw-add-cron that appears in /openclaw-status with a local (non-UTC) tz field.</what-built>
  <how-to-verify>
    Step 1 — Invoke the /openclaw-add-cron skill in Claude Code to create a test cron job. The skill should produce a jobs.json entry with at minimum:
      - name: e.g., "test-infra-health" (or similar identifying name)
      - schedule.cron: any valid cron expression (e.g., `0 9 * * *`)
      - schedule.tz: a LOCAL timezone (e.g., `America/New_York`, `America/Los_Angeles`, `Asia/Kolkata` — whichever matches your machine). It MUST NOT be `UTC`.
      - enabled: true
      - sessionTarget: "isolated" (default; recommended per cc-openclaw conventions)

    Step 2 — After /openclaw-add-cron completes, run /openclaw-status again. The output must include the new cron job AND show its tz field set to the local timezone (not UTC).

    Step 3 — Verify the underlying file:
      `cat ~/.openclaw/cron/jobs.json | jq '.[].schedule.tz'`
      The output should NOT include "UTC".

    Step 4 — Capture and paste back:
      - The /openclaw-add-cron output (skill-printed JSON or success message)
      - The /openclaw-status output showing the cron entry
      - The output of `cat ~/.openclaw/cron/jobs.json | jq '.[].schedule.tz'`

    Note (RESEARCH.md Pitfall 1): /openclaw-add-cron writes to ~/.openclaw/cron/jobs.json. The next time stow-deploy.sh runs, this file will be removed (it is gateway-owned and recreated on every restart). This is expected behavior — the jobs themselves live in the gateway state, not in the stow package.
  </how-to-verify>
  <resume-signal>Reply with the /openclaw-add-cron output, the /openclaw-status output showing the test cron job, and the jq output confirming the tz field is not "UTC".</resume-signal>
  <files>~/.openclaw/cron/jobs.json (gateway-owned; the test job entry is appended here by /openclaw-add-cron)</files>
  <action>This is a blocking human-verify checkpoint. Claude asks the user to invoke the `/openclaw-add-cron` skill in Claude Code to create a test job with name "test-infra-health", a benign cron expression (e.g., `0 9 * * *`), and an explicit local-timezone tz field (e.g., America/New_York, America/Los_Angeles, Asia/Kolkata — whichever matches the user's machine — NEVER UTC). Then Claude asks the user to invoke `/openclaw-status` again and confirm the test job appears with the local tz field. Claude verifies the underlying file with `cat ~/.openclaw/cron/jobs.json | jq '.[].schedule.tz'` and confirms no entry has tz "UTC". Claude resumes when all three captures are pasted back.</action>
  <verify>
    <automated>test -f "$HOME/.openclaw/cron/jobs.json" && ! jq -r '.[].schedule.tz' "$HOME/.openclaw/cron/jobs.json" 2>/dev/null | grep -Fxq 'UTC' && jq -r '.[].schedule.tz' "$HOME/.openclaw/cron/jobs.json" 2>/dev/null | grep -E '^[A-Za-z]+/[A-Za-z_]+'</automated>
  </verify>
  <done>~/.openclaw/cron/jobs.json contains at least one job whose schedule.tz field is a local timezone (matching the pattern Region/City, e.g., America/New_York) and no job has tz "UTC". The job is visible in /openclaw-status output.</done>
</task>

<task type="auto">
  <name>Task 3: Write 01-05-SUMMARY.md sign-off document</name>
  <files>.planning/phases/01-infrastructure/01-05-SUMMARY.md</files>
  <read_first>
    - .planning/phases/01-infrastructure/01-VALIDATION.md (sign-off checklist)
    - .planning/phases/01-infrastructure/01-CONTEXT.md (decisions checklist for traceability)
    - All prior summaries: 01-01-SUMMARY.md, 01-02-SUMMARY.md, 01-03-SUMMARY.md, 01-04-SUMMARY.md
  </read_first>
  <action>Write .planning/phases/01-infrastructure/01-05-SUMMARY.md as a Phase 1 sign-off document containing: (1) Plan-by-plan summary linking to each preceding SUMMARY (01-01 through 01-04); (2) Requirements coverage table mapping INFRA-01, INFRA-02, INFRA-03, INFRA-04, INFRA-06 to evidence (smoke command outputs, /openclaw-status capture, jobs.json tz value); (3) Decision audit checking each of D-01 through D-15 against its implementation (e.g., D-01 → scripts/stow-deploy.sh:line N — `stow --dir=` invocation; D-09 → rm -f ~/.openclaw/cron/jobs.json present); (4) The full captured outputs from Task 1 and Task 2 (infra-verify JSON, gateway status JSON, /openclaw-status output, jobs.json tz output); (5) Open items or follow-ups for Phase 2 (none expected, but document anything surfaced during execution). Use the template at $HOME/.claude/get-shit-done/templates/summary.md as the structural starting point.</action>
  <verify>
    <automated>test -f .planning/phases/01-infrastructure/01-05-SUMMARY.md && grep -q 'INFRA-01' .planning/phases/01-infrastructure/01-05-SUMMARY.md && grep -q 'INFRA-02' .planning/phases/01-infrastructure/01-05-SUMMARY.md && grep -q 'INFRA-03' .planning/phases/01-infrastructure/01-05-SUMMARY.md && grep -q 'INFRA-04' .planning/phases/01-infrastructure/01-05-SUMMARY.md && grep -q 'INFRA-06' .planning/phases/01-infrastructure/01-05-SUMMARY.md && grep -q 'openclaw-status' .planning/phases/01-infrastructure/01-05-SUMMARY.md</automated>
  </verify>
  <acceptance_criteria>
    - File .planning/phases/01-infrastructure/01-05-SUMMARY.md exists
    - The summary contains the strings INFRA-01, INFRA-02, INFRA-03, INFRA-04, and INFRA-06 (each requirement mentioned at least once)
    - The summary references /openclaw-status output capture
    - The summary references the jobs.json tz field value (non-UTC)
    - The summary references scripts/infra-verify.sh result
    - The summary links to or summarizes each of the preceding plan summaries (01-01 through 01-04)
    - The summary contains a decision audit referencing at least D-01, D-04, D-06, D-09 (the most consequential decisions)
  </acceptance_criteria>
  <done>01-05-SUMMARY.md is committed and Phase 1 is signed off. The user can confidently invoke /gsd:verify-work for the phase and then move to /gsd:plan-phase 2.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Gateway → jobs.json | The OpenClaw gateway owns ~/.openclaw/cron/jobs.json and writes to it on startup; this test plan exercises that write path |
| /openclaw-status → gateway health endpoint | The status skill queries http://127.0.0.1:18789/health or equivalent; the endpoint is bound to localhost only |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-01 | Tampering | Test cron job payload | accept | The job has a benign payload (a health-check ping). It does not invoke autonomous actions. Can be disabled or deleted at any time via /openclaw-status + /openclaw-add-cron tooling. |
| T-05-02 | Information Disclosure | /openclaw-status output may include channel tokens | mitigate | RESEARCH.md confirms /openclaw-status uses Keychain refs (not values) when displaying channel config. CLAUDE.md "Constraints → Secrets" requires this. Spot-check: paste /openclaw-status output redacted if any literal token appears (it should not). |
| T-05-03 | Denial of Service | Gateway not restarted after first stow leaves daemon reading the old plain-file config | mitigate | Task 1 Step 2 explicitly runs `launchctl kickstart -k gui/$(id -u)/ai.openclaw.gateway` and verifies the gateway runtime status is not "unknown" (RESEARCH.md Pitfall 7). |
| T-05-04 | Information Disclosure | jobs.json includes secrets via payload | accept | Phase 1 test job has no sensitive payload. Phase 2+ will use SecretRef pattern (RESEARCH.md). Recorded for tracking. |
</threat_model>

<verification>
After Task 3 completes, Phase 1 acceptance is:

1. `bash scripts/infra-verify.sh` exits 0 with `.ok == true` and `.data.passed >= 8`
2. `openclaw gateway status --json | jq -r '.service.runtime.status'` returns a value that is NOT `unknown`
3. `/openclaw-status` (skill invocation) reports green for gateway, channels (may be empty), cron jobs (test job present), agents (may be empty)
4. `cat ~/.openclaw/cron/jobs.json | jq -r '.[].schedule.tz'` returns at least one non-UTC value
5. .planning/phases/01-infrastructure/01-05-SUMMARY.md exists with full coverage of INFRA-01/02/03/04/06
</verification>

<success_criteria>
INFRA-06 is operational, and all Phase 1 ROADMAP Success Criteria are met:

1. ✅ User runs `/openclaw-status` and receives green status (Task 1)
2. ✅ `/openclaw-add-secret` propagates to all three files (Plan 01-03)
3. ✅ Stow deploy from ~/Documents/agentic-setup with jobs.json conflict auto-resolved (Plan 01-04)
4. ✅ All 9 cc-openclaw skills available as Claude Code slash commands (Plan 01-02)
5. ✅ Test cron job created via /openclaw-add-cron with local timezone in /openclaw-status output (Task 2)
</success_criteria>

<output>
Phase 1 sign-off complete. Subsequent plan: `/gsd:plan-phase 2` (Core Channels — Telegram + WhatsApp provisioning).
</output>

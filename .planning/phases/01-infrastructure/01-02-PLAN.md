---
phase: 01-infrastructure
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - .gitmodules
  - cc-openclaw
  - .claude/skills/openclaw-add-channel
  - .claude/skills/openclaw-add-cron
  - .claude/skills/openclaw-add-script
  - .claude/skills/openclaw-add-secret
  - .claude/skills/openclaw-dream-setup
  - .claude/skills/openclaw-new-agent
  - .claude/skills/openclaw-restart
  - .claude/skills/openclaw-status
  - .claude/skills/openclaw-stow
autonomous: true
requirements:
  - INFRA-02
tags:
  - infrastructure
  - cc-openclaw
  - skills
  - stow
  - submodule

must_haves:
  truths:
    - "cc-openclaw is a registered git submodule of github.com/rahulsub-be/cc-openclaw"
    - "All 9 cc-openclaw skill directories appear as symlinks under .claude/skills/"
    - "Each skill symlink resolves to a directory inside cc-openclaw containing a SKILL.md file"
    - "The .claude/skills/ tree is a real directory (NOT a directory-level symlink) — Claude Code can traverse it to discover individual skills"
  artifacts:
    - path: ".gitmodules"
      provides: "Git submodule registration for cc-openclaw"
      contains: "cc-openclaw"
    - path: "cc-openclaw/.claude/skills"
      provides: "Source directory of 9 cc-openclaw skills"
      contains: "openclaw-status"
    - path: ".claude/skills/openclaw-status"
      provides: "Symlink to cc-openclaw/.claude/skills/openclaw-status (one example of 9)"
      contains: "SKILL.md"
  key_links:
    - from: ".claude/skills/openclaw-status (symlink)"
      to: "cc-openclaw/.claude/skills/openclaw-status (real directory inside submodule)"
      via: "GNU Stow --no-folding"
      pattern: "openclaw-"
---

<objective>
Add cc-openclaw as a git submodule and stow its skills INTO the project directory using `--no-folding`, producing 9 individual skill symlinks under `.claude/skills/`. This satisfies INFRA-02 and makes the 9 cc-openclaw slash commands discoverable by Claude Code.

Purpose: cc-openclaw is the institutional knowledge layer — every config change in subsequent phases flows through one of these 9 skills. Until they are discoverable by Claude Code, no skill can run.

Output: .gitmodules registered, cc-openclaw/ submodule cloned at the project's HEAD pin, and .claude/skills/openclaw-*/ symlinks (9 of them) in the project directory.

Note: The skills won't be FUNCTIONAL until Plan 01-04 runs stow on .openclaw/ (because they detect OPENCLAW_REPO via `readlink ~/.openclaw/openclaw.json`). This plan only places them where Claude Code can find them.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-infrastructure/01-CONTEXT.md
@.planning/phases/01-infrastructure/01-RESEARCH.md
@.planning/phases/01-infrastructure/01-PATTERNS.md
@.planning/phases/01-infrastructure/SKELETON.md
@CLAUDE.md

<interfaces>
Frozen decisions for cc-openclaw placement:
- D-05: cc-openclaw is a git submodule at agentic-setup/cc-openclaw/ (origin: github.com/rahulsub-be/cc-openclaw)
- D-06: Skills are stowed FROM the submodule INTO the project (not into ~/) using `stow --no-folding -t /Users/trilogy/Documents/agentic-setup .` run from cc-openclaw/
- D-07: After clone, inspect cc-openclaw repo structure to confirm skills live under .claude/skills/<name>/ (verified by RESEARCH.md as the actual layout)
- D-08: Skills update via `git pull` inside the submodule; no parent-repo re-stow required

Anti-pattern (RESEARCH.md Pitfall 3): Running stow WITHOUT --no-folding creates a single .claude/ directory-level symlink. Claude Code cannot traverse directory symlinks. Always use --no-folding.

Anti-pattern (D-03/Pattern 2): Stow from cc-openclaw must target the PROJECT directory, NOT $HOME. Skills must land under repo .claude/skills/, not ~/.claude/skills/.

Expected 9 skill directory names (alphabetical):
- openclaw-add-channel
- openclaw-add-cron
- openclaw-add-script
- openclaw-add-secret
- openclaw-dream-setup
- openclaw-new-agent
- openclaw-restart
- openclaw-status
- openclaw-stow
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add cc-openclaw as a git submodule and initialize it</name>
  <files>.gitmodules, cc-openclaw</files>
  <read_first>
    - .planning/phases/01-infrastructure/01-CONTEXT.md (decisions D-05, D-07, D-08)
    - .planning/phases/01-infrastructure/01-RESEARCH.md (section "Architecture Patterns → Pattern 2: cc-openclaw Submodule Stow" and Sources confirming .claude/skills/ layout)
    - .planning/phases/01-infrastructure/01-PATTERNS.md (section "cc-openclaw/ (git submodule)")
  </read_first>
  <action>From the repo root /Users/trilogy/Documents/agentic-setup run `git submodule add https://github.com/rahulsub-be/cc-openclaw cc-openclaw` to register the submodule and clone it at HEAD. Then run `git submodule update --init --recursive` for completeness. After clone, inspect the submodule layout to confirm skills are under cc-openclaw/.claude/skills/ (D-07 — adapt if not, but RESEARCH.md verified this is the actual layout). Verify all 9 expected skill directories are present: openclaw-add-channel, openclaw-add-cron, openclaw-add-script, openclaw-add-secret, openclaw-dream-setup, openclaw-new-agent, openclaw-restart, openclaw-status, openclaw-stow. The .gitmodules file at repo root must register the cc-openclaw path with the github.com/rahulsub-be/cc-openclaw URL.</action>
  <verify>
    <automated>test -f .gitmodules && grep -q 'cc-openclaw' .gitmodules && grep -q 'rahulsub-be/cc-openclaw' .gitmodules && test -d cc-openclaw/.claude/skills && ls cc-openclaw/.claude/skills | grep -c '^openclaw-' | awk '{exit ($1==9)?0:1}'</automated>
  </verify>
  <acceptance_criteria>
    - .gitmodules exists at repo root
    - .gitmodules contains the literal string `cc-openclaw`
    - .gitmodules contains the URL `rahulsub-be/cc-openclaw` (full URL with github.com is acceptable)
    - Directory cc-openclaw/ exists and is populated (not empty)
    - Directory cc-openclaw/.claude/skills/ exists
    - All 9 skill directories exist under cc-openclaw/.claude/skills/ (each name prefixed `openclaw-`): openclaw-add-channel, openclaw-add-cron, openclaw-add-script, openclaw-add-secret, openclaw-dream-setup, openclaw-new-agent, openclaw-restart, openclaw-status, openclaw-stow
    - Each of the 9 skill directories contains a SKILL.md file (spot-check at least openclaw-status/SKILL.md)
    - `git submodule status` lists cc-openclaw with a resolved commit hash
  </acceptance_criteria>
  <done>cc-openclaw is a registered, initialized git submodule with all 9 expected skill directories present at cc-openclaw/.claude/skills/.</done>
</task>

<task type="auto">
  <name>Task 2: Stow cc-openclaw skills into .claude/skills/ using --no-folding</name>
  <files>.claude/skills/openclaw-add-channel, .claude/skills/openclaw-add-cron, .claude/skills/openclaw-add-script, .claude/skills/openclaw-add-secret, .claude/skills/openclaw-dream-setup, .claude/skills/openclaw-new-agent, .claude/skills/openclaw-restart, .claude/skills/openclaw-status, .claude/skills/openclaw-stow</files>
  <read_first>
    - .planning/phases/01-infrastructure/01-CONTEXT.md (decisions D-06, D-07)
    - .planning/phases/01-infrastructure/01-RESEARCH.md (sections "Architecture Patterns → Pattern 2", "Common Pitfalls → Pitfall 3: Missing --no-folding")
    - .planning/phases/01-infrastructure/01-PATTERNS.md (section "cc-openclaw/ (git submodule)" — exact stow invocation)
    - cc-openclaw/.stow-local-ignore (if present — verify what the submodule excludes; RESEARCH.md notes it excludes README.md and LICENSE)
  </read_first>
  <action>From the cc-openclaw submodule directory, run GNU Stow with --no-folding targeting the project directory: `cd /Users/trilogy/Documents/agentic-setup/cc-openclaw && stow --no-folding -t /Users/trilogy/Documents/agentic-setup .`. Per RESEARCH.md Pitfall 3, --no-folding is mandatory — without it stow creates a single .claude/ directory-level symlink that Claude Code cannot traverse. After stow, verify the result: .claude/skills/ inside the project must be a real directory (NOT a symlink), and each of the 9 skill subdirectories under it (openclaw-add-channel, openclaw-add-cron, openclaw-add-script, openclaw-add-secret, openclaw-dream-setup, openclaw-new-agent, openclaw-restart, openclaw-status, openclaw-stow) must be a symlink resolving into cc-openclaw/.claude/skills/. If the stow output reports any conflict, stop and report the conflict — do NOT pass --adopt or force flags. Per D-06 this invocation targets the PROJECT directory, never $HOME.</action>
  <verify>
    <automated>test -d .claude/skills && ! test -L .claude/skills && test -L .claude/skills/openclaw-status && test -L .claude/skills/openclaw-add-secret && test -L .claude/skills/openclaw-stow && test -L .claude/skills/openclaw-restart && test -L .claude/skills/openclaw-add-cron && test -L .claude/skills/openclaw-add-channel && test -L .claude/skills/openclaw-add-script && test -L .claude/skills/openclaw-dream-setup && test -L .claude/skills/openclaw-new-agent && readlink .claude/skills/openclaw-status | grep -q 'cc-openclaw' && ls .claude/skills | grep -c '^openclaw-' | awk '{exit ($1==9)?0:1}'</automated>
  </verify>
  <acceptance_criteria>
    - .claude/skills/ exists as a real directory (`test -d .claude/skills && ! test -L .claude/skills`)
    - All 9 specific paths exist and are symlinks (test -L on each): .claude/skills/openclaw-add-channel, openclaw-add-cron, openclaw-add-script, openclaw-add-secret, openclaw-dream-setup, openclaw-new-agent, openclaw-restart, openclaw-status, openclaw-stow
    - `readlink .claude/skills/openclaw-status` returns a path containing the substring `cc-openclaw`
    - .claude/skills/openclaw-status/SKILL.md is readable (follows symlink to real file)
    - Count of entries in .claude/skills/ matching `openclaw-*` is exactly 9
    - .claude/ at repo root is a real directory (not a symlink)
    - No `README.md` or `LICENSE` symlink appears under .claude/ or at repo root from the stow run (cc-openclaw's .stow-local-ignore excludes them)
  </acceptance_criteria>
  <done>All 9 cc-openclaw skills are symlinked under .claude/skills/ in the project directory and discoverable by Claude Code. Claude Code's `/openclaw-` slash commands now resolve (though they will fail at runtime until Plan 01-04 establishes the openclaw.json stow symlink for OPENCLAW_REPO detection).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| GitHub → host (submodule clone) | cc-openclaw repo content cloned over HTTPS from github.com/rahulsub-be/cc-openclaw becomes executable code referenced by Claude Code slash commands |
| Claude Code → skill SKILL.md | Slash commands read SKILL.md and execute the procedures they describe in the user's shell |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Tampering | cc-openclaw submodule content | mitigate | Pin to a specific commit (whatever HEAD resolves to at clone time). git submodule status records the SHA. CLAUDE.md identifies cc-openclaw as the institutional knowledge source — accepted upstream. Any subsequent submodule update will be a deliberate `git pull` inside cc-openclaw/, reviewed by the user. |
| T-02-02 | Tampering | Symlinks under .claude/skills/ pointing into the submodule | accept | Symlinks owned by stow; resolve into the repo (not outside it). `stow -D` cleanly removes them. No elevation of trust. |
| T-02-03 | Information Disclosure | Skills read repo state via OPENCLAW_REPO detection | accept | Detection is read-only and points only at the user's own repo. No external endpoints. |
| T-02-SC | Tampering | Submodule add via git clone | mitigate | Source is github.com/rahulsub-be/cc-openclaw, cited in CLAUDE.md as the canonical skill source. RESEARCH.md "Package Legitimacy Audit" treats cc-openclaw as an institutional dependency. No npm/pip/cargo installs in this plan — git clone over HTTPS only. |
</threat_model>

<verification>
1. `git submodule status | grep cc-openclaw` shows a resolved commit
2. `ls cc-openclaw/.claude/skills | wc -l` returns 9
3. `ls .claude/skills | wc -l` returns 9 (matching skill names)
4. `test -L .claude/skills/openclaw-status` succeeds
5. `readlink .claude/skills/openclaw-status` returns a path containing `cc-openclaw`
6. `test -d .claude/skills` succeeds AND `! test -L .claude/skills` (real directory, not a folded symlink)
</verification>

<success_criteria>
INFRA-02 is structurally operational: all 9 cc-openclaw skills are present in the project's .claude/skills/ tree as individual symlinks resolving into the submodule. Claude Code can discover the 9 `/openclaw-*` slash commands. Runtime functionality is contingent on Plan 01-04's stow of .openclaw/.
</success_criteria>

<output>
Create `.planning/phases/01-infrastructure/01-02-SUMMARY.md` when done listing the submodule commit hash, skill count, and any conflicts encountered during stow.
</output>

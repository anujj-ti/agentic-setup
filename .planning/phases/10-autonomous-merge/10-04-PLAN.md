---
phase: 10-autonomous-merge
plan: 04
type: execute
wave: 3
depends_on:
  - 10-01
  - 10-02
  - 10-03
files_modified:
  - /Users/trilogy/Documents/agentic-setup/scripts/verify-phase-10.sh
autonomous: true
requirements:
  - DEV-05

must_haves:
  truths:
    - "verify-phase-10.sh confirms devbot-merge-pr.sh blocks when invoked without OPENCLAW_NOTION_DECISIONS_DB_ID set"
    - "verify-phase-10.sh confirms SECURITY.md contains the Notion page ID gate rule"
    - "verify-phase-10.sh confirms devbot-revert-merge.sh exits 1 when called without arguments"
    - "All checks output structured results readable at a glance"
  artifacts:
    - path: "/Users/trilogy/Documents/agentic-setup/scripts/verify-phase-10.sh"
      provides: "Phase 10 automated gate + structure verification script"
  key_links:
    - from: "verify-phase-10.sh"
      to: "devbot-merge-pr.sh"
      via: "invocation without env var to confirm block"
      pattern: "devbot-merge-pr"
---

<objective>
Create and run scripts/verify-phase-10.sh — the phase gate verification that confirms the merge security invariant holds without requiring a real GitHub PR or live Notion API. The script runs structural checks plus a negative test (confirm blocked without Notion env var) to prove the gate is code-level, not policy-only.

Purpose: Satisfy DEV-05 SC#3 — "DevBot does not merge any PR that lacks a Notion log entry (enforced in SECURITY.md rules loaded in DevBot SOUL.md)."
Output: verify-phase-10.sh that passes cleanly in the agentic-setup repo.
</objective>

<execution_context>
@/Users/trilogy/Documents/agentic-setup/.claude/get-shit-done/workflows/execute-plan.md
@/Users/trilogy/Documents/agentic-setup/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/trilogy/Documents/agentic-setup/.planning/PROJECT.md
@/Users/trilogy/Documents/agentic-setup/.planning/ROADMAP.md
@/Users/trilogy/Documents/agentic-setup/.planning/phases/10-autonomous-merge/10-RESEARCH.md

Verification architecture from research:
- No unit test framework for shell scripts; zsh scripts + manual verification
- Negative test (blocked without token) is the key DEV-05 gate check
- Revert test on a real repo is optional in this verify script (live test requires actual repo state)

Script location: /Users/trilogy/Documents/agentic-setup/scripts/ (repo-level, not inside ~/.openclaw)
This script verifies the state of files in ~/.openclaw/agents/devbot/ without modifying them.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create and run verify-phase-10.sh</name>
  <files>
    /Users/trilogy/Documents/agentic-setup/scripts/verify-phase-10.sh
  </files>
  <action>
Create scripts/verify-phase-10.sh with #!/usr/bin/env zsh and set -euo pipefail.

The script runs the following checks in order, printing PASS or FAIL for each:

CHECK 1 — Script files exist:
  [[ -f "$HOME/.openclaw/agents/devbot/scripts/devbot-merge-pr.sh" ]] && echo "PASS: devbot-merge-pr.sh exists" || { echo "FAIL: devbot-merge-pr.sh missing"; FAILURES+=("merge script missing") }
  [[ -f "$HOME/.openclaw/agents/devbot/scripts/devbot-revert-merge.sh" ]] && echo "PASS: devbot-revert-merge.sh exists" || { echo "FAIL: devbot-revert-merge.sh missing"; FAILURES+=("revert script missing") }
  [[ -f "$HOME/.openclaw/agents/devbot/scripts/notion-log-decision.js" ]] && echo "PASS: notion-log-decision.js exists" || { echo "FAIL: notion-log-decision.js missing"; FAILURES+=("notion-log-decision.js missing") }

CHECK 2 — Script syntax valid:
  zsh -n "$HOME/.openclaw/agents/devbot/scripts/devbot-merge-pr.sh" && echo "PASS: devbot-merge-pr.sh syntax ok" || { echo "FAIL: syntax error in merge script"; FAILURES+=("merge script syntax") }
  zsh -n "$HOME/.openclaw/agents/devbot/scripts/devbot-revert-merge.sh" && echo "PASS: devbot-revert-merge.sh syntax ok" || { echo "FAIL: syntax error in revert script"; FAILURES+=("revert script syntax") }

CHECK 3 — SECURITY.md contains the gate rule:
  grep -q "Notion page ID" "$HOME/.openclaw/agents/devbot/SECURITY.md" && echo "PASS: SECURITY.md contains Notion page ID gate rule" || { echo "FAIL: SECURITY.md missing gate rule"; FAILURES+=("SECURITY.md gate rule") }

CHECK 4 — SOUL.md contains merge protocol:
  grep -q "Autonomous Merge Protocol" "$HOME/.openclaw/agents/devbot/SOUL.md" && echo "PASS: SOUL.md contains Autonomous Merge Protocol" || { echo "FAIL: SOUL.md missing merge protocol"; FAILURES+=("SOUL.md merge protocol") }
  grep -q "NEVER" "$HOME/.openclaw/agents/devbot/SOUL.md" && echo "PASS: SOUL.md contains NEVER directive" || { echo "FAIL: SOUL.md missing NEVER directive"; FAILURES+=("SOUL.md NEVER directive") }

CHECK 5 — Gate test (negative): invoke devbot-merge-pr.sh without OPENCLAW_NOTION_DECISIONS_DB_ID
  Temporarily unset OPENCLAW_NOTION_DECISIONS_DB_ID and OPENCLAW_NOTION_TOKEN in a subshell, then invoke the merge script with a fake PR number (e.g., "0"). The script MUST exit non-zero (the Notion write will fail because the env var is unset, which blocks the merge). Capture exit code; if non-zero: PASS. If zero: FAIL (gate is broken).
  Use: ( unset OPENCLAW_NOTION_DECISIONS_DB_ID; unset OPENCLAW_NOTION_TOKEN; zsh "$HOME/.openclaw/agents/devbot/scripts/devbot-merge-pr.sh" "0" 2>/dev/null ); EXIT_CODE=$?
  [[ $EXIT_CODE -ne 0 ]] && echo "PASS: merge blocked when Notion env vars absent (exit $EXIT_CODE)" || { echo "FAIL: merge script did NOT block without Notion env vars"; FAILURES+=("gate not enforced") }

CHECK 6 — Revert script arg validation:
  ( zsh "$HOME/.openclaw/agents/devbot/scripts/devbot-revert-merge.sh" 2>/dev/null ); EXIT_CODE=$?
  [[ $EXIT_CODE -ne 0 ]] && echo "PASS: revert script exits non-zero without args" || { echo "FAIL: revert script should require args"; FAILURES+=("revert arg validation") }

CHECK 7 — @notionhq/client installed:
  /opt/homebrew/opt/node@24/bin/node -e "require('@notionhq/client')" 2>/dev/null && echo "PASS: @notionhq/client importable" || { echo "FAIL: @notionhq/client not installed in devbot scripts"; FAILURES+=("notionhq/client not installed") }

CHECK 8 — No -m 1 flag in revert script (squash merge — single parent):
  grep -v "^#" "$HOME/.openclaw/agents/devbot/scripts/devbot-revert-merge.sh" | grep -q "\-m 1" && { echo "FAIL: revert script uses -m 1 (squash commits are single-parent — per D-103)"; FAILURES+=("wrong revert flag") } || echo "PASS: revert script correctly omits -m 1"

At the end: if FAILURES array is empty, print "=== Phase 10 PASSED (8/8 checks) ===" and exit 0. Otherwise print "=== Phase 10 FAILED (${#FAILURES[@]} failures): ${FAILURES[*]} ===" and exit 1.

chmod +x the script. Run it: zsh scripts/verify-phase-10.sh
  </action>
  <verify>
    <automated>cd /Users/trilogy/Documents/agentic-setup && zsh scripts/verify-phase-10.sh</automated>
  </verify>
  <done>
    verify-phase-10.sh exits 0 with "Phase 10 PASSED (8/8 checks)". The negative gate test confirms the merge script is blocked when Notion env vars are absent.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| verify script → devbot scripts | Verification only reads and calls scripts; does not write to ~/.openclaw |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-10 | Tampering | verify script using real PR number | mitigate | Gate test uses PR number "0" in a subshell with unset env vars — script will exit at Notion write failure before any gh pr merge is reached |
| T-10-11 | Denial of Service | verify script modifying live agent files | accept | Script is read-only; it only invokes existing scripts in controlled subshells |
</threat_model>

<verification>
zsh scripts/verify-phase-10.sh exits 0 with PASSED output for all 8 checks.
</verification>

<success_criteria>
- All 8 checks pass (scripts exist, syntax valid, SECURITY.md gate rule present, SOUL.md merge protocol present, gate enforced when env vars absent, revert requires args, @notionhq/client installed, no -m 1 in revert script)
- Phase 10 gate verification exits 0
</success_criteria>

<output>
Create .planning/phases/10-autonomous-merge/10-04-SUMMARY.md when done
</output>

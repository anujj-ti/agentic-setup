---
phase: 10-autonomous-merge
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - ~/.openclaw/agents/devbot/SOUL.md
  - ~/.openclaw/agents/devbot/TOOLS.md
autonomous: true
requirements:
  - DEV-05

must_haves:
  truths:
    - "DevBot SOUL.md contains 'Autonomous Merge Protocol' section with mandatory Notion pre-log rule"
    - "SOUL.md names devbot-merge-pr.sh as the ONLY permitted merge path"
    - "TOOLS.md documents the required env vars, the merge script invocation, and the revert command reference"
  artifacts:
    - path: "~/.openclaw/agents/devbot/SOUL.md"
      provides: "Behavioral rule: no autonomous merge without devbot-merge-pr.sh (which enforces Notion gate)"
      contains: "Autonomous Merge Protocol"
    - path: "~/.openclaw/agents/devbot/TOOLS.md"
      provides: "Merge command reference, required env vars, revert procedure link"
      contains: "devbot-merge-pr.sh"
  key_links:
    - from: "SOUL.md Autonomous Merge Protocol"
      to: "devbot-merge-pr.sh"
      via: "named script reference"
      pattern: "devbot-merge-pr"
---

<objective>
Add the "Autonomous Merge Protocol" to DevBot's SOUL.md, encoding the Notion pre-log gate as a behavioral rule. Also update TOOLS.md with the merge and revert command reference, required environment variables, and the known limitation of the revert workflow (PR reopens but head branch is not recreated).

Purpose: The SOUL.md rule closes any LLM-reasoning path that might attempt to call gh pr merge directly. The script-level gate (Plan 10-01) is the hard enforcement; the SOUL.md rule is the behavioral layer that prevents the agent from ever routing around the script.
Output: SOUL.md merge protocol section, TOOLS.md command reference.
</objective>

<execution_context>
@/Users/trilogy/Documents/agentic-setup/.claude/get-shit-done/workflows/execute-plan.md
@/Users/trilogy/Documents/agentic-setup/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/trilogy/Documents/agentic-setup/.planning/PROJECT.md
@/Users/trilogy/Documents/agentic-setup/.planning/ROADMAP.md
@/Users/trilogy/Documents/agentic-setup/.planning/phases/10-autonomous-merge/10-RESEARCH.md

DevBot context:
- DevBot is an execution-tier sub-agent of Task Orchestrator — no Telegram binding (per D-75 from Phase 7)
- DevBot uses sessions_spawn-only pattern — Task Orchestrator delegates merge requests to it
- SOUL.md and TOOLS.md live at ~/.openclaw/agents/devbot/ in the live openclaw workspace
- If SOUL.md or TOOLS.md do not yet exist (Phase 7 not yet executed), create them; if they exist, append to the appropriate section
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Autonomous Merge Protocol to DevBot SOUL.md</name>
  <files>
    ~/.openclaw/agents/devbot/SOUL.md
  </files>
  <action>
Read the existing ~/.openclaw/agents/devbot/SOUL.md to understand its current structure and sections.

Append (do not overwrite) a new section titled "## Autonomous Merge Protocol (DEV-05 — D-100, D-101)" with the following rules, verbatim as behavioral directives:

1. NEVER invoke gh pr merge directly. The ONLY permitted merge path is: scripts/devbot-merge-pr.sh <PR_NUMBER>.
2. devbot-merge-pr.sh enforces the Notion pre-log gate at the code level. If the script exits non-zero for any reason (Notion write failed, CI not passing, any other error), report BLOCKED to Task Orchestrator and do NOT retry merge independently.
3. Merge strategy is squash (--squash) per D-101. Do not use --merge, --rebase, --admin, or --auto.
4. After a successful merge, the Notion page ID and merge commit SHA are returned in the script's JSON output. Include both in your completion report to Task Orchestrator.
5. If asked to merge a PR where CI is not fully passing: refuse, report BLOCKED with reason "CI checks not all SUCCESS," and escalate to Task Orchestrator.
6. If OPENCLAW_NOTION_DECISIONS_DB_ID is not in the environment when you attempt a merge: abort immediately, report BLOCKED with "Phase 9 prerequisite not satisfied — Notion DB ID missing," do NOT attempt the merge.
  </action>
  <verify>
    <automated>grep -c "devbot-merge-pr.sh" /Users/trilogy/.openclaw/agents/devbot/SOUL.md</automated>
  </verify>
  <done>
    SOUL.md contains the Autonomous Merge Protocol section. grep for "devbot-merge-pr.sh" returns >= 1. The word "NEVER" precedes "gh pr merge" in the file.
  </done>
</task>

<task type="auto">
  <name>Task 2: Update DevBot TOOLS.md with merge command reference and env var requirements</name>
  <files>
    ~/.openclaw/agents/devbot/TOOLS.md
  </files>
  <action>
Read existing ~/.openclaw/agents/devbot/TOOLS.md to understand its structure.

Append a section "## Merge and Revert Commands (Phase 10)" containing:

Required env vars (sourced from Keychain via openclaw-secrets.sh):
- OPENCLAW_NOTION_TOKEN — Notion API auth
- OPENCLAW_NOTION_DECISIONS_DB_ID — ID of the decisions database (set by Phase 9 prerequisite)

Merge:
  scripts/devbot-merge-pr.sh <PR_NUMBER>
  Returns: {"ok":true,"pageId":"<id>","mergeCommitSha":"<sha>","prNumber":"<n>"}
  Fails fast with {"ok":false,"error":"..."} if CI not passing or Notion write fails.

Revert (after merge):
  scripts/devbot-revert-merge.sh <MERGE_SHA> <PR_NUMBER> <ORIGINAL_PAGE_ID>
  Returns: {"ok":true,"revertedSha":"<sha>","prNumber":"<n>"}
  Note: gh pr reopen reopens the PR state. The head branch is NOT recreated — if --delete-branch was used during merge, the user must recreate the branch manually to push new commits.

Notion log entry fields written per merge:
  Action, Timestamp, Rationale, Evidence, Reversibility, Status, MergeCommitSha (added post-merge)
  </action>
  <verify>
    <automated>grep -c "OPENCLAW_NOTION_DECISIONS_DB_ID" /Users/trilogy/.openclaw/agents/devbot/TOOLS.md</automated>
  </verify>
  <done>
    TOOLS.md contains the merge and revert command reference, lists both required env vars, and documents the known limitation about branch recreation after revert.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| SOUL.md rule → LLM behavior | Behavioral rule prevents agent from reasoning its way to a direct gh pr merge call |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-05 | Elevation of Privilege | DevBot LLM reasoning around merge gate | mitigate | SOUL.md explicit NEVER directive + SECURITY.md rule (Plan 10-01) — two-layer behavioral enforcement |
| T-10-06 | Repudiation | TOOLS.md env var documentation | accept | Documenting required env vars is informational; actual enforcement is in the script |
</threat_model>

<verification>
1. grep -c "Autonomous Merge Protocol" ~/.openclaw/agents/devbot/SOUL.md returns >= 1
2. grep -c "NEVER" ~/.openclaw/agents/devbot/SOUL.md returns >= 1
3. grep -c "devbot-merge-pr.sh" ~/.openclaw/agents/devbot/TOOLS.md returns >= 1
4. grep -c "OPENCLAW_NOTION_DECISIONS_DB_ID" ~/.openclaw/agents/devbot/TOOLS.md returns >= 1
5. grep -c "head branch is NOT recreated" ~/.openclaw/agents/devbot/TOOLS.md returns >= 1 (known limitation documented)
</verification>

<success_criteria>
- SOUL.md: "NEVER invoke gh pr merge directly" rule present; devbot-merge-pr.sh named as the only permitted path
- SOUL.md: Rule for BLOCKED escalation when Notion DB ID is missing from environment
- TOOLS.md: Both required env vars documented with Keychain source
- TOOLS.md: Revert workflow known limitation (branch not recreated) documented
</success_criteria>

<output>
Create .planning/phases/10-autonomous-merge/10-02-SUMMARY.md when done
</output>

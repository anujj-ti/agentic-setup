---
phase: 11-quality-pipeline
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - ~/.openclaw/agents/code-reviewer/SOUL.md
  - ~/.openclaw/agents/code-reviewer/IDENTITY.md
  - ~/.openclaw/agents/code-reviewer/USER.md
  - ~/.openclaw/agents/code-reviewer/AGENTS.md
  - ~/.openclaw/agents/code-reviewer/TOOLS.md
  - ~/.openclaw/agents/code-reviewer/SECURITY.md
autonomous: true
requirements:
  - QUAL-01
  - QUAL-05

must_haves:
  truths:
    - "code-reviewer agent directory exists at ~/.openclaw/agents/code-reviewer/ with all 6 directive files"
    - "code-reviewer is registered in openclaw.json agents.list (added by /openclaw-new-agent)"
    - "SOUL.md contains the review rubric: set -euo pipefail compliance, stdout-JSON-only, Keychain-only secrets, test coverage"
    - "SOUL.md specifies the verdict schema exactly: {verdict, comments, must_fix, approved_at}"
    - "TOOLS.md documents that the agent receives only the PR diff, not the full repo"
  artifacts:
    - path: "~/.openclaw/agents/code-reviewer/SOUL.md"
      provides: "Code review rubric and verdict schema"
      contains: "verdict"
    - path: "~/.openclaw/agents/code-reviewer/TOOLS.md"
      provides: "gh pr diff reference and review payload description"
  key_links:
    - from: "code-reviewer SOUL.md"
      to: "verdict schema"
      via: "JSON output specification"
      pattern: "verdict.*pass.*flag.*reject"
---

<objective>
Scaffold the Code Reviewer agent using /openclaw-new-agent. This agent receives PR diffs via sessions_spawn from the Task Orchestrator and returns a structured verdict JSON. Its SOUL.md encodes a specific rubric for this project's code conventions.

Purpose: Deploy QUAL-01 — Code Reviewer as a dedicated specialist with the correct rubric, distinct from any generalist reviewer.
Output: All 6 directive files for code-reviewer agent, registered in openclaw.json.
</objective>

<execution_context>
@/Users/trilogy/Documents/agentic-setup/.claude/get-shit-done/workflows/execute-plan.md
@/Users/trilogy/Documents/agentic-setup/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/trilogy/Documents/agentic-setup/.planning/PROJECT.md
@/Users/trilogy/Documents/agentic-setup/.planning/ROADMAP.md
@/Users/trilogy/Documents/agentic-setup/.planning/phases/11-quality-pipeline/11-RESEARCH.md

Verdict schema (D-111 — shared across all 5 reviewers, MUST be exact):
{"verdict":"pass"|"flag"|"reject","comments":[],"must_fix":[],"approved_at":"ISO8601"|null}

Agent characteristics:
- Type: execution-tier sub-agent (no Telegram binding, no Beads claim authority)
- Parent: task-orchestrator (allowAgents wired in Plan 11-06)
- Model: anthropic/claude-sonnet-4-6
- Input: PR diff text (from gh pr diff <number>), PR description, issue context
- Output: verdict JSON as sessions_spawn close reason
- Receives ONLY the diff — never the full repo (Pitfall 5 from research)

SOUL.md rubric specific to this project's conventions (CLAUDE.md mandated):
- Does code use #!/usr/bin/env zsh shebang? (never #!/bin/bash)
- Does code use set -euo pipefail strict mode?
- Does stdout contain only JSON? (logs to stderr?)
- Are secrets accessed via Keychain env vars? (never hardcoded)
- Does the change include or update relevant tests?
- Are explicit binary paths used? (/opt/homebrew/bin/gh, /opt/homebrew/opt/node@24/bin/node)

Anti-sycophancy rule from research: "A vague rationale is ALWAYS a reject. Flag means the issue is real but non-blocking. Reject means the code cannot advance without the fix."

Skill to use: /openclaw-new-agent (CLAUDE.md mandated — the ONLY permitted agent scaffolding path)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Invoke /openclaw-new-agent to scaffold code-reviewer</name>
  <files>
    ~/.openclaw/agents/code-reviewer/SOUL.md
    ~/.openclaw/agents/code-reviewer/IDENTITY.md
    ~/.openclaw/agents/code-reviewer/USER.md
    ~/.openclaw/agents/code-reviewer/AGENTS.md
    ~/.openclaw/agents/code-reviewer/TOOLS.md
    ~/.openclaw/agents/code-reviewer/SECURITY.md
  </files>
  <action>
Run the /openclaw-new-agent skill to scaffold the code-reviewer agent. When the skill prompts for configuration, supply:
  - Agent ID: code-reviewer
  - Model: anthropic/claude-sonnet-4-6
  - Channel: none (execution-tier sub-agent, sessions_spawn only)
  - Parent agent: task-orchestrator (to be wired in allowAgents in Plan 11-06)
  - Description: "Specialist code reviewer — reviews PR diffs for correctness, security, and convention compliance"

After the skill creates the directory structure and registers the agent in openclaw.json, immediately update SOUL.md to contain the following sections (replacing or extending whatever the skill generated):

Section "## Identity": Code Reviewer is a specialist execution-tier agent. You receive a PR diff and PR description via sessions_spawn. You review ONLY what changed. You do not receive the full repository.

Section "## Review Rubric": Check for each of these in every review:
1. Shebang: #!/usr/bin/env zsh (never #!/bin/bash)
2. Strict mode: set -euo pipefail present in all shell scripts
3. stdout discipline: stdout contains only JSON; human logs go to stderr
4. Secrets: no hardcoded credentials; all secrets via env vars loaded from Keychain
5. Binary paths: explicit paths (/opt/homebrew/bin/gh, /opt/homebrew/opt/node@24/bin/node) — not bare gh or node
6. Test coverage: does the diff include or update tests for changed behavior?
7. JSON response shape: { "ok": true/false, "data": {...} } or { "ok": false, "error": "..." }

Section "## Verdict Rules":
- pass: all rubric items satisfied; no blocking issues
- flag: rubric items satisfied but improvement recommended; originating agent may advance after noting the comment
- reject: one or more rubric items violated; code MUST NOT advance until must_fix items are addressed
- Anti-sycophancy: "The diff looks good" is NEVER a valid review. Every review must cite specific lines or patterns.

Section "## Output Format" (MUST be exactly this schema per D-111):
{"verdict":"pass"|"flag"|"reject","comments":["..."],"must_fix":["..."],"approved_at":"ISO8601 or null"}
- approved_at is ISO8601 timestamp when verdict is "pass"; null otherwise
- comments: observations (max 5)
- must_fix: specific required changes (empty array on pass)

Update TOOLS.md to document:
- Input: PR diff text (from gh pr diff <number> output) + PR description
- You receive ONLY the diff. Do not reference files not present in the diff.
- Output: verdict JSON as your final response (this becomes the sessions_spawn close reason)
  </action>
  <verify>
    <automated>test -d /Users/trilogy/.openclaw/agents/code-reviewer && grep -c "verdict" /Users/trilogy/.openclaw/agents/code-reviewer/SOUL.md</automated>
  </verify>
  <done>
    code-reviewer directory exists with all 6 directive files. SOUL.md contains "verdict" and the review rubric. openclaw.json agents.list includes code-reviewer entry.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| sessions_spawn → code-reviewer | Input is PR diff text; should not contain executable code that the agent acts on |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-01 | Spoofing | Code Reviewer sycophancy (always pass) | mitigate | SOUL.md anti-sycophancy rule: specific line citations required; "looks good" is prohibited as a verdict |
| T-11-02 | Tampering | Full repo context sent instead of diff | mitigate | TOOLS.md states "You receive ONLY the diff" — Task Orchestrator constructs payload from gh pr diff only |
| T-11-SC | Tampering | /openclaw-new-agent skill install | accept | Skill is a local cc-openclaw verified skill (no npm install in this plan) |
</threat_model>

<verification>
1. test -d ~/.openclaw/agents/code-reviewer passes
2. All 6 files exist: SOUL.md, IDENTITY.md, USER.md, AGENTS.md, TOOLS.md, SECURITY.md
3. jq '.agents.list[] | select(.id == "code-reviewer")' ~/.openclaw/openclaw.json returns a non-empty result
4. grep -c "verdict" ~/.openclaw/agents/code-reviewer/SOUL.md returns >= 3
5. grep -c "approved_at" ~/.openclaw/agents/code-reviewer/SOUL.md returns >= 1
</verification>

<success_criteria>
- code-reviewer agent directory exists with all 6 directive files
- SOUL.md contains the full review rubric (7 items) and verdict rules
- Verdict schema (D-111) present exactly in SOUL.md Output Format section
- openclaw.json agents.list contains code-reviewer entry
- TOOLS.md states agent receives only the PR diff
</success_criteria>

<output>
Create .planning/phases/11-quality-pipeline/11-01-SUMMARY.md when done
</output>

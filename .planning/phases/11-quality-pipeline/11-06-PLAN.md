---
phase: 11-quality-pipeline
plan: 06
type: execute
wave: 2
depends_on:
  - 11-01
  - 11-02
  - 11-03
  - 11-04
  - 11-05
files_modified:
  - ~/.openclaw/openclaw.json
  - ~/.openclaw/agents/task-orchestrator/SOUL.md
  - ~/.openclaw/agents/task-orchestrator/TOOLS.md
  - /Users/trilogy/Documents/agentic-setup/openclaw.json
autonomous: true
requirements:
  - QUAL-01
  - QUAL-02
  - QUAL-03
  - QUAL-04
  - QUAL-05
  - QUAL-06
  - QUAL-07
  - QUAL-08

must_haves:
  truths:
    - "Task Orchestrator allowAgents includes all 5 new agents: code-reviewer, document-reviewer, decision-reviewer, skill-reviewer, skill-creation (per D-112)"
    - "Task Orchestrator SOUL.md documents which reviewer to use for each pipeline: Code→DevBot PR, Document→Notion writes, Decision→pre-Notion-log, Skill→new SKILL.md, SkillCreation→pattern repeat trigger"
    - "Task Orchestrator TOOLS.md documents the verdict routing script invocation"
    - "stow+restart runs after openclaw.json change"
    - "Feedback loop max-retry rule (3 rejects → escalate to user) is in Task Orchestrator SOUL.md"
  artifacts:
    - path: "~/.openclaw/openclaw.json"
      provides: "Updated allowAgents with all 5 quality pipeline agents"
      contains: "code-reviewer"
    - path: "~/.openclaw/agents/task-orchestrator/SOUL.md"
      provides: "Reviewer routing rules per pipeline"
  key_links:
    - from: "Task Orchestrator allowAgents"
      to: "code-reviewer, document-reviewer, decision-reviewer, skill-reviewer, skill-creation"
      via: "openclaw.json subagents.allowAgents array"
      pattern: "code-reviewer"
---

<objective>
Wire all five quality pipeline agents into the Task Orchestrator. Update openclaw.json to add all five agents to Task Orchestrator's allowAgents (per D-112). Update Task Orchestrator SOUL.md with routing rules for each pipeline. Run stow+restart. Add the feedback loop convergence rule (max 3 revision cycles per output before escalating to user).

Purpose: Complete the quality pipeline by making the Task Orchestrator aware of when and how to route to each reviewer.
Output: Updated openclaw.json, Task Orchestrator SOUL.md and TOOLS.md updates, stow+restart completed.
</objective>

<execution_context>
@/Users/trilogy/Documents/agentic-setup/.claude/get-shit-done/workflows/execute-plan.md
@/Users/trilogy/Documents/agentic-setup/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/trilogy/Documents/agentic-setup/.planning/PROJECT.md
@/Users/trilogy/Documents/agentic-setup/.planning/ROADMAP.md
@/Users/trilogy/Documents/agentic-setup/.planning/phases/11-quality-pipeline/11-RESEARCH.md

D-112 mandate: Task Orchestrator allowAgents must be updated to include all 5 new agents in Plan 11-06.

Pipeline routing rules (from research Architecture Patterns, Pattern 3 and Pattern 4):
  Code review path: DevBot opens PR → Task Orchestrator collects diff via gh pr diff → sessions_spawn(code-reviewer) → verdict routes
  Document review path: any Notion page draft → sessions_spawn(document-reviewer) → verdict routes
  Decision review path: every autonomous decision entry → sessions_spawn(decision-reviewer) → if pass: Notion write → execute
  Skill review path: Skill Creation authors SKILL.md → sessions_spawn(skill-reviewer) → if pass: Task Orchestrator runs /openclaw-stow
  Skill creation trigger: pattern repeat ≥2 → sessions_spawn(skill-creation) → skill-creation runs search + authors → returns SKILL.md → routes to skill-reviewer

Feedback loop convergence (from research STRIDE):
  Max 3 revision cycles per output — after 3 consecutive rejects on the same artifact, escalate to user via Telegram: "Quality gate: <artifact> rejected 3 times by <reviewer>. Manual intervention needed."

Stow deploy path: scripts/stow-deploy.sh in /Users/trilogy/Documents/agentic-setup/ (canonical deploy path from earlier phases)
Gateway restart: launchctl kickstart -k gui/$(id -u)/ai.openclaw.gateway

openclaw.json location for stow source: /Users/trilogy/Documents/agentic-setup/openclaw.json (source repo) → symlinked to ~/.openclaw/openclaw.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update openclaw.json allowAgents and add reviewer routing to Task Orchestrator SOUL.md</name>
  <files>
    /Users/trilogy/Documents/agentic-setup/openclaw.json
    ~/.openclaw/agents/task-orchestrator/SOUL.md
    ~/.openclaw/agents/task-orchestrator/TOOLS.md
  </files>
  <action>
Read /Users/trilogy/Documents/agentic-setup/openclaw.json (the source file). Find the task-orchestrator agent entry (look for "id": "task-orchestrator"). Locate its subagents.allowAgents array. Add all five new agent IDs if not already present: "code-reviewer", "document-reviewer", "decision-reviewer", "skill-reviewer", "skill-creation". Preserve all existing entries in the allowAgents array.

Write the updated openclaw.json back to /Users/trilogy/Documents/agentic-setup/openclaw.json.

Read ~/.openclaw/agents/task-orchestrator/SOUL.md. Append a new section "## Quality Pipeline Routing (Phase 11 — D-112)":

Code Review Gate:
  When: before any PR is opened by DevBot (or any agent)
  How: run /opt/homebrew/bin/gh pr diff <PR_NUMBER> to get the diff; construct sessions_spawn payload; spawn code-reviewer session
  After verdict: if pass → advance PR; if flag → advance with noted comments; if reject → send must_fix to DevBot; if 3 consecutive rejects → Telegram escalation

Document Review Gate:
  When: before any Notion page is finalized or before any TOOLS.md/SOUL.md update is committed as the final version
  How: send document text as sessions_spawn payload to document-reviewer
  After verdict: if pass → finalize; if reject → revise and resubmit

Decision Review Gate:
  When: before EVERY autonomous action (merge, issue create, PR close, Notion write, agent creation)
  How: prepare decision entry {action, rationale, reversibility, evidence}; sessions_spawn(decision-reviewer)
  After verdict: if pass → write to Notion → execute action; if reject → do NOT execute; report BLOCKED
  Exception: spawning decision-reviewer itself is pre-approved (anti-circular rule)

Skill Review Gate:
  When: after Skill Creation returns a SKILL.md
  How: send SKILL.md text as sessions_spawn payload to skill-reviewer
  After verdict: if pass → run /openclaw-stow; if reject → send must_fix to skill-creation for revision

Skill Creation Trigger:
  When: pattern repeat count reaches 2 in MEMORY.md (Phase 12 adds this counter)
  How: sessions_spawn(skill-creation, {pattern_description: "<name>", context: "<examples>"})
  After skill-creation session: skill-creation returns SKILL.md → route to skill-reviewer → if pass: stow

Feedback Loop Convergence Rule:
  Track revision cycle count per artifact. If any reviewer rejects the same artifact 3 times consecutively: send Telegram message "Quality gate: <artifact type> rejected 3 times by <reviewer id>. Human review needed." Mark the related Beads task as BLOCKED.

Update TOOLS.md — append section "## Quality Pipeline Tools":
  - gh pr diff <number>: get PR diff for Code Reviewer payload
  - jq -r '.verdict' <verdict-json>: extract verdict from reviewer response
  - Pipeline: sessions_spawn(<reviewer-id>, <payload>) → read close reason as verdict JSON
  </action>
  <verify>
    <automated>jq '.agents.list[] | select(.id == "task-orchestrator") | .subagents.allowAgents | map(select(. == "code-reviewer" or . == "document-reviewer" or . == "decision-reviewer" or . == "skill-reviewer" or . == "skill-creation")) | length' /Users/trilogy/Documents/agentic-setup/openclaw.json</automated>
  </verify>
  <done>
    jq query returns 5 (all five new agents present in allowAgents). Task Orchestrator SOUL.md contains "Quality Pipeline Routing" section with routing rules for all five agents. Feedback loop convergence rule (3 rejects → Telegram) is present.
  </done>
</task>

<task type="auto">
  <name>Task 2: Run stow+restart to deploy openclaw.json changes</name>
  <files>
    ~/.openclaw/openclaw.json
  </files>
  <action>
Run the canonical stow deploy script: zsh /Users/trilogy/Documents/agentic-setup/scripts/stow-deploy.sh

This script handles the jobs.json symlink conflict removal before stow runs (established pattern from Phase 1).

After stow completes, restart the gateway: launchctl kickstart -k gui/$(id -u)/ai.openclaw.gateway

Wait 3 seconds for the gateway to restart, then verify it is running: /opt/homebrew/bin/openclaw status (or equivalent health check command confirmed in prior phases).

Verify that the live openclaw.json at ~/.openclaw/openclaw.json now contains all five new agent IDs in the task-orchestrator allowAgents: jq '.agents.list[] | select(.id == "task-orchestrator") | .subagents.allowAgents' ~/.openclaw/openclaw.json
  </action>
  <verify>
    <automated>jq '.agents.list[] | select(.id == "task-orchestrator") | .subagents.allowAgents | map(select(. == "code-reviewer")) | length' /Users/trilogy/.openclaw/openclaw.json</automated>
  </verify>
  <done>
    jq on the LIVE openclaw.json at ~/.openclaw/ returns 1 for code-reviewer (confirming stow propagated the change). Gateway is running.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Task Orchestrator → quality pipeline agents | sessions_spawn now permitted to all 5 agents via allowAgents |
| openclaw.json change → stow deploy | Must use stow-deploy.sh (not raw stow) to handle jobs.json conflict |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-15 | Denial of Service | Feedback loop that never converges | mitigate | Max 3 revision cycles rule in SOUL.md; after 3 rejects → BLOCKED + Telegram escalation |
| T-11-16 | Elevation of Privilege | Agent added to allowAgents without Decision Reviewer gate | accept | allowAgents is a static config change; does not grant execution rights beyond sessions_spawn delegation |
| T-11-17 | Tampering | stow-deploy.sh bypassed for manual stow | mitigate | Plan action explicitly uses stow-deploy.sh path; jobs.json conflict would cause stow failure without it |
</threat_model>

<verification>
1. jq task-orchestrator allowAgents on /Users/trilogy/Documents/agentic-setup/openclaw.json returns array containing all 5 new agent IDs
2. jq same check on ~/.openclaw/openclaw.json (live) returns same result (stow propagated)
3. grep -c "Quality Pipeline Routing" ~/.openclaw/agents/task-orchestrator/SOUL.md >= 1
4. grep -c "3 times" ~/.openclaw/agents/task-orchestrator/SOUL.md >= 1 (convergence rule)
5. grep -c "code-reviewer" ~/.openclaw/agents/task-orchestrator/SOUL.md >= 1
</verification>

<success_criteria>
- All 5 new agents in task-orchestrator allowAgents in both source and live openclaw.json
- Task Orchestrator SOUL.md: routing rules for all 5 pipelines; exception for anti-circular decision-reviewer self-invocation
- Feedback loop convergence rule: max 3 rejects → Telegram escalation + BLOCKED
- stow+restart completed successfully; gateway running
</success_criteria>

<output>
Create .planning/phases/11-quality-pipeline/11-06-SUMMARY.md when done
</output>

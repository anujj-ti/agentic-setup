---
phase: 11-quality-pipeline
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - ~/.openclaw/agents/decision-reviewer/SOUL.md
  - ~/.openclaw/agents/decision-reviewer/IDENTITY.md
  - ~/.openclaw/agents/decision-reviewer/USER.md
  - ~/.openclaw/agents/decision-reviewer/AGENTS.md
  - ~/.openclaw/agents/decision-reviewer/TOOLS.md
  - ~/.openclaw/agents/decision-reviewer/SECURITY.md
autonomous: true
requirements:
  - QUAL-03
  - QUAL-05

must_haves:
  truths:
    - "decision-reviewer agent directory exists with all 6 directive files"
    - "SOUL.md rubric covers: rationale soundness, reversibility specificity, evidence observability"
    - "SOUL.md contains the anti-circular-review rule (does not review its own invocation)"
    - "SOUL.md contains the D-111 verdict schema"
  artifacts:
    - path: "~/.openclaw/agents/decision-reviewer/SOUL.md"
      provides: "Decision review rubric with anti-vagueness and anti-circular rules"
      contains: "verdict"
  key_links:
    - from: "decision-reviewer SOUL.md"
      to: "verdict schema"
      via: "D-111 output format"
      pattern: "verdict.*pass.*flag.*reject"
---

<objective>
Scaffold the Decision Reviewer agent using /openclaw-new-agent. This agent is positioned in the pre-Notion-log path for all autonomous actions. It validates that decision entries have sound rationale, specific reversibility, and observable evidence before they are written to the Notion decision log. It must also contain the anti-circular review rule to prevent system deadlock.

Purpose: Deploy QUAL-03 — Decision Reviewer as the gating agent for all autonomous Notion decision log entries.
Output: All 6 directive files for decision-reviewer, registered in openclaw.json.
</objective>

<execution_context>
@/Users/trilogy/Documents/agentic-setup/.claude/get-shit-done/workflows/execute-plan.md
@/Users/trilogy/Documents/agentic-setup/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/trilogy/Documents/agentic-setup/.planning/PROJECT.md
@/Users/trilogy/Documents/agentic-setup/.planning/ROADMAP.md
@/Users/trilogy/Documents/agentic-setup/.planning/phases/11-quality-pipeline/11-RESEARCH.md

Decision Reviewer position in the pipeline:
  Task Orchestrator prepares decision entry → sessions_spawn(decision-reviewer) → verdict
  If pass: Notion pre-log write executes → action executes
  If reject: action does NOT execute; Task Orchestrator is BLOCKED

Anti-circular-review rule (from research Pitfall 2):
  Decision Reviewer reviews decisions ABOUT THE WORLD (merges, issue creates, Notion writes).
  Decision Reviewer does NOT review the decision to invoke Decision Reviewer itself.
  "Review requests for your own invocation are pre-approved — do not recurse."

Review rubric focuses (from research Pattern 4):
1. Rationale soundness: Is the rationale a real reason or a vague assertion? "Seems like a good idea" is a real reason. Wrong. "CI passing and quality review passed" is a real reason.
2. Reversibility specificity: Is the reversibility field a specific command sequence or "unknown"? "unknown" is always a reject. Must be either a specific command (e.g., "gh pr reopen <n> + git revert <sha>") or "permanent — creates new commit, cannot auto-revert."
3. Evidence observability: Is the evidence an observable fact ("statusCheckRollup: all SUCCESS on PR #42") or an inference ("the code looked correct")? Inferences are rejects.
4. Action specificity: Is the action field a specific operation ("merge PR #42 with squash") or vague ("handle PR")?

Timeout rule from research: Decision Reviewer sessions should close quickly. Task Orchestrator enforces a 2-minute timeout; if session does not close, action falls back to BLOCKED state.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Invoke /openclaw-new-agent to scaffold decision-reviewer</name>
  <files>
    ~/.openclaw/agents/decision-reviewer/SOUL.md
    ~/.openclaw/agents/decision-reviewer/IDENTITY.md
    ~/.openclaw/agents/decision-reviewer/USER.md
    ~/.openclaw/agents/decision-reviewer/AGENTS.md
    ~/.openclaw/agents/decision-reviewer/TOOLS.md
    ~/.openclaw/agents/decision-reviewer/SECURITY.md
  </files>
  <action>
Run /openclaw-new-agent to scaffold decision-reviewer:
  - Agent ID: decision-reviewer
  - Model: anthropic/claude-sonnet-4-6
  - Channel: none
  - Description: "Specialist decision reviewer — validates autonomous decision entries before Notion log write; rejects vague rationale, unknown reversibility, and inferred evidence"

After scaffolding, update SOUL.md with the following sections:

Section "## Identity": Decision Reviewer is a gating agent in the pre-Notion-log path. Every autonomous decision the Task Orchestrator intends to execute passes through you first. If you reject: the action does NOT execute. You review decisions ABOUT THE WORLD. You do NOT review the decision to invoke you — that meta-action is pre-approved.

Section "## Anti-Circular Review Rule": If the review request is "should I invoke Decision Reviewer for action X?" — this is your own invocation. It is pre-approved. Do not recurse. Respond immediately with {"verdict":"pass","comments":[],"must_fix":[],"approved_at":"<ISO8601>"}.

Section "## Review Rubric":
1. Rationale soundness: Must be a specific reason, not a vague assertion.
   REJECT: "seemed appropriate", "standard procedure", "looked right"
   PASS: "CI passing on all 3 checks", "user explicitly requested via Telegram at 14:32", "duplicate issue confirmed by gh search"
2. Reversibility specificity: Must be either a specific command sequence or "permanent — creates new commit."
   REJECT: "unknown", "might be reversible", "TBD"
   PASS: "reversible: devbot-revert-merge.sh <sha> <pr> <notion-id>", "permanent — creates new commit, cannot auto-revert"
3. Evidence observability: Must be an observable fact, not an inference.
   REJECT: "the code looked correct", "seemed to be passing", "probably fine"
   PASS: "statusCheckRollup: all SUCCESS", "gh issue list --search returned 0 duplicates", "Notion page ID abc123 confirmed"
4. Action specificity: Must identify the exact operation.
   REJECT: "handle PR", "process issue"
   PASS: "merge PR #42 with squash --delete-branch", "close issue #17 with comment: resolved in PR #42"

Section "## Verdict Rules":
- pass: all 4 rubric items satisfied; Notion log write proceeds, then action executes
- flag: minor specificity improvement recommended; action may proceed (uncommon — bias toward pass or reject)
- reject: any rubric item violated; action does NOT execute; Task Orchestrator receives must_fix array
- Speed mandate: deliver verdict with minimal preamble. The Task Orchestrator enforces a 2-minute timeout.

Section "## Output Format" (D-111):
{"verdict":"pass"|"flag"|"reject","comments":["..."],"must_fix":["..."],"approved_at":"ISO8601 or null"}
  </action>
  <verify>
    <automated>test -d /Users/trilogy/.openclaw/agents/decision-reviewer && grep -c "Reversibility" /Users/trilogy/.openclaw/agents/decision-reviewer/SOUL.md</automated>
  </verify>
  <done>
    decision-reviewer directory exists. SOUL.md contains "Reversibility" in rubric, anti-circular rule, and D-111 verdict schema. openclaw.json updated.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Task Orchestrator → decision-reviewer | Decision text is passed as sessions_spawn payload; reviewer gates whether action proceeds |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-05 | Repudiation | Decision Reviewer always passes vague entries | mitigate | SOUL.md lists explicit reject examples by name ("seemed appropriate" = reject); no borderline categories |
| T-11-06 | Denial of Service | Circular review deadlock | mitigate | Anti-circular rule in SOUL.md: own invocation is pre-approved; respond immediately |
| T-11-07 | Elevation of Privilege | Decision Reviewer rejecting all actions (over-blocking) | accept | Task Orchestrator falls back to BLOCKED state and escalates to user via Telegram after 3 consecutive rejects on same action |
</threat_model>

<verification>
1. test -d ~/.openclaw/agents/decision-reviewer passes
2. All 6 directive files exist
3. jq '.agents.list[] | select(.id == "decision-reviewer")' ~/.openclaw/openclaw.json returns non-empty
4. grep -c "Anti-Circular" ~/.openclaw/agents/decision-reviewer/SOUL.md >= 1
5. grep -c "Reversibility" ~/.openclaw/agents/decision-reviewer/SOUL.md >= 2
</verification>

<success_criteria>
- decision-reviewer agent scaffolded with all 6 directive files
- SOUL.md rubric covers: rationale soundness, reversibility specificity, evidence observability, action specificity
- Anti-circular rule present: own invocation is pre-approved
- Speed mandate documented (2-minute timeout context)
- Verdict schema matches D-111 exactly
- openclaw.json updated
</success_criteria>

<output>
Create .planning/phases/11-quality-pipeline/11-03-SUMMARY.md when done
</output>

---
phase: 11-quality-pipeline
plan: 07
type: execute
wave: 3
depends_on:
  - 11-06
files_modified:
  - /Users/trilogy/Documents/agentic-setup/scripts/verify-phase-11.sh
autonomous: true
requirements:
  - QUAL-01
  - QUAL-02
  - QUAL-03
  - QUAL-04
  - QUAL-05
  - QUAL-06
  - QUAL-07
  - QUAL-08

must_haves:
  truths:
    - "verify-phase-11.sh confirms all 5 agent directories exist with required files"
    - "verify-phase-11.sh confirms all 5 agents are in task-orchestrator allowAgents in live openclaw.json"
    - "verify-phase-11.sh confirms each SOUL.md contains the D-111 verdict schema"
    - "verify-phase-11.sh confirms skill-creation SOUL.md contains NEVER stow rule"
    - "verify-phase-11.sh confirms search-skill-registries.sh exists and exits 0 with test pattern"
    - "verify-phase-11.sh exits 0 (all checks pass)"
  artifacts:
    - path: "/Users/trilogy/Documents/agentic-setup/scripts/verify-phase-11.sh"
      provides: "Phase 11 structural verification covering all 8 QUAL requirements"
---

<objective>
Create and run scripts/verify-phase-11.sh — the phase gate for the quality pipeline. The script runs structural verification across all 5 new agents plus the Task Orchestrator wiring, covering all 8 QUAL requirements without requiring live sessions_spawn invocations (which need the full gateway running with real agent sessions).

Purpose: Confirm the quality pipeline is correctly scaffolded, wired, and ready for live use.
Output: verify-phase-11.sh that passes all checks.
</objective>

<execution_context>
@/Users/trilogy/Documents/agentic-setup/.claude/get-shit-done/workflows/execute-plan.md
@/Users/trilogy/Documents/agentic-setup/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/trilogy/Documents/agentic-setup/.planning/PROJECT.md
@/Users/trilogy/Documents/agentic-setup/.planning/ROADMAP.md
@/Users/trilogy/Documents/agentic-setup/.planning/phases/11-quality-pipeline/11-RESEARCH.md

Verification approach: structural checks + script execution tests
All 8 QUAL requirements have observable structural evidence:
- QUAL-01: code-reviewer exists + rubric in SOUL.md + in allowAgents
- QUAL-02: document-reviewer exists + rubric in SOUL.md + in allowAgents
- QUAL-03: decision-reviewer exists + rubric in SOUL.md + in allowAgents
- QUAL-04: skill-reviewer exists + rubric in SOUL.md + in allowAgents
- QUAL-05: all SOUL.md files contain D-111 verdict schema
- QUAL-06: skill-creation exists + SOUL.md authoring mandate
- QUAL-07: search-skill-registries.sh exists + runs successfully
- QUAL-08: skill-reviewer exists + stow gate rule + skill-creation NEVER stow rule

Live sessions_spawn feedback loop verification is documented as a manual step (requires running gateway + active sessions).
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create and run verify-phase-11.sh</name>
  <files>
    /Users/trilogy/Documents/agentic-setup/scripts/verify-phase-11.sh
  </files>
  <action>
Create scripts/verify-phase-11.sh with #!/usr/bin/env zsh and set -euo pipefail.

Declare FAILURES=() array at the top.

Define a helper function: check() { local desc="$1" result="$2"; [[ "$result" == "0" ]] && echo "PASS: $desc" || { echo "FAIL: $desc"; FAILURES+=("$desc") } }

SECTION 1 — Agent directories and files (QUAL-01 through QUAL-06, QUAL-08):
For each of the 5 agents (code-reviewer, document-reviewer, decision-reviewer, skill-reviewer, skill-creation):
  - Check directory exists: test -d "$HOME/.openclaw/agents/<agent-id>"
  - Check SOUL.md exists: test -f "$HOME/.openclaw/agents/<agent-id>/SOUL.md"
  - Check TOOLS.md exists: test -f "$HOME/.openclaw/agents/<agent-id>/TOOLS.md"
  - Check SECURITY.md exists: test -f "$HOME/.openclaw/agents/<agent-id>/SECURITY.md"

SECTION 2 — Verdict schema present in all SOUL.md files (QUAL-05):
For each agent, grep -q "approved_at" in SOUL.md (D-111 schema marker)
grep -q "verdict" in SOUL.md

SECTION 3 — Task Orchestrator allowAgents (D-112):
  ALLOWED=$(jq -r '.agents.list[] | select(.id == "task-orchestrator") | .subagents.allowAgents[]' "$HOME/.openclaw/openclaw.json" 2>/dev/null || echo "")
  For each of the 5 agent IDs: check "task-orchestrator allowAgents includes <id>" using echo "$ALLOWED" | grep -qx "<id>"

SECTION 4 — Specific SOUL.md content checks:
  skill-creation NEVER stow rule: grep -q "NEVER" "$HOME/.openclaw/agents/skill-creation/SOUL.md"
  skill-reviewer stow gate ownership: grep -q "Task Orchestrator" "$HOME/.openclaw/agents/skill-reviewer/SOUL.md"
  decision-reviewer anti-circular rule: grep -q "Anti-Circular\|pre-approved" "$HOME/.openclaw/agents/decision-reviewer/SOUL.md"
  document-reviewer anti-vagueness: grep -qi "seems reasonable\|vague" "$HOME/.openclaw/agents/document-reviewer/SOUL.md"
  code-reviewer diff-only rule: grep -qi "ONLY the diff\|only.*diff" "$HOME/.openclaw/agents/code-reviewer/SOUL.md"
  Task Orchestrator quality routing: grep -q "Quality Pipeline Routing" "$HOME/.openclaw/agents/task-orchestrator/SOUL.md"
  Task Orchestrator convergence rule: grep -q "3 times\|3 consecutive" "$HOME/.openclaw/agents/task-orchestrator/SOUL.md"

SECTION 5 — Registry search script (QUAL-07):
  test -f "$HOME/.openclaw/agents/skill-creation/scripts/search-skill-registries.sh"
  zsh -n "$HOME/.openclaw/agents/skill-creation/scripts/search-skill-registries.sh" (syntax check)
  Run: ( zsh "$HOME/.openclaw/agents/skill-creation/scripts/search-skill-registries.sh" "test-skill-pattern" 2>/dev/null ); EXIT_CODE=$?
  Check exit code is 0

SECTION 6 — openclaw.json registration for all 5 agents:
  For each agent: jq -e ".agents.list[] | select(.id == \"<agent-id>\")" "$HOME/.openclaw/openclaw.json" > /dev/null

Final summary: if FAILURES is empty → echo "=== Phase 11 PASSED (all checks) ===" && exit 0
Otherwise → echo "=== Phase 11 FAILED: ${FAILURES[*]} ===" && exit 1

chmod +x the script. Run: zsh scripts/verify-phase-11.sh

Also print the following as a MANUAL VERIFICATION NOTE at the end:
  "Live feedback loop test (requires running gateway): send a known-bad diff to code-reviewer via Task Orchestrator sessions_spawn and confirm verdict='reject' is returned and routed back to DevBot. This is a manual verification step."
  </action>
  <verify>
    <automated>cd /Users/trilogy/Documents/agentic-setup && zsh scripts/verify-phase-11.sh</automated>
  </verify>
  <done>
    verify-phase-11.sh exits 0 with "Phase 11 PASSED" output. All structural checks pass. The manual feedback loop test note is printed at the end.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| verify script → live openclaw.json | Read-only verification; no modifications |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-18 | Tampering | verify script modifying agent files | accept | Script is read-only (grep, jq, test); no write operations |
| T-11-19 | Denial of Service | search-skill-registries.sh making live network calls during verify | mitigate | search-skill-registries.sh has --max-time 5 and || echo fallbacks; total verify runtime stays under 30s |
</threat_model>

<verification>
zsh scripts/verify-phase-11.sh exits 0 with "Phase 11 PASSED" output.
</verification>

<success_criteria>
- verify-phase-11.sh exits 0
- All 5 agent directories verified (existence, SOUL.md, TOOLS.md, SECURITY.md)
- D-111 verdict schema present in all 5 SOUL.md files
- All 5 agents in task-orchestrator allowAgents in live openclaw.json
- Specific rule checks pass: NEVER stow (skill-creation), stow gate (skill-reviewer), anti-circular (decision-reviewer), anti-vagueness (document-reviewer), diff-only (code-reviewer)
- Registry search script exists, passes syntax check, exits 0 on test pattern
- Task Orchestrator routing rules and convergence rule verified
</success_criteria>

<output>
Create .planning/phases/11-quality-pipeline/11-07-SUMMARY.md when done
</output>

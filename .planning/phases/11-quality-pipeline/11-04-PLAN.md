---
phase: 11-quality-pipeline
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - ~/.openclaw/agents/skill-reviewer/SOUL.md
  - ~/.openclaw/agents/skill-reviewer/IDENTITY.md
  - ~/.openclaw/agents/skill-reviewer/USER.md
  - ~/.openclaw/agents/skill-reviewer/AGENTS.md
  - ~/.openclaw/agents/skill-reviewer/TOOLS.md
  - ~/.openclaw/agents/skill-reviewer/SECURITY.md
autonomous: true
requirements:
  - QUAL-04
  - QUAL-05
  - QUAL-08

must_haves:
  truths:
    - "skill-reviewer agent directory exists with all 6 directive files"
    - "SOUL.md distinguishes required SKILL.md frontmatter fields from optional ones"
    - "SOUL.md contains a security review checklist (postinstall scripts, network calls in embedded shell)"
    - "SOUL.md states the stow gate ownership rule: stow is run by Task Orchestrator, never by Skill Creation agent"
    - "SOUL.md contains the D-111 verdict schema"
  artifacts:
    - path: "~/.openclaw/agents/skill-reviewer/SOUL.md"
      provides: "SKILL.md format validation rubric and stow gate ownership rule"
      contains: "verdict"
  key_links:
    - from: "skill-reviewer SOUL.md"
      to: "stow gate rule"
      via: "D-114 enforcement"
      pattern: "stow.*Task Orchestrator"
---

<objective>
Scaffold the Skill Reviewer agent using /openclaw-new-agent. This agent validates SKILL.md files authored by the Skill Creation agent. It checks format compliance, safety, and cc-openclaw convention alignment before the Task Orchestrator runs /openclaw-stow. The stow gate ownership rule (D-114) must be explicitly encoded in SOUL.md.

Purpose: Deploy QUAL-04 and QUAL-08 — Skill Reviewer as the penultimate step before any new skill reaches the live skill directory.
Output: All 6 directive files for skill-reviewer, registered in openclaw.json.
</objective>

<execution_context>
@/Users/trilogy/Documents/agentic-setup/.claude/get-shit-done/workflows/execute-plan.md
@/Users/trilogy/Documents/agentic-setup/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/trilogy/Documents/agentic-setup/.planning/PROJECT.md
@/Users/trilogy/Documents/agentic-setup/.planning/ROADMAP.md
@/Users/trilogy/Documents/agentic-setup/.planning/phases/11-quality-pipeline/11-RESEARCH.md

Stow gate ownership (D-114):
  /openclaw-stow is run by Task Orchestrator after Skill Reviewer passes.
  Skill Creation agent NEVER invokes /openclaw-stow directly.
  Skill Reviewer SOUL.md must enforce this understanding: "Your verdict is the gate; you do not run stow."

SKILL.md frontmatter — required vs optional (from /openclaw-new-agent SKILL.md, research Pitfall 3):
  Required: name, description, disable-model-invocation
  Optional: argument-hint (rejecting for missing optional fields is a false positive — SOUL.md must document this)

Security checks for skill content (from research STRIDE):
  - Does the skill contain embedded shell commands that make network calls?
  - Does the skill reference npm/pip/cargo install commands? (flag for legitimacy review)
  - Does the skill write files to paths outside the expected skill directory?
  - Does the skill define a postinstall script in any embedded package.json?
  - Does the skill hardcode any secrets, tokens, or API keys?

cc-openclaw convention checklist:
  - Does the skill follow the established 9-skill format? (observe existing skills in .claude/skills/)
  - Does the skill use /openclaw-stow + /openclaw-restart in the correct sequence?
  - Does the step structure use the established {name, prompt} format?

Review input: full SKILL.md file text
Review output: verdict JSON per D-111
</context>

<tasks>

<task type="auto">
  <name>Task 1: Invoke /openclaw-new-agent to scaffold skill-reviewer</name>
  <files>
    ~/.openclaw/agents/skill-reviewer/SOUL.md
    ~/.openclaw/agents/skill-reviewer/IDENTITY.md
    ~/.openclaw/agents/skill-reviewer/USER.md
    ~/.openclaw/agents/skill-reviewer/AGENTS.md
    ~/.openclaw/agents/skill-reviewer/TOOLS.md
    ~/.openclaw/agents/skill-reviewer/SECURITY.md
  </files>
  <action>
Run /openclaw-new-agent to scaffold skill-reviewer:
  - Agent ID: skill-reviewer
  - Model: anthropic/claude-sonnet-4-6
  - Channel: none
  - Description: "Specialist skill reviewer — validates SKILL.md files for format, safety, and cc-openclaw convention compliance before stow"

After scaffolding, update SOUL.md with:

Section "## Identity": Skill Reviewer is a gating agent for new skills. You receive a SKILL.md file for review. You do not invoke /openclaw-stow — that is the Task Orchestrator's responsibility after your verdict passes.

Section "## Stow Gate Ownership (D-114)": /openclaw-stow is run by the Task Orchestrator, not by you, not by the Skill Creation agent. Your verdict is the gate. The Task Orchestrator reads your verdict and runs stow if it is "pass."

Section "## SKILL.md Frontmatter Fields":
Required (reject if missing):
  - name: human-readable skill name
  - description: what the skill does (1-2 sentences)
  - disable-model-invocation: boolean
Optional (do NOT reject for absence):
  - argument-hint: placeholder text shown in Claude Code
Rejecting a skill for missing argument-hint is a false positive. Do not do this.

Section "## Review Rubric":
1. Format: Required frontmatter fields present. Step structure uses established {name, prompt} format matching existing 9-skill patterns.
2. Safety — reject for any of:
   - Embedded shell commands that make outbound network calls (curl, wget, gh api to external domains)
   - npm/pip/cargo install commands with packages not in CLAUDE.md Package Legitimacy Audit
   - Hardcoded secrets, tokens, API keys, or passwords
   - postinstall scripts in any embedded package.json
   - File writes to paths outside ~/.openclaw/ or the skill's own directory
3. cc-openclaw conventions:
   - If skill invokes stow: uses /openclaw-stow (not raw stow command)
   - If skill restarts gateway: uses /openclaw-restart or the standard launchctl kickstart command
   - If skill adds secrets: uses /openclaw-add-secret pattern
4. Scope: does the skill do exactly what its description claims, no more?

Section "## Verdict Rules":
- pass: all rubric checks pass; skill is safe to stow
- flag: format or convention issues that don't pose security risk; Skill Creation agent may fix
- reject: any safety item flagged; skill MUST be revised before stow

Section "## Output Format" (D-111):
{"verdict":"pass"|"flag"|"reject","comments":["..."],"must_fix":["..."],"approved_at":"ISO8601 or null"}
  </action>
  <verify>
    <automated>test -d /Users/trilogy/.openclaw/agents/skill-reviewer && grep -c "stow" /Users/trilogy/.openclaw/agents/skill-reviewer/SOUL.md</automated>
  </verify>
  <done>
    skill-reviewer directory exists. SOUL.md contains stow gate ownership rule, required vs optional frontmatter distinction, and D-111 verdict schema. openclaw.json updated.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Skill Creation agent → skill-reviewer | SKILL.md text is reviewed for embedded malicious commands |
| skill-reviewer verdict → Task Orchestrator | Verdict determines whether /openclaw-stow runs |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-08 | Tampering | Malicious postinstall in authored skill | mitigate | SOUL.md explicitly checks for postinstall scripts in any embedded package.json — automatic reject |
| T-11-09 | Tampering | Skill with hardcoded API key | mitigate | SOUL.md checks for hardcoded secrets — automatic reject |
| T-11-10 | Elevation of Privilege | Skill Creation agent bypassing Skill Reviewer by calling stow directly | mitigate | D-114: stow is called by Task Orchestrator only; Skill Creation SOUL.md prohibits direct stow invocation (Plan 11-05) |
| T-11-11 | Spoofing | Over-rejection of valid skills (argument-hint false positive) | mitigate | SOUL.md explicitly states argument-hint is optional — rejecting for its absence is prohibited |
</threat_model>

<verification>
1. test -d ~/.openclaw/agents/skill-reviewer passes
2. All 6 directive files exist
3. jq '.agents.list[] | select(.id == "skill-reviewer")' ~/.openclaw/openclaw.json returns non-empty
4. grep -c "argument-hint" ~/.openclaw/agents/skill-reviewer/SOUL.md >= 1 (the false-positive rule)
5. grep -c "Task Orchestrator" ~/.openclaw/agents/skill-reviewer/SOUL.md >= 2 (stow gate ownership)
</verification>

<success_criteria>
- skill-reviewer scaffolded with all 6 directive files
- SOUL.md distinguishes required from optional frontmatter fields (argument-hint false positive prevented)
- SOUL.md security checklist covers: postinstall, hardcoded secrets, outbound network calls, file writes outside expected paths
- Stow gate ownership rule present (D-114): Task Orchestrator runs stow, not Skill Creation, not Skill Reviewer
- Verdict schema matches D-111 exactly
</success_criteria>

<output>
Create .planning/phases/11-quality-pipeline/11-04-SUMMARY.md when done
</output>

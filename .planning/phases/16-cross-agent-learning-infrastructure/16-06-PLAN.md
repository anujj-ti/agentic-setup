---
phase: 16-cross-agent-learning-infrastructure
plan: 06
type: execute
wave: 3
depends_on:
  - 16-01
  - 16-02
  - 16-03
  - 16-04
  - 16-05
files_modified:
  - scripts/verify-phase-16.sh
autonomous: true
requirements:
  - LEARN-01
  - LEARN-02
  - LEARN-03
  - LEARN-04

must_haves:
  truths:
    - "verify-phase-16.sh exists and is executable"
    - "All 4 execution-tier agents have synapse-query-learnings.sh in their AGENTS.md"
    - "ci-monitor DREAM-ROUTINE.md exists"
    - "devbot DREAM-ROUTINE.md exists"
    - "task-orchestrator DREAM-ROUTINE.md has cross-silo learnings merge step"
    - "task-orchestrator TOOLS.md has evidence_artifact_id in schema reminder"
    - "synapse-query-learnings.sh exits 0 with missing SYNAPSE_TOKEN"
  artifacts:
    - path: "scripts/verify-phase-16.sh"
      provides: "Phase 16 verification script — 10 structural checks"
      contains: "synapse-query-learnings"
  key_links:
    - from: "scripts/verify-phase-16.sh"
      to: "all four agent AGENTS.md files"
      via: "grep checks"
      pattern: "grep.*synapse-query-learnings"
---

<objective>
Create and run `scripts/verify-phase-16.sh` — a 10-check structural verification script that confirms all Phase 16 deliverables are in place across all four execution-tier agents.

Purpose: Provides automated, repeatable verification that all LEARN-01 through LEARN-04 requirements are structurally satisfied before the phase is marked complete.

Output: verify-phase-16.sh that passes 10/10 checks and exits 0.
</objective>

<execution_context>
@/Users/trilogy/Documents/agentic-setup/.planning/../../../.claude/get-shit-done/workflows/execute-plan.md
@/Users/trilogy/Documents/agentic-setup/.planning/../../../.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/trilogy/Documents/agentic-setup/.planning/phases/16-cross-agent-learning-infrastructure/16-CONTEXT.md
@/Users/trilogy/Documents/agentic-setup/.planning/ROADMAP.md

<interfaces>
<!-- Pattern from scripts/verify-phase-15.sh — follow this exactly -->
verify-phase-15.sh pattern:
- #!/usr/bin/env zsh + set -euo pipefail
- PASS/FAIL counters, CHECK_N labels
- json_ok output with pass/total counts at the end
- Non-zero exit on any failure (FAIL > 0 → exit 1)

Files to check (Phase 16 deliverables):
1. scripts/synapse-query-learnings.sh — exists and executable
2. synapse-query-learnings.sh exits 0 with no token (non-blocking test)
3. task-orchestrator AGENTS.md — contains synapse-query-learnings.sh (LEARN-01)
4. devbot AGENTS.md — contains synapse-query-learnings.sh (LEARN-01)
5. ci-monitor AGENTS.md — contains synapse-query-learnings.sh (LEARN-01)
6. email-triage AGENTS.md — contains synapse-query-learnings.sh (LEARN-01)
7. devbot AGENTS.md — contains ci-monitor cross-silo tag (LEARN-02)
8. task-orchestrator TOOLS.md — contains evidence_artifact_id (LEARN-03)
9. ci-monitor DREAM-ROUTINE.md — exists (LEARN-04)
10. devbot DREAM-ROUTINE.md — exists (LEARN-04)

Bonus check (if feasible):
- task-orchestrator DREAM-ROUTINE.md contains "Cross-Silo Learnings" (LEARN-04)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create and run verify-phase-16.sh</name>
  <files>scripts/verify-phase-16.sh</files>
  <action>
Create `scripts/verify-phase-16.sh` following the exact pattern of `scripts/verify-phase-15.sh`. Use `#!/usr/bin/env zsh` + `set -euo pipefail`. Use PASS/FAIL counters and CHECK_N labels. Print PASS/FAIL per check to stderr. Output `{"ok":true,"data":{"pass":N,"total":10}}` to stdout on success, `{"ok":false,...}` on failure. Exit 1 if FAIL > 0.

Implement these 10 checks:

**CHECK 1 — synapse-query-learnings.sh exists and is executable (LEARN-01 shared script)**
```zsh
test -x ~/Documents/agentic-setup/scripts/synapse-query-learnings.sh
```

**CHECK 2 — synapse-query-learnings.sh exits 0 with no token (non-blocking, D-304)**
```zsh
SYNAPSE_TOKEN="" zsh ~/Documents/agentic-setup/scripts/synapse-query-learnings.sh \
  project.agentic-setup openclaw 5 2>/dev/null
# test exit code == 0
```

**CHECK 3 — task-orchestrator AGENTS.md contains synapse-query-learnings.sh (LEARN-01)**
```zsh
grep -q "synapse-query-learnings" \
  ~/.openclaw/agents/task-orchestrator/AGENTS.md
```

**CHECK 4 — devbot AGENTS.md contains synapse-query-learnings.sh (LEARN-01)**
```zsh
grep -q "synapse-query-learnings" \
  ~/.openclaw/agents/devbot/AGENTS.md
```

**CHECK 5 — ci-monitor AGENTS.md contains synapse-query-learnings.sh (LEARN-01)**
```zsh
grep -q "synapse-query-learnings" \
  ~/.openclaw/agents/ci-monitor/AGENTS.md
```

**CHECK 6 — email-triage AGENTS.md contains synapse-query-learnings.sh (LEARN-01)**
```zsh
grep -q "synapse-query-learnings" \
  ~/.openclaw/agents/email-triage/AGENTS.md
```

**CHECK 7 — devbot AGENTS.md has ci-monitor cross-silo tag (LEARN-02)**
```zsh
grep -q "ci-monitor" ~/.openclaw/agents/devbot/AGENTS.md
```

**CHECK 8 — task-orchestrator TOOLS.md has evidence_artifact_id schema reminder (LEARN-03)**
```zsh
grep -q "evidence_artifact_id" \
  ~/.openclaw/agents/task-orchestrator/TOOLS.md
```

**CHECK 9 — ci-monitor DREAM-ROUTINE.md exists (LEARN-04)**
```zsh
test -f ~/.openclaw/agents/ci-monitor/DREAM-ROUTINE.md
```

**CHECK 10 — devbot DREAM-ROUTINE.md exists (LEARN-04)**
```zsh
test -f ~/.openclaw/agents/devbot/DREAM-ROUTINE.md
```

Use `~/.openclaw/agents/...` paths for the agent file checks (the stow symlinks) to confirm the stowed versions are in place. The source files live in `.openclaw/agents/...` in the git repo — these are stowed into `~/.openclaw/agents/...` via stow-deploy.sh. Note: if stow has not been run yet, these paths may not exist — the verify script should WARN (not fail) if stow hasn't been run, since stow is a Phase 1 prerequisite and the plan executor may run verification before stow.

Actually — use the repo paths (`~/Documents/agentic-setup/.openclaw/agents/...`) for the grep checks since that is what gets modified. The symlinks will match after stow runs. Use `REPO_DIR="$HOME/Documents/agentic-setup"` at the top of the script.

After writing the script, make it executable:
```zsh
chmod +x scripts/verify-phase-16.sh
```

Then run it:
```zsh
zsh scripts/verify-phase-16.sh
```

Report the output. If any checks fail, fix the underlying issue before marking this task complete.
  </action>
  <verify>
    <automated>
      test -x /Users/trilogy/Documents/agentic-setup/scripts/verify-phase-16.sh
      zsh /Users/trilogy/Documents/agentic-setup/scripts/verify-phase-16.sh
    </automated>
  </verify>
  <done>
    verify-phase-16.sh exists, is executable, runs 10 checks, and exits 0 with all 10 checks passing. Output includes {"ok":true,"data":{"pass":10,"total":10}}.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| verify script→file system | Read-only checks; no mutations; no secrets accessed |
| verify script→Synapse (CHECK 2) | Invokes synapse-query-learnings.sh with empty token — purely a non-blocking test; no real API call made |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16-12 | Denial of Service | verify script hangs on Synapse call | mitigate | CHECK 2 uses SYNAPSE_TOKEN="" which triggers the token guard and exits 0 immediately — no network call made |
| T-16-SC | Tampering | npm/pip/cargo installs | accept | No package installs — single zsh verification script |
</threat_model>

<verification>
1. `test -x scripts/verify-phase-16.sh` exits 0
2. `zsh scripts/verify-phase-16.sh` exits 0 with 10/10 pass
3. Output contains `"ok":true`
4. Checks 3-6 confirm all four execution-tier agents have synapse-query-learnings.sh wired
5. Check 7 confirms devbot cross-silo ci-monitor tag (LEARN-02)
6. Check 8 confirms TOOLS.md schema reminder (LEARN-03)
7. Checks 9-10 confirm both new DREAM-ROUTINE.md files exist (LEARN-04)
</verification>

<success_criteria>
verify-phase-16.sh exists, is executable, runs to completion with all 10 checks passing, and exits 0. The output {"ok":true,"data":{"pass":10,"total":10}} confirms all LEARN-01 through LEARN-04 structural requirements are met.
</success_criteria>

<output>
Create `.planning/phases/16-cross-agent-learning-infrastructure/16-06-SUMMARY.md` when done.
</output>

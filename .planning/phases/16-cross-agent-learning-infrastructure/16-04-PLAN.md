---
phase: 16-cross-agent-learning-infrastructure
plan: 04
type: execute
wave: 2
depends_on:
  - 16-01
files_modified:
  - .openclaw/agents/devbot/AGENTS.md
  - .openclaw/agents/devbot/DREAM-ROUTINE.md
autonomous: true
requirements:
  - LEARN-01
  - LEARN-02
  - LEARN-04

must_haves:
  truths:
    - "devbot AGENTS.md has a Synapse learning query step at session startup before any GitHub operation"
    - "DevBot queries CI Monitor learnings (cross_silo per D-307 — tags: openclaw, github, ci-monitor)"
    - "devbot DREAM-ROUTINE.md is created with cross-silo learnings merge step capped at 500 tokens"
    - "Dream routine for devbot runs at a distinct time from other agents to avoid concurrent LLM load"
  artifacts:
    - path: ".openclaw/agents/devbot/AGENTS.md"
      provides: "Updated startup checklist with Synapse learning query including ci-monitor cross-silo"
      contains: "synapse-query-learnings.sh"
    - path: ".openclaw/agents/devbot/DREAM-ROUTINE.md"
      provides: "New DevBot dream routine with cross-silo learnings merge"
      contains: "cross-silo learnings"
  key_links:
    - from: ".openclaw/agents/devbot/AGENTS.md"
      to: "scripts/synapse-query-learnings.sh"
      via: "startup checklist exec call"
      pattern: "synapse-query-learnings"
    - from: ".openclaw/agents/devbot/DREAM-ROUTINE.md"
      to: "scripts/synapse-query-learnings.sh"
      via: "dream cycle Synapse query"
      pattern: "synapse-query-learnings"
---

<objective>
Update devbot AGENTS.md startup checklist to add a mandatory Synapse learning query step with cross-silo CI Monitor learnings. Create the missing devbot DREAM-ROUTINE.md with a cross-silo learnings merge step and a nightly distillation routine for DevBot's GitHub activity context.

Purpose: DevBot is the primary cross-silo beneficiary — it should query CI Monitor learnings (LEARN-02) before PR triage so that CI failure patterns inform its review decisions. The new DREAM-ROUTINE.md ensures DevBot's nightly distillation surfaces these cross-silo learnings.

Output: Updated AGENTS.md (new startup step 0) + new DREAM-ROUTINE.md for devbot.
</objective>

<execution_context>
@/Users/trilogy/Documents/agentic-setup/.planning/../../../.claude/get-shit-done/workflows/execute-plan.md
@/Users/trilogy/Documents/agentic-setup/.planning/../../../.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/trilogy/Documents/agentic-setup/.planning/phases/16-cross-agent-learning-infrastructure/16-CONTEXT.md

<interfaces>
<!-- Extracted from .openclaw/agents/devbot/AGENTS.md -->
Current devbot AGENTS.md startup checklist (7 numbered steps):
1. Read SOUL.md
2. Read memory/MEMORY.md
3. If task specifies a repo: read CONTEXT.md
4. Confirm gh auth has project scope
5. Emit status: STARTED
6. Check for pending issue pickup queue

Note: Step 0 (Synapse query) needs to be inserted BEFORE step 1.

<!-- D-307: DevBot domain tags -->
devbot queries:
  - "openclaw" (primary)
  - "github" (own domain)
  - "ci-monitor" (cross-silo — CI failure patterns inform PR triage per D-307/LEARN-02)

<!-- devbot has no DREAM-ROUTINE.md — create from scratch -->
devbot is an execution-tier agent with persistent MEMORY.md — it distills GitHub activity and PR patterns.
Dream routine timing: task-orchestrator = 23:05, ci-monitor = 23:10 → use 23:15 for devbot.
Delivery: silent (devbot has no Telegram channel binding — sessions_spawn only).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update devbot AGENTS.md with Synapse learning query startup step</name>
  <files>.openclaw/agents/devbot/AGENTS.md</files>
  <action>
Read the current `.openclaw/agents/devbot/AGENTS.md`. Insert a new "Step 0 — Query Synapse Learnings (MANDATORY)" BEFORE the existing "## Session Startup" step 1 (Read SOUL.md). This step must be the absolute first action on every DevBot session.

Add the following section at the top, before "## Session Startup":

---
## Step 0 — Query Synapse Learnings (MANDATORY)

Before reading SOUL.md or any other context, query Synapse for domain learnings. This is a cross-silo query — DevBot reads CI Monitor learnings to inform PR triage decisions (LEARN-02).

```zsh
# Own domain learnings
SYNAPSE_GH=$(bash ~/Documents/agentic-setup/scripts/synapse-query-learnings.sh \
  project.agentic-setup github 5 2>/dev/null)

# Cross-silo: CI Monitor failure patterns (informs PR triage — LEARN-02)
SYNAPSE_CI=$(bash ~/Documents/agentic-setup/scripts/synapse-query-learnings.sh \
  project.agentic-setup ci-monitor 3 2>/dev/null)
```

- If both are empty (Synapse unavailable): proceed — this step is non-blocking (D-304)
- If non-empty: read the bullet lists before step 1. Apply any CI failure pattern learnings when triaging PRs with failing checks.
- Example cross-silo use: if SYNAPSE_CI mentions "flaky test in repo X", flag that PR's CI failure as likely flaky rather than blocking.
---

Renumber the existing steps in "## Session Startup" to show they come after Step 0 (no need to renumber them literally — just preserve the existing numbering 1-6 in place).

Keep all existing AGENTS.md sections (Session Startup, Workspace Hygiene, Task Completion Protocol, Beads Execution Contract) exactly unchanged after the new section.
  </action>
  <verify>
    <automated>
      grep -c "synapse-query-learnings.sh" /Users/trilogy/Documents/agentic-setup/.openclaw/agents/devbot/AGENTS.md
    </automated>
  </verify>
  <done>
    devbot AGENTS.md has a Step 0 Synapse query section before Session Startup; queries github (5) and ci-monitor (3) tags; step is non-blocking; cross-silo purpose (PR triage) is explained; all existing content preserved.
  </done>
</task>

<task type="auto">
  <name>Task 2: Create devbot DREAM-ROUTINE.md</name>
  <files>.openclaw/agents/devbot/DREAM-ROUTINE.md</files>
  <action>
Create `.openclaw/agents/devbot/DREAM-ROUTINE.md`. DevBot has persistent memory (memory/MEMORY.md exists in its workspace) and accumulates per-repo context. The dream routine distills daily GitHub activity and merges cross-silo CI learnings.

Write the full DREAM-ROUTINE.md with these sections:

**## Trigger**
Nightly cron at 23:15 Asia/Kolkata. (Staggered after task-orchestrator at 23:05 and ci-monitor at 23:10 to prevent concurrent LLM load.)

**## Process**

1. Read `memory/MEMORY.md` for existing long-term DevBot context.
2. Scan `memory/` for any daily activity logs written during today's sessions (if any).
3. Query Synapse for cross-silo learnings:
   ```zsh
   SYNAPSE_GH=$(bash ~/Documents/agentic-setup/scripts/synapse-query-learnings.sh \
     project.agentic-setup github 5 2>/dev/null)
   SYNAPSE_CI=$(bash ~/Documents/agentic-setup/scripts/synapse-query-learnings.sh \
     project.agentic-setup ci-monitor 5 2>/dev/null)
   ```
4. Distill into `memory/YYYY-MM-DD-DISTILLED.md` (max 2,500 tokens). If no activity today: write a one-line entry noting no activity and exit.
5. Update `memory/MEMORY.md` with a `## Cross-Silo Learnings (updated: YYYY-MM-DD)` section — replace any prior version. Cap at 500 tokens of new Synapse content per cycle (D-311 pattern).
6. Update `memory/MEMORY-DIGEST.md` — rolling 3-day GitHub activity digest (max 7,500 tokens).
7. Archive distillations older than 3 days: move to `memory/archives/YYYY-MM-DD-DISTILLED.md`.

**## Distillation Format**

Each daily distillation uses these sections:

### GitHub Activity
Issues created, PRs reviewed, PRs merged, CI failures triaged — with repo and number references.

### PR Patterns
Recurring PR patterns observed: stale reviewers, common CI failure steps, repos with frequent failures.

### Per-Repo Context Updates
Any changes to per-repo CONTEXT.md files made today.

### Blockers
Issues that could not be completed — awaiting CI, awaiting review, missing context.

### Cross-Silo Learnings
Bullets from SYNAPSE_GH and SYNAPSE_CI, if non-empty. Cap at 500 tokens.

**## Rules**

1. NEVER include credentials, tokens, or API keys in any distillation.
2. Stay within 2,500-token daily distillation budget.
3. Focus on CHANGED context — skip repeating standing rules already in MEMORY.md.
4. Synapse unavailability is non-blocking — skip the Cross-Silo Learnings section if both variables are empty.

**## Token Budgets**

- Daily distillation: max 2,500 tokens.
- 3-day rolling MEMORY-DIGEST.md: max 7,500 tokens.
- Cross-silo Synapse section: max 500 tokens per cycle.

**## Delivery**

Silent. DevBot has no Telegram channel binding — sessions_spawn only. No channel notification sent.
  </action>
  <verify>
    <automated>
      test -f /Users/trilogy/Documents/agentic-setup/.openclaw/agents/devbot/DREAM-ROUTINE.md
      grep -c "cross-silo\|Cross-Silo" /Users/trilogy/Documents/agentic-setup/.openclaw/agents/devbot/DREAM-ROUTINE.md
      grep -c "synapse-query-learnings" /Users/trilogy/Documents/agentic-setup/.openclaw/agents/devbot/DREAM-ROUTINE.md
      grep -c "500 token" /Users/trilogy/Documents/agentic-setup/.openclaw/agents/devbot/DREAM-ROUTINE.md
    </automated>
  </verify>
  <done>
    devbot DREAM-ROUTINE.md exists; triggers at 23:15 IST; queries github and ci-monitor Synapse tags; merges cross-silo learnings with 500-token cap; uses 6-section distillation format; delivery is silent; file is within context budget for agent execution.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| script→Synapse API | Bearer token from launchd env; HTTPS; read-only query |
| Synapse CI claims→devbot PR triage | Text advisory used as soft signal — no automated PR action taken without human-facing Notion pre-log |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16-08 | Denial of Service | Synapse query at devbot startup blocks PR triage | mitigate | D-304: exit 0 always; non-blocking by design |
| T-16-09 | Tampering | Malicious learning claim influences DevBot PR merge decision | accept | DevBot still requires Notion pre-log gate before any merge action; Synapse claims are advisory context only, not executable instructions |
| T-16-SC | Tampering | npm/pip/cargo installs | accept | No package installs — markdown edits and script references only |
</threat_model>

<verification>
1. `grep -c "synapse-query-learnings.sh" .openclaw/agents/devbot/AGENTS.md` >= 2 (github + ci-monitor queries)
2. `grep "ci-monitor" .openclaw/agents/devbot/AGENTS.md` matches (cross-silo tag present)
3. `test -f .openclaw/agents/devbot/DREAM-ROUTINE.md` exits 0
4. `grep "23:15" .openclaw/agents/devbot/DREAM-ROUTINE.md` matches (staggered timing)
5. `grep "500 token" .openclaw/agents/devbot/DREAM-ROUTINE.md` matches
6. `grep "LEARN-02\|cross-silo" .openclaw/agents/devbot/AGENTS.md` matches
</verification>

<success_criteria>
devbot AGENTS.md has a Step 0 Synapse learning query section querying both github and ci-monitor domain tags before any session startup steps. devbot DREAM-ROUTINE.md exists, triggers at 23:15 IST, merges up to 500 tokens of Synapse cross-silo learnings per cycle, and runs silently. LEARN-02 cross-silo requirement is visibly implemented with explanation of how CI Monitor learnings inform PR triage.
</success_criteria>

<output>
Create `.planning/phases/16-cross-agent-learning-infrastructure/16-04-SUMMARY.md` when done.
</output>

---
phase: 16-cross-agent-learning-infrastructure
plan: 05
type: execute
wave: 2
depends_on:
  - 16-01
files_modified:
  - .openclaw/agents/email-triage/AGENTS.md
autonomous: true
requirements:
  - LEARN-01
  - LEARN-02

must_haves:
  truths:
    - "email-triage AGENTS.md has a Synapse learning query step before triage begins"
    - "email-triage queries its own historical pattern learnings using the email-triage domain tag (D-307)"
    - "Synapse query step is non-blocking — triage proceeds if Synapse is unavailable"
  artifacts:
    - path: ".openclaw/agents/email-triage/AGENTS.md"
      provides: "Updated startup checklist with Synapse learning query"
      contains: "synapse-query-learnings.sh"
  key_links:
    - from: ".openclaw/agents/email-triage/AGENTS.md"
      to: "scripts/synapse-query-learnings.sh"
      via: "startup checklist step"
      pattern: "synapse-query-learnings"
---

<objective>
Update email-triage AGENTS.md startup checklist to add a mandatory Synapse learning query step. Email-triage queries its own historical pattern learnings (domain tag: email-triage) before beginning triage. This gives the agent context from prior sessions about noise senders, category drift, and priority calibration.

Purpose: Satisfies LEARN-01 (all execution-tier agents query learnings at session start) for the email-triage agent. The email-triage agent queries its own historical pattern learnings (LEARN-02 cross-silo: patterns accumulated across sessions).

Output: Updated AGENTS.md with a pre-triage Synapse query step.
</objective>

<execution_context>
@/Users/trilogy/Documents/agentic-setup/.planning/../../../.claude/get-shit-done/workflows/execute-plan.md
@/Users/trilogy/Documents/agentic-setup/.planning/../../../.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/trilogy/Documents/agentic-setup/.planning/phases/16-cross-agent-learning-infrastructure/16-CONTEXT.md

<interfaces>
<!-- Extracted from .openclaw/agents/email-triage/AGENTS.md -->
Current startup checklist (7 numbered steps):
1. Verify email-triage.sh exists
2. Verify gogcli auth is ready
3. Load noise-senders list from memory/noise-senders.md
4. Load processed-IDs guard from memory/processed-ids.jsonl
5. [Legacy — skip on gogcli path] Refresh token check
6. [Legacy — skip on gogcli path] gmail-triage.js existence check
7. Load recent categorization context from memory/

New step 0 (Synapse query) must be inserted BEFORE step 1.

D-307: email-triage domain tags: ["openclaw","email-triage"]
D-304: exit 0 on unavailability — never block triage

Note: email-triage.sh (gogcli) is the primary path; the agent does actual triage work after these startup checks. The Synapse query contextualizes the categorization pass.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update email-triage AGENTS.md with Synapse learning query step</name>
  <files>.openclaw/agents/email-triage/AGENTS.md</files>
  <action>
Read the current `.openclaw/agents/email-triage/AGENTS.md`. Insert a new "Step 0 — Query Synapse Learnings (MANDATORY)" section at the very top of the Startup Checklist, before the existing step 1 (Verify email-triage.sh exists).

Add the following section immediately before "## Startup Checklist" step 1:

---
**0. Query Synapse for historical email-triage learnings (MANDATORY, before triage begins):**

```zsh
SYNAPSE_TRIAGE=$(bash ~/Documents/agentic-setup/scripts/synapse-query-learnings.sh \
  project.agentic-setup email-triage 5 2>/dev/null)
```

- Domain tag: `email-triage` — queries historical pattern learnings for this agent (D-307)
- If SYNAPSE_TRIAGE is empty (Synapse unavailable or no learnings yet): proceed to step 1 — non-blocking (D-304)
- If non-empty: read the bullet list before categorizing. Apply relevant insights:
  - Priority calibration adjustments from prior sessions
  - Newly-identified noise sender patterns not yet in noise-senders.md
  - Category drift signals (e.g., "GitHub notification emails for repo X trend toward FYI, not Action Required")
- After triage completes: if you identified a reusable pattern (a new noise sender, a calibration adjustment), record it via `scripts/synapse-record-learning.sh` using the `email-triage` tag.
---

Keep all existing numbered steps (1 through 7) and all existing sections (Execution Flow, No Beads Integration in Phase 6, Memory Structure) exactly unchanged.

The step should be formatted consistently with the existing numbered steps — use bold "0." prefix to match the markdown style of steps 1-7. The description should make clear this is a cross-session learning query that feeds into the categorization pass, not a blocking prerequisite.
  </action>
  <verify>
    <automated>
      grep -c "synapse-query-learnings.sh" /Users/trilogy/Documents/agentic-setup/.openclaw/agents/email-triage/AGENTS.md
    </automated>
  </verify>
  <done>
    email-triage AGENTS.md has a Step 0 Synapse query section before step 1; uses project.agentic-setup with email-triage tag; step is non-blocking; includes a note to record new pattern learnings after triage; all existing content (steps 1-7, Execution Flow, Memory Structure) preserved unchanged.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| script→Synapse API | Bearer token from launchd env; HTTPS; read-only query |
| Synapse learning claims→email categorization | Text advisory; no automated send or mark-read decisions based solely on Synapse content |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16-10 | Denial of Service | Synapse query blocks email triage startup | mitigate | D-304: exit 0 always; startup check is non-blocking; triage proceeds |
| T-16-11 | Tampering | Malicious Synapse claim causes incorrect email categorization | accept | Categorization follows SOUL.md rules; Synapse claims are advisory context; no automated outbound action (D-160: never auto-send) |
| T-16-SC | Tampering | npm/pip/cargo installs | accept | No package installs — single AGENTS.md markdown edit |
</threat_model>

<verification>
1. `grep -c "synapse-query-learnings.sh" .openclaw/agents/email-triage/AGENTS.md` >= 1
2. `grep "email-triage" .openclaw/agents/email-triage/AGENTS.md` matches the Synapse tag
3. `grep -c "non-block\|non-blocking\|proceed" .openclaw/agents/email-triage/AGENTS.md` >= 1 in Step 0
4. All existing steps 1-7 still present: `grep -c "Verify email-triage.sh" .openclaw/agents/email-triage/AGENTS.md` still >= 1
5. `grep "noise-senders.md" .openclaw/agents/email-triage/AGENTS.md` still matches (step 3 unchanged)
</verification>

<success_criteria>
email-triage AGENTS.md has a new Step 0 before the existing startup checklist that queries Synapse for email-triage domain learnings using synapse-query-learnings.sh. The step is non-blocking, explains how learnings feed into categorization, and instructs the agent to record new patterns after triage. All existing startup steps and sections are preserved unchanged.
</success_criteria>

<output>
Create `.planning/phases/16-cross-agent-learning-infrastructure/16-05-SUMMARY.md` when done.
</output>

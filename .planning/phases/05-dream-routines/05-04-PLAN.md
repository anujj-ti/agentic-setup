---
phase: 05-dream-routines
plan: "04"
type: execute
wave: 3
depends_on:
  - "05-03"
files_modified:
  - scripts/verify-phase-05.sh
autonomous: true
requirements:
  - ORCH-06
must_haves:
  truths:
    - "All ORCH-06 pre-run checks pass: cron jobs registered, timezone correct, archive dirs exist, DREAM-ROUTINE.md files present"
    - "verify-phase-05.sh script exists and exits 0"
    - "Phase 5 smoke test confirms readiness for the first nightly run at 23:00/23:05 IST"
  artifacts:
    - path: "scripts/verify-phase-05.sh"
      provides: "Automated ORCH-06 verification suite"
      contains: "Asia/Kolkata"
  key_links:
    - from: "scripts/verify-phase-05.sh"
      to: "/opt/homebrew/bin/openclaw gateway status"
      via: "shell exec"
      pattern: "openclaw.*status"
---

<objective>
Write and execute `scripts/verify-phase-05.sh` — the automated pre-run verification suite for ORCH-06. This plan does not wait for the first nightly dream run (that's a post-run manual verification); it confirms all the wiring is correct before 23:00 IST arrives.

Purpose: The RESEARCH.md Validation Architecture identified a Wave 0 gap: `scripts/verify-phase-05.sh` must exist before the phase gate. This plan creates it and runs it. If any check fails, the executor reports the specific failing check and its output so the issue can be diagnosed.

Output: A passing verify-phase-05.sh exit 0 that confirms all six pre-run ORCH-06 smoke checks. Post-first-run token verification (ROADMAP success criteria 3 and 4) is deferred to the morning after first execution — noted in the summary.
</objective>

<execution_context>
@/Users/trilogy/Documents/agentic-setup/.planning/phases/05-dream-routines/05-CONTEXT.md
</execution_context>

<context>
@/Users/trilogy/Documents/agentic-setup/.planning/ROADMAP.md
@/Users/trilogy/Documents/agentic-setup/.planning/phases/05-dream-routines/05-RESEARCH.md

<interfaces>
RESEARCH.md Validation Architecture — Phase Requirements to Test Map (ORCH-06):

| Check | Automated Command |
|-------|-------------------|
| Dream cron jobs exist in gateway with correct tz | `openclaw gateway status --json` → python3 filter for Dream Routine jobs, assert tz=Asia/Kolkata |
| Archive directories exist for both agents | `test -d ~/.openclaw/agents/user-orchestrator/memory/archives && test -d ~/.openclaw/agents/task-orchestrator/memory/archives` |
| DREAM-ROUTINE.md present for both agents | `test -f ~/.openclaw/agents/user-orchestrator/DREAM-ROUTINE.md && test -f ~/.openclaw/agents/task-orchestrator/DREAM-ROUTINE.md` |
| jobs.json symlink is valid stow symlink | `test -L ~/.openclaw/cron/jobs.json` |
| Cron jobs have correct timezone (not UTC) | `python3 -c "..."` checking schedule.tz field |
| DREAM-ROUTINE.md contains token budget language | `grep -c "2,500 token" ~/.openclaw/agents/*/DREAM-ROUTINE.md` |

Post-first-run checks (deferred — require actual dream execution):
- Daily distillation word count proxy: `wc -w` on DISTILLED.md (≤1875 words ≈ ≤2500 tokens)
- 3-day digest word count proxy: `wc -w` on MEMORY-DIGEST.md (≤5625 words ≈ ≤7500 tokens)

RESEARCH.md Pitfall 4 — Token budget proxy:
2,500 tokens ≈ 1,875 words at ~1.33 tokens/word average
7,500 tokens ≈ 5,625 words

CLAUDE.md shell conventions:
- Shebang: `#!/usr/bin/env zsh`
- Strict mode: `set -euo pipefail`
- stdout: JSON only (`{"ok": true, "data": {...}}` or `{"ok": false, "error": "..."}`)
- stderr: human-readable logs (`print "..." >&2`)
- Exit code: non-zero on failure, 0 on success
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write scripts/verify-phase-05.sh</name>
  <files>scripts/verify-phase-05.sh</files>
  <action>
    Create `scripts/verify-phase-05.sh` following CLAUDE.md shell conventions exactly:
    - Shebang: `#!/usr/bin/env zsh`
    - Strict mode: `set -euo pipefail`
    - stdout: structured JSON result
    - stderr: per-check progress logs
    - Exit 0 on all pass, exit 1 on any failure

    The script runs six ORCH-06 pre-run checks in order. For each check, print progress to stderr and accumulate pass/fail state. Output a final JSON summary to stdout.

    Check 1 — DREAM-ROUTINE.md files present (stow already ran):
    Test that `~/.openclaw/agents/user-orchestrator/DREAM-ROUTINE.md` and `~/.openclaw/agents/task-orchestrator/DREAM-ROUTINE.md` both exist. Also verify each contains the token budget language ("2,500 token").

    Check 2 — MEMORY.md files present (stow already ran):
    Test that `~/.openclaw/agents/user-orchestrator/MEMORY.md` and `~/.openclaw/agents/task-orchestrator/MEMORY.md` both exist.

    Check 3 — Archive directories exist:
    Test that `~/.openclaw/agents/user-orchestrator/memory/archives` and `~/.openclaw/agents/task-orchestrator/memory/archives` both exist as directories. These were confirmed pre-existing, but verify defensively.

    Check 4 — jobs.json is a stow symlink:
    `test -L ~/.openclaw/cron/jobs.json` — if it's a plain file (gateway overwrote), report as FAIL with the message "jobs.json is a plain file, not a symlink — run stow-deploy.sh again".

    Check 5 — jobs.json has correct timezone:
    Parse `~/.openclaw/cron/jobs.json` via Python3. Assert all jobs have `schedule.tz == "Asia/Kolkata"`. Also assert no job has `schedule.tz == "UTC"`.

    Check 6 — Gateway reports both dream cron jobs:
    Run `/opt/homebrew/bin/openclaw gateway status --json 2>/dev/null` and parse output. Filter for jobs whose name contains "Dream Routine". Assert exactly 2 jobs found, both with `schedule.tz == "Asia/Kolkata"`. If the gateway JSON command fails or times out, fall back to checking jobs.json directly (the symlink check in Check 4 already verifies the file is live).

    Final output to stdout — JSON format per CLAUDE.md:
    If all pass: `{"ok": true, "data": {"checks_passed": 6, "phase": "05-dream-routines", "note": "Post-run checks (token caps) require manual verification after first nightly run at 23:00 IST"}}`
    If any fail: `{"ok": false, "error": "<first_failing_check>: <reason>", "checks_passed": N}`

    Make the script executable: `chmod +x scripts/verify-phase-05.sh`
  </action>
  <verify>
    <automated>test -f /Users/trilogy/Documents/agentic-setup/scripts/verify-phase-05.sh && test -x /Users/trilogy/Documents/agentic-setup/scripts/verify-phase-05.sh && head -2 /Users/trilogy/Documents/agentic-setup/scripts/verify-phase-05.sh | grep -c "zsh"</automated>
  </verify>
  <done>scripts/verify-phase-05.sh exists, is executable, starts with `#!/usr/bin/env zsh` + `set -euo pipefail`, and implements all six ORCH-06 checks.</done>
</task>

<task type="auto">
  <name>Task 2: Run verify-phase-05.sh and confirm all checks pass</name>
  <files></files>
  <action>
    Run the verification script and capture its output:
    ```
    zsh /Users/trilogy/Documents/agentic-setup/scripts/verify-phase-05.sh
    ```

    Capture both stdout (JSON result) and stderr (check-by-check progress). The script must exit 0.

    If any check fails:
    - Diagnose the specific failing check from stderr output
    - Common failure modes and their resolutions:
      - Check 1/2 fail ("DREAM-ROUTINE.md not found"): stow-deploy.sh from Plan 05-03 did not complete — re-run it
      - Check 4 fail ("jobs.json is a plain file"): gateway recreated it after stow — run `rm -f ~/.openclaw/cron/jobs.json && zsh scripts/stow-deploy.sh`
      - Check 5 fail ("wrong timezone"): jobs.json was written with UTC — inspect the file, fix the tz field, re-run stow
      - Check 6 fail (gateway not showing jobs): gateway restart from Plan 05-03 may not have completed — re-run `launchctl kickstart -k gui/$(id -u)/ai.openclaw.gateway`, wait 5 seconds, retry

    Report the full stdout JSON and stderr output in the task completion summary.

    After the script passes, note in the summary that ROADMAP.md success criteria 3 (daily MEMORY.md within 2,500-token cap) and 4 (3-day rolling digest within 7,500-token cap) require manual post-run verification the morning after the first nightly run at 23:00 IST. These cannot be automated until the dream routine has actually run.
  </action>
  <verify>
    <automated>zsh /Users/trilogy/Documents/agentic-setup/scripts/verify-phase-05.sh 2>/dev/null | python3 -c "import json,sys; d=json.load(sys.stdin); assert d['ok'], f'verify script failed: {d}'; print(f\"PASS: {d['data']['checks_passed']} checks passed\")"</automated>
  </verify>
  <done>verify-phase-05.sh exits 0. stdout JSON has `"ok": true` with `"checks_passed": 6`. All six ORCH-06 pre-run checks pass. Summary notes that token cap verification (ROADMAP success criteria 3 and 4) is deferred to post-first-run manual check.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| verify script → gateway status API | Read-only status query; no write operations |
| verify script → filesystem | Read-only checks on ~/.openclaw/agents/; no modifications |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-11 | Information Disclosure | verify script stdout | accept | Script outputs pass/fail counts, not file contents or credential values |
| T-05-12 | Tampering | verify script writes to agent dirs | accept | Script is read-only (test/grep/python3 parse only — no writes) |
| T-05-SC | Tampering | npm/pip/cargo installs | accept | No packages installed in this plan |
</threat_model>

<verification>
```bash
# Script exists and is executable
test -f /Users/trilogy/Documents/agentic-setup/scripts/verify-phase-05.sh && \
  test -x /Users/trilogy/Documents/agentic-setup/scripts/verify-phase-05.sh && \
  echo "PASS: script exists and executable"

# Script passes all checks
zsh /Users/trilogy/Documents/agentic-setup/scripts/verify-phase-05.sh 2>/dev/null | \
  python3 -c "import json,sys; d=json.load(sys.stdin); print(d)"

# Post-run check reminders (manual — after 23:00 IST first run)
# wc -w ~/.openclaw/agents/user-orchestrator/memory/*-DISTILLED.md   # should be ≤1875
# wc -w ~/.openclaw/agents/user-orchestrator/memory/MEMORY-DIGEST.md  # should be ≤5625
# wc -w ~/.openclaw/agents/task-orchestrator/memory/*-DISTILLED.md
# wc -w ~/.openclaw/agents/task-orchestrator/memory/MEMORY-DIGEST.md
```
</verification>

<success_criteria>
1. `scripts/verify-phase-05.sh` exists, is executable, follows CLAUDE.md conventions (zsh shebang, strict mode, JSON stdout, stderr logs)
2. Script exits 0 with `"ok": true` and 6 checks passed
3. All six ORCH-06 pre-run smoke checks confirmed green:
   - Both DREAM-ROUTINE.md files present with token budget language
   - Both MEMORY.md files present
   - Both memory/archives/ directories exist
   - ~/.openclaw/cron/jobs.json is a symlink (not plain file)
   - Both jobs have timezone Asia/Kolkata (not UTC)
   - Gateway status reports two dream cron jobs
4. Summary documents post-run manual checks required the morning after first nightly execution
</success_criteria>

<output>
Create `.planning/phases/05-dream-routines/05-04-SUMMARY.md` when done.
</output>

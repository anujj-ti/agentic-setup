---
phase: 05-dream-routines
plan: "03"
type: execute
wave: 2
depends_on:
  - "05-01"
  - "05-02"
files_modified:
  - .openclaw/cron/jobs.json
  - .openclaw/openclaw.json
autonomous: true
requirements:
  - ORCH-06
must_haves:
  truths:
    - "`.openclaw/cron/` directory exists in the repo (new — was absent before this phase)"
    - "`.openclaw/cron/jobs.json` exists with two dream routine entries (user-orchestrator at 23:00, task-orchestrator at 23:05, both Asia/Kolkata)"
    - "openclaw.json has `memory.qmd.paths` with four entries covering both agents' memory/ and agent-root *.md files"
    - "Stow symlink `~/.openclaw/cron/jobs.json` is valid after deploy"
    - "Gateway restarted and both cron jobs visible in gateway status"
  artifacts:
    - path: ".openclaw/cron/jobs.json"
      provides: "Both dream routine cron job definitions"
      contains: "Asia/Kolkata"
    - path: ".openclaw/openclaw.json"
      provides: "QMD semantic indexing paths for both agents"
      contains: "user-orchestrator-memory"
  key_links:
    - from: ".openclaw/cron/jobs.json"
      to: "~/.openclaw/cron/jobs.json"
      via: "stow --no-folding symlink"
      pattern: "jobs\\.json.*->.*agentic-setup"
    - from: ".openclaw/openclaw.json"
      to: "memory.qmd.paths"
      via: "gateway QMD indexer"
      pattern: "qmd"
---

<objective>
Create the `.openclaw/cron/` directory in the repo, write `jobs.json` with both dream routine cron entries, add QMD path entries to `openclaw.json`, then run stow-deploy.sh and restart the gateway so changes are live.

Purpose: DREAM-ROUTINE.md files from Plans 05-01/02 are inert until the cron jobs exist to trigger them. This plan wires the scheduling layer. The `.openclaw/cron/` directory does not yet exist in the repo (confirmed) — creating it is a prerequisite for stow to produce the `~/.openclaw/cron/jobs.json` symlink.

Output: `~/.openclaw/cron/jobs.json` as a stow-managed symlink pointing to the repo; openclaw.json updated with QMD paths; gateway running with both dream cron jobs loaded.
</objective>

<execution_context>
@/Users/trilogy/Documents/agentic-setup/.planning/phases/05-dream-routines/05-CONTEXT.md
</execution_context>

<context>
@/Users/trilogy/Documents/agentic-setup/.planning/ROADMAP.md
@/Users/trilogy/Documents/agentic-setup/.openclaw/openclaw.json
@/Users/trilogy/Documents/agentic-setup/scripts/stow-deploy.sh

<interfaces>
From RESEARCH.md Pattern 1 — Canonical cron job schema (D-40, D-41, D-42, D-43):

User Orchestrator job:
```json
{
  "id": "<python3-uuid>",
  "agentId": "user-orchestrator",
  "name": "User Orchestrator Dream Routine",
  "enabled": true,
  "createdAtMs": <python3-epoch-ms>,
  "schedule": {"kind": "cron", "expr": "0 23 * * *", "tz": "Asia/Kolkata"},
  "sessionTarget": "isolated",
  "wakeMode": "now",
  "payload": {
    "kind": "agentTurn",
    "message": "Run your nightly dream routine. Read DREAM-ROUTINE.md and follow the process exactly. Read today's daily log from memory/, distill it, update the rolling digest, and archive old distillations.",
    "model": "anthropic/claude-sonnet-4-6",
    "timeoutSeconds": 120
  },
  "delivery": {"mode": "announce", "channel": "last"}
}
```

Task Orchestrator job (D-42 — silent delivery, no channel):
```json
{
  "id": "<python3-uuid>",
  "agentId": "task-orchestrator",
  "name": "Task Orchestrator Dream Routine",
  "enabled": true,
  "createdAtMs": <python3-epoch-ms>,
  "schedule": {"kind": "cron", "expr": "5 23 * * *", "tz": "Asia/Kolkata"},
  "sessionTarget": "isolated",
  "wakeMode": "now",
  "payload": {
    "kind": "agentTurn",
    "message": "Run your nightly dream routine. Read DREAM-ROUTINE.md and follow the process exactly. Read today's daily log from memory/, distill it, update the rolling digest, and archive old distillations.",
    "model": "anthropic/claude-sonnet-4-6",
    "timeoutSeconds": 120
  },
  "delivery": {"mode": "silent"}
}
```

jobs.json top-level envelope (D-49):
```json
{"version": 1, "jobs": [...]}
```

QMD path entries to add under `memory.qmd.paths` (D-47 — literal /Users/trilogy/ prefix):
```json
[
  {"path": "/Users/trilogy/.openclaw/agents/user-orchestrator/memory", "name": "user-orchestrator-memory", "pattern": "**/*.md"},
  {"path": "/Users/trilogy/.openclaw/agents/user-orchestrator", "name": "user-orchestrator-docs", "pattern": "*.md"},
  {"path": "/Users/trilogy/.openclaw/agents/task-orchestrator/memory", "name": "task-orchestrator-memory", "pattern": "**/*.md"},
  {"path": "/Users/trilogy/.openclaw/agents/task-orchestrator", "name": "task-orchestrator-docs", "pattern": "*.md"}
]
```

stow-deploy.sh key behavior (D-48):
- Uses `--dir=$REPO_DIR --target=$HOME/.openclaw --no-folding .openclaw`
- Repo `.openclaw/cron/jobs.json` → live `~/.openclaw/cron/jobs.json`
- Script runs `rm -f ~/.openclaw/cron/jobs.json` before stow (D-09 conflict cleanup)

Gateway restart command (D-50):
```bash
launchctl kickstart -k gui/$(id -u)/ai.openclaw.gateway
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create .openclaw/cron/ directory and write jobs.json</name>
  <files>.openclaw/cron/jobs.json</files>
  <action>
    The `.openclaw/cron/` directory does not yet exist in the repo (confirmed — RESEARCH.md Runtime State Inventory). Create it and write `jobs.json`.

    Step 1 — Detect OPENCLAW_REPO:
    ```
    OPENCLAW_REPO=$(readlink ~/.openclaw/openclaw.json 2>/dev/null | sed 's|/.openclaw/openclaw.json||')
    ```
    Verify it resolves to `/Users/trilogy/Documents/agentic-setup`. If it doesn't (symlink absent), use the hardcoded fallback `/Users/trilogy/Documents/agentic-setup`.

    Step 2 — Create the cron directory:
    ```
    mkdir -p "$OPENCLAW_REPO/.openclaw/cron"
    ```

    Step 3 — Generate two UUIDs and one epoch-ms timestamp:
    ```
    UUID_UO=$(python3 -c "import uuid; print(uuid.uuid4())")
    UUID_TO=$(python3 -c "import uuid; print(uuid.uuid4())")
    EPOCH_MS=$(python3 -c "import time; print(int(time.time() * 1000))")
    ```

    Step 4 — Write jobs.json using Python3 (not jq — avoids shell quoting issues with the message string):
    Build the jobs dict in Python with the two entries as specified in the interfaces section above. Write to `$OPENCLAW_REPO/.openclaw/cron/jobs.json`. Key field values:
    - user-orchestrator: schedule `"0 23 * * *"` tz `Asia/Kolkata`, delivery `announce/last`
    - task-orchestrator: schedule `"5 23 * * *"` tz `Asia/Kolkata`, delivery `silent` (D-42 — no channel field)
    - Both: model `anthropic/claude-sonnet-4-6` (D-41), timeoutSeconds 120, sessionTarget `isolated`, wakeMode `now`
    - Top-level envelope: `{"version": 1, "jobs": [...]}`

    Use `json.dumps(jobs, indent=2)` for readable output. Write via Python file open, not shell redirection.
  </action>
  <verify>
    <automated>test -f /Users/trilogy/Documents/agentic-setup/.openclaw/cron/jobs.json && python3 -c "import json; d=json.load(open('/Users/trilogy/Documents/agentic-setup/.openclaw/cron/jobs.json')); assert d['version']==1; assert len(d['jobs'])==2; print('PASS jobs.json valid JSON with 2 jobs')" && python3 -c "import json; d=json.load(open('/Users/trilogy/Documents/agentic-setup/.openclaw/cron/jobs.json')); tz_vals=[j['schedule']['tz'] for j in d['jobs']]; assert all(t=='Asia/Kolkata' for t in tz_vals), f'Wrong tz: {tz_vals}'; print('PASS timezone')" && python3 -c "import json; d=json.load(open('/Users/trilogy/Documents/agentic-setup/.openclaw/cron/jobs.json')); to_job=[j for j in d['jobs'] if j['agentId']=='task-orchestrator'][0]; assert to_job['delivery']['mode']=='silent', 'task-orchestrator must use silent delivery'; assert 'channel' not in to_job['delivery'], 'channel field must be absent for silent mode'; print('PASS silent delivery')"</automated>
  </verify>
  <done>`.openclaw/cron/jobs.json` exists with version=1 and two jobs: user-orchestrator at "0 23 * * *" Asia/Kolkata with announce/last delivery; task-orchestrator at "5 23 * * *" Asia/Kolkata with silent delivery and no channel field.</done>
</task>

<task type="auto">
  <name>Task 2: Add QMD paths to openclaw.json, stow, and restart gateway</name>
  <files>.openclaw/openclaw.json</files>
  <action>
    Step A — Add QMD paths to openclaw.json:
    Read `/Users/trilogy/Documents/agentic-setup/.openclaw/openclaw.json`. It currently has no `memory` key at the top level. Add a `"memory"` block with the four QMD path entries (D-47 — literal `/Users/trilogy/` paths, never `~` or `$HOME`):

    ```json
    "memory": {
      "qmd": {
        "paths": [
          {"path": "/Users/trilogy/.openclaw/agents/user-orchestrator/memory", "name": "user-orchestrator-memory", "pattern": "**/*.md"},
          {"path": "/Users/trilogy/.openclaw/agents/user-orchestrator", "name": "user-orchestrator-docs", "pattern": "*.md"},
          {"path": "/Users/trilogy/.openclaw/agents/task-orchestrator/memory", "name": "task-orchestrator-memory", "pattern": "**/*.md"},
          {"path": "/Users/trilogy/.openclaw/agents/task-orchestrator", "name": "task-orchestrator-docs", "pattern": "*.md"}
        ]
      }
    }
    ```

    Insert this block into the JSON object. The JSON is not JSON5 — do not add comments. Preserve all existing keys (gateway, agents, channels, bindings, meta, skills, wizard). Update `meta.lastTouchedAt` to the current ISO timestamp.

    Use Python3 to read, mutate, and write the JSON — do not use sed or manual text editing. This preserves exact formatting and avoids invalid JSON.

    Step B — Stow deploy:
    Run the canonical stow deploy script (D-48):
    ```
    zsh /Users/trilogy/Documents/agentic-setup/scripts/stow-deploy.sh
    ```
    This runs `rm -f ~/.openclaw/cron/jobs.json` then `stow --dir=$REPO_DIR --target=$HOME/.openclaw --no-folding .openclaw`.

    Verify the symlink was created:
    ```
    test -L ~/.openclaw/cron/jobs.json && echo "symlink OK"
    ```

    If stow reports "existing target is not owned by stow" for any file, run `rm -f ~/.openclaw/cron/jobs.json` manually and retry stow. The script already handles this but log the outcome.

    Step C — Restart gateway (D-50):
    ```
    launchctl kickstart -k gui/$(id -u)/ai.openclaw.gateway
    ```
    Wait 3 seconds, then check gateway status:
    ```
    sleep 3 && /opt/homebrew/bin/openclaw gateway status --json 2>/dev/null | python3 -c "import json,sys; d=json.load(sys.stdin); print('gateway:', d.get('status', 'unknown'))" 2>/dev/null || /opt/homebrew/bin/openclaw status 2>/dev/null | head -5
    ```
  </action>
  <verify>
    <automated>
      python3 -c "import json; d=json.load(open('/Users/trilogy/Documents/agentic-setup/.openclaw/openclaw.json')); paths=d['memory']['qmd']['paths']; assert len(paths)==4; names=[p['name'] for p in paths]; assert 'user-orchestrator-memory' in names and 'task-orchestrator-memory' in names; print('PASS QMD paths')" && test -L /Users/trilogy/.openclaw/cron/jobs.json && echo "PASS stow symlink" && python3 -c "import json; src=open('/Users/trilogy/.openclaw/cron/jobs.json').read(); d=json.loads(src); assert len(d['jobs'])==2; print('PASS live jobs.json readable')"
    </automated>
  </verify>
  <done>openclaw.json has memory.qmd.paths with four entries (no ~ or $HOME). ~/.openclaw/cron/jobs.json is a symlink (not a plain file). Gateway is running. Live jobs.json is readable with two dream routine jobs.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| repo jobs.json → gateway cron runner | stow symlink; gateway reads on each tick |
| openclaw.json memory.qmd.paths → gateway QMD indexer | gateway reads at startup to configure semantic index |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-07 | Tampering | jobs.json stow conflict | mitigate | stow-deploy.sh always runs `rm -f ~/.openclaw/cron/jobs.json` before stow (D-09 convention from Phase 1) |
| T-05-08 | Information Disclosure | QMD path indexing | mitigate | Paths scoped to each agent's own memory/ — no cross-agent directory traversal; daily logs are gitignored |
| T-05-09 | Denial of Service | Concurrent dream sessions at 23:00 | mitigate | 5-minute stagger (23:00 vs 23:05) prevents two isolated LLM sessions overlapping |
| T-05-10 | Tampering | openclaw.json direct text edit | mitigate | Python3 JSON parse+serialize used — cannot produce invalid JSON; existing keys preserved |
| T-05-SC | Tampering | npm/pip/cargo installs | accept | No packages installed in this plan |
</threat_model>

<verification>
After both tasks complete:

```bash
# jobs.json valid with two jobs
python3 -c "
import json
d = json.load(open('/Users/trilogy/Documents/agentic-setup/.openclaw/cron/jobs.json'))
assert d['version'] == 1
assert len(d['jobs']) == 2
agents = [j['agentId'] for j in d['jobs']]
assert 'user-orchestrator' in agents
assert 'task-orchestrator' in agents
tzs = [j['schedule']['tz'] for j in d['jobs']]
assert all(t == 'Asia/Kolkata' for t in tzs)
to_job = next(j for j in d['jobs'] if j['agentId'] == 'task-orchestrator')
assert to_job['delivery']['mode'] == 'silent'
assert 'channel' not in to_job['delivery']
print('PASS jobs.json')
"

# QMD paths in openclaw.json
python3 -c "
import json
d = json.load(open('/Users/trilogy/Documents/agentic-setup/.openclaw/openclaw.json'))
paths = d['memory']['qmd']['paths']
names = [p['name'] for p in paths]
assert 'user-orchestrator-memory' in names
assert 'task-orchestrator-memory' in names
assert all('/Users/trilogy/' in p['path'] for p in paths), 'Must use literal path not ~ or \$HOME'
print('PASS QMD paths')
"

# Stow symlink
test -L /Users/trilogy/.openclaw/cron/jobs.json && echo "PASS symlink" || echo "FAIL not a symlink"

# Gateway sees both cron jobs
/opt/homebrew/bin/openclaw gateway status --json 2>/dev/null | python3 -c "
import json, sys
d = json.load(sys.stdin)
jobs = d.get('cron', {}).get('jobs', [])
dream = [j for j in jobs if 'Dream Routine' in j.get('name','')]
print(f'Dream jobs found: {len(dream)}')
for j in dream:
    print(f\"  {j['agentId']}: {j['schedule']['expr']} tz={j['schedule']['tz']}\")
assert len(dream) == 2, f'Expected 2 dream jobs, got {len(dream)}'
print('PASS gateway')
" 2>/dev/null || echo "NOTE: gateway status check — verify manually if JSON parse fails"
```
</verification>

<success_criteria>
1. `.openclaw/cron/jobs.json` exists with version=1 and exactly two jobs: user-orchestrator at "0 23 * * *" and task-orchestrator at "5 23 * * *", both tz Asia/Kolkata
2. Task Orchestrator job delivery.mode is "silent" with no channel field
3. `openclaw.json` has memory.qmd.paths with four entries, all using literal `/Users/trilogy/` paths
4. `~/.openclaw/cron/jobs.json` is a symlink (test -L passes)
5. Gateway is running; `/openclaw-status` or `openclaw gateway status` shows both dream cron jobs
</success_criteria>

<output>
Create `.planning/phases/05-dream-routines/05-03-SUMMARY.md` when done.
</output>

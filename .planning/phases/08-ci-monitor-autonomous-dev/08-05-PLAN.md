---
phase: 08-ci-monitor-autonomous-dev
plan: 05
type: execute
wave: 4
depends_on:
  - 08-02
  - 08-04
files_modified:
  - scripts/verify-phase-08.sh
autonomous: false
requirements:
  - DEV-03
  - DEV-04

must_haves:
  truths:
    - "verify-phase-08.sh runs all 6 automated smoke checks and exits 0 when all pass"
    - "CI Monitor appears in openclaw status output"
    - "CI Monitor cron job with */4 * * * * appears in openclaw status"
    - "poll-ci.sh passes syntax check and is executable"
    - "devbot-intake-issue.sh --dry-run returns ok:true"
    - "devbot-create-epic.sh and devbot-execute-cycle.sh pass syntax checks"
    - "User understands the manual steps needed to verify live CI alerting"
  artifacts:
    - path: "scripts/verify-phase-08.sh"
      provides: "Phase 8 automated smoke test covering DEV-03 and DEV-04"
      contains: "ci-monitor"
  key_links:
    - from: "verify-phase-08.sh"
      to: "openclaw status"
      via: "PATH=/opt/homebrew/opt/node@24/bin:$PATH /opt/homebrew/bin/openclaw status"
      pattern: "openclaw status"
---

<objective>
Create and run the Phase 8 verification script, confirm all automated smoke checks pass, and document the manual verification steps required to prove live CI alerting and end-to-end autonomous dev flow.

Purpose: Plans 08-01 through 08-04 create the CI Monitor agent, cron job, and DevBot autonomous dev scaffold. This plan is the phase gate: it runs a single verification script, reports pass/fail per check, and hands off clear manual test instructions to the user for the parts that require a live CI failure or a real GitHub issue.

Output: `scripts/verify-phase-08.sh` that covers all 6 automated checks from the research validation architecture. A human verification checkpoint for the Telegram alert test and Beads claim/close cycle.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/08-ci-monitor-autonomous-dev/08-CONTEXT.md
@.planning/phases/08-ci-monitor-autonomous-dev/08-RESEARCH.md
@.planning/phases/08-ci-monitor-autonomous-dev/08-02-SUMMARY.md
@.planning/phases/08-ci-monitor-autonomous-dev/08-04-SUMMARY.md

<interfaces>
<!-- Checks to implement — from research Validation Architecture section -->

DEV-03 checks:
  DEV-03a: CI Monitor agent in openclaw status
    command: PATH="/opt/homebrew/opt/node@24/bin:$PATH" /opt/homebrew/bin/openclaw status | grep -c "ci-monitor"
    pass: count >= 1
  DEV-03b: CI Monitor cron with */4 * * * * in openclaw status
    command: PATH="/opt/homebrew/opt/node@24/bin:$PATH" /opt/homebrew/bin/openclaw status | grep -c "CI Monitor Poll"
    pass: count >= 1
  DEV-03c: poll-ci.sh syntax + executable
    command: zsh -n $CI_AGENT_DIR/scripts/poll-ci.sh && [[ -x $CI_AGENT_DIR/scripts/poll-ci.sh ]]
    pass: both succeed (exit 0)
  DEV-03d: Deduplication: poll-ci.sh run with empty repos file outputs ok:true, alerted:false
    command: Create temp repos file with one comment line; run poll-ci.sh with AGENT_DIR pointing to temp; verify JSON output has alerted:false
    Note: this is an integration smoke test; skip if setting up temp AGENT_DIR is complex — mark as MANUAL

DEV-04 checks:
  DEV-04a: devbot-intake-issue.sh --dry-run returns ok:true
    command: zsh .openclaw/agents/devbot/scripts/devbot-intake-issue.sh anujj-ti/agentic-setup 1 --dry-run | python3 -c "import json,sys; d=json.load(sys.stdin); exit(0 if d['ok'] else 1)"
    pass: exit 0
  DEV-04b: devbot-create-epic.sh and devbot-execute-cycle.sh pass syntax checks
    command: zsh -n .openclaw/agents/devbot/scripts/devbot-create-epic.sh && zsh -n .openclaw/agents/devbot/scripts/devbot-execute-cycle.sh
    pass: exit 0
  DEV-04c: devbot-execute-cycle.sh does not contain gh pr merge (merge guard)
    command: grep -c "pr merge" .openclaw/agents/devbot/scripts/devbot-execute-cycle.sh
    pass: count == 0

Manual-only checks (documented in checkpoint):
  M-01: Trigger a CI failure on anujj-ti/agentic-setup and verify Telegram alert arrives within 5 minutes
  M-02: Run full claim/close cycle on a test Beads task and verify evidence strings
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create and run verify-phase-08.sh</name>
  <files>
    scripts/verify-phase-08.sh
  </files>
  <action>
Create `scripts/verify-phase-08.sh` and run it. The script reports pass/fail for each of the 6 automated checks and exits non-zero if any check fails.

Script requirements:
- Shebang: `#!/usr/bin/env zsh` + `set -uo pipefail` (NOT `set -e` — script must run all checks and report failures before exiting)
- Constants: `CI_AGENT_DIR="$HOME/.openclaw/agents/ci-monitor"`, `DEVBOT_DIR="$HOME/.openclaw/agents/devbot"`, `OC_BIN="/opt/homebrew/bin/openclaw"`, `REPO_DIR="/Users/trilogy/Documents/agentic-setup"`
- Tracking: `PASS=0`, `FAIL=0`, `SKIP=0`
- Helper function `check <id> <description> <command>`: runs the command, prints "[PASS] id: description" or "[FAIL] id: description — <error>", increments counters
- Summary at end: print PASS/FAIL/SKIP counts; exit 1 if FAIL > 0; exit 0 otherwise

Implement these checks in order:

DEV-03a — CI Monitor in openclaw status:
  `PATH="/opt/homebrew/opt/node@24/bin:$PATH" "$OC_BIN" status 2>/dev/null | grep -qc "ci-monitor"`

DEV-03b — CI Monitor cron job in openclaw status:
  `PATH="/opt/homebrew/opt/node@24/bin:$PATH" "$OC_BIN" status 2>/dev/null | grep -qc "CI Monitor Poll"`

DEV-03c — poll-ci.sh syntax + executable:
  `zsh -n "$CI_AGENT_DIR/scripts/poll-ci.sh" && [[ -x "$CI_AGENT_DIR/scripts/poll-ci.sh" ]]`

DEV-03d — state/last-seen-runs.json initialized:
  `python3 -c "import json; d=json.load(open('$CI_AGENT_DIR/state/last-seen-runs.json')); assert isinstance(d, dict)"`
  (validates the file exists and is a valid JSON object, even if empty)

DEV-04a — devbot-intake-issue.sh --dry-run:
  `zsh "$DEVBOT_DIR/scripts/devbot-intake-issue.sh" anujj-ti/agentic-setup 1 --dry-run | python3 -c "import json,sys; d=json.load(sys.stdin); exit(0 if d.get('ok') else 1)"`

DEV-04b — devbot scripts syntax check:
  `zsh -n "$DEVBOT_DIR/scripts/devbot-create-epic.sh" && zsh -n "$DEVBOT_DIR/scripts/devbot-execute-cycle.sh"`

DEV-04c — merge guard (gh pr merge absent from execute-cycle.sh):
  `count=$(grep -c "pr merge" "$DEVBOT_DIR/scripts/devbot-execute-cycle.sh" 2>/dev/null || echo 0); [[ "$count" -eq 0 ]]`

After implementing all checks, make the script executable and run it:
  `chmod +x /Users/trilogy/Documents/agentic-setup/scripts/verify-phase-08.sh`
  `zsh /Users/trilogy/Documents/agentic-setup/scripts/verify-phase-08.sh`

If any check fails: investigate and fix the root cause before proceeding to Task 2. Do not skip failures. If DEV-03a or DEV-03b fails, re-run stow-deploy.sh + gateway restart from Plan 08-02. If DEV-04a fails, re-examine devbot-intake-issue.sh.
  </action>
  <verify>
    <automated>zsh /Users/trilogy/Documents/agentic-setup/scripts/verify-phase-08.sh</automated>
  </verify>
  <done>verify-phase-08.sh exits 0 with all 7 checks reporting [PASS]; FAIL count = 0</done>
</task>

<task type="checkpoint:human-verify">
  <what-built>All Phase 8 automated smoke checks pass. The CI Monitor agent is live with its 4-minute polling cron, and the DevBot autonomous dev scaffold (intake, epic creation, execute cycle) is in place. Two verification scenarios require manual interaction that cannot be automated.</what-built>
  <how-to-verify>
    Manual test M-01: Live CI Alert (requires OPENCLAW_ANUJ_CHAT_ID to be set in Keychain first):

    1. Verify your chat ID is stored: `security find-generic-password -s openclaw.anuj-chat-id -a trilogy -w`
       Should return a numeric value. If empty, complete the Plan 08-01 checkpoint first.

    2. Trigger a CI failure on anujj-ti/agentic-setup:
       - Push a branch with a broken test or add a workflow that always fails
       - Example: create `.github/workflows/ci-fail-test.yml` that runs `exit 1`
       - Push it: `git push origin HEAD`

    3. Wait up to 5 minutes. You should receive a Telegram message in the format:
       `CI FAILED [anujj-ti/agentic-setup] <workflow> on <branch> — step: <step> — https://github.com/...`

    4. Verify deduplication: wait for the next 4-minute poll tick. You should NOT receive a second alert for the same run.

    5. Delete the test workflow and push again to clean up.

    Manual test M-02: Beads claim/close cycle (requires Phase 7 DevBot to be complete and Beads live):

    1. Create a test epic manually:
       `BEADS_DIR="$HOME/.openclaw/beads" /opt/homebrew/opt/node@24/bin/bd create "Test epic for Phase 8 verification" -t epic -p 1 --json | jq -r '.id'`

    2. Create a single test task under it:
       `BEADS_DIR="$HOME/.openclaw/beads" /opt/homebrew/opt/node@24/bin/bd create "Test task" --parent <epic-id> --json | jq -r '.id'`

    3. Run the execute-cycle script in design mode:
       `zsh .openclaw/agents/devbot/scripts/devbot-execute-cycle.sh <task-id> design anujj-ti/agentic-setup 999`

    4. Verify the task is closed:
       `BEADS_DIR="$HOME/.openclaw/beads" /opt/homebrew/opt/node@24/bin/bd list --status closed --json | jq '.[] | .reason'`
       Should show the factual evidence string.
  </how-to-verify>
  <resume-signal>Type "phase-8-verified" if both manual tests pass, or describe any issues found. If OPENCLAW_ANUJ_CHAT_ID is not yet set, type "alert-pending-chat-id" to acknowledge the CI alerting is scaffolded but not yet live.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| verify-phase-08.sh → openclaw status | Script reads openclaw gateway output; read-only, no state changes |
| verify-phase-08.sh → devbot scripts | Script invokes devbot-intake-issue.sh with --dry-run only; no live gh calls |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-13 | Information Disclosure | verify-phase-08.sh stdout | accept | Script output contains only pass/fail signals; no secrets echoed; OPENCLAW_ANUJ_CHAT_ID not referenced in verify script |
| T-08-14 | Tampering | Manual CI failure test | accept | Test workflow is deliberately ephemeral; user creates and deletes it; no persistent state risk |
| T-08-SC | Tampering | npm/pip/cargo installs | accept | No new packages installed in this plan |
</threat_model>

<verification>
- `zsh scripts/verify-phase-08.sh` exits 0 with all [PASS] results
- grep checks from the research Validation Architecture are all covered
- Manual test M-01 (CI alert) documented clearly with exact steps
- Manual test M-02 (Beads cycle) documented with exact bd commands
</verification>

<success_criteria>
`scripts/verify-phase-08.sh` exists, is executable, and exits 0 with all 7 automated checks [PASS]. User has confirmed manual verification results via checkpoint. Phase 8 success criteria from ROADMAP:
1. CI Monitor appears in `/openclaw-status` — verified by DEV-03a/DEV-03b checks
2. DevBot creates 5-subtask Beads epic before writing code — verified by DEV-04b check (script structure)
3. DevBot completes full claim/close cycle with factual evidence — verified by M-02 manual test
4. CI Monitor sends Telegram alert within 5 minutes of failure — verified by M-01 manual test
</success_criteria>

<output>
Create `.planning/phases/08-ci-monitor-autonomous-dev/08-05-SUMMARY.md` when done
</output>

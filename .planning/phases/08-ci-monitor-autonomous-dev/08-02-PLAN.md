---
phase: 08-ci-monitor-autonomous-dev
plan: 02
type: execute
wave: 2
depends_on:
  - 08-01
files_modified:
  - .openclaw/cron/jobs.json
  - .openclaw/openclaw.json
autonomous: true
requirements:
  - DEV-03

must_haves:
  truths:
    - "CI Monitor agent appears in openclaw.json agents.list"
    - "A cron job with expr `*/4 * * * *` and agentId `ci-monitor` exists in jobs.json"
    - "Cron job delivery mode is silent (no channel binding needed)"
    - "Gateway is restarted via stow-deploy.sh and launchctl kickstart"
    - "openclaw status shows ci-monitor in agent list and cron job list"
  artifacts:
    - path: ".openclaw/cron/jobs.json"
      provides: "CI Monitor poll cron entry"
      contains: "ci-monitor"
    - path: ".openclaw/openclaw.json"
      provides: "ci-monitor agent registration"
      contains: "ci-monitor"
  key_links:
    - from: "jobs.json ci-monitor cron"
      to: ".openclaw/agents/ci-monitor"
      via: "agentId field"
      pattern: "\"agentId\": \"ci-monitor\""
---

<objective>
Register the CI Monitor agent in openclaw.json and add the `*/4 * * * *` polling cron job to jobs.json. Deploy via stow-deploy.sh and restart the gateway so the agent and cron are live.

Purpose: The agent scaffold from Plan 08-01 has no runtime presence until it is registered in openclaw.json and a cron job drives its sessions. This plan wires both and verifies them via `openclaw status`.

Output: A live CI Monitor cron job polling every 4 minutes in an isolated agent session. DEV-03 infrastructure is fully deployed (alert delivery depends on OPENCLAW_ANUJ_CHAT_ID being set — see Plan 08-01 checkpoint).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/08-ci-monitor-autonomous-dev/08-CONTEXT.md
@.planning/phases/08-ci-monitor-autonomous-dev/08-RESEARCH.md
@.planning/phases/08-ci-monitor-autonomous-dev/08-01-SUMMARY.md

<interfaces>
<!-- Exact schema to follow — extracted from live files -->

From .openclaw/cron/jobs.json (live structure — must follow exactly):
```json
{
  "version": 1,
  "jobs": [
    {
      "id": "<uuid>",
      "agentId": "<agent-id>",
      "name": "<display name>",
      "enabled": true,
      "createdAtMs": <epoch-ms>,
      "schedule": {
        "kind": "cron",
        "expr": "<cron-expr>",
        "tz": "Asia/Kolkata"
      },
      "sessionTarget": "isolated",
      "wakeMode": "now",
      "payload": {
        "kind": "agentTurn",
        "message": "<instruction>",
        "model": "anthropic/claude-sonnet-4-6",
        "timeoutSeconds": <N>
      },
      "delivery": {
        "mode": "silent"
      }
    }
  ]
}
```

CI Monitor cron entry values (per D-81, D-82, D-89, research Pattern 2):
- agentId: "ci-monitor"
- name: "CI Monitor Poll"
- schedule.expr: "*/4 * * * *"
- schedule.tz: "Asia/Kolkata"
- payload.timeoutSeconds: 90  (D-89 — not 60; see research Pitfall 5)
- payload.message: "Run your CI polling routine. Execute scripts/poll-ci.sh and report the result. If new failures are found, the script will send Telegram alerts automatically."
- delivery.mode: "silent"  (D-82 — no channel binding; alert is imperative from script)

Generate fresh uuid and epoch-ms via:
python3 -c "import uuid, time; print(uuid.uuid4()); print(int(time.time()*1000))"

From openclaw.json agents.list pattern (Phase 3/4/6 established):
- paths use `/Users/trilogy/...` literal, never `~` or `$HOME`
- tools.alsoAllow includes "exec" for all script-running agents
- No channel binding for agents that are not user-facing
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Register ci-monitor in openclaw.json and add cron job to jobs.json</name>
  <files>
    .openclaw/openclaw.json
    .openclaw/cron/jobs.json
  </files>
  <action>
Two file modifications. Read both files first, then write updated versions.

1. openclaw.json — Add ci-monitor to the `agents.list` array (alongside existing user-orchestrator, task-orchestrator, email-triage entries). New entry:
   - `agentId`: "ci-monitor"
   - `name`: "CI Monitor"
   - `description`: "Polls tracked GitHub repositories for CI failures and sends Telegram alerts within 5 minutes of a failure"
   - `model`: "anthropic/claude-sonnet-4-6"
   - `workspaceDir`: "/Users/trilogy/.openclaw/agents/ci-monitor" (literal path — D-47 pattern, never `~`)
   - `tools.alsoAllow`: ["exec"]
   - No channel binding (no `channels` or `dmPolicy` field — D-80, D-82)

2. jobs.json — Append the CI Monitor Poll cron job entry to the `jobs` array. Keep all existing entries (user-orchestrator dream, task-orchestrator dream, morning standup). Generate a fresh UUID and epoch-ms for the new entry. Follow the exact schema shown in the interfaces block. Critical field values:
   - `agentId`: "ci-monitor"
   - `schedule.expr`: "*/4 * * * *"
   - `schedule.tz`: "Asia/Kolkata"
   - `payload.timeoutSeconds`: 90 (per D-89 — not 60)
   - `delivery.mode`: "silent" (per D-82 — no channel)

Both files must remain valid JSON after editing. Validate with `python3 -c "import json; json.load(open('.openclaw/openclaw.json'))"` and `python3 -c "import json; json.load(open('.openclaw/cron/jobs.json'))"` before proceeding to Task 2.
  </action>
  <verify>
    <automated>python3 -c "import json; d=json.load(open('/Users/trilogy/Documents/agentic-setup/.openclaw/openclaw.json')); agents=[a['agentId'] for a in d.get('agents',{}).get('list',[])]; print('ci-monitor in agents:', 'ci-monitor' in agents)" && python3 -c "import json; d=json.load(open('/Users/trilogy/Documents/agentic-setup/.openclaw/cron/jobs.json')); ci=[j for j in d['jobs'] if j['agentId']=='ci-monitor']; print('CI cron entries:', len(ci)); print('expr:', ci[0]['schedule']['expr'] if ci else 'MISSING'); print('timeout:', ci[0]['payload']['timeoutSeconds'] if ci else 'MISSING')"</automated>
  </verify>
  <done>openclaw.json contains ci-monitor in agents.list; jobs.json contains a ci-monitor cron entry with expr `*/4 * * * *` and timeoutSeconds 90; both files are valid JSON</done>
</task>

<task type="auto">
  <name>Task 2: Deploy via stow-deploy.sh and verify openclaw status</name>
  <files></files>
  <action>
Deploy the config changes and verify the gateway sees the new agent and cron job. Use the established deployment sequence (D-48, D-50 pattern from prior phases):

1. Run stow deploy:
   `zsh /Users/trilogy/Documents/agentic-setup/scripts/stow-deploy.sh`
   This removes the `~/.openclaw/cron/jobs.json` symlink conflict and runs stow to symlink the repo into `~/.openclaw/`.

2. Restart the gateway:
   `launchctl kickstart -k gui/$(id -u)/ai.openclaw.gateway`
   Wait 3 seconds for gateway to initialize.

3. Verify agent registration:
   `PATH="/opt/homebrew/opt/node@24/bin:$PATH" /opt/homebrew/bin/openclaw status`
   The output must show `ci-monitor` in the agents section.

4. Verify cron job:
   The status output must show a cron job named "CI Monitor Poll" with the `*/4 * * * *` schedule.

5. Capture the full status output to stderr for the SUMMARY. Write `{"ok": true, "deployed": true}` to stdout.

If stow-deploy.sh fails: check for the `rm -f ~/.openclaw/cron/jobs.json` step — this is mandatory before stow runs on cron/jobs.json (established in Phase 1). If the stow-deploy.sh script does not include this, add it.
  </action>
  <verify>
    <automated>PATH="/opt/homebrew/opt/node@24/bin:$PATH" /opt/homebrew/bin/openclaw status 2>/dev/null | grep -c "ci-monitor"</automated>
  </verify>
  <done>openclaw status output contains "ci-monitor" at least once (agent registration confirmed); no gateway startup errors in launchctl logs</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| jobs.json → gateway cron runner | Cron job config is read by the gateway; malformed JSON would prevent gateway startup |
| openclaw.json → agent loader | Agent registration; incorrect workspaceDir would cause agent session failures |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-05 | Tampering | jobs.json edit | mitigate | Validate JSON after edit with python3 before running stow-deploy.sh; invalid JSON aborts deployment |
| T-08-06 | Denial of Service | gateway restart loop | accept | Gateway restart is idempotent; launchctl kickstart -k forces a clean restart; low risk |
| T-08-SC | Tampering | npm/pip/cargo installs | accept | No new packages installed in this plan |
</threat_model>

<verification>
- `python3 -c "import json; json.load(open('.openclaw/openclaw.json'))"` — valid JSON
- `python3 -c "import json; json.load(open('.openclaw/cron/jobs.json'))"` — valid JSON
- `openclaw status | grep ci-monitor` — agent appears in status
- `openclaw status | grep "CI Monitor Poll"` — cron job appears in status
</verification>

<success_criteria>
CI Monitor agent is registered in openclaw.json. The `*/4 * * * *` cron job with `agentId: "ci-monitor"`, `timeoutSeconds: 90`, and `delivery.mode: "silent"` exists in jobs.json. Both files are valid JSON. Gateway is restarted and `openclaw status` shows ci-monitor in both the agent list and cron job list.
</success_criteria>

<output>
Create `.planning/phases/08-ci-monitor-autonomous-dev/08-02-SUMMARY.md` when done
</output>

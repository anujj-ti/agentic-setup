---
phase: 17-proactive-standup-insights
plan: 02
type: execute
wave: 2
depends_on:
  - 17-01
files_modified:
  - .openclaw/agents/user-orchestrator/SOUL.md
  - .openclaw/agents/user-orchestrator/TOOLS.md
autonomous: true
requirements:
  - STANDUP-01
  - STANDUP-02
  - STANDUP-03

must_haves:
  truths:
    - "User Orchestrator SOUL.md instructs the agent to call standup-insights.sh after standup-brief.sh in every morning cron session"
    - "SOUL.md specifies the output format: numbered Tackle First list, then Patterns (if any), then the standard facts section"
    - "TOOLS.md documents the pipe invocation and the insights JSON fields consumed"
    - "Formatting is label-only: each item shows title, status label, and source_field reference — no free-form interpretation (D-413)"
  artifacts:
    - path: ".openclaw/agents/user-orchestrator/SOUL.md"
      provides: "standup insights formatting rules"
      contains: "standup-insights.sh"
    - path: ".openclaw/agents/user-orchestrator/TOOLS.md"
      provides: "invocation pattern for standup-insights.sh"
      contains: "standup-insights.sh"
  key_links:
    - from: ".openclaw/agents/user-orchestrator/SOUL.md"
      to: "scripts/standup-insights.sh"
      via: "exec tool in cron session"
      pattern: "standup-insights\\.sh"
---

<objective>
Update User Orchestrator SOUL.md and TOOLS.md to call standup-insights.sh after
standup-brief.sh and format the enhanced morning standup Telegram message.

Purpose: Wire the classification engine from Plan 17-01 into the Telegram
message the user receives every morning. The agent must pipe standup JSON into
standup-insights.sh and use the returned insights section to prepend a Tackle
First list and Pattern alerts before the standard facts section.

Output: Updated SOUL.md (standup section with insights format rules) and
updated TOOLS.md (invocation pattern and field map for the insights JSON).
</objective>

<execution_context>
@/Users/trilogy/Documents/agentic-setup/.planning/../.claude/../../../.claude/get-shit-done/workflows/execute-plan.md
@/Users/trilogy/Documents/agentic-setup/.planning/../.claude/../../../.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/trilogy/Documents/agentic-setup/.planning/ROADMAP.md

Existing SOUL.md sections to PRESERVE (do not overwrite):
  - Identity
  - Responsibilities
  - Delegation Rules (MANDATORY)
  - Boundaries
  - Tone
  - Model Policy
  - Decision Retrieval Protocol (MEM-02)

Existing TOOLS.md sections to PRESERVE (do not overwrite):
  - Local context reference
  - Available Tools
  - Tool Policy
  - Environment
  - GitHub Identity Note
  - Standup Script Invocation (extend, do not replace)

standup-insights.sh output shape (produced by Plan 17-01):
{
  "ok": true,
  "data": {
    "insights": {
      "classified_items": [
        { "title": "...", "status": "Blocked|At Risk|On Track",
          "source_field": "ci_failures[0]", "reason": "..." }
      ],
      "tackle_first": [
        { "title": "...", "status": "Blocked|At Risk|On Track",
          "source_field": "ci_failures[0]", "reason": "..." }
      ],
      "patterns": [
        { "type": "ci_failures", "count": 4,
          "label": "4 CI failures overnight — possible systemic issue" }
      ]
    }
  }
}

Decisions governing format (D-411, D-412, D-413):
  D-411: Call standup-insights.sh after standup-brief.sh; format:
         Tackle First list (numbered) → Patterns (if any) → standard facts section
  D-412: standup-insights.sh takes standup JSON on stdin, outputs insights JSON
  D-413: Label-only: every item cites source_field — no free-form interpretation

Claude's Discretion:
  - Telegram formatting detail (numbered vs bullets → use numbered list)
  - Whether patterns section is bolded → bold the patterns label line

Phase 17 requires adding "Morning Standup Insights (STANDUP-01/02/03)" as a
new named section in SOUL.md immediately after the existing standup integration
note (line referencing "Phase 6 CHAN-04"), so the agent knows how to invoke the
new script and format the Telegram message.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Morning Standup Insights section to SOUL.md</name>
  <files>.openclaw/agents/user-orchestrator/SOUL.md</files>
  <action>
Append a new section to SOUL.md after the existing "Morning standup integration note"
paragraph (the line that says "The morning standup brief (Phase 6 CHAN-04) also calls
query-decisions.sh...").

The new section is titled: "## Morning Standup Insights (STANDUP-01/02/03)"

Content to add (per D-411, D-412, D-413):

---
## Morning Standup Insights (STANDUP-01/02/03)

### Overview
After calling standup-brief.sh in a morning cron session, pipe its JSON output
into standup-insights.sh to obtain classified items, a ranked tackle-first list,
and pattern alerts. Use the insights output to structure the Telegram message.

### Invocation sequence
1. Call standup-brief.sh (via exec in cron session) and capture its stdout as STANDUP_JSON.
2. Pipe STANDUP_JSON into standup-insights.sh and capture its stdout as INSIGHTS_JSON.
3. Check INSIGHTS_JSON .ok field — if false, fall back to the standard (pre-Phase 17) format.
4. Format the Telegram message using the structure below.

### Telegram message structure

Order of sections (top to bottom):
1. Header: "Good morning! Here is your standup brief:"
2. Tackle First (always present, even if empty):
   - Header: "Tackle First:"
   - If tackle_first is non-empty: numbered list, one item per line
     Format per item: "[N]. [title] — [status] ([source_field])"
     Example: "1. Fix broken auth pipeline — Blocked (ci_failures[0])"
   - If tackle_first is []: write "Nothing critical — clear runway."
3. Patterns (only when patterns array is non-empty):
   - Bold header: "*Patterns Detected:*"
   - One line per pattern: "  - [label]"
   - Example: "  - 4 CI failures overnight — possible systemic issue"
4. Standard facts section (as before Phase 17):
   - Merged PRs overnight
   - CI failures (raw list if any)
   - Stale PRs awaiting review
   - Autonomous decisions count
   - Overnight email count (if gog_available)
   - Today's calendar events (if gog_available)

### Label-only rule (D-413)
Each tackle-first item cites its source_field verbatim — do not interpret or
rephrase the source_field value. Do not generate free-form analysis beyond the
reason field already in the insights JSON.

### Fallback behavior
If standup-insights.sh returns ok:false or is unavailable, send the standard
standup brief (as before Phase 17) without the insights section. Do not block
the standup on insights failure.
---

Do not modify any existing section of SOUL.md. Append only.
  </action>
  <verify>
    <automated>grep -c "standup-insights.sh" /Users/trilogy/Documents/agentic-setup/.openclaw/agents/user-orchestrator/SOUL.md</automated>
  </verify>
  <done>
    SOUL.md contains the new "Morning Standup Insights" section with the invocation
    sequence, Telegram message structure, label-only rule, and fallback behavior.
    grep -c "standup-insights.sh" returns >= 1.
    All pre-existing sections (Identity, Responsibilities, Delegation Rules, etc.) are intact.
  </done>
</task>

<task type="auto">
  <name>Task 2: Update TOOLS.md with standup-insights.sh invocation and field map</name>
  <files>.openclaw/agents/user-orchestrator/TOOLS.md</files>
  <action>
Append a new subsection to the existing "Standup Script Invocation" section of TOOLS.md.
The existing content (standup-brief.sh invocation block and the "Parse the JSON output"
block) must be preserved in full. Add new content below the existing standup invocation
block.

New subsection title: "### Insights Enhancement (Phase 17)"

Content to add:

---
### Insights Enhancement (Phase 17)

After calling standup-brief.sh and capturing its output, pipe it into standup-insights.sh:

```zsh
STANDUP_JSON=$(  /opt/homebrew/bin/zsh ~/Documents/agentic-setup/scripts/standup-brief.sh --repo anujj-ti/agentic-setup )
INSIGHTS_JSON=$( printf '%s' "$STANDUP_JSON" | /opt/homebrew/bin/zsh ~/Documents/agentic-setup/scripts/standup-insights.sh )
```

Parse the insights output:
- `INSIGHTS_JSON | jq '.ok'` — if false, use STANDUP_JSON only (graceful fallback)
- `INSIGHTS_JSON | jq '.data.insights.tackle_first'` — array of ranked items (max 5)
- `INSIGHTS_JSON | jq '.data.insights.patterns'` — pattern alert array (empty [] if no patterns)
- `INSIGHTS_JSON | jq '.data.insights.classified_items'` — all classified items (for reference)

tackle_first item fields:
- `.title` — display name of the PR / CI run / issue
- `.status` — "Blocked", "At Risk", or "On Track"
- `.source_field` — e.g. "ci_failures[0]", "stale_prs[2]" (cite verbatim, D-413)
- `.reason` — one-sentence explanation from standup-insights.sh

patterns item fields:
- `.type` — "ci_failures" or "stale_prs"
- `.count` — number of items sharing this signal
- `.label` — display string, e.g. "4 CI failures overnight — possible systemic issue"

Note: standup-insights.sh binary path is
~/Documents/agentic-setup/scripts/standup-insights.sh (exec policy unchanged —
this script is also CRON SESSIONS ONLY, same as standup-brief.sh).
---

Do not remove or reorder any existing content in TOOLS.md.
  </action>
  <verify>
    <automated>grep -c "standup-insights.sh" /Users/trilogy/Documents/agentic-setup/.openclaw/agents/user-orchestrator/TOOLS.md</automated>
  </verify>
  <done>
    TOOLS.md contains the new Insights Enhancement subsection with the pipe invocation,
    field map for tackle_first and patterns, and the cron-sessions-only note.
    grep -c "standup-insights.sh" returns >= 1.
    All pre-existing TOOLS.md content (Available Tools, Tool Policy, Environment,
    GitHub Identity Note, original Standup Script Invocation block) is preserved.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| standup-insights.sh stdout → SOUL.md format rules | Insight labels and source_field values are field-name references (e.g. "ci_failures[0]") — safe to embed in Telegram messages |
| SOUL.md format rules → Telegram message | Agent follows label-only rule; no user-controlled content is re-interpreted |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-17-04 | Tampering | SOUL.md append | mitigate | append-only — preserve all existing sections; verified by grep on key existing phrases |
| T-17-05 | Denial of Service | insights fallback | mitigate | SOUL.md mandates fallback to standard standup if insights returns ok:false — standup is never blocked |
| T-17-06 | Information Disclosure | source_field in Telegram | accept | source_field values are internal field-path references (not secrets); branch names and PR numbers are already user-visible |
| T-17-SC | Tampering | npm/pip/cargo installs | accept | no package manager installs in this plan |
</threat_model>

<verification>
After both tasks complete:

1. grep -c "standup-insights.sh" .openclaw/agents/user-orchestrator/SOUL.md → >= 1
2. grep -c "standup-insights.sh" .openclaw/agents/user-orchestrator/TOOLS.md → >= 1
3. grep -c "Tackle First" .openclaw/agents/user-orchestrator/SOUL.md → >= 1
4. grep -c "Patterns Detected" .openclaw/agents/user-orchestrator/SOUL.md → >= 1
5. grep -c "Delegation Rules" .openclaw/agents/user-orchestrator/SOUL.md → >= 1 (original section preserved)
6. grep -c "Available Tools" .openclaw/agents/user-orchestrator/TOOLS.md → >= 1 (original section preserved)
7. grep -c "Insights Enhancement" .openclaw/agents/user-orchestrator/TOOLS.md → >= 1
</verification>

<success_criteria>
- SOUL.md contains "Morning Standup Insights (STANDUP-01/02/03)" section (D-411)
- SOUL.md specifies: invoke standup-insights.sh, format Tackle First then Patterns then facts (D-411)
- SOUL.md includes label-only rule citing source_field verbatim (D-413)
- SOUL.md includes fallback behavior for insights failure
- TOOLS.md contains pipe invocation and field map for tackle_first and patterns (D-412)
- All pre-existing content in both files is preserved
</success_criteria>

<output>
Create .planning/phases/17-proactive-standup-insights/17-02-SUMMARY.md when done
</output>

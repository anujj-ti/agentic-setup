---
phase: 17-proactive-standup-insights
plan: 03
type: execute
wave: 3
depends_on:
  - 17-01
  - 17-02
files_modified:
  - scripts/verify-phase-17.sh
autonomous: true
requirements:
  - STANDUP-01
  - STANDUP-02
  - STANDUP-03

must_haves:
  truths:
    - "verify-phase-17.sh exists, is executable, and runs without errors"
    - "All 10 checks pass green on the verification run"
    - "standup-insights.sh produces correct Blocked/At Risk/On Track classification for fixture inputs"
    - "tackle_first cap of 5 is enforced"
    - "tackle_first is always present as an array"
    - "pattern alert fires only when 3+ items share a signal type"
    - "SOUL.md and TOOLS.md contain the required standup-insights.sh references"
  artifacts:
    - path: "scripts/verify-phase-17.sh"
      provides: "10-check automated phase verification"
      min_lines: 60
  key_links:
    - from: "scripts/verify-phase-17.sh"
      to: "scripts/standup-insights.sh"
      via: "functional fixture tests"
      pattern: "standup-insights\\.sh"
---

<objective>
Create and run scripts/verify-phase-17.sh — a 10-check automated smoke test
that validates the standup-insights.sh script and the User Orchestrator config
updates from Plans 17-01 and 17-02.

Purpose: Prove that STANDUP-01 (classification), STANDUP-02 (tackle-first list),
and STANDUP-03 (pattern detection) are all correctly implemented before the phase
is marked complete.

Output: verify-phase-17.sh that exits 0 only when all 10 checks pass.
</objective>

<execution_context>
@/Users/trilogy/Documents/agentic-setup/.planning/../.claude/../../../.claude/get-shit-done/workflows/execute-plan.md
@/Users/trilogy/Documents/agentic-setup/.planning/../.claude/../../../.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/trilogy/Documents/agentic-setup/.planning/ROADMAP.md

Fixture JSON for verification tests (used inline in verify-phase-17.sh):
  - EMPTY_STANDUP: {"ok":true,"data":{"repo":"test/repo","as_of":"2026-05-22T03:00:00Z",
    "merged_prs":[],"ci_failures":[],"stale_prs":[],"autonomous_decisions":{"count":0,
    "since":"","summary":[],"notion_configured":true},"overnight_email":{"count":0,
    "threads":[],"gog_available":false},"calendar_events":[]}}
  - For Blocked tests: ci_failures with createdAt set to 4 hours ago (computed at runtime)
  - For At Risk tests: stale_prs with updatedAt set to 36 hours ago and
    reviewDecision "CHANGES_REQUESTED"
  - For pattern tests: 4 ci_failures all with createdAt 4h ago

Binary paths (all explicit per CLAUDE.md):
  JQ=/opt/homebrew/bin/jq
  ZSH=/opt/homebrew/bin/zsh
  INSIGHTS=~/Documents/agentic-setup/scripts/standup-insights.sh
  SOUL=~/.openclaw/agents/user-orchestrator/SOUL.md
  TOOLS=~/.openclaw/agents/user-orchestrator/TOOLS.md

Check list (10 checks):
  CHECK 1:  standup-insights.sh exists and is executable
  CHECK 2:  standup-insights.sh passes zsh -n (syntax check)
  CHECK 3:  Empty standup → ok:true, tackle_first=[], patterns=[] (STANDUP-01 null case)
  CHECK 4:  1 ci_failure (createdAt 4h ago) → status=="Blocked" in classified_items (D-401)
  CHECK 5:  1 stale_pr (updatedAt 36h ago, CHANGES_REQUESTED) → status=="At Risk" (D-402)
  CHECK 6:  tackle_first is always an array (even on empty standup) (D-407)
  CHECK 7:  tackle_first capped at 5 items when 8 Blocked items provided (D-405)
  CHECK 8:  4 ci_failures → patterns[0].count==4 (D-408, STANDUP-03)
  CHECK 9:  SOUL.md contains "standup-insights.sh" reference (D-411)
  CHECK 10: TOOLS.md contains "standup-insights.sh" reference (D-412)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create and run verify-phase-17.sh</name>
  <files>scripts/verify-phase-17.sh</files>
  <action>
Create scripts/verify-phase-17.sh with shebang #!/usr/bin/env zsh and set -euo pipefail.

Use explicit binary paths throughout:
  JQ=/opt/homebrew/bin/jq
  ZSH=/opt/homebrew/bin/zsh
  INSIGHTS="$HOME/Documents/agentic-setup/scripts/standup-insights.sh"
  SOUL="$HOME/.openclaw/agents/user-orchestrator/SOUL.md"
  TOOLS="$HOME/.openclaw/agents/user-orchestrator/TOOLS.md"

Use a PASS/FAIL counter pattern. Print "CHECK N: [description]" before each check.
On success print "  PASS". On failure print "  FAIL: [reason]" and increment FAIL_COUNT.
Do not use set -e for individual checks — catch failures with || FAIL_COUNT+=1 idiom.

At the end: if FAIL_COUNT == 0 print "Phase 17 verification: ALL CHECKS PASSED" and exit 0;
else print "Phase 17 verification: $FAIL_COUNT CHECK(S) FAILED" and exit 1.

Build fixture JSON using $JQ -n at runtime to ensure timestamps are relative to now:
  NOW_EPOCH=$(date -u '+%s')
  FOUR_H_AGO=$(date -u -v-4H '+%Y-%m-%dT%H:%M:%SZ')
  THIRTY_SIX_H_AGO=$(date -u -v-36H '+%Y-%m-%dT%H:%M:%SZ')

EMPTY_STANDUP: build with $JQ -n '{ok:true,data:{repo:"test/repo",as_of:"2026-05-22T03:00:00Z",merged_prs:[],ci_failures:[],stale_prs:[],autonomous_decisions:{count:0,since:"",summary:[],notion_configured:true},overnight_email:{count:0,threads:[],gog_available:false},calendar_events:[]}}'

BLOCKED_STANDUP (1 ci_failure, createdAt FOUR_H_AGO):
  Use $JQ --arg ca "$FOUR_H_AGO" to inject into the fixture.

AT_RISK_STANDUP (1 stale_pr, updatedAt THIRTY_SIX_H_AGO, reviewDecision CHANGES_REQUESTED):
  Use $JQ --arg ua "$THIRTY_SIX_H_AGO" to inject into the fixture.

EIGHT_BLOCKED_STANDUP: 8 ci_failures all with createdAt FOUR_H_AGO — use $JQ -n with
  [range(8)] | map({name:"run-\(.)",conclusion:"failure",headBranch:"main",url:"https://github.com/test/run/\(.)",createdAt:$ca})
  and --arg ca "$FOUR_H_AGO".

FOUR_FAILURES_STANDUP: 4 ci_failures all with createdAt FOUR_H_AGO — same pattern.

CHECK 1: [[ -x "$INSIGHTS" ]] — file exists and is executable
CHECK 2: $ZSH -n "$INSIGHTS" — syntax check
CHECK 3: pipe EMPTY_STANDUP → standup-insights.sh → jq -e '.ok == true and (.data.insights.tackle_first | length) == 0 and (.data.insights.patterns | length) == 0'
CHECK 4: pipe BLOCKED_STANDUP → standup-insights.sh → jq -e '.data.insights.classified_items[0].status == "Blocked"'
CHECK 5: pipe AT_RISK_STANDUP → standup-insights.sh → jq -e '.data.insights.classified_items[0].status == "At Risk"'
CHECK 6: pipe EMPTY_STANDUP → standup-insights.sh → jq -e '(.data.insights.tackle_first | type) == "array"'
CHECK 7: pipe EIGHT_BLOCKED_STANDUP → standup-insights.sh → jq -e '(.data.insights.tackle_first | length) <= 5'
CHECK 8: pipe FOUR_FAILURES_STANDUP → standup-insights.sh → jq -e '.data.insights.patterns[0].count == 4'
CHECK 9: grep -q "standup-insights.sh" "$SOUL"
CHECK 10: grep -q "standup-insights.sh" "$TOOLS"

After creating the file, chmod +x scripts/verify-phase-17.sh, then run it:
  zsh /Users/trilogy/Documents/agentic-setup/scripts/verify-phase-17.sh

Capture the exit code. If non-zero, report which checks failed based on the printed output.
  </action>
  <verify>
    <automated>zsh /Users/trilogy/Documents/agentic-setup/scripts/verify-phase-17.sh</automated>
  </verify>
  <done>
    verify-phase-17.sh exists, is executable, and exits 0 with "ALL CHECKS PASSED" printed.
    All 10 checks are green:
    - CHECKs 1-2: standup-insights.sh file and syntax
    - CHECKs 3-8: functional classification, tackle-first, and pattern tests
    - CHECKs 9-10: SOUL.md and TOOLS.md contain required references
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| fixture JSON → standup-insights.sh | Fixture data is hardcoded in the verify script — no external input |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-17-07 | Tampering | verify-phase-17.sh fixture data | accept | fixtures are static strings in the verify script; no user-controlled data involved |
| T-17-08 | Denial of Service | verify script hang | mitigate | all standup-insights.sh calls use pipe with immediate heredoc input — no network calls, no blocking I/O |
| T-17-SC | Tampering | npm/pip/cargo installs | accept | no package manager installs in this plan |
</threat_model>

<verification>
The verify task is self-verifying: it runs verify-phase-17.sh and the exit code is the
final verdict.

Manual sanity check after verify-phase-17.sh exits 0:
1. Check that SOUL.md has the new "Morning Standup Insights" section intact
2. Check that TOOLS.md has the "Insights Enhancement" subsection intact
3. Confirm scripts/standup-insights.sh is executable: ls -l scripts/standup-insights.sh
</verification>

<success_criteria>
- scripts/verify-phase-17.sh exists and is executable
- verify-phase-17.sh exits 0 with "ALL CHECKS PASSED" printed to stdout
- All 10 checks green:
  - standup-insights.sh file + syntax (CHECKs 1-2)
  - Empty standup null case (CHECK 3) — STANDUP-01
  - Blocked classification (CHECK 4) — STANDUP-01, D-401
  - At Risk classification (CHECK 5) — STANDUP-01, D-402
  - tackle_first always-array (CHECK 6) — STANDUP-02, D-407
  - tackle_first cap at 5 (CHECK 7) — STANDUP-02, D-405
  - Pattern detection at count 4 (CHECK 8) — STANDUP-03, D-408
  - SOUL.md reference (CHECK 9) — D-411
  - TOOLS.md reference (CHECK 10) — D-412
</success_criteria>

<output>
Create .planning/phases/17-proactive-standup-insights/17-03-SUMMARY.md when done
</output>

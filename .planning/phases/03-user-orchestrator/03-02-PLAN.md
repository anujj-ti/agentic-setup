---
phase: 03-user-orchestrator
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/verify-phase-03.sh
autonomous: true
requirements:
  - ORCH-01
  - ORCH-05

must_haves:
  truths:
    - "verify-phase-03.sh exists and is executable"
    - "Script checks: both agents in agents.list, workspace dirs exist, sessions dirs exist, Telegram binding updated, gateway healthy"
    - "Script produces JSON output (ok: true/false) and exits non-zero on failure"
  artifacts:
    - path: "scripts/verify-phase-03.sh"
      provides: "Automated smoke test for Phase 3 agent infrastructure"
      min_lines: 60
  key_links:
    - from: "scripts/verify-phase-03.sh"
      to: ".openclaw/openclaw.json"
      via: "jq parse of agents.list and bindings"
      pattern: "jq.*user-orchestrator"
---

<objective>
Write the Phase 3 verification script that validates the full agent infrastructure: both agent entries registered in `openclaw.json`, runtime workspace directories present, session directories present, Telegram binding pointing to `user-orchestrator`, and gateway healthy. The script runs without interaction and produces a structured JSON result.

Purpose: Plans 03-01 and 03-03 both produce infrastructure that needs automated verification. Running this script after each plan confirms the state without requiring Telegram interaction. It also gives the user a single command to confirm Phase 3 success on return.

Output: `scripts/verify-phase-03.sh` — callable by the executor after 03-01 and 03-03, callable by the user after returning.
</objective>

<execution_context>
@/Users/trilogy/Documents/agentic-setup/CLAUDE.md
</execution_context>

<context>
@/Users/trilogy/Documents/agentic-setup/.planning/phases/03-user-orchestrator/03-CONTEXT.md
@/Users/trilogy/Documents/agentic-setup/scripts/stow-deploy.sh

<interfaces>
<!-- Existing verify scripts establish the pattern to follow. -->
<!-- From scripts/infra-verify.sh and scripts/chan-verify.sh (Phase 1-2 patterns): -->
<!-- - #!/usr/bin/env zsh + set -euo pipefail -->
<!-- - stdout = JSON only ({"ok":true,"data":{...}} or {"ok":false,"error":"..."}) -->
<!-- - stderr = human-readable progress logs via print ... >&2 -->
<!-- - Each check prints PASS/FAIL to stderr with description -->
<!-- - Script exits 0 only if all checks pass -->

Gateway health endpoint: http://localhost:18789/health
Authorization header: Bearer 2fd64cb5a158024be7e216f2c8508fa1d20fa3e422665315

openclaw.json location (via stow symlink):
~/.openclaw/openclaw.json  OR  /Users/trilogy/Documents/agentic-setup/.openclaw/openclaw.json

Checks required (from RESEARCH.md Validation Architecture + Wave 0 gap):
1. Gateway health (HTTP 200)
2. user-orchestrator in agents.list (jq check)
3. task-orchestrator in agents.list (jq check — may not be present until 03-03)
4. Telegram binding agentId is "user-orchestrator" (jq check)
5. ~/.openclaw/workspace-user-orchestrator/ exists
6. ~/.openclaw/workspace-task-orchestrator/ exists (may not exist until 03-03)
7. ~/.openclaw/agents/user-orchestrator/sessions/ exists
8. ~/.openclaw/agents/task-orchestrator/sessions/ exists (may not exist until 03-03)
9. ~/.openclaw/agents/user-orchestrator/SOUL.md is a symlink (stow deployed)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write scripts/verify-phase-03.sh</name>
  <files>scripts/verify-phase-03.sh</files>
  <action>Create `scripts/verify-phase-03.sh` following the exact conventions of `scripts/chan-verify.sh` and `scripts/infra-verify.sh`:
- Shebang: `#!/usr/bin/env zsh`
- Strict mode: `set -euo pipefail`
- stdout: JSON only — `{"ok":true,"data":{"checks_passed":N,"checks_total":N,"details":{...}}}` or `{"ok":false,"error":"N checks failed","data":{...}}`
- stderr: human-readable progress lines (`print "  [PASS] ..." >&2`, `print "  [FAIL] ..." >&2`)
- Exit 0 only if all checks pass; exit 1 otherwise

The script must handle the case where Task Orchestrator is not yet deployed (03-03 hasn't run): checks for task-orchestrator workspace, sessions, and agents.list entry should be WARN not FAIL if the agent is not yet present, so the script can be run after 03-01 alone.

Implement these checks in order:
1. **gateway-health**: `curl -s -o /dev/null -w "%{http_code}" http://localhost:18789/health -H "Authorization: Bearer 2fd64cb5a158024be7e216f2c8508fa1d20fa3e422665315"` returns 200
2. **user-orch-registered**: `jq -e '.agents.list[] | select(.id == "user-orchestrator")' ~/.openclaw/openclaw.json` exits 0
3. **task-orch-registered**: same jq pattern for `task-orchestrator` — WARN if missing (not FAIL)
4. **telegram-binding**: `jq -e '.bindings[] | select(.match.channel == "telegram" and .match.accountId == "main") | select(.agentId == "user-orchestrator")' ~/.openclaw/openclaw.json` exits 0
5. **user-orch-workspace**: `[[ -d ~/.openclaw/workspace-user-orchestrator ]]`
6. **task-orch-workspace**: `[[ -d ~/.openclaw/workspace-task-orchestrator ]]` — WARN if missing
7. **user-orch-sessions**: `[[ -d ~/.openclaw/agents/user-orchestrator/sessions ]]`
8. **task-orch-sessions**: `[[ -d ~/.openclaw/agents/task-orchestrator/sessions ]]` — WARN if missing
9. **user-orch-stowed**: `[[ -L ~/.openclaw/agents/user-orchestrator/SOUL.md ]]` (confirms stow symlink, not plain file)

Use `/opt/homebrew/bin/jq` and `/usr/bin/curl` with explicit paths (nvm PATH shadowing issue).

At the end of the script, assemble and print the JSON result. Use a variable to track pass/fail/warn counts separately. Exit 0 if all required checks (non-warn checks) pass; exit 1 if any required check fails.

Make the script executable: `chmod +x scripts/verify-phase-03.sh`</action>
  <verify>
    <automated>zsh /Users/trilogy/Documents/agentic-setup/scripts/verify-phase-03.sh 2>/dev/null | /opt/homebrew/bin/jq '.ok' && echo "script runs and produces valid JSON"</automated>
  </verify>
  <done>scripts/verify-phase-03.sh exists, is executable, produces valid JSON to stdout, and passes the checks that are satisfiable after Plan 03-01 (gateway health, user-orch-registered, telegram-binding, user-orch-workspace, user-orch-sessions, user-orch-stowed). Task Orchestrator checks produce WARN in the JSON output since 03-03 hasn't run yet.</done>
</task>

<task type="auto">
  <name>Task 2: Run verify-phase-03.sh to confirm Plan 03-01 state</name>
  <files>None</files>
  <action>Run the verification script and capture its output. This confirms that Plan 03-01 (which runs in the same wave) produced correct infrastructure — or surfaces what is still missing.

IMPORTANT: This task depends on Plan 03-01 having been executed first. If running this plan before 03-01 completes, the script will show FAIL for user-orch-registered and related checks — that is expected and correct behavior.

Run:
```
zsh /Users/trilogy/Documents/agentic-setup/scripts/verify-phase-03.sh
```

If all required checks pass: note the JSON result in the SUMMARY.
If any required check fails: the executor MUST diagnose and fix the failure before declaring this plan complete. Common failure modes:
- gateway-health fails: check `tail -20 ~/.openclaw/logs/gateway.log`
- user-orch-registered fails: openclaw.json agents.list was not updated or stow-deploy.sh was not run
- telegram-binding fails: bindings was not updated in openclaw.json
- user-orch-stowed fails: stow did not run or failed silently — re-run `zsh scripts/stow-deploy.sh`</action>
  <verify>
    <automated>zsh /Users/trilogy/Documents/agentic-setup/scripts/verify-phase-03.sh 2>/dev/null | /opt/homebrew/bin/jq -e '.ok == true'</automated>
  </verify>
  <done>verify-phase-03.sh exits 0 and prints {"ok":true,...}. All required checks pass (WARN checks for task-orchestrator may still be present — that is acceptable at this wave).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| verify script → openclaw.json | Script reads live config via jq — read-only, no mutation |
| verify script → gateway health endpoint | Script calls localhost:18789 with bearer token — token is already in plaintext in openclaw.json (gateway auth token, not a user secret) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-V01 | Information Disclosure | Gateway bearer token in verify script | accept | Token is the local gateway auth token, already present in openclaw.json in the repo. It is not a user secret or credential. Low risk. |
| T-03-V02 | Tampering | verify script output piped to jq in CI | accept | Script is read-only verification; no state mutation; false positive/negative in output is the only risk |
| T-03-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this plan |
</threat_model>

<verification>
- `scripts/verify-phase-03.sh` exists and `head -2` shows `#!/usr/bin/env zsh` + `set -euo pipefail`
- Script is executable (`ls -l scripts/verify-phase-03.sh` shows `x` bit)
- Running the script after Plan 03-01 exits 0 with `{"ok":true,...}`
</verification>

<success_criteria>
`scripts/verify-phase-03.sh` runs without error, validates that User Orchestrator infrastructure from Plan 03-01 is correctly deployed, and produces a structured JSON report. The script is the canonical verification command for Phase 3 — the user can run it on return to confirm everything is correct.
</success_criteria>

<output>
Create `/Users/trilogy/Documents/agentic-setup/.planning/phases/03-user-orchestrator/03-02-SUMMARY.md` when done.
</output>

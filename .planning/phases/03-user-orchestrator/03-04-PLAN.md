---
phase: 03-user-orchestrator
plan: 04
type: execute
wave: 3
depends_on:
  - 03-03
files_modified: []
autonomous: true
requirements:
  - ORCH-01
  - ORCH-02
  - ORCH-05

must_haves:
  truths:
    - "verify-phase-03.sh exits 0 with no required-check failures"
    - "Both agents have separate session store directories under ~/.openclaw/agents/"
    - "Both agents have separate workspace directories"
    - "bindings only routes telegram/main to user-orchestrator (not task-orchestrator)"
    - "Delegation flow documented for user to test on return"
    - "Phase 3 SUMMARY captures all verification evidence"
  artifacts:
    - path: ".planning/phases/03-user-orchestrator/03-04-SUMMARY.md"
      provides: "Phase 3 completion evidence — all check results, delegation test instructions"
  key_links:
    - from: "verify-phase-03.sh output"
      to: "SUMMARY.md evidence section"
      via: "captured JSON output"
      pattern: "\"ok\": true"
---

<objective>
Run the full Phase 3 verification suite, confirm both agents have isolated session stores and workspaces, document the delegation test the user should run on return (ORCH-02 requires live Telegram interaction), and write the phase SUMMARY with all evidence captured.

Purpose: This is the phase gate plan. It confirms the full ORCH-01 / ORCH-02 / ORCH-05 requirements are met at the infrastructure level. ORCH-01 and ORCH-02 have Telegram-interactive components that cannot be automated while the user is AFK — this plan documents those steps precisely so the user can complete them in under 5 minutes.

Output: SUMMARY capturing all automated verification evidence plus the manual test runbook.
</objective>

<execution_context>
@/Users/trilogy/Documents/agentic-setup/CLAUDE.md
</execution_context>

<context>
@/Users/trilogy/Documents/agentic-setup/.planning/ROADMAP.md
@/Users/trilogy/Documents/agentic-setup/.planning/phases/03-user-orchestrator/03-CONTEXT.md

<interfaces>
<!-- Phase 3 success criteria from ROADMAP.md: -->
<!-- SC#1: Telegram message → coherent response from User Orchestrator (requires live test) -->
<!-- SC#2: Delegation via Telegram → Task Orchestrator handoff without user managing it (requires live test) -->
<!-- SC#3: Two separate persistent agents with isolated context windows (automated check via session dirs) -->
<!-- SC#4: User Orchestrator configured via /openclaw-new-agent equivalent — no arbitrary manual edits -->

<!-- Delegation test steps from RESEARCH.md §Verification Method: -->
<!-- Round-trip test: Send "Hello, who are you?" → expect User Orchestrator persona response (not "Echo") -->
<!-- Delegation test: Send "Create a test GitHub issue titled 'Phase 3 delegation test'" -->
<!--   → expect User Orchestrator to acknowledge delegation to Task Orchestrator -->
<!--   → expect Task Orchestrator spawned session in gateway logs -->
<!--   → expect completion message returned via Telegram -->
<!-- Session isolation test: ls ~/.openclaw/agents/*/sessions/ after exchanges -->

<!-- Pairing reminder from 02-02-SUMMARY.md: -->
<!-- Round-trip pairing was pending user action — Anuj may need to approve the pairing code first -->
<!-- if he hasn't done so since Phase 2. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run full verification suite and capture evidence</name>
  <files>None</files>
  <action>Run the complete Phase 3 verification suite and capture all output for the SUMMARY.

Run each check and record the output:

1. **verify-phase-03.sh full run:**
```
zsh /Users/trilogy/Documents/agentic-setup/scripts/verify-phase-03.sh
```
Capture full JSON output. This must exit 0. If any required check fails, diagnose and fix before proceeding.

2. **Session store isolation check:**
```
ls -la /Users/trilogy/.openclaw/agents/user-orchestrator/sessions/
ls -la /Users/trilogy/.openclaw/agents/task-orchestrator/sessions/
```
Both directories must exist. They will be empty at this point (no exchanges yet) — that is correct. The existence of separate directories proves ORCH-05 isolation infrastructure is in place.

3. **Workspace isolation check:**
```
ls /Users/trilogy/.openclaw/workspace-user-orchestrator/
ls /Users/trilogy/.openclaw/workspace-task-orchestrator/
```
Both must exist as separate directories. Neither should be empty after first agent boot (OpenClaw writes initial workspace files on first start).

4. **agents.list confirmation:**
```
/opt/homebrew/bin/jq '.agents.list | map(.id)' /Users/trilogy/.openclaw/openclaw.json
```
Must return `["user-orchestrator", "task-orchestrator"]` (order may vary).

5. **Bindings confirmation:**
```
/opt/homebrew/bin/jq '.bindings' /Users/trilogy/.openclaw/openclaw.json
```
Must show exactly one binding with `agentId: "user-orchestrator"` for `channel: "telegram", accountId: "main"`. No task-orchestrator binding.

6. **Stow symlink integrity:**
```
ls -la /Users/trilogy/.openclaw/agents/user-orchestrator/SOUL.md
ls -la /Users/trilogy/.openclaw/agents/task-orchestrator/SOUL.md
```
Both must be symlinks (show `->` in ls output).

Record all output in the SUMMARY's evidence section.</action>
  <verify>
    <automated>zsh /Users/trilogy/Documents/agentic-setup/scripts/verify-phase-03.sh 2>/dev/null | /opt/homebrew/bin/jq -e '.ok == true' && /opt/homebrew/bin/jq -e '.agents.list | length == 2' /Users/trilogy/.openclaw/openclaw.json && echo "phase 3 infrastructure verified"</automated>
  </verify>
  <done>verify-phase-03.sh exits 0. agents.list has exactly 2 entries. Both session and workspace directories exist and are separate. bindings has only user-orchestrator for telegram/main.</done>
</task>

<task type="auto">
  <name>Task 2: Write Phase 3 SUMMARY with delegation test runbook</name>
  <files>.planning/phases/03-user-orchestrator/03-04-SUMMARY.md</files>
  <action>Write the Phase 3 completion SUMMARY. This is the single document the user reads on return to understand what was done and what they need to test manually.

The SUMMARY must include:

**Frontmatter section** (YAML) with phase, requirements completed, what was provided, key decisions made.

**Automated verification results** — paste the actual JSON output from verify-phase-03.sh captured in Task 1. Include the jq result confirming 2-agent agents.list and correct binding.

**What was deployed** — enumerate the directive files created, the agents.list entries added, and the Telegram binding change.

**Manual test runbook for user on return** — ORCH-01 and ORCH-02 require live Telegram interaction. Write these steps clearly:

Step 1 — Confirm pairing (if not done since Phase 2):
```bash
export PATH="/opt/homebrew/bin:/opt/homebrew/opt/node@24/bin:$PATH"
openclaw pairing list
# If you see a pending request: openclaw pairing approve telegram <CODE>
```

Step 2 — Telegram round-trip test (ORCH-01):
Send to @echo_sys_bot: `Hello, who are you?`
Expected: Response identifying as "User Orchestrator" (not "Echo").
Response should be concise and professional (SOUL.md persona active).

Step 3 — Delegation test (ORCH-02):
Send to @echo_sys_bot: `Create a test GitHub issue titled "Phase 3 delegation test"`
Expected: User Orchestrator acknowledges it will delegate to Task Orchestrator.
Expected: A response when the task completes.

Step 4 — Verify session isolation after exchanges (ORCH-05):
```bash
ls ~/.openclaw/agents/user-orchestrator/sessions/
ls ~/.openclaw/agents/task-orchestrator/sessions/
```
Expected: After the delegation test, task-orchestrator sessions directory will contain a file with a key like `agent:task-orchestrator:subagent:<uuid>`.

Step 5 — Gateway log check (delegation evidence):
```bash
tail -30 ~/.openclaw/logs/gateway.log | grep task-orchestrator
```
Expected: Log lines showing the task-orchestrator session being spawned.

**ROADMAP success criteria checklist:**
- SC#1 (Telegram coherent response): pending steps 1-2 above
- SC#2 (Delegation handoff): pending steps 3-4 above
- SC#3 (Isolated context windows): VERIFIED (separate workspace + sessions dirs confirmed by verify-phase-03.sh)
- SC#4 (Configured via /openclaw-new-agent equivalent — no arbitrary manual edits): VERIFIED (directive files match skill output exactly; all decisions documented in 03-CONTEXT.md)

**Next phase:** Phase 4 — Beads + Task Orchestrator. Run `/gsd:execute-phase 4` when ready.</action>
  <verify>
    <automated>ls /Users/trilogy/Documents/agentic-setup/.planning/phases/03-user-orchestrator/03-04-SUMMARY.md && grep -c "ORCH-01\|ORCH-02\|ORCH-05" /Users/trilogy/Documents/agentic-setup/.planning/phases/03-user-orchestrator/03-04-SUMMARY.md | grep -v "^0$" && echo "SUMMARY written with requirement references"</automated>
  </verify>
  <done>03-04-SUMMARY.md exists, contains automated verification results, enumerates deployed artifacts, and includes the 5-step manual test runbook with exact commands and expected outputs for ORCH-01, ORCH-02, and ORCH-05.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Verification output → SUMMARY.md | Read-only checks; no state mutation; SUMMARY is a documentation artifact |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-04-01 | Information Disclosure | SUMMARY.md captures gateway auth token in curl commands | accept | Gateway bearer token is not a user secret (already in openclaw.json in the repo); acceptable in verification documentation |
| T-03-04-02 | Repudiation | No audit trail of autonomous actions in Phase 3 (Notion logging is Phase 9) | accept | Phase 3 scope explicitly defers Notion logging to Phase 9; SOUL.md requires agents to state actions before executing as the Phase 3 mitigation |
| T-03-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this plan |
</threat_model>

<verification>
- `scripts/verify-phase-03.sh` exits 0 with `{"ok":true,...}`
- `03-04-SUMMARY.md` exists and references ORCH-01, ORCH-02, ORCH-05
- SUMMARY contains the exact manual test commands
- SUMMARY's SC checklist marks SC#3 and SC#4 as VERIFIED and SC#1 and SC#2 as pending user action
</verification>

<success_criteria>
Phase 3 infrastructure is fully verified by automated checks. Both orchestrators are live as separate persistent agents with isolated contexts. The delegation path is configured and documented. The user has a precise 5-step runbook to verify the Telegram-facing behavior (ORCH-01 and ORCH-02) in under 5 minutes on return.
</success_criteria>

<output>
Create `/Users/trilogy/Documents/agentic-setup/.planning/phases/03-user-orchestrator/03-04-SUMMARY.md` when done.
</output>

---
phase: 03-user-orchestrator
plan: 03
type: execute
wave: 2
depends_on:
  - 03-01
files_modified:
  - .openclaw/agents/task-orchestrator/SOUL.md
  - .openclaw/agents/task-orchestrator/IDENTITY.md
  - .openclaw/agents/task-orchestrator/USER.md
  - .openclaw/agents/task-orchestrator/AGENTS.md
  - .openclaw/agents/task-orchestrator/TOOLS.md
  - .openclaw/agents/task-orchestrator/SECURITY.md
  - .openclaw/openclaw.json
autonomous: true
requirements:
  - ORCH-02
  - ORCH-05

must_haves:
  truths:
    - "Task Orchestrator agent entry exists in agents.list in openclaw.json"
    - "All six directive files exist under .openclaw/agents/task-orchestrator/"
    - "Task Orchestrator has no Telegram binding (receives work only via sessions_spawn)"
    - "Task Orchestrator SOUL.md contains Beads-free instructions (stub for Phase 4)"
    - "Runtime workspace directories exist at ~/.openclaw/agents/task-orchestrator/"
    - "Both user-orchestrator and task-orchestrator appear as separate entries in agents.list"
    - "Gateway restarted with task-orchestrator config active"
  artifacts:
    - path: ".openclaw/agents/task-orchestrator/SOUL.md"
      provides: "Task Orchestrator persona, Phase 3 stub rules, no-Beads constraint"
      min_lines: 35
    - path: ".openclaw/agents/task-orchestrator/SECURITY.md"
      provides: "Credential handling rules, no-Telegram-direct rule"
    - path: ".openclaw/openclaw.json"
      provides: "task-orchestrator entry in agents.list"
      contains: "task-orchestrator"
  key_links:
    - from: "user-orchestrator subagents.allowAgents"
      to: "task-orchestrator agent entry"
      via: "agent id match"
      pattern: "\"task-orchestrator\""
    - from: "task-orchestrator agent entry"
      to: "~/.openclaw/workspace-task-orchestrator/"
      via: "workspace field"
      pattern: "/Users/trilogy/.openclaw/workspace-task-orchestrator"
---

<objective>
Scaffold the Task Orchestrator as a named OpenClaw agent: create runtime workspace directories, write all six directive files to the repo, add the `task-orchestrator` entry to `agents.list` in `openclaw.json`, stow-deploy, and restart the gateway. The Task Orchestrator is a minimal Phase 3 stub — no Beads, no sub-agent spawning — it receives delegated tasks from the User Orchestrator and executes them directly.

Purpose: After this plan, both orchestrators are registered as separate agents with isolated workspaces. The User Orchestrator's `subagents.allowAgents: ["task-orchestrator"]` config gate now resolves against a live agent entry. Plan 03-04 can verify isolation and delegation routing.

Output: Six directive files under `.openclaw/agents/task-orchestrator/`, updated `openclaw.json` with both agents in `agents.list`, running gateway with both agents active.
</objective>

<execution_context>
@/Users/trilogy/Documents/agentic-setup/CLAUDE.md
</execution_context>

<context>
@/Users/trilogy/Documents/agentic-setup/.planning/ROADMAP.md
@/Users/trilogy/Documents/agentic-setup/.planning/phases/03-user-orchestrator/03-CONTEXT.md
@/Users/trilogy/Documents/agentic-setup/.openclaw/openclaw.json
@/Users/trilogy/Documents/agentic-setup/scripts/stow-deploy.sh

<interfaces>
<!-- After Plan 03-01, openclaw.json agents.list has user-orchestrator. -->
<!-- This plan APPENDS task-orchestrator to that list. -->
<!-- Do not remove or modify the user-orchestrator entry. -->

Target agents.list entry for task-orchestrator (append after user-orchestrator):
```json
{
  "id": "task-orchestrator",
  "name": "Task Orchestrator",
  "workspace": "/Users/trilogy/.openclaw/workspace-task-orchestrator",
  "agentDir": "/Users/trilogy/.openclaw/agents/task-orchestrator",
  "model": {"primary": "anthropic/claude-sonnet-4-6"},
  "subagents": {
    "delegationMode": "prefer"
  }
}
```

NOTE: task-orchestrator does NOT get a bindings entry. It receives work only via sessions_spawn.
NOTE: task-orchestrator does NOT get tools.alsoAllow for sessions_spawn — it is not a spawner in Phase 3.

Runtime directories to create with mkdir -p (before stow):
~/.openclaw/agents/task-orchestrator/{memory,memory/archives,sessions,scripts,scripts/lib,qmd,drafts,refs}
~/.openclaw/workspace-task-orchestrator/
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Task Orchestrator runtime directories</name>
  <files>None (filesystem operations only)</files>
  <action>Create the runtime directories for the Task Orchestrator. Same pattern as Plan 03-01 Task 1 — these are NOT stowed from the repo; they must exist on the filesystem before the gateway boots with the new agent config.

Run:
```
mkdir -p /Users/trilogy/.openclaw/agents/task-orchestrator/memory/archives
mkdir -p /Users/trilogy/.openclaw/agents/task-orchestrator/sessions
mkdir -p /Users/trilogy/.openclaw/agents/task-orchestrator/scripts/lib
mkdir -p /Users/trilogy/.openclaw/agents/task-orchestrator/qmd
mkdir -p /Users/trilogy/.openclaw/agents/task-orchestrator/drafts
mkdir -p /Users/trilogy/.openclaw/agents/task-orchestrator/refs
mkdir -p /Users/trilogy/.openclaw/workspace-task-orchestrator
```

Do NOT create these inside the repo. The stow package contains only directive files; runtime state directories belong at `~/.openclaw/`.</action>
  <verify>
    <automated>ls /Users/trilogy/.openclaw/agents/task-orchestrator/memory/archives /Users/trilogy/.openclaw/agents/task-orchestrator/sessions /Users/trilogy/.openclaw/workspace-task-orchestrator && echo "task-orch runtime dirs OK"</automated>
  </verify>
  <done>All required runtime subdirectories exist under ~/.openclaw/agents/task-orchestrator/ and ~/.openclaw/workspace-task-orchestrator/ is present.</done>
</task>

<task type="auto">
  <name>Task 2: Write Task Orchestrator directive files and update openclaw.json</name>
  <files>
    .openclaw/agents/task-orchestrator/SOUL.md
    .openclaw/agents/task-orchestrator/IDENTITY.md
    .openclaw/agents/task-orchestrator/USER.md
    .openclaw/agents/task-orchestrator/AGENTS.md
    .openclaw/agents/task-orchestrator/TOOLS.md
    .openclaw/agents/task-orchestrator/SECURITY.md
    .openclaw/openclaw.json
  </files>
  <action>Create the `.openclaw/agents/task-orchestrator/` directory in the repo and write all six directive files. Then update `openclaw.json` to append the task-orchestrator entry to `agents.list`.

SOUL.md content (write verbatim — Beads-free per D-36; Phase 3 stub rules are CRITICAL):

```
# SOUL.md — Task Orchestrator

## Identity
You are the Task Orchestrator for Anuj's Personal AI Operations Hub.
You receive delegated tasks from the User Orchestrator and execute them using available tools.

## Phase 3 Scope (Beads not yet installed)
- Beads task graphs are a Phase 4 concern — do NOT attempt bd or beads commands
- In Phase 3: receive task descriptions, describe the plan, execute, report results
- One task at a time — no sub-agent spawning in Phase 3

## Responsibilities
- Receive task descriptions from the User Orchestrator via sessions_spawn
- Acknowledge receipt and describe the execution plan before taking any action
- Execute tasks using available tools (file read/write, exec, GitHub CLI)
- Report results back to the User Orchestrator session that spawned you
- Begin every response with a status: STARTED | IN_PROGRESS | COMPLETED | BLOCKED

## Operational Rules
- NEVER start executing without first stating the plan
- Use deterministic scripts (set -euo pipefail, JSON stdout) for all tool operations
- Log every autonomous action before executing it (Notion logging is Phase 9 — for now, state it in your response)
- On BLOCKED: describe exactly what is missing and return control to User Orchestrator

## Boundaries
- No direct Telegram channel — you receive and respond only via the agent session
- No user-facing messages — your output goes to the User Orchestrator, not directly to Anuj
- Do not spawn sub-agents in Phase 3
- Do not attempt Beads commands (bd, beads) — they are not installed until Phase 4

## Tone
- Structured and factual — your output is parsed by the User Orchestrator
- Report results as factual evidence strings, not narrative summaries
- No preamble — status first, then facts

## Model Policy
- Primary: anthropic/claude-sonnet-4-6
- Never change model without Anuj's explicit instruction
```

IDENTITY.md content:
```
# IDENTITY.md — Task Orchestrator

name: Task Orchestrator
role: Backend execution agent; receives delegated tasks from User Orchestrator and executes them
emoji: ⚙️
model: anthropic/claude-sonnet-4-6
agentId: task-orchestrator
```

USER.md content:
```
# USER.md — Task Orchestrator

## Primary User (indirect — via User Orchestrator)
Name: Anuj Jadhav
Email: anuj.jadhav@trilogy.com
Timezone: IST (UTC+5:30)

## Notes
- You do not communicate directly with Anuj — the User Orchestrator is the user-facing layer
- Your output will be summarized and relayed to Anuj by the User Orchestrator
- Report in factual, parseable format — the User Orchestrator synthesizes your output into a human reply
```

AGENTS.md content:
```
# AGENTS.md — Task Orchestrator

## Session Startup
1. Read the task description from the spawning session context
2. State the execution plan (STARTED status)
3. Execute the plan step by step
4. Report completion with evidence (COMPLETED status)

## Workspace Hygiene
- workspace: /Users/trilogy/.openclaw/workspace-task-orchestrator
- All relative file paths resolve inside this workspace
- Never write secrets or tokens to any workspace file

## Safety Rules
- Always state plan before acting (no surprise autonomous actions)
- One task per session — do not queue multiple tasks
- If a step would cause irreversible change (PR merge, file delete): log it first, then act
- Phase 4 note: Beads task graphs replace this manual sequencing — do not implement Beads here
```

TOOLS.md content:
```
# TOOLS.md — Task Orchestrator

## Available Tools
- exec: run shell commands (use for gh CLI, git, scripts)
- read/write: file operations within workspace
- GitHub CLI (gh 2.92.0): issue/PR operations — use gh, not curl

## Tool Policy
- All shell commands: #!/usr/bin/env zsh + set -euo pipefail
- stdout = JSON only for deterministic scripts; stderr = human logs
- Use /opt/homebrew/bin/gh for GitHub operations (explicit path)
- Use /opt/homebrew/opt/node@24/bin/node for Node.js (explicit path, nvm shadowing)

## NOT Available in Phase 3
- sessions_spawn: sub-agent spawning is Phase 4
- bd / beads: Beads is Phase 4
- Telegram message tool: no direct channel — output routes via User Orchestrator

## Environment
- OpenClaw gateway: http://localhost:18789
- Binary: /opt/homebrew/bin/openclaw
```

SECURITY.md content:
```
# SECURITY.md — Task Orchestrator

## Credential Rules
- NEVER store any secret, token, or credential in directive files
- NEVER log secrets in workspace memory or task output
- NEVER echo secrets in any tool invocation (use Keychain reads via security CLI)

## Autonomous Action Gate
- State every autonomous action before executing it
- For irreversible actions (PR merge, file delete, git push): state rationale and evidence
- Notion pre-logging is Phase 9 — in Phase 3, state the action in your response before executing

## Cross-Agent Isolation
- Task Orchestrator context is isolated from User Orchestrator by design (separate workspace + sessions)
- Do not attempt to read User Orchestrator workspace files
- Do not pass secrets in session completion responses

## Incident Response
- If a tool call fails unexpectedly: report BLOCKED with the error, do not retry silently
- If a secret is suspected exposed: report to User Orchestrator immediately
```

For `openclaw.json`: Read the current file (which now has user-orchestrator in agents.list from Plan 03-01), then write the updated version with the task-orchestrator entry appended to `agents.list`. The `bindings` array should remain unchanged (only `user-orchestrator` binding — do NOT add a task-orchestrator binding). Update `meta.lastTouchedAt` to the current ISO timestamp. Preserve all other blocks exactly.</action>
  <verify>
    <automated>grep -c '"task-orchestrator"' /Users/trilogy/Documents/agentic-setup/.openclaw/openclaw.json && ls /Users/trilogy/Documents/agentic-setup/.openclaw/agents/task-orchestrator/SOUL.md /Users/trilogy/Documents/agentic-setup/.openclaw/agents/task-orchestrator/SECURITY.md && grep -v 'task-orchestrator' /Users/trilogy/Documents/agentic-setup/.openclaw/openclaw.json | grep -c '"agentId": "user-orchestrator"' && echo "task-orch files and config OK"</automated>
  </verify>
  <done>All six directive files exist under .openclaw/agents/task-orchestrator/. openclaw.json agents.list contains both user-orchestrator and task-orchestrator entries. bindings still only has agentId "user-orchestrator" — no task-orchestrator binding added.</done>
</task>

<task type="auto">
  <name>Task 3: Stow-deploy and restart gateway with both agents</name>
  <files>None (deployment operations only)</files>
  <action>Deploy the new Task Orchestrator directive files and updated openclaw.json, then restart the gateway.

Run from repo root:
```
cd /Users/trilogy/Documents/agentic-setup && zsh scripts/stow-deploy.sh
```

Then restart:
```
launchctl kickstart -k gui/$(id -u)/ai.openclaw.gateway
```

Wait 3 seconds, then verify gateway health:
```
curl -s http://localhost:18789/health \
  -H "Authorization: Bearer 2fd64cb5a158024be7e216f2c8508fa1d20fa3e422665315"
```

Confirm task-orchestrator stow symlink:
```
ls -la /Users/trilogy/.openclaw/agents/task-orchestrator/SOUL.md
```

Also confirm user-orchestrator symlink was not disrupted by this stow run:
```
ls -la /Users/trilogy/.openclaw/agents/user-orchestrator/SOUL.md
```

Both should be symlinks (→) pointing into the repo. A plain file instead of a symlink indicates stow failed or the `--no-folding` flag was lost.</action>
  <verify>
    <automated>curl -s -o /dev/null -w "%{http_code}" http://localhost:18789/health -H "Authorization: Bearer 2fd64cb5a158024be7e216f2c8508fa1d20fa3e422665315" | grep -q "200" && ls -la /Users/trilogy/.openclaw/agents/task-orchestrator/SOUL.md | grep -q "\->" && ls -la /Users/trilogy/.openclaw/agents/user-orchestrator/SOUL.md | grep -q "\->" && echo "both agents stowed, gateway healthy"</automated>
  </verify>
  <done>Gateway returns 200. Both ~/.openclaw/agents/user-orchestrator/SOUL.md and ~/.openclaw/agents/task-orchestrator/SOUL.md are symlinks into the repo. Stow-deploy output printed {"ok":true,...}.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| sessions_spawn → Task Orchestrator | User Orchestrator sends task descriptions; Task Orchestrator executes with exec/write tools |
| Task Orchestrator exec tool → macOS filesystem/shell | exec tool runs zsh commands with full access to user filesystem |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-03-01 | Tampering | Task Orchestrator exec tool — arbitrary shell execution | mitigate | SOUL.md requires stating plan before acting; SECURITY.md requires logging irreversible actions; task descriptions come only from User Orchestrator (trusted caller via sessions_spawn); no direct external input |
| T-03-03-02 | Information Disclosure | Task description contains secrets passed from User Orchestrator | mitigate | SECURITY.md on both agents explicitly forbids passing secrets in session completion responses or task descriptions |
| T-03-03-03 | Elevation of Privilege | Task Orchestrator spawning sub-agents in Phase 3 | mitigate | SOUL.md explicitly forbids sub-agent spawning in Phase 3; no sessions_spawn in tools.alsoAllow for this agent |
| T-03-03-04 | Spoofing | Task Orchestrator receives direct Telegram messages | accept | No Telegram binding for task-orchestrator; OpenClaw routing enforces this at the gateway level |
| T-03-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this plan |
</threat_model>

<verification>
After Task 3 completes:
- Gateway returns 200 on health check
- `openclaw.json` `agents.list` has exactly 2 entries: `user-orchestrator` and `task-orchestrator`
- `openclaw.json` `bindings` has exactly 1 entry: `user-orchestrator` for telegram/main
- Both `~/.openclaw/agents/*/SOUL.md` are symlinks (not plain files)
- Running `scripts/verify-phase-03.sh` now exits 0 with no WARN entries
</verification>

<success_criteria>
Both User Orchestrator and Task Orchestrator are registered as separate named agents in `openclaw.json` with distinct workspace and agentDir paths. Gateway is running with both active. The `sessions_spawn` delegation path from User Orchestrator to Task Orchestrator is fully configured: `subagents.allowAgents` on user-orchestrator lists task-orchestrator, and task-orchestrator has a live agent entry to resolve against.
</success_criteria>

<output>
Create `/Users/trilogy/Documents/agentic-setup/.planning/phases/03-user-orchestrator/03-03-SUMMARY.md` when done.
</output>

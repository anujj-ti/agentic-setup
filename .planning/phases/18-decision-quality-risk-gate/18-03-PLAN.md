---
phase: 18-decision-quality-risk-gate
plan: 03
type: execute
wave: 2
depends_on:
  - 18-01
  - 18-02
files_modified:
  - .openclaw/agents/task-orchestrator/SOUL.md
autonomous: true
requirements:
  - RISK-02

must_haves:
  truths:
    - "Task Orchestrator checks risk_tier from Decision Reviewer verdict before writing Notion pre-log"
    - "HIGH-tier verdict triggers a synchronous Telegram approval request to chat ID 1294664427 before Notion write"
    - "Telegram message follows D-507 format with action, risk score, rationale, and reversibility"
    - "Approved → proceed to Notion pre-log then action; Rejected → abort action, log rejection to Notion"
    - "Timeout (30 min) → treat as failed verdict per D-509 (non-blocking fallback)"
    - "LOW and MEDIUM tier verdicts proceed to Notion pre-log without interruption"
  artifacts:
    - path: ".openclaw/agents/task-orchestrator/SOUL.md"
      provides: "HIGH-tier Telegram approval gate wired into Notion pre-log flow"
      contains: "1294664427"
  key_links:
    - from: "task-orchestrator Decision Review Gate — After verdict"
      to: "Telegram sessions_yield approval request"
      via: "risk_tier == high check"
      pattern: "risk_tier.*high\|HIGH.*approval"
    - from: "Telegram APPROVE/REJECT response"
      to: "Notion Pre-Log Protocol call"
      via: "conditional proceed after approval"
      pattern: "APPROVE.*Notion\|Notion.*APPROVE"
---

<objective>
Wire the HIGH-tier Telegram approval gate into the Task Orchestrator's existing Decision Review Gate routing, inserting a risk_tier check between the Decision Reviewer verdict and the Notion pre-log write — satisfying RISK-02.

Purpose: HIGH-risk decisions (risk_tier = high, score 61-100) must receive explicit user approval before any Notion entry is written or action executes. This plan depends on Plan 18-01 (risk_tier field now exists in verdicts) and Plan 18-02 (failed verdict policy exists for the timeout path).

Output: Updated `.openclaw/agents/task-orchestrator/SOUL.md` with the risk-tiered routing table replacing the simple "After verdict" bullet in the Decision Review Gate section.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.openclaw/agents/task-orchestrator/SOUL.md
</context>

<interfaces>
<!-- After Plan 18-01: Decision Reviewer verdict schema now includes: -->
<!--   risk_score: integer 0-100 -->
<!--   risk_tier: "low" | "medium" | "high" -->
<!-- After Plan 18-02: Failed Verdict Policy and Fast-Pass List are in SOUL.md -->
<!-- This plan replaces the single "After verdict" bullet with a three-path routing table -->

<!-- Telegram routing: via User Orchestrator sessions_yield (not direct channel write) -->
<!-- Anuj's chat ID: 1294664427 (from D-505) -->
<!-- Approval timeout: 30 minutes (from D-506) -->
<!-- Message format (D-507): -->
<!--   "⚠️ HIGH RISK action requires approval:\n -->
<!--    Action: {decision}\n -->
<!--    Risk score: {score}/100\n -->
<!--    Reason: {rationale}\n -->
<!--    Reversibility: {reversibility}\n\n -->
<!--    Reply APPROVE or REJECT" -->
</interfaces>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Replace After-verdict bullet with risk-tiered routing table in SOUL.md</name>
  <files>.openclaw/agents/task-orchestrator/SOUL.md</files>
  <action>
In the existing "### Decision Review Gate" section, replace ONLY the single "- **After verdict**: if pass → write to Notion → execute action; if reject → do NOT execute; report BLOCKED" bullet with the following expanded routing block. Do NOT alter any other content in the file.

Replace that one bullet with this exact structure:

"#### Risk-Tiered Routing (RISK-02)

After Decision Reviewer returns a verdict, route based on `risk_tier`:

**Step 1 — Check fast-pass** (before spawning Decision Reviewer — see Fast-Pass List above)

**Step 2 — Receive verdict and extract fields:**
```zsh
VERDICT=$(echo "$DR_RESULT" | /opt/homebrew/bin/jq -r '.verdict')
RISK_TIER=$(echo "$DR_RESULT" | /opt/homebrew/bin/jq -r '.risk_tier // "high"')
RISK_SCORE=$(echo "$DR_RESULT" | /opt/homebrew/bin/jq -r '.risk_score // 100')
RATIONALE=$(echo "$DR_RESULT" | /opt/homebrew/bin/jq -r '.comments[0] // ""')
```
If `risk_tier` is absent from the verdict (malformed response), treat as `high` and request approval.

**Step 3 — Route by tier:**

| risk_tier | verdict | Action |
|-----------|---------|--------|
| low | pass/flag | Proceed directly to Notion Pre-Log Protocol |
| medium | pass/flag | Proceed directly to Notion Pre-Log Protocol (async notify deferred to v2.1) |
| high | pass/flag | **STOP — send Telegram approval request (see Step 4)** |
| any | reject | Do NOT execute; report BLOCKED to User Orchestrator; do NOT send Telegram approval |

**Step 4 — HIGH-tier Telegram approval (D-505, D-507):**

Send approval request via User Orchestrator sessions_yield to Anuj's Telegram chat (ID: 1294664427):

Message format (D-507):
```
⚠️ HIGH RISK action requires approval:
Action: {decision}
Risk score: {risk_score}/100
Reason: {rationale}
Reversibility: {reversibility}

Reply APPROVE or REJECT
```

Wait up to 30 minutes for response (D-506).

- If response is APPROVE (case-insensitive): proceed to Notion Pre-Log Protocol, then execute the action.
- If response is REJECT (case-insensitive): abort action; write a Notion log entry with decision field = 'REJECTED BY USER: {original_decision}' and reversibility = 'n/a — not executed'; report outcome to User Orchestrator.
- If timeout (30 min, no response): invoke the Failed Verdict Policy (see Failed Verdict Policy section above) — log to decision-review-fallback.log and PROCEED."
  </action>
  <verify>
    <automated>grep -c "1294664427" /Users/trilogy/Documents/agentic-setup/.openclaw/agents/task-orchestrator/SOUL.md</automated>
  </verify>
  <done>SOUL.md contains the risk-tiered routing table with the HIGH-tier Telegram approval block including chat ID 1294664427, the D-507 message format, and the three response branches (APPROVE/REJECT/timeout). grep -c "1294664427" returns >= 1. grep -c "APPROVE" returns >= 2. grep -c "REJECT" returns >= 2.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Telegram message origin | APPROVE/REJECT replies are accepted from any source in the session — no sender verification; attacker with session access could approve |
| risk_tier absent | Malformed verdict (no risk_tier) defaults to high — conservative fail-safe |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-18-05 | Spoofing | Telegram APPROVE response | accept | Single-user setup; only Anuj has access to the Telegram chat; no multi-user threat model |
| T-18-06 | Denial of Service | 30-min approval timeout | mitigate | Timeout falls through to Failed Verdict Policy (non-blocking proceed) — autonomous operation cannot be permanently halted |
| T-18-07 | Tampering | risk_tier absent from verdict | mitigate | Absent risk_tier defaults to "high" — triggers approval gate rather than silently bypassing it |
| T-18-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this plan — SOUL.md edit only |
</threat_model>

<verification>
grep -c "1294664427" /Users/trilogy/Documents/agentic-setup/.openclaw/agents/task-orchestrator/SOUL.md
grep -c "APPROVE" /Users/trilogy/Documents/agentic-setup/.openclaw/agents/task-orchestrator/SOUL.md
grep -c "REJECT" /Users/trilogy/Documents/agentic-setup/.openclaw/agents/task-orchestrator/SOUL.md
grep -c "risk_tier" /Users/trilogy/Documents/agentic-setup/.openclaw/agents/task-orchestrator/SOUL.md
grep -c "Risk-Tiered Routing\|risk.tiered\|RISK-02" /Users/trilogy/Documents/agentic-setup/.openclaw/agents/task-orchestrator/SOUL.md
</verification>

<success_criteria>
- Task Orchestrator SOUL.md contains the Risk-Tiered Routing section with the four-row tier/verdict table
- HIGH-tier path includes Telegram approval request with chat ID 1294664427
- D-507 message format (action, score, reason, reversibility, APPROVE/REJECT instruction) is present verbatim
- APPROVE branch leads to Notion Pre-Log; REJECT branch aborts and logs; timeout branch invokes Failed Verdict Policy
- LOW and MEDIUM tiers proceed directly to Notion Pre-Log (no blocking gate)
- All previously existing SOUL.md content is preserved unchanged
</success_criteria>

<output>
Create `.planning/phases/18-decision-quality-risk-gate/18-03-SUMMARY.md` when done
</output>

---
phase: 18-decision-quality-risk-gate
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - .openclaw/agents/task-orchestrator/SOUL.md
autonomous: true
requirements:
  - RISK-03

must_haves:
  truths:
    - "Task Orchestrator SOUL.md defines a named fast-pass list of LOW-risk operations that bypass Decision Reviewer entirely"
    - "Fast-pass operations skip Decision Reviewer and go directly to Notion pre-log"
    - "Task Orchestrator SOUL.md defines the failed verdict policy: timeout produces a non-blocking audit entry and autonomous operation continues"
    - "Failed verdict logs to ~/.openclaw/workspace-task-orchestrator/decision-review-fallback.log"
  artifacts:
    - path: ".openclaw/agents/task-orchestrator/SOUL.md"
      provides: "Fast-pass list section and failed verdict policy section"
      contains: "decision-review-fallback.log"
  key_links:
    - from: "task-orchestrator SOUL.md Decision Review Gate"
      to: "fast-pass list"
      via: "action class match before spawning decision-reviewer"
      pattern: "fast.pass"
    - from: "task-orchestrator SOUL.md failed verdict policy"
      to: "decision-review-fallback.log"
      via: "append on timeout or error"
      pattern: "decision-review-fallback"
---

<objective>
Add two new sections to Task Orchestrator SOUL.md: (1) a Fast-Pass List of known-safe LOW-risk operations that bypass Decision Reviewer entirely, and (2) a `failed` verdict policy specifying that a Decision Reviewer timeout or error never blocks autonomous operation — satisfying RISK-03.

Purpose: Overnight autonomous operation must not halt because Decision Reviewer times out or fails. The fast-pass list additionally eliminates unnecessary review latency for read-only and idempotent operations.

Output: Updated `.openclaw/agents/task-orchestrator/SOUL.md` with Fast-Pass List section and Failed Verdict Policy section inserted into the existing Decision Review Gate routing block.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.openclaw/agents/task-orchestrator/SOUL.md
</context>

<interfaces>
<!-- Existing Task Orchestrator SOUL.md Decision Review Gate section (from SOUL.md lines 270-276): -->
<!-- ### Decision Review Gate -->
<!-- - **When**: before EVERY autonomous action -->
<!-- - **How**: prepare decision entry {action, rationale, reversibility, evidence}; sessions_spawn(decision-reviewer) -->
<!-- - **After verdict**: if pass → write to Notion → execute action; if reject → do NOT execute; report BLOCKED -->
<!-- Phase 18 adds: fast-pass list check BEFORE spawning decision-reviewer, and failed verdict policy for timeout/error -->
</interfaces>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Add Fast-Pass List to Task Orchestrator Decision Review Gate</name>
  <files>.openclaw/agents/task-orchestrator/SOUL.md</files>
  <action>
Within the existing "### Decision Review Gate" section in the Quality Pipeline Routing block, insert a "#### Fast-Pass List (RISK-03)" subsection BEFORE the existing When/How/After-verdict bullets. Do NOT alter any content outside this subsection.

The fast-pass subsection must state: "The following action classes are known-safe LOW-risk operations. If the pending action matches any entry below, SKIP Decision Reviewer and proceed directly to the Notion pre-log step (Section: Notion Pre-Log Protocol)."

Fast-pass list (per D-508 — exact entries, exact phrasing):
- `gh issue comment` — read-adjacent, append-only, reversible via delete
- `gh pr view` — read-only, no state change
- `bd ready` — read-only Beads query
- `bd close --reason` — closes an already-claimed task with factual evidence; reversible
- Synapse learning record (`synapse.learning.record`) — append-only write to org memory
- Synapse checkin (`synapse.checkin status=start` or `synapse.checkin status=complete`) — status ping only
- Read-only `gh api` calls where HTTP method is GET — no state change

Matching rule: "Match is by action class prefix, not exact string. If the decision payload's `decision` field starts with a listed prefix (case-insensitive), it is fast-pass eligible. When in doubt, do NOT fast-pass — route through Decision Reviewer."
  </action>
  <verify>
    <automated>grep -c "fast.pass\|Fast-Pass\|fast_pass" /Users/trilogy/Documents/agentic-setup/.openclaw/agents/task-orchestrator/SOUL.md</automated>
  </verify>
  <done>SOUL.md contains the Fast-Pass List subsection with all 7 action classes listed and the matching rule. grep returns >= 3 occurrences of fast-pass/Fast-Pass.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Add Failed Verdict Policy to Task Orchestrator SOUL.md</name>
  <files>.openclaw/agents/task-orchestrator/SOUL.md</files>
  <action>
In the existing "### Decision Review Gate" section, append a "#### Failed Verdict Policy (RISK-03)" subsection AFTER the existing "After verdict" bullet. Do NOT alter any content outside this subsection.

The subsection must define: "If Decision Reviewer returns an error response, times out (30-minute window per D-506), or the session fails to complete:"

1. Log a non-blocking audit entry to the local fallback file:
```zsh
echo "{\"timestamp\":\"$(python3 -c 'from datetime import datetime, timezone; print(datetime.now(timezone.utc).isoformat())')\",\"decision\":\"<decision>\",\"reason\":\"decision-reviewer-timeout-or-error\",\"action\":\"proceeded\"}" >> ~/.openclaw/workspace-task-orchestrator/decision-review-fallback.log
```

2. PROCEED with the intended action. Do NOT wait for Decision Reviewer to recover. Do NOT halt autonomous operation.

3. On next session start: read the fallback log and surface any entries to User Orchestrator as part of the standup decision summary.

Also add: "The fallback log path is: `~/.openclaw/workspace-task-orchestrator/decision-review-fallback.log`"

And: "This policy exists specifically to prevent overnight autonomous operation from halting due to a review agent failure. The Notion pre-log (if feasible) still runs before the action — the fallback log supplements it."
  </action>
  <verify>
    <automated>grep -c "decision-review-fallback.log" /Users/trilogy/Documents/agentic-setup/.openclaw/agents/task-orchestrator/SOUL.md</automated>
  </verify>
  <done>SOUL.md contains the Failed Verdict Policy subsection referencing decision-review-fallback.log, the non-blocking proceed instruction, and the next-session surface instruction. grep returns >= 2 occurrences.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Fast-pass bypass | Fast-pass operations skip Decision Reviewer — if an action is misclassified as fast-pass, it proceeds without review |
| Fallback log | decision-review-fallback.log is a local file — not synced to Notion automatically |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-18-03 | Elevation of Privilege | Fast-pass list | mitigate | Matching rule explicitly states "when in doubt, route through Decision Reviewer"; prefix match is conservative, not greedy |
| T-18-04 | Repudiation | Failed verdict fallback log | mitigate | Fallback log is surfaced to User Orchestrator on next session start — user sees all bypassed reviews |
| T-18-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this plan — SOUL.md edit only |
</threat_model>

<verification>
grep -c "fast.pass\|Fast-Pass\|fast_pass" /Users/trilogy/Documents/agentic-setup/.openclaw/agents/task-orchestrator/SOUL.md
grep -c "decision-review-fallback.log" /Users/trilogy/Documents/agentic-setup/.openclaw/agents/task-orchestrator/SOUL.md
grep -c "gh issue comment" /Users/trilogy/Documents/agentic-setup/.openclaw/agents/task-orchestrator/SOUL.md
grep -c "PROCEED" /Users/trilogy/Documents/agentic-setup/.openclaw/agents/task-orchestrator/SOUL.md
</verification>

<success_criteria>
- Task Orchestrator SOUL.md contains Fast-Pass List with all 7 action classes from D-508
- Fast-pass matching rule is present (prefix match, "when in doubt route through reviewer")
- Failed Verdict Policy section references decision-review-fallback.log
- Policy states: log → PROCEED — no blocking, no halt
- Next-session surface instruction is present
- All existing SOUL.md content is preserved unchanged
</success_criteria>

<output>
Create `.planning/phases/18-decision-quality-risk-gate/18-02-SUMMARY.md` when done
</output>

---
phase: 18-decision-quality-risk-gate
plan: 04
type: execute
wave: 3
depends_on:
  - 18-01
  - 18-02
  - 18-03
files_modified:
  - scripts/verify-phase-18.sh
autonomous: true
requirements:
  - RISK-01
  - RISK-02
  - RISK-03

must_haves:
  truths:
    - "verify-phase-18.sh runs all checks and exits 0 when phase is correctly implemented"
    - "Script checks decision-reviewer SOUL.md for risk_score, risk_tier, four scoring dimensions, tier mapping"
    - "Script checks task-orchestrator SOUL.md for fast-pass list, failed verdict policy, Telegram approval gate"
    - "Script checks chat ID 1294664427 and decision-review-fallback.log path are present"
    - "All checks report pass/fail with clear labels"
  artifacts:
    - path: "scripts/verify-phase-18.sh"
      provides: "10-check verification script for Phase 18"
      contains: "RISK-01"
  key_links:
    - from: "verify-phase-18.sh"
      to: "decision-reviewer/SOUL.md"
      via: "grep checks for risk_score, risk_tier, tier mapping"
      pattern: "risk_score.*SOUL"
    - from: "verify-phase-18.sh"
      to: "task-orchestrator/SOUL.md"
      via: "grep checks for fast-pass list, fallback log, Telegram chat ID"
      pattern: "1294664427.*SOUL"
---

<objective>
Create `scripts/verify-phase-18.sh` — a 10-check verification script that confirms all three RISK requirements are correctly implemented across both agent SOUL.md files — satisfying the phase gate.

Purpose: Automated structural verification that the phase is complete. Checks grep for required strings in both SOUL.md files, providing a clear pass/fail per requirement group.

Output: `scripts/verify-phase-18.sh` that exits 0 when all checks pass, exits 1 on first failure with a clear error message.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Create scripts/verify-phase-18.sh with 10 structural checks</name>
  <files>scripts/verify-phase-18.sh</files>
  <action>
Create `scripts/verify-phase-18.sh` using `#!/usr/bin/env zsh` and `set -euo pipefail`. Output format: one line per check, "CHECK N (LABEL): PASS" or "CHECK N (LABEL): FAIL — {reason}". Exit 0 if all pass, exit 1 on first failure.

The 10 checks must be:

**RISK-01 checks (decision-reviewer SOUL.md):**
1. CHECK 1 (RISK-01 risk_score field): `grep -q "risk_score" "$DR_SOUL"` — decision-reviewer SOUL.md contains risk_score
2. CHECK 2 (RISK-01 risk_tier field): `grep -q "risk_tier" "$DR_SOUL"` — decision-reviewer SOUL.md contains risk_tier
3. CHECK 3 (RISK-01 four dimensions): `grep -q "Reversibility" "$DR_SOUL" && grep -q "Blast radius\|blast_radius\|blast radius" "$DR_SOUL" && grep -q "External side effects\|side.effects" "$DR_SOUL" && grep -q "Action recency\|action.recency" "$DR_SOUL"` — all four scoring dimensions present
4. CHECK 4 (RISK-01 tier mapping): `grep -q "61-100" "$DR_SOUL" && grep -q "31-60" "$DR_SOUL" && grep -q "0-30" "$DR_SOUL"` — all three tier ranges present
5. CHECK 5 (RISK-01 D-111 schema updated): `grep -q '"risk_score"' "$DR_SOUL"` — D-111 output format block includes risk_score field in JSON schema

**RISK-02 checks (task-orchestrator SOUL.md):**
6. CHECK 6 (RISK-02 Telegram chat ID): `grep -q "1294664427" "$TO_SOUL"` — Anuj's chat ID present in approval gate
7. CHECK 7 (RISK-02 APPROVE/REJECT): `grep -q "APPROVE" "$TO_SOUL" && grep -q "REJECT" "$TO_SOUL"` — both response branches documented
8. CHECK 8 (RISK-02 D-507 message format): `grep -q "Risk score" "$TO_SOUL" && grep -q "Reversibility" "$TO_SOUL"` — D-507 message fields present in Telegram block

**RISK-03 checks (task-orchestrator SOUL.md):**
9. CHECK 9 (RISK-03 fast-pass list): `grep -q "gh issue comment" "$TO_SOUL" && grep -q "bd ready" "$TO_SOUL" && grep -q "synapse.learning.record\|Synapse learning record" "$TO_SOUL"` — three representative fast-pass entries present
10. CHECK 10 (RISK-03 fallback log path): `grep -q "decision-review-fallback.log" "$TO_SOUL"` — fallback log path present in failed verdict policy

Variable definitions at top of script:
```zsh
DR_SOUL="$HOME/Documents/agentic-setup/.openclaw/agents/decision-reviewer/SOUL.md"
TO_SOUL="$HOME/Documents/agentic-setup/.openclaw/agents/task-orchestrator/SOUL.md"
PASS=0
FAIL=0
```

Use a helper function `check_result` that takes check number, label, and exit code — increments PASS or FAIL counters and prints the result line. At end: print "Phase 18 verification: $PASS passed, $FAIL failed" and exit with $FAIL count (0 = all pass).

Make the script executable: `chmod +x scripts/verify-phase-18.sh`
  </action>
  <verify>
    <automated>zsh /Users/trilogy/Documents/agentic-setup/scripts/verify-phase-18.sh</automated>
  </verify>
  <done>verify-phase-18.sh exists, is executable, and exits 0 with all 10 checks reporting PASS.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Verification script | grep-based structural checks — verifies presence of strings, not semantic correctness of agent behavior |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-18-08 | Repudiation | verify-phase-18.sh | accept | Script is tracked in git; output is deterministic grep on tracked files |
| T-18-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this plan — shell script only |
</threat_model>

<verification>
zsh /Users/trilogy/Documents/agentic-setup/scripts/verify-phase-18.sh
echo "Exit code: $?"
</verification>

<success_criteria>
- scripts/verify-phase-18.sh exists and is executable
- Running the script exits 0 with all 10 checks PASS
- Each check label clearly identifies which requirement (RISK-01/02/03) it verifies
- Script outputs "Phase 18 verification: 10 passed, 0 failed" on success
</success_criteria>

<output>
Create `.planning/phases/18-decision-quality-risk-gate/18-04-SUMMARY.md` when done
</output>

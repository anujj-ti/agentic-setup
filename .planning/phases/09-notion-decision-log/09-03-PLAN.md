---
phase: 09-notion-decision-log
plan: 03
type: execute
wave: 2
depends_on:
  - "09-01"
files_modified:
  - .openclaw/agents/task-orchestrator/scripts/notion/query-decisions.js
  - .openclaw/agents/task-orchestrator/scripts/notion/query-decisions.sh
  - .openclaw/agents/user-orchestrator/SOUL.md
autonomous: true
requirements:
  - MEM-02

must_haves:
  truths:
    - "query-decisions.js accepts --since <ISO8601> and returns all decisions logged after that timestamp"
    - "query-decisions.js reads last-session.json from user-orchestrator workspace when --since is not provided"
    - "query-decisions.js uses created_time timestamp filter (not property filter) per RESEARCH.md pitfall warning"
    - "query-decisions.js exits 0 with skipped=true when OPENCLAW_NOTION_TOKEN is absent"
    - "User Orchestrator SOUL.md documents how to invoke query-decisions for 'what did you do while I was away?'"
    - "last-session.json is updated at session end by User Orchestrator"
  artifacts:
    - path: ".openclaw/agents/task-orchestrator/scripts/notion/query-decisions.js"
      provides: "Decision retrieval since timestamp (MEM-02)"
      exports: ["queryDecisionsSince function", "TODO_NOTION guard"]
    - path: ".openclaw/agents/task-orchestrator/scripts/notion/query-decisions.sh"
      provides: "Shell wrapper for query-decisions.js"
      contains: "set -euo pipefail"
    - path: ".openclaw/agents/user-orchestrator/SOUL.md"
      provides: "Instructions for surfacing decisions to user via Telegram"
      contains: "query-decisions"
  key_links:
    - from: ".openclaw/agents/task-orchestrator/scripts/notion/query-decisions.js"
      to: "Notion databases.query API"
      via: "notion.databases.query with created_time filter"
      pattern: "timestamp.*created_time"
    - from: ".openclaw/agents/user-orchestrator/SOUL.md"
      to: ".openclaw/agents/task-orchestrator/scripts/notion/query-decisions.sh"
      via: "sessions_spawn to Task Orchestrator → exec query-decisions.sh"
      pattern: "query-decisions"
---

<objective>
Create the decision retrieval script (query-decisions.js) and update the User Orchestrator SOUL.md to handle "what did you do while I was away?" queries.

Purpose: Delivers MEM-02 — the user can retrieve a chronological list of all autonomous decisions since their last session on demand via Telegram. This plan also establishes the last-session.json convention (D-96) and wires the User Orchestrator's response path.

Output: query-decisions.js, query-decisions.sh, and User Orchestrator SOUL.md updated with decision retrieval instructions.
</objective>

<execution_context>
@/Users/trilogy/.claude/get-shit-done/workflows/execute-plan.md
@/Users/trilogy/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/trilogy/Documents/agentic-setup/.planning/ROADMAP.md
@/Users/trilogy/Documents/agentic-setup/.planning/phases/09-notion-decision-log/09-CONTEXT.md
@/Users/trilogy/Documents/agentic-setup/.planning/phases/09-notion-decision-log/09-RESEARCH.md
@/Users/trilogy/Documents/agentic-setup/.planning/phases/09-notion-decision-log/09-01-SUMMARY.md

<interfaces>
<!-- databases.query with created_time filter (RESEARCH.md Pattern 3 — CRITICAL anti-pitfall): -->
<!-- CORRECT: -->
<!--   filter: { timestamp: "created_time", created_time: { on_or_after: isoString } } -->
<!-- WRONG (throws APIResponseError): -->
<!--   filter: { property: "timestamp", ... } -->

<!-- Response shape per page: -->
<!--   { id, url, created_time, -->
<!--     properties.decision.rich_text[0].text.content, -->
<!--     properties.rationale.rich_text[0].text.content, -->
<!--     properties.reversibility.select.name, -->
<!--     properties.revert_status.select.name } -->

<!-- last-session.json location (D-96): -->
<!-- ~/.openclaw/workspace-user-orchestrator/last-session.json -->
<!-- Format: { "session_end": "2026-05-20T18:30:00.000Z" } -->
<!-- User Orchestrator writes this file on session end -->

<!-- query-decisions.js output shape: -->
<!-- { "ok": true, "decisions": [...], "count": N, "since": "<ISO>" } -->
<!-- Each decision: { id, url, created_time, decision, rationale, reversibility, revert_status } -->

<!-- User Orchestrator SOUL.md current content: read before editing -->
<!-- File: .openclaw/agents/user-orchestrator/SOUL.md -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create query-decisions.js and query-decisions.sh</name>
  <files>.openclaw/agents/task-orchestrator/scripts/notion/query-decisions.js, .openclaw/agents/task-orchestrator/scripts/notion/query-decisions.sh</files>
  <behavior>
    - TODO_NOTION guard: {"ok":true,"skipped":true,"reason":"OPENCLAW_NOTION_TOKEN not configured"}, exit 0
    - --since <ISO8601>: use the provided timestamp as the lower bound for created_time filter
    - --since absent: read ~/.openclaw/workspace-user-orchestrator/last-session.json and use session_end value; if file missing or malformed, default to 24 hours ago from now
    - Correct filter structure: { timestamp: "created_time", created_time: { on_or_after: sinceISO } } — NO "property" field
    - Sort: ascending created_time (oldest first)
    - Returns: {"ok":true,"decisions":[...],"count":N,"since":"<ISO>"} where each decision has id, url, created_time, decision, rationale, reversibility, revert_status
    - Empty result (no decisions since timestamp): {"ok":true,"decisions":[],"count":0,"since":"<ISO>"} — not an error
    - On Notion API error: {"ok":false,"error":"<message>"}, exit 0 (non-blocking)
  </behavior>
  <action>
    Create `.openclaw/agents/task-orchestrator/scripts/notion/query-decisions.js` as a CommonJS Node.js script.

    Structure:
    1. TODO_NOTION guard (identical pattern to log-decision.js)
    2. Load config.json from `path.join(__dirname, '..', 'config.json')` and extract NOTION_DECISIONS_DB_ID
    3. Initialize @notionhq/client with notionVersion "2026-03-11" (per D-91)
    4. Parse --since argument from process.argv; if absent, attempt to read `$HOME/.openclaw/workspace-user-orchestrator/last-session.json` and extract `session_end`; if that also fails, default to `new Date(Date.now() - 86400000).toISOString()` (24h ago)
    5. `queryDecisionsSince(sinceISO)` async function:
       - Calls notion.databases.query with the CORRECT filter structure (no "property" field — this is the critical pitfall from RESEARCH.md)
       - Maps results to the output shape
    6. Main: call queryDecisionsSince, write the full response JSON to stdout, exit 0
    7. Catch block: write {"ok":false,"error":"<message>"} to stdout, exit 0

    Create `.openclaw/agents/task-orchestrator/scripts/notion/query-decisions.sh` as thin shell wrapper:
    - `#!/usr/bin/env zsh`, `set -euo pipefail`
    - Resolve SCRIPT_DIR
    - NODE="/opt/homebrew/opt/node@24/bin/node"
    - exec "$NODE" "$SCRIPT_DIR/query-decisions.js" "$@"
    - chmod +x

    Also create the last-session.json file at `~/.openclaw/workspace-user-orchestrator/last-session.json` with the current timestamp as the initial session_end value (so a fresh query returns decisions from now, not from the beginning of time). This file is in the live workspace, not in git.
  </action>
  <verify>
    <automated>
      OPENCLAW_NOTION_TOKEN="" /opt/homebrew/opt/node@24/bin/node /Users/trilogy/Documents/agentic-setup/.openclaw/agents/task-orchestrator/scripts/notion/query-decisions.js --since 2026-05-20T00:00:00.000Z 2>/dev/null | /opt/homebrew/bin/jq -e '.ok == true and .skipped == true' && echo "Guard: PASS"
      /opt/homebrew/opt/node@24/bin/node --check /Users/trilogy/Documents/agentic-setup/.openclaw/agents/task-orchestrator/scripts/notion/query-decisions.js && echo "Syntax check: PASS"
    </automated>
  </verify>
  <done>query-decisions.js passes syntax check; TODO_NOTION guard returns skipped=true when token absent; --since argument is parsed correctly; last-session.json fallback path is implemented; query-decisions.sh is executable</done>
</task>

<task type="auto">
  <name>Task 2: Update User Orchestrator SOUL.md with decision retrieval protocol</name>
  <files>.openclaw/agents/user-orchestrator/SOUL.md</files>
  <action>
    Read the current `.openclaw/agents/user-orchestrator/SOUL.md` content first.

    Append a new section called "## Decision Retrieval Protocol (MEM-02)" at the end of the file (before the Model Policy section if it's last, otherwise at the very end).

    The section must cover:

    1. **Trigger phrases:** When Anuj asks anything matching "what did you do while I was away?", "show me what happened", "decisions since last session", "autonomous actions" — delegate to Task Orchestrator to run query-decisions.sh.

    2. **Delegation instruction:** The Task Orchestrator should run:
       ```
       zsh ~/.openclaw/agents/task-orchestrator/scripts/notion/query-decisions.sh
       ```
       (No --since argument needed — it reads last-session.json automatically per D-96.)

    3. **Formatting the response:** When query-decisions returns a list, format as:
       - Header: "Since your last session, I took N autonomous actions:"
       - Each decision on its own line: "[reversibility icon] [decision summary] — [rationale summary]"
       - Use (R) for reversible, (I) for irreversible, (?) for unknown
       - End with: "Reply with a decision's number to mark it for revert."
       - If count is 0: "No autonomous actions were taken since your last session."

    4. **Session end hook:** At the end of every Telegram session (when Anuj says goodbye, goodnight, or similar), update last-session.json:
       ```
       echo '{"session_end":"<current_ISO8601>"}' > ~/.openclaw/workspace-user-orchestrator/last-session.json
       ```
       Use Python for the timestamp: `python3 -c "from datetime import datetime, timezone; print(datetime.now(timezone.utc).isoformat())"` (CLAUDE.md pattern — no date command format inconsistency).

    5. **Morning standup integration note:** The morning standup brief (Phase 6 CHAN-04) should also call query-decisions.sh and include the count in the brief — this is wired in Plan 09-06.
  </action>
  <verify>
    <automated>grep -q 'query-decisions' /Users/trilogy/Documents/agentic-setup/.openclaw/agents/user-orchestrator/SOUL.md && grep -q 'Decision Retrieval Protocol' /Users/trilogy/Documents/agentic-setup/.openclaw/agents/user-orchestrator/SOUL.md && grep -q 'last-session.json' /Users/trilogy/Documents/agentic-setup/.openclaw/agents/user-orchestrator/SOUL.md && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <done>User Orchestrator SOUL.md has a Decision Retrieval Protocol section covering trigger phrases, delegation instruction, response formatting, and session-end last-session.json update</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Notion API → stdout | Decision data returned from Notion is agent-internal; no PII in decision log |
| last-session.json → query | File lives in live workspace; timestamp is written by User Orchestrator — not user-supplied |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-07 | Information Disclosure | Decision content via Telegram | accept | All decisions logged by Task Orchestrator are agent-internal operational decisions; no PII or secrets are part of decision rationale |
| T-09-08 | Tampering | last-session.json modification | accept | File is in ~/.openclaw/ workspace — same trust level as other agent state files; no cross-user risk on single-user macOS |
| T-09-09 | Denial of Service | query-decisions blocking Telegram response | mitigate | Script exits 0 on all error paths; User Orchestrator receives {"ok":false} and surfaces "Could not retrieve decisions — Notion may be unavailable" message to user |
| T-09-10 | Spoofing | Incorrect timestamp filter causes all decisions to appear | mitigate | last-session.json is written at session end; if file is missing, fallback is 24h (not epoch 0) — bounded result set |
</threat_model>

<verification>
- `node --check scripts/notion/query-decisions.js` passes
- Script returns {"ok":true,"skipped":true} when OPENCLAW_NOTION_TOKEN is empty
- User Orchestrator SOUL.md contains "Decision Retrieval Protocol" section
- last-session.json exists in ~/.openclaw/workspace-user-orchestrator/ with a valid ISO8601 session_end value
</verification>

<success_criteria>
- query-decisions.js: TODO_NOTION guard, --since CLI arg, last-session.json fallback, correct created_time filter (no "property" field), ascending sort, complete output shape
- query-decisions.sh: zsh strict mode, explicit node@24 path, chmod +x
- User Orchestrator SOUL.md: trigger phrases, delegation path, response formatting, session-end update hook
- MEM-02 (on-demand decision retrieval) fully implemented
</success_criteria>

<output>
Create `.planning/phases/09-notion-decision-log/09-03-SUMMARY.md` when done
</output>

---
phase: 09-notion-decision-log
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .openclaw/agents/task-orchestrator/scripts/package.json
  - .openclaw/agents/task-orchestrator/scripts/config.json
  - scripts/openclaw-secrets.sh
  - scripts/openclaw-env.sh
  - scripts/secrets.sh
autonomous: false
requirements:
  - MEM-01
  - MEM-02
  - MEM-03
  - MEM-04
user_setup:
  - service: notion
    why: "Notion integration token required for all decision logging scripts"
    env_vars:
      - name: OPENCLAW_NOTION_TOKEN
        source: "Notion → Settings & members → My connections → Develop or manage integrations → New integration → copy Internal Integration Token"
    dashboard_config:
      - task: "Create 'Autonomous Decision Log' database with 8 properties: Name (title), decision (rich_text), rationale (rich_text), evidence (rich_text), reversibility (select: reversible/irreversible/unknown), revert_status (select: active/reverted/pending_revert), timestamp (date), agent_id (rich_text)"
        location: "Notion → any page → /database → full page"
      - task: "Create 'Experiments' parent page and share it with the integration"
        location: "Notion → new page → share → add integration by name"
      - task: "Share the Decision Log database with the integration"
        location: "Notion → open database → Share → Invite → select integration"
      - task: "Run /openclaw-add-secret notion-token <integration_token>"
        location: "Claude Code terminal in ~/agentic-setup"
      - task: "Add NOTION_DECISIONS_DB_ID and NOTION_EXPERIMENTS_PAGE_ID to .openclaw/agents/task-orchestrator/scripts/config.json (copy IDs from Notion page URLs)"
        location: "Notion URL contains the ID after the last slash, without hyphens"

must_haves:
  truths:
    - "scripts/package.json declares @notionhq/client@5.22.0 as a dependency"
    - "scripts/config.json exists with NOTION_DECISIONS_DB_ID and NOTION_EXPERIMENTS_PAGE_ID placeholder fields"
    - "Notion token Keychain stub is present in all three secrets pipeline files"
    - "User knows exactly what to create in Notion and what commands to run on return"
  artifacts:
    - path: ".openclaw/agents/task-orchestrator/scripts/package.json"
      provides: "Reproducible @notionhq/client dependency declaration"
      contains: "@notionhq/client"
    - path: ".openclaw/agents/task-orchestrator/scripts/config.json"
      provides: "Runtime config for Notion DB IDs"
      contains: "NOTION_DECISIONS_DB_ID"
  key_links:
    - from: ".openclaw/agents/task-orchestrator/scripts/notion/log-decision.js"
      to: ".openclaw/agents/task-orchestrator/scripts/node_modules/@notionhq/client"
      via: "npm install in scripts/ directory"
      pattern: "node_modules/@notionhq/client"
---

<objective>
Install the @notionhq/client npm package, create the scripts/config.json template, stub the Notion token in all three secrets pipeline files, and pause for the user to create the Notion integration and databases.

Purpose: Wave 1 scaffolding — everything automated before the human step. All Notion scripts require the integration token (OPENCLAW_NOTION_TOKEN) and database IDs (stored in config.json) which only the user can provision.

Output: package.json declaring @notionhq/client@5.22.0, config.json with placeholder IDs, Notion token stub in secrets pipeline, and a documented human checkpoint.
</objective>

<execution_context>
@/Users/trilogy/.claude/get-shit-done/workflows/execute-plan.md
@/Users/trilogy/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/trilogy/Documents/agentic-setup/.planning/PROJECT.md
@/Users/trilogy/Documents/agentic-setup/.planning/ROADMAP.md
@/Users/trilogy/Documents/agentic-setup/.planning/phases/09-notion-decision-log/09-CONTEXT.md
@/Users/trilogy/Documents/agentic-setup/.planning/phases/09-notion-decision-log/09-RESEARCH.md

<interfaces>
<!-- Secrets pipeline convention from CLAUDE.md + prior phases -->
<!-- Three files that must receive every new Keychain entry: -->
<!-- scripts/openclaw-secrets.sh — launchd environment -->
<!-- scripts/openclaw-env.sh    — shell sessions -->
<!-- scripts/secrets.sh         — provisioning / setup -->

<!-- Pattern from Phase 6 Gmail secrets stub (for reference): -->
<!-- scripts/openclaw-secrets.sh adds: -->
<!--   export OPENCLAW_GMAIL_CLIENT_ID=$(security find-generic-password -s openclaw.gmail-client-id -w 2>/dev/null || echo "") -->
<!-- Same pattern for OPENCLAW_NOTION_TOKEN / openclaw.notion-token -->

<!-- config.json is a plain JSON file (not a secrets file): -->
<!-- { "NOTION_DECISIONS_DB_ID": "TODO_SET_THIS", "NOTION_EXPERIMENTS_PAGE_ID": "TODO_SET_THIS" } -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create package.json and config.json in task-orchestrator scripts/</name>
  <files>.openclaw/agents/task-orchestrator/scripts/package.json, .openclaw/agents/task-orchestrator/scripts/config.json</files>
  <action>
    Create `.openclaw/agents/task-orchestrator/scripts/package.json` declaring `@notionhq/client@5.22.0` as a dependency. Use the standard package structure with name `task-orchestrator-scripts`, version `1.0.0`, and private true. Set engines to require node `>=22.19`.

    Create `.openclaw/agents/task-orchestrator/scripts/config.json` with two placeholder fields:
    - `NOTION_DECISIONS_DB_ID` set to `"TODO_SET_THIS"` (user copies from Notion database URL after creating it — per D-94)
    - `NOTION_EXPERIMENTS_PAGE_ID` set to `"TODO_SET_THIS"` (user copies from Notion experiments page URL)

    Add a comment-style field `_instructions` explaining: "Copy IDs from Notion page URLs. The ID is the string of characters after the last slash in the URL, without hyphens. This file is committed to git — it contains no secrets."

    Do NOT run `npm install` yet — that step belongs after the human checkpoint when the token and DB IDs are available. The package.json is committed to git so the dependency is reproducible; node_modules/ lives in the live workspace only.

    Also create `.openclaw/agents/task-orchestrator/scripts/notion/` directory placeholder: create a `.gitkeep` file in it so git tracks the directory.
  </action>
  <verify>
    <automated>grep -q '"@notionhq/client": "5.22.0"' /Users/trilogy/Documents/agentic-setup/.openclaw/agents/task-orchestrator/scripts/package.json && grep -q 'NOTION_DECISIONS_DB_ID' /Users/trilogy/Documents/agentic-setup/.openclaw/agents/task-orchestrator/scripts/config.json && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <done>package.json declares @notionhq/client@5.22.0; config.json has NOTION_DECISIONS_DB_ID and NOTION_EXPERIMENTS_PAGE_ID placeholders; scripts/notion/.gitkeep exists</done>
</task>

<task type="auto">
  <name>Task 2: Stub OPENCLAW_NOTION_TOKEN in all three secrets pipeline files</name>
  <files>scripts/openclaw-secrets.sh, scripts/openclaw-env.sh, scripts/secrets.sh</files>
  <action>
    Add the Notion token stub to all three secrets pipeline files, following the exact same pattern used in prior phases for Gmail and Telegram credentials.

    In `scripts/openclaw-secrets.sh` (launchd environment — sourced at gateway start), append:
    ```
    export OPENCLAW_NOTION_TOKEN=$(security find-generic-password -s openclaw.notion-token -w 2>/dev/null || echo "")
    ```

    In `scripts/openclaw-env.sh` (interactive shell sessions), append the same export line.

    In `scripts/secrets.sh` (provisioning / setup script), append a comment block:
    ```
    # Notion integration token — run: /openclaw-add-secret notion-token <integration_token>
    # Service: openclaw.notion-token | Env var: OPENCLAW_NOTION_TOKEN
    # Create at: https://www.notion.so/my-integrations
    ```

    Do NOT add an actual token value — the three files receive the Keychain-lookup pattern only. The actual token is added via `/openclaw-add-secret notion-token <value>` which the user runs after creating the Notion integration (per D-92).

    After editing, verify the pattern is present in all three files with a quick grep — do NOT run stow yet (stow is blocked on the human checkpoint completing).
  </action>
  <verify>
    <automated>grep -q 'openclaw.notion-token' /Users/trilogy/Documents/agentic-setup/scripts/openclaw-secrets.sh && grep -q 'openclaw.notion-token' /Users/trilogy/Documents/agentic-setup/scripts/openclaw-env.sh && grep -q 'openclaw.notion-token' /Users/trilogy/Documents/agentic-setup/scripts/secrets.sh && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <done>All three secrets pipeline files contain the openclaw.notion-token Keychain lookup stub; no actual token value is stored in any file</done>
</task>

</tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    All automated scaffolding is complete:
    - scripts/package.json declares @notionhq/client@5.22.0
    - scripts/config.json has NOTION_DECISIONS_DB_ID and NOTION_EXPERIMENTS_PAGE_ID as TODO placeholders
    - All three secrets pipeline files have the OPENCLAW_NOTION_TOKEN Keychain lookup stub

    The Notion scripts themselves (log-decision.js, query-decisions.js, etc.) are built in Plans 09-02 through 09-05. All of them contain TODO_NOTION guards and exit 0 when the token is absent, so the agent fleet continues operating normally until you complete this setup step.
  </what-built>
  <how-to-verify>
    Complete all steps in this order:

    1. Go to https://www.notion.so/my-integrations → click "New integration"
       - Name: "AI Operations Hub"
       - Capabilities: Read content, Update content, Insert content
       - Copy the "Internal Integration Token" (starts with `secret_`)

    2. In Notion, create a full-page database called "Autonomous Decision Log" with these 8 properties:
       - Name (title) — already exists
       - decision (rich_text)
       - rationale (rich_text)
       - evidence (rich_text)
       - reversibility (select) — options: reversible, irreversible, unknown
       - revert_status (select) — options: active, reverted, pending_revert
       - timestamp (date)
       - agent_id (rich_text)

    3. Share the "Autonomous Decision Log" database with the "AI Operations Hub" integration:
       Open the database → Share (top right) → Invite → select "AI Operations Hub"

    4. Create a new page in Notion called "Experiments" and share it with the integration the same way.

    5. In Claude Code terminal (~/agentic-setup):
       ```
       /openclaw-add-secret notion-token <your-integration-token>
       ```

    6. Copy the database ID from the Notion URL of "Autonomous Decision Log":
       - URL looks like: https://www.notion.so/workspace/<32-char-hex-without-hyphens>?v=...
       - The ID is the 32-char part without hyphens

    7. Copy the page ID from the Notion URL of your "Experiments" page (same format).

    8. Edit `.openclaw/agents/task-orchestrator/scripts/config.json` and replace:
       - `"TODO_SET_THIS"` for NOTION_DECISIONS_DB_ID with the database ID
       - `"TODO_SET_THIS"` for NOTION_EXPERIMENTS_PAGE_ID with the experiments page ID

    9. Run `scripts/stow-deploy.sh` to deploy the updated secrets pipeline files.

    10. Verify the token is in the gateway environment:
        ```
        /openclaw-status
        ```
        Then verify the env var resolves:
        ```
        security find-generic-password -s openclaw.notion-token -w
        ```
        It should return your integration token (not empty).
  </how-to-verify>
  <resume-signal>Type "notion-configured" after completing all 10 steps, or describe which step you're stuck on.</resume-signal>
</task>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| scripts/ → Notion API | Integration token crosses this boundary; must come from Keychain, never hardcoded |
| config.json → Notion API | Database IDs are non-secret but define which Notion workspace receives data |
| git history → secrets | Keychain pattern must never be replaced with literal token values in committed files |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-01 | Information Disclosure | scripts/openclaw-secrets.sh | mitigate | Keychain lookup pattern only — `security find-generic-password -s openclaw.notion-token -w` — no literal token. Verified by grep gate in Task 2. |
| T-09-02 | Information Disclosure | scripts/config.json | accept | Database IDs are non-secret (visible in Notion URL to any collaborator); committing to git is acceptable per D-94 and research. |
| T-09-03 | Denial of Service | Notion logging blocking agent execution | mitigate | TODO_NOTION guard exits 0 when token absent; logging failure must not stop agent work (D-93). Enforced in all Notion scripts created in Plans 09-02 through 09-05. |
| T-09-SC | Tampering | npm install @notionhq/client | mitigate | Package manually verified: makenotion org, 5 years old, 97 versions, no postinstall scripts. slopcheck returned false positive (wrong registry) — manual verification supersedes. Checkpoint in plan structure gates the npm install until after user reviews this plan. |
</threat_model>

<verification>
After human checkpoint completes:
- `security find-generic-password -s openclaw.notion-token -w` returns a non-empty token
- `cat .openclaw/agents/task-orchestrator/scripts/config.json` shows real IDs (not TODO_SET_THIS)
- `grep OPENCLAW_NOTION_TOKEN scripts/openclaw-secrets.sh` shows the Keychain lookup line
</verification>

<success_criteria>
- package.json with @notionhq/client@5.22.0 committed to git
- config.json template committed with placeholder IDs (user fills in real IDs via human checkpoint)
- All three secrets pipeline files contain openclaw.notion-token Keychain lookup stub
- Human checkpoint documents every step the user needs to perform on return
- No literal token value appears in any committed file
</success_criteria>

<output>
Create `.planning/phases/09-notion-decision-log/09-01-SUMMARY.md` when done
</output>

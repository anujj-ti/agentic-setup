---
phase: 09-notion-decision-log
plan: 05
type: execute
wave: 3
depends_on:
  - "09-02"
  - "09-03"
  - "09-04"
files_modified:
  - .openclaw/agents/task-orchestrator/SOUL.md
  - .openclaw/agents/task-orchestrator/scripts/notion/create-experiment.js
  - .openclaw/agents/task-orchestrator/scripts/notion/create-experiment.sh
  - .openclaw/agents/task-orchestrator/scripts/notion/append-experiment-results.js
  - .openclaw/agents/task-orchestrator/scripts/notion/append-experiment-results.sh
autonomous: true
requirements:
  - MEM-01
  - MEM-04

must_haves:
  truths:
    - "create-experiment.js creates a structured Notion page with 4 heading+paragraph block sections"
    - "append-experiment-results.js appends a Results block to an existing experiment page"
    - "Task Orchestrator SOUL.md has a mandatory Notion Pre-Log Protocol section for all autonomous actions"
    - "SOUL.md specifies the local fallback file path when token is absent"
    - "All experiment scripts use the TODO_NOTION guard and exit 0 when token is absent"
  artifacts:
    - path: ".openclaw/agents/task-orchestrator/scripts/notion/create-experiment.js"
      provides: "Experiment page creation with structured template (MEM-04)"
      exports: ["createExperimentPage function with heading+paragraph blocks"]
    - path: ".openclaw/agents/task-orchestrator/scripts/notion/append-experiment-results.js"
      provides: "Post-execution results append (MEM-04)"
      exports: ["appendExperimentResults function"]
    - path: ".openclaw/agents/task-orchestrator/SOUL.md"
      provides: "Notion Pre-Log Protocol — mandatory for all autonomous actions (MEM-01)"
      contains: "Notion Pre-Log Protocol"
  key_links:
    - from: ".openclaw/agents/task-orchestrator/SOUL.md"
      to: ".openclaw/agents/task-orchestrator/scripts/notion/log-decision.sh"
      via: "SOUL.md Notion Pre-Log Protocol section references log-decision.sh"
      pattern: "log-decision.sh"
    - from: "create-experiment.js"
      to: "Notion pages.create with children blocks"
      via: "@notionhq/client pages.create with heading_2 + paragraph block children"
      pattern: "heading_2"
---

<objective>
Create the experiment logging scripts (create-experiment.js, append-experiment-results.js) and update the Task Orchestrator SOUL.md with the mandatory Notion pre-log protocol for all autonomous actions.

Purpose: Delivers MEM-04 (experiment page creation + results append) and the final mandatory wiring of MEM-01 (every autonomous action must call log-decision.sh before execution, with local fallback when token is absent). This plan completes all four MEM requirements at the behavioral/SOUL.md level.

Output: create-experiment.js, create-experiment.sh, append-experiment-results.js, append-experiment-results.sh, and Task Orchestrator SOUL.md with the mandatory pre-log section.
</objective>

<execution_context>
@/Users/trilogy/.claude/get-shit-done/workflows/execute-plan.md
@/Users/trilogy/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/trilogy/Documents/agentic-setup/.planning/ROADMAP.md
@/Users/trilogy/Documents/agentic-setup/.planning/phases/09-notion-decision-log/09-CONTEXT.md
@/Users/trilogy/Documents/agentic-setup/.planning/phases/09-notion-decision-log/09-RESEARCH.md
@/Users/trilogy/Documents/agentic-setup/.planning/phases/09-notion-decision-log/09-02-SUMMARY.md
@/Users/trilogy/Documents/agentic-setup/.planning/phases/09-notion-decision-log/09-03-SUMMARY.md
@/Users/trilogy/Documents/agentic-setup/.planning/phases/09-notion-decision-log/09-04-SUMMARY.md

<interfaces>
<!-- pages.create with block children (RESEARCH.md Pattern 5): -->
<!-- parent: { page_id: process.env.NOTION_EXPERIMENTS_PAGE_ID } -->
<!-- properties: { Name: { title: [{ text: { content: "Experiment: <hypothesis.slice(0,100)>" } }] } } -->
<!-- children: array of heading_2 and paragraph blocks -->
<!-- Each block: { object: "block", type: "heading_2", heading_2: { rich_text: [{ text: { content: "..." } }] } } -->
<!-- Each block: { object: "block", type: "paragraph", paragraph: { rich_text: [{ text: { content: "..." } }] } } -->
<!-- Sections: Hypothesis, Method, Success Criteria, Results (with "Pending — experiment in progress.") -->

<!-- blocks.children.append (from RESEARCH.md MEM-04 pattern): -->
<!-- notion.blocks.children.append({ block_id: pageId, children: [result blocks] }) -->

<!-- create-experiment.js stdin/CLI: -->
<!-- { "hypothesis": "...", "method": "...", "success_criteria": "..." } -->
<!-- Returns: { "ok": true, "page_id": "...", "url": "..." } -->

<!-- append-experiment-results.js CLI: -->
<!-- --page-id <experiment_page_id> --results <results_text> -->
<!-- Returns: { "ok": true, "page_id": "..." } -->

<!-- NOTION_EXPERIMENTS_PAGE_ID is read from config.json (not a database_id — experiments use page parent) -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create create-experiment.js, append-experiment-results.js, and their shell wrappers</name>
  <files>
    .openclaw/agents/task-orchestrator/scripts/notion/create-experiment.js,
    .openclaw/agents/task-orchestrator/scripts/notion/create-experiment.sh,
    .openclaw/agents/task-orchestrator/scripts/notion/append-experiment-results.js,
    .openclaw/agents/task-orchestrator/scripts/notion/append-experiment-results.sh
  </files>
  <behavior>
    create-experiment.js:
    - TODO_NOTION guard: {"ok":true,"skipped":true,"reason":"..."}, exit 0 when token absent
    - Reads JSON from stdin: { "hypothesis": "...", "method": "...", "success_criteria": "..." }
    - All three fields required; missing field: {"ok":false,"error":"hypothesis, method, success_criteria required"}, exit 0
    - --dry-run: prints the pages.create payload without calling the API, exit 0
    - Creates a Notion page as child of NOTION_EXPERIMENTS_PAGE_ID (from config.json) with 4 section pairs: Hypothesis, Method, Success Criteria, Results (Results body = "Pending — experiment in progress.")
    - Each section is: heading_2 block + paragraph block
    - Returns: {"ok":true,"page_id":"...","url":"..."}
    - truncate helper: all text inputs truncated to 1990 chars

    append-experiment-results.js:
    - TODO_NOTION guard: same pattern
    - CLI: --page-id <experiment_page_id> --results <results_text>
    - Missing either arg: {"ok":false,"error":"..."}, exit 0
    - Appends a new heading_2 block ("Execution Results — <ISO timestamp>") + paragraph block with results text to the page's block children
    - Returns: {"ok":true,"page_id":"..."}
    - On Notion API error: {"ok":false,"error":"..."}, exit 0
  </behavior>
  <action>
    Create `.openclaw/agents/task-orchestrator/scripts/notion/create-experiment.js` as a CommonJS Node.js script.

    Structure:
    1. TODO_NOTION guard
    2. Load config.json from `path.join(__dirname, '..', 'config.json')` and extract NOTION_EXPERIMENTS_PAGE_ID
    3. Initialize @notionhq/client with notionVersion "2026-03-11" (per D-91)
    4. truncate helper (1990 chars max)
    5. Read stdin JSON; validate hypothesis, method, success_criteria present
    6. --dry-run flag: construct and print payload, exit 0
    7. `createExperimentPage` async function: calls notion.pages.create with:
       - parent: { page_id: NOTION_EXPERIMENTS_PAGE_ID }
       - properties: Name title with "Experiment: " + hypothesis.slice(0,97)
       - children: 8 blocks total — heading_2 + paragraph for each of: Hypothesis, Method, Success Criteria, Results
       - Results paragraph body: "Pending — experiment in progress."
    8. Returns {page_id, url}
    9. Catch: {"ok":false,"error":"..."} to stdout, exit 0

    Create `.openclaw/agents/task-orchestrator/scripts/notion/append-experiment-results.js` as a CommonJS Node.js script.

    Structure:
    1. TODO_NOTION guard
    2. Initialize @notionhq/client with notionVersion "2026-03-11"
    3. Parse --page-id and --results from process.argv
    4. Validate both present
    5. `appendResults` async function: calls notion.blocks.children.append with:
       - block_id: pageId
       - children: [
           heading_2 block: "Execution Results — " + new Date().toISOString(),
           paragraph block: truncate(results, 1990)
         ]
    6. Returns {"ok":true,"page_id":"..."}
    7. Catch: {"ok":false,"error":"..."}, exit 0

    Create shell wrappers for both scripts:
    - create-experiment.sh: `#!/usr/bin/env zsh` + `set -euo pipefail` + exec node create-experiment.js "$@"
    - append-experiment-results.sh: same pattern for append-experiment-results.js
    - chmod +x both wrappers
  </action>
  <verify>
    <automated>
      OPENCLAW_NOTION_TOKEN="" /opt/homebrew/opt/node@24/bin/node /Users/trilogy/Documents/agentic-setup/.openclaw/agents/task-orchestrator/scripts/notion/create-experiment.js 2>/dev/null | /opt/homebrew/bin/jq -e '.ok == true and .skipped == true' && echo "create-experiment guard: PASS"
      OPENCLAW_NOTION_TOKEN="" /opt/homebrew/opt/node@24/bin/node /Users/trilogy/Documents/agentic-setup/.openclaw/agents/task-orchestrator/scripts/notion/append-experiment-results.js --page-id abc --results "test" 2>/dev/null | /opt/homebrew/bin/jq -e '.ok == true and .skipped == true' && echo "append-results guard: PASS"
      /opt/homebrew/opt/node@24/bin/node --check /Users/trilogy/Documents/agentic-setup/.openclaw/agents/task-orchestrator/scripts/notion/create-experiment.js && echo "Syntax check create-experiment: PASS"
      /opt/homebrew/opt/node@24/bin/node --check /Users/trilogy/Documents/agentic-setup/.openclaw/agents/task-orchestrator/scripts/notion/append-experiment-results.js && echo "Syntax check append-results: PASS"
    </automated>
  </verify>
  <done>Both scripts pass syntax check; TODO_NOTION guard returns skipped=true when token absent; missing args return {"ok":false}; shell wrappers are executable; MEM-04 experiment scripts are complete</done>
</task>

<task type="auto">
  <name>Task 2: Add Notion Pre-Log Protocol (MANDATORY) to Task Orchestrator SOUL.md</name>
  <files>.openclaw/agents/task-orchestrator/SOUL.md</files>
  <action>
    Read the current `.openclaw/agents/task-orchestrator/SOUL.md` content first (it was also modified in Plan 09-04 to add the Revert Workflow Protocol).

    Add a new section called "## Notion Pre-Log Protocol (MANDATORY — NO EXCEPTIONS)" immediately after the "## Beads-Enforced Execution Contract" section (before the Decomposition Templates section). This placement makes it the second mandatory protocol block in the SOUL.md, co-equal in authority with the Beads contract.

    The section must contain:

    1. **Mandatory rule:** Before executing ANY autonomous action (API call, git operation, PR creation, issue creation, file modification), the Task Orchestrator MUST call log-decision.sh to create a Notion decision log entry.

    2. **Call sequence:**
       ```
       DECISION_PAYLOAD='{"decision":"<what you are about to do>","rationale":"<why>","evidence":"<factual basis>","reversibility":"<reversible|irreversible|unknown>","agent_id":"task-orchestrator"}'
       LOG_RESULT=$(echo "$DECISION_PAYLOAD" | zsh /Users/trilogy/.openclaw/agents/task-orchestrator/scripts/notion/log-decision.sh)
       PAGE_ID=$(echo "$LOG_RESULT" | /opt/homebrew/bin/jq -r '.page_id // ""')
       ```

    3. **Non-blocking rule:** If log-decision.sh returns `{"ok":false,...}` or `{"ok":true,"skipped":true,...}` (token absent or Notion unavailable), proceed with the action anyway. Log the failure to a local fallback file at `~/.openclaw/workspace-task-orchestrator/notion-fallback.log`:
       ```
       echo "{\"timestamp\":\"$(python3 -c 'from datetime import datetime, timezone; print(datetime.now(timezone.utc).isoformat())')\",\"decision\":\"<decision>\",\"log_result\":$LOG_RESULT}" >> ~/.openclaw/workspace-task-orchestrator/notion-fallback.log
       ```

    4. **Phase 9 note:** In Phase 9, Notion logging is an audit trail — actions proceed regardless of logging outcome. Phase 10 introduces hard gates for autonomous merges.

    5. **Decision payload guidance:**
       - `decision`: concise description of the action (e.g., "Close GitHub issue #42 as completed")
       - `rationale`: why this action is appropriate (e.g., "Beads task T3 closed with evidence: all tests pass")
       - `evidence`: factual basis (e.g., "gh pr checks output: 3/3 passed, no failures")
       - `reversibility`: use "reversible" for most GitHub operations; "irreversible" for email sends, resource deletions
  </action>
  <verify>
    <automated>grep -q 'Notion Pre-Log Protocol' /Users/trilogy/Documents/agentic-setup/.openclaw/agents/task-orchestrator/SOUL.md && grep -q 'log-decision.sh' /Users/trilogy/Documents/agentic-setup/.openclaw/agents/task-orchestrator/SOUL.md && grep -q 'notion-fallback.log' /Users/trilogy/Documents/agentic-setup/.openclaw/agents/task-orchestrator/SOUL.md && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <done>Task Orchestrator SOUL.md contains Notion Pre-Log Protocol section marked MANDATORY, with log-decision.sh call sequence, non-blocking fallback to notion-fallback.log, and decision payload guidance; section appears immediately after Beads-Enforced Execution Contract</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| stdin → Notion experiments page | Experiment hypothesis/method from Task Orchestrator's own planning — not user-typed |
| notion-fallback.log → filesystem | Local fallback file in agent workspace; no secrets; plain text audit trail |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-14 | Information Disclosure | notion-fallback.log on disk | accept | Log contains decision descriptions, no secrets; file is in ~/.openclaw/workspace-task-orchestrator/ (user-owned, not in git) |
| T-09-15 | Denial of Service | create-experiment.js blocking agent start | mitigate | TODO_NOTION guard + exit 0 on all error paths; agent can start experiments even when Notion is unconfigured |
| T-09-16 | Tampering | SOUL.md Notion Pre-Log section bypassed | accept | SOUL.md instructs Claude; if Claude decides not to follow it, that is a model compliance issue, not a code security issue. Phase 10 adds a hard programmatic gate for merge operations. |
</threat_model>

<verification>
- `node --check scripts/notion/create-experiment.js` passes
- `node --check scripts/notion/append-experiment-results.js` passes
- Both guard correctly with empty token
- Task Orchestrator SOUL.md: "Notion Pre-Log Protocol (MANDATORY)" section present, log-decision.sh call sequence documented, notion-fallback.log fallback documented
</verification>

<success_criteria>
- create-experiment.js: TODO_NOTION guard, stdin JSON, 4 heading+paragraph section pairs, --dry-run, truncation, exit 0 on all paths
- append-experiment-results.js: TODO_NOTION guard, --page-id + --results CLI args, blocks.children.append with timestamped heading
- Both shell wrappers: zsh strict mode, explicit node@24 path, chmod +x
- Task Orchestrator SOUL.md: Notion Pre-Log Protocol section is MANDATORY, co-equal with Beads contract, covers call sequence, non-blocking fallback, and payload guidance
- MEM-04 (experiment logging) and MEM-01 mandatory behavioral wiring both complete
</success_criteria>

<output>
Create `.planning/phases/09-notion-decision-log/09-05-SUMMARY.md` when done
</output>

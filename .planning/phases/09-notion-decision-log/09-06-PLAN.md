---
phase: 09-notion-decision-log
plan: 06
type: execute
wave: 4
depends_on:
  - "09-05"
files_modified:
  - scripts/standup-brief.sh
  - scripts/verify-phase-09.sh
autonomous: true
requirements:
  - MEM-01
  - MEM-02
  - MEM-03
  - MEM-04

must_haves:
  truths:
    - "verify-phase-09.sh --smoke passes with zero Notion token (syntax + guard checks only)"
    - "verify-phase-09.sh (full) passes with live Notion token and configured DB IDs"
    - "standup-brief.sh includes autonomous decision count from query-decisions.sh"
    - "All 7 notion/ scripts exist and pass node --check"
    - "Phase 9 success criteria are all verifiable via verify-phase-09.sh"
  artifacts:
    - path: "scripts/verify-phase-09.sh"
      provides: "Phase gate verification — smoke + integration modes"
      contains: "verify-phase-09"
    - path: "scripts/standup-brief.sh"
      provides: "Morning standup includes autonomous decision count (ROADMAP SC#5)"
      contains: "query-decisions"
  key_links:
    - from: "scripts/standup-brief.sh"
      to: ".openclaw/agents/task-orchestrator/scripts/notion/query-decisions.sh"
      via: "exec call from standup-brief.sh to query-decisions.sh --since last-session"
      pattern: "query-decisions"
    - from: "scripts/verify-phase-09.sh"
      to: ".openclaw/agents/task-orchestrator/scripts/notion/"
      via: "node --check on all 7 notion scripts + guard behavior tests"
      pattern: "notion"
---

<objective>
Wire autonomous decision count into the morning standup brief and create the phase verification script.

Purpose: Delivers ROADMAP success criterion #5 (morning standup includes decision count) and provides the automated phase gate for all four MEM requirements. This is the final integration plan that proves Phase 9 is complete and operational.

Output: verify-phase-09.sh (two modes: smoke and full integration) and updated standup-brief.sh with decision count section.
</objective>

<execution_context>
@/Users/trilogy/.claude/get-shit-done/workflows/execute-plan.md
@/Users/trilogy/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/trilogy/Documents/agentic-setup/.planning/ROADMAP.md
@/Users/trilogy/Documents/agentic-setup/.planning/phases/09-notion-decision-log/09-CONTEXT.md
@/Users/trilogy/Documents/agentic-setup/.planning/phases/09-notion-decision-log/09-RESEARCH.md
@/Users/trilogy/Documents/agentic-setup/.planning/phases/09-notion-decision-log/09-05-SUMMARY.md

<interfaces>
<!-- standup-brief.sh current location: scripts/standup-brief.sh -->
<!-- Current output: JSON with github_activity section (from Phase 6) -->
<!-- Output schema extension: add "autonomous_decisions": { "count": N, "since": "<ISO>", "summary": [...] } -->
<!-- query-decisions.sh returns: {"ok":true,"decisions":[...],"count":N,"since":"<ISO>"} -->
<!-- If query-decisions returns skipped=true: autonomous_decisions count = 0, note "Notion not configured" -->

<!-- verify-phase-09.sh checks (from RESEARCH.md Validation Architecture): -->
<!-- Smoke mode (no token needed): -->
<!--   1. All 7 notion/ script files exist -->
<!--   2. node --check on all 7 scripts passes -->
<!--   3. Each script returns skipped=true with empty OPENCLAW_NOTION_TOKEN -->
<!--   4. standup-brief.sh contains query-decisions reference -->
<!--   5. Task Orchestrator SOUL.md contains "Notion Pre-Log Protocol" -->
<!--   6. config.json has NOTION_DECISIONS_DB_ID field -->
<!-- Full integration mode (requires token + DB IDs): -->
<!--   7. log-decision.js --dry-run returns valid payload -->
<!--   8. query-decisions.js --since <past> returns ok:true decisions array -->
<!--   Plus all smoke checks -->

<!-- Notion script file paths to verify: -->
<!-- .openclaw/agents/task-orchestrator/scripts/notion/log-decision.js -->
<!-- .openclaw/agents/task-orchestrator/scripts/notion/update-decision.js -->
<!-- .openclaw/agents/task-orchestrator/scripts/notion/query-decisions.js -->
<!-- .openclaw/agents/task-orchestrator/scripts/notion/revert-decision.js -->
<!-- .openclaw/agents/task-orchestrator/scripts/notion/create-experiment.js -->
<!-- .openclaw/agents/task-orchestrator/scripts/notion/append-experiment-results.js -->
<!-- (6 .js files + 6 .sh wrappers = 12 notion/ files total) -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update standup-brief.sh to include autonomous decision count</name>
  <files>scripts/standup-brief.sh</files>
  <action>
    Read the current `scripts/standup-brief.sh` content first (created in Phase 6, Plan 06-04).

    Add an "Autonomous Decisions" section to the standup brief output. Locate where the JSON output object is constructed and add an `autonomous_decisions` field.

    The addition should:
    1. Call query-decisions.sh to get decisions since last session:
       ```zsh
       DECISIONS_RAW=$(zsh /Users/trilogy/.openclaw/agents/task-orchestrator/scripts/notion/query-decisions.sh 2>/dev/null || echo '{"ok":false,"error":"query failed"}')
       ```
    2. Extract count and determine skipped status:
       ```zsh
       DECISION_COUNT=$(echo "$DECISIONS_RAW" | /opt/homebrew/bin/jq '.count // 0')
       DECISION_SKIPPED=$(echo "$DECISIONS_RAW" | /opt/homebrew/bin/jq '.skipped // false')
       ```
    3. If DECISION_SKIPPED is true: set decision_summary to "Notion not configured — run /openclaw-add-secret notion-token"
    4. If ok=false: set decision_summary to "Could not retrieve decisions from Notion"
    5. If ok=true and count > 0: extract the first 3 decisions' decision field as a summary array
    6. Add to the JSON output:
       ```json
       "autonomous_decisions": {
         "count": <N>,
         "since": "<ISO>",
         "summary": ["<decision1>", "<decision2>", ...],
         "notion_configured": <true|false>
       }
       ```

    Important: standup-brief.sh uses `set -euo pipefail` — wrap the query-decisions call in a subshell with `|| echo '{"ok":false}'` so a non-zero exit from query-decisions does not abort the whole standup brief. The standup brief must always produce output regardless of Notion status.

    Do not restructure the existing standup-brief.sh logic — add the decision query before the final JSON output construction and inject the `autonomous_decisions` field into the existing output object.
  </action>
  <verify>
    <automated>
      zsh /Users/trilogy/Documents/agentic-setup/scripts/standup-brief.sh --repo anujj-ti/agentic-setup 2>/dev/null | /opt/homebrew/bin/jq -e '.autonomous_decisions != null' && echo "autonomous_decisions field: PASS" || echo "autonomous_decisions field: FAIL"
      grep -q 'query-decisions' /Users/trilogy/Documents/agentic-setup/scripts/standup-brief.sh && echo "query-decisions wired: PASS" || echo "query-decisions wired: FAIL"
    </automated>
  </verify>
  <done>standup-brief.sh includes autonomous_decisions.count, autonomous_decisions.since, autonomous_decisions.summary, and autonomous_decisions.notion_configured in its JSON output; brief runs to completion even when Notion token is absent</done>
</task>

<task type="auto">
  <name>Task 2: Create verify-phase-09.sh with smoke and full integration modes</name>
  <files>scripts/verify-phase-09.sh</files>
  <action>
    Create `scripts/verify-phase-09.sh` as a zsh strict-mode verification script covering all Phase 9 success criteria.

    Shebang: `#!/usr/bin/env zsh`
    Strict mode: `set -euo pipefail`

    The script accepts an optional `--smoke` flag. When `--smoke` is passed, only run checks that do not require a live Notion token. Without `--smoke`, run all checks (requires OPENCLAW_NOTION_TOKEN set and config.json populated with real IDs).

    Script structure:
    1. Define PASS/FAIL counters
    2. Define `check` helper function: `check "<description>" <command>` — runs the command, prints "[PASS]" or "[FAIL]" with description, increments counter
    3. Run smoke checks (always):
       - Check 1: `test -f ~/.openclaw/agents/task-orchestrator/scripts/notion/log-decision.js` — log-decision.js exists
       - Check 2: `test -f ~/.openclaw/agents/task-orchestrator/scripts/notion/update-decision.js` — update-decision.js exists
       - Check 3: `test -f ~/.openclaw/agents/task-orchestrator/scripts/notion/query-decisions.js` — query-decisions.js exists
       - Check 4: `test -f ~/.openclaw/agents/task-orchestrator/scripts/notion/revert-decision.js` — revert-decision.js exists
       - Check 5: `test -f ~/.openclaw/agents/task-orchestrator/scripts/notion/create-experiment.js` — create-experiment.js exists
       - Check 6: `test -f ~/.openclaw/agents/task-orchestrator/scripts/notion/append-experiment-results.js` — append-experiment-results.js exists
       - Check 7: `/opt/homebrew/opt/node@24/bin/node --check ~/.openclaw/agents/task-orchestrator/scripts/notion/log-decision.js` — log-decision.js syntax valid
       - Check 8: OPENCLAW_NOTION_TOKEN="" node log-decision.js --dry-run | jq -e '.skipped == true' — TODO_NOTION guard active
       - Check 9: `grep -q 'Notion Pre-Log Protocol' ~/.openclaw/agents/task-orchestrator/SOUL.md` — SOUL.md has mandatory pre-log rule
       - Check 10: `grep -q 'query-decisions' /Users/trilogy/Documents/agentic-setup/scripts/standup-brief.sh` — standup brief wired
       - Check 11: `grep -q 'NOTION_DECISIONS_DB_ID' ~/.openclaw/agents/task-orchestrator/scripts/config.json` — config.json has DB ID field
       - Check 12: `grep -q 'query-decisions' ~/.openclaw/agents/user-orchestrator/SOUL.md` — User Orchestrator knows how to retrieve decisions

    4. If NOT --smoke (full integration mode), also run:
       - Check 13: `echo '{"decision":"test","rationale":"test","evidence":"test","reversibility":"reversible","agent_id":"test"}' | node log-decision.js | jq -e '.ok == true and .page_id != null'` — MEM-01: log-decision creates live Notion page
       - Check 14: `node query-decisions.js --since 2026-05-01T00:00:00.000Z | jq -e '.ok == true and (.decisions | type) == "array"'` — MEM-02: query-decisions returns decisions array
       - Check 15: `node create-experiment.js --dry-run < /dev/null | jq -e '.' > /dev/null` — MEM-04: create-experiment dry-run succeeds (requires stdin reading with empty input — guard should handle missing fields gracefully)
       - Check 16: `zsh standup-brief.sh --repo anujj-ti/agentic-setup | jq -e '.autonomous_decisions.count >= 0'` — standup brief includes decision count

    5. Summary output:
       ```
       Phase 9 Verification: PASS_COUNT passed, FAIL_COUNT failed
       ```
       If FAIL_COUNT > 0: exit 1. Otherwise exit 0.

    chmod +x `scripts/verify-phase-09.sh`.

    Run `zsh scripts/verify-phase-09.sh --smoke` immediately after creation to confirm all smoke checks pass.
  </action>
  <verify>
    <automated>zsh /Users/trilogy/Documents/agentic-setup/scripts/verify-phase-09.sh --smoke && echo "Smoke checks: PASS" || echo "Smoke checks: FAIL"</automated>
  </verify>
  <done>verify-phase-09.sh exists and is executable; --smoke mode runs 12 checks covering all 6 notion/ script existence checks, syntax validity, TODO_NOTION guard behavior, SOUL.md pre-log protocol, standup brief wiring, and config.json structure; all 12 smoke checks pass</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| verify-phase-09.sh → node scripts | Verification runs scripts locally as the same user — no privilege escalation |
| standup-brief.sh → query-decisions.sh | Standup brief calls Notion read-only query — no write operations in the standup flow |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-17 | Information Disclosure | standup-brief.sh Notion decision data in Telegram | accept | Decision summaries are sent to Anuj via Telegram — he is the intended recipient; no third parties |
| T-09-18 | Denial of Service | standup-brief.sh failure when Notion unavailable | mitigate | query-decisions call wrapped in `|| echo '{"ok":false}'` — standup brief always completes with count=0 and notion_configured=false when Notion is down |
| T-09-19 | Tampering | verify-phase-09.sh --smoke passes but integration fails | accept | Smoke mode is explicitly documented as pre-token validation only; full integration tests require user to complete the human checkpoint in Plan 09-01 first |
</threat_model>

<verification>
- `zsh scripts/verify-phase-09.sh --smoke` passes all 12 checks
- `zsh scripts/standup-brief.sh --repo anujj-ti/agentic-setup 2>/dev/null | jq '.autonomous_decisions'` returns an object with count, since, summary, notion_configured fields
- standup-brief.sh completes without error even when OPENCLAW_NOTION_TOKEN is empty
</verification>

<success_criteria>
Phase 9 complete when:
- All 6 notion/ scripts (log-decision.js, update-decision.js, query-decisions.js, revert-decision.js, create-experiment.js, append-experiment-results.js) exist and pass node --check
- All 6 shell wrappers (.sh) exist and are executable
- verify-phase-09.sh --smoke passes all 12 smoke checks
- standup-brief.sh autonomous_decisions section present and works even when token is absent
- Task Orchestrator SOUL.md has "Notion Pre-Log Protocol (MANDATORY)" section
- User Orchestrator SOUL.md has "Decision Retrieval Protocol" section
- config.json has NOTION_DECISIONS_DB_ID and NOTION_EXPERIMENTS_PAGE_ID fields
- Human checkpoint in Plan 09-01 is documented and ready for user to complete on return
- Full integration verification (verify-phase-09.sh without --smoke) available after human checkpoint completes
</success_criteria>

<output>
Create `.planning/phases/09-notion-decision-log/09-06-SUMMARY.md` when done
</output>

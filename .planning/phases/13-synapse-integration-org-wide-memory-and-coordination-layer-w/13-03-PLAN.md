---
phase: 13-synapse-integration
plan: 03
type: execute
wave: 2
depends_on: [13-01, 13-02]
files_modified:
  - scripts/verify-phase-13.sh
  - CLAUDE.md
autonomous: true
requirements: [SYN-03]
must_haves:
  truths:
    - "A single script verifies the full Phase 13 integration in under 30 seconds"
    - "CLAUDE.md documents how to create project.agentic-setup in the Synapse dashboard"
    - "Any missing piece produces a named FAIL line — not a silent pass"
  artifacts:
    - path: "scripts/verify-phase-13.sh"
      provides: "Phase 13 smoke test (10 checks)"
      contains: "SYNAPSE_TOKEN"
    - path: "CLAUDE.md"
      provides: "Synapse project setup runbook for operators"
      contains: "project.agentic-setup"
  key_links:
    - from: "scripts/verify-phase-13.sh"
      to: ".openclaw/agents/*/TOOLS.md"
      via: "grep loop across all 10 agent dirs"
      pattern: "Synapse.*Mandatory"
---

<objective>
Write the phase verification script and document the project.agentic-setup creation steps in CLAUDE.md. The verification script is the exit gate for the whole phase.

Purpose: Make phase completion objectively measurable; give the operator a one-liner to confirm the integration is healthy at any time.
Output: scripts/verify-phase-13.sh (executable, 10 checks) + CLAUDE.md Synapse project setup subsection.
</objective>

<execution_context>
@/Users/trilogy/Documents/agentic-setup/.claude/get-shit-done/workflows/execute-plan.md
@/Users/trilogy/Documents/agentic-setup/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/trilogy/Documents/agentic-setup/.planning/ROADMAP.md
@/Users/trilogy/Documents/agentic-setup/.planning/phases/13-synapse-integration-org-wide-memory-and-coordination-layer-w/13-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create scripts/verify-phase-13.sh</name>
  <files>scripts/verify-phase-13.sh</files>
  <action>
Create scripts/verify-phase-13.sh with #!/usr/bin/env zsh and set -euo pipefail.

Track PASS/FAIL counts. Each check prints "CHECK N: PASS — <description>" or "CHECK N: FAIL — <description>". At end: print summary and exit 1 if any FAILs, exit 0 if all pass.

The 10 checks (in order):

1. KEYCHAIN: `security find-generic-password -s 'openclaw.synapse-token' -a 'trilogy' -w 2>/dev/null | grep -q .` — SYNAPSE_TOKEN exists in Keychain

2. SECRETS_SH: `grep -q 'SYNAPSE_TOKEN' ~/Documents/agentic-setup/openclaw-secrets.sh` — SYNAPSE_TOKEN in openclaw-secrets.sh

3. ENV_SH: `grep -q 'SYNAPSE_TOKEN' ~/Documents/agentic-setup/openclaw-env.sh` — SYNAPSE_TOKEN in openclaw-env.sh

4. SYNAPSE_URL_SECRETS: `grep -q 'SYNAPSE_URL' ~/Documents/agentic-setup/openclaw-secrets.sh` — SYNAPSE_URL in openclaw-secrets.sh

5. SYNAPSE_URL_ENV: `grep -q 'SYNAPSE_URL' ~/Documents/agentic-setup/openclaw-env.sh` — SYNAPSE_URL in openclaw-env.sh

6. CHECKIN_SCRIPT: `test -x ~/Documents/agentic-setup/scripts/synapse-checkin.sh` — synapse-checkin.sh exists and is executable

7. LEARNING_SCRIPT: `test -x ~/Documents/agentic-setup/scripts/synapse-record-learning.sh` — synapse-record-learning.sh exists and is executable

8. EXECUTION_AGENTS (loop): for each of the 8 execution-tier agents (devbot ci-monitor email-triage code-reviewer document-reviewer decision-reviewer skill-reviewer skill-creation), check that their TOOLS.md contains "Synapse (Mandatory)". A single FAIL if ANY agent is missing the section. Print the agent name in the FAIL message.

9. TASK_ORCH: `grep -q 'Synapse' ~/.openclaw/agents/task-orchestrator/AGENTS.md` — Task Orchestrator AGENTS.md references Synapse

10. USER_ORCH: `grep -q 'brief.fetch' ~/.openclaw/agents/user-orchestrator/AGENTS.md` — User Orchestrator AGENTS.md has brief.fetch

Make executable: chmod +x scripts/verify-phase-13.sh.
  </action>
  <verify>
    <automated>bash -c 'cd /Users/trilogy/Documents/agentic-setup && zsh -n scripts/verify-phase-13.sh && test -x scripts/verify-phase-13.sh && grep -q "SYNAPSE_TOKEN" scripts/verify-phase-13.sh && echo "PASS"'</automated>
  </verify>
  <done>verify-phase-13.sh exists, is executable, passes zsh -n syntax check, contains all 10 check labels (KEYCHAIN through USER_ORCH).</done>
</task>

<task type="auto">
  <name>Task 2: Add Synapse Project Setup section to CLAUDE.md</name>
  <files>CLAUDE.md</files>
  <action>
Read CLAUDE.md. Find the existing Synapse section (search for "Synapse" heading). If a "## Synapse Project Setup" subsection already exists, skip this task.

If absent, append the following subsection under the existing Synapse content (or at end of CLAUDE.md if no Synapse section found):

---
## Synapse Project Setup

### Creating project.agentic-setup in the Synapse dashboard

The project ID `project.agentic-setup` is used by all agents in this hub. If this project does not exist yet, create it once:

1. Visit https://cnu.synapse-os.ai and log in with your Synapse credentials
2. Navigate to Projects → New Project
3. Set Project ID to exactly: `project.agentic-setup`
4. Set Team to: `team.edullm` (or your org team ID)
5. Set Display Name to: `Personal AI Operations Hub`
6. Save. Copy the project ID shown — it must match `project.agentic-setup` exactly.

Once created, all agent curl calls using `"project_id": "project.agentic-setup"` will route correctly.

### Consistent project_id usage

All execution-tier agents (devbot, ci-monitor, email-triage, code-reviewer, document-reviewer, decision-reviewer, skill-reviewer, skill-creation) use `project.agentic-setup`.

Note: task-orchestrator AGENTS.md currently uses `project.edullm-sat-math` (pre-existing; not changed here). This will be unified in a future phase.

### Verifying Synapse integration

```zsh
bash ~/Documents/agentic-setup/scripts/verify-phase-13.sh
```

All 10 checks must pass. If CHECK 1 (KEYCHAIN) fails, re-run:
```zsh
security add-generic-password -s 'openclaw.synapse-token' -a 'trilogy' -w '<token>'
```
---
  </action>
  <verify>
    <automated>bash -c 'grep -q "project.agentic-setup" /Users/trilogy/Documents/agentic-setup/CLAUDE.md && grep -q "verify-phase-13.sh" /Users/trilogy/Documents/agentic-setup/CLAUDE.md && echo "PASS"'</automated>
  </verify>
  <done>CLAUDE.md contains project.agentic-setup creation steps and verify-phase-13.sh reference. Operator knows exactly where to go to create the project and how to confirm integration health.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| verify script→Keychain | security CLI reads password; output not printed, only grep-tested for non-empty |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-06 | Information Disclosure | verify script printing token | mitigate | Use `grep -q .` on token read — existence check only, value never echoed |
| T-13-07 | Spoofing | CLAUDE.md dashboard URL | accept | URL is the known Synapse base URL already in SKILL.md |
| T-13-SC | Tampering | npm/pip/cargo installs | accept | No package installs; shell script and markdown edits only |
</threat_model>

<verification>
`bash scripts/verify-phase-13.sh` — all 10 checks pass once Plans 13-01 and 13-02 are complete.
</verification>

<success_criteria>
verify-phase-13.sh exits 0 with 10/10 checks passing. CLAUDE.md documents project.agentic-setup creation with exact dashboard steps and verify command.
</success_criteria>

<output>
Create `.planning/phases/13-synapse-integration-org-wide-memory-and-coordination-layer-w/13-03-SUMMARY.md` when done.
</output>

---
phase: 13-synapse-integration
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - .openclaw/agents/devbot/TOOLS.md
  - .openclaw/agents/ci-monitor/TOOLS.md
  - .openclaw/agents/email-triage/TOOLS.md
  - .openclaw/agents/code-reviewer/TOOLS.md
  - .openclaw/agents/document-reviewer/TOOLS.md
  - .openclaw/agents/decision-reviewer/TOOLS.md
  - .openclaw/agents/skill-reviewer/TOOLS.md
  - .openclaw/agents/skill-creation/TOOLS.md
autonomous: true
requirements: [SYN-02]
must_haves:
  truths:
    - "All 8 execution-tier agents have a Synapse section in their TOOLS.md"
    - "Each section shows the 3 core commands: brief.fetch, workflow.create, checkin"
    - "Project ID is project.agentic-setup in all 8 sections"
    - "Sections reference the synapse-checkin.sh and synapse-record-learning.sh scripts"
  artifacts:
    - path: ".openclaw/agents/devbot/TOOLS.md"
      provides: "DevBot tool reference with Synapse section"
      contains: "Synapse (Mandatory)"
    - path: ".openclaw/agents/ci-monitor/TOOLS.md"
      provides: "CI Monitor tool reference with Synapse section"
      contains: "Synapse (Mandatory)"
    - path: ".openclaw/agents/email-triage/TOOLS.md"
      provides: "Email Triage tool reference with Synapse section"
      contains: "Synapse (Mandatory)"
    - path: ".openclaw/agents/code-reviewer/TOOLS.md"
      provides: "Code Reviewer tool reference with Synapse section"
      contains: "Synapse (Mandatory)"
    - path: ".openclaw/agents/document-reviewer/TOOLS.md"
      provides: "Document Reviewer tool reference with Synapse section"
      contains: "Synapse (Mandatory)"
    - path: ".openclaw/agents/decision-reviewer/TOOLS.md"
      provides: "Decision Reviewer tool reference with Synapse section"
      contains: "Synapse (Mandatory)"
    - path: ".openclaw/agents/skill-reviewer/TOOLS.md"
      provides: "Skill Reviewer tool reference with Synapse section"
      contains: "Synapse (Mandatory)"
    - path: ".openclaw/agents/skill-creation/TOOLS.md"
      provides: "Skill Creation tool reference with Synapse section"
      contains: "Synapse (Mandatory)"
  key_links:
    - from: "agent TOOLS.md Synapse section"
      to: "scripts/synapse-checkin.sh"
      via: "exec tool invocation pattern"
      pattern: "synapse-checkin\\.sh"
---

<objective>
Append a "## Synapse (Mandatory)" section to the TOOLS.md of every execution-tier agent. This makes Synapse a first-class tool reference — agents boot with the protocol visible, not buried in SKILL.md they may not read.

Purpose: After this plan, grepping any agent TOOLS.md for "Synapse" confirms the integration is wired.
Output: 8 TOOLS.md files each with an identical Synapse section at the end.
</objective>

<execution_context>
@/Users/trilogy/Documents/agentic-setup/.claude/get-shit-done/workflows/execute-plan.md
@/Users/trilogy/Documents/agentic-setup/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/trilogy/Documents/agentic-setup/.planning/ROADMAP.md
@/Users/trilogy/Documents/agentic-setup/.planning/phases/13-synapse-integration-org-wide-memory-and-coordination-layer-w/13-CONTEXT.md
@/Users/trilogy/Documents/agentic-setup/.openclaw/agents/task-orchestrator/TOOLS.md

<interfaces>
The Synapse section to append is identical across all 8 agents (project.agentic-setup per D-135).
Exact section template to append to each TOOLS.md:

---
## Synapse (Mandatory)

Every non-trivial task (file changes, API calls, artifact creation) must use the full loop.
Env vars injected by launchd via openclaw-secrets.sh: $SYNAPSE_TOKEN, $SYNAPSE_URL.

### Step 0 — Fetch briefs (ALWAYS first)
```zsh
/usr/bin/curl -sS -X POST "$SYNAPSE_URL/v1/intent/synapse.brief.fetch" \
  -H "Authorization: Bearer $SYNAPSE_TOKEN" \
  -H "Content-Type: application/json" \
  -d '{"project_id": "project.agentic-setup", "include_acked": false}'
```
Ack each brief: POST synapse.brief.ack with the brief_id.

### Step 1 — Open a workflow
```zsh
WF=$(/usr/bin/curl -sS -X POST "$SYNAPSE_URL/v1/intent/synapse.workflow.create" \
  -H "Authorization: Bearer $SYNAPSE_TOKEN" \
  -H "Content-Type: application/json" \
  -d '{"project_id": "project.agentic-setup", "workflow_class": "investigation", "title": "<task description>"}')
BD_ID=$(echo "$WF" | python3 -c "import sys,json; print(json.load(sys.stdin)['data']['bd_id'])")
```

### Step 2 — Check in (use the shared script)
```zsh
# Arguments: <project_id> <bd_id> <status> <current_task>
bash ~/Documents/agentic-setup/scripts/synapse-checkin.sh \
  project.agentic-setup "$BD_ID" progress "what I just did"
```
Status values: start | progress | blocked | complete | failed

### Step 3 — Record learnings (use the shared script)
```zsh
# Arguments: <project_id> <bd_id> <claim> <applies_to_tags_csv>
bash ~/Documents/agentic-setup/scripts/synapse-record-learning.sh \
  project.agentic-setup "$BD_ID" \
  "non-obvious reusable insight" \
  "openclaw,<domain-tag>"
```

### Step 4 — Close the workflow
```zsh
bash ~/Documents/agentic-setup/scripts/synapse-checkin.sh \
  project.agentic-setup "$BD_ID" complete "task completed: <outcome summary>"
```

Full protocol: ~/.claude/skills/synapse/SKILL.md
---
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Append Synapse section to devbot, ci-monitor, email-triage, code-reviewer TOOLS.md</name>
  <files>.openclaw/agents/devbot/TOOLS.md, .openclaw/agents/ci-monitor/TOOLS.md, .openclaw/agents/email-triage/TOOLS.md, .openclaw/agents/code-reviewer/TOOLS.md</files>
  <action>
Read each of the four TOOLS.md files. Verify no Synapse section already exists (grep for "Synapse (Mandatory)"). Then append the exact Synapse section from the interfaces block above to the end of each file.

The section to append is the same for all four agents — project_id is always project.agentic-setup.

Do not modify any existing content in these files. Append only.

Files to modify:
- .openclaw/agents/devbot/TOOLS.md
- .openclaw/agents/ci-monitor/TOOLS.md
- .openclaw/agents/email-triage/TOOLS.md
- .openclaw/agents/code-reviewer/TOOLS.md
  </action>
  <verify>
    <automated>bash -c 'for f in devbot ci-monitor email-triage code-reviewer; do grep -q "Synapse (Mandatory)" /Users/trilogy/Documents/agentic-setup/.openclaw/agents/$f/TOOLS.md || { echo "MISSING: $f"; exit 1; }; done && echo "PASS: all 4 have Synapse section"'</automated>
  </verify>
  <done>All 4 TOOLS.md files contain "## Synapse (Mandatory)" section with project.agentic-setup and references to synapse-checkin.sh and synapse-record-learning.sh.</done>
</task>

<task type="auto">
  <name>Task 2: Append Synapse section to document-reviewer, decision-reviewer, skill-reviewer, skill-creation TOOLS.md</name>
  <files>.openclaw/agents/document-reviewer/TOOLS.md, .openclaw/agents/decision-reviewer/TOOLS.md, .openclaw/agents/skill-reviewer/TOOLS.md, .openclaw/agents/skill-creation/TOOLS.md</files>
  <action>
Read each of the four remaining TOOLS.md files. Verify no Synapse section already exists. Then append the exact same Synapse section (project.agentic-setup) to the end of each file.

Files to modify:
- .openclaw/agents/document-reviewer/TOOLS.md
- .openclaw/agents/decision-reviewer/TOOLS.md
- .openclaw/agents/skill-reviewer/TOOLS.md
- .openclaw/agents/skill-creation/TOOLS.md
  </action>
  <verify>
    <automated>bash -c 'for f in document-reviewer decision-reviewer skill-reviewer skill-creation; do grep -q "Synapse (Mandatory)" /Users/trilogy/Documents/agentic-setup/.openclaw/agents/$f/TOOLS.md || { echo "MISSING: $f"; exit 1; }; done && echo "PASS: all 4 have Synapse section"'</automated>
  </verify>
  <done>All 4 TOOLS.md files contain "## Synapse (Mandatory)" section. Combined with Task 1, all 8 execution-tier agents now have Synapse wired.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| TOOLS.md→agent runtime | TOOLS.md is an instruction document; appending only — no secrets embedded |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-04 | Information Disclosure | TOOLS.md curl examples | accept | Examples show env var refs ($SYNAPSE_TOKEN) only — no actual token values; env vars sourced at runtime |
| T-13-05 | Tampering | TOOLS.md file integrity | accept | TOOLS.md files are config/instruction docs in git; any modification is visible in git diff |
| T-13-SC | Tampering | npm/pip/cargo installs | accept | No package installs; text file appends only |
</threat_model>

<verification>
Run the two automated verify commands (Task 1 + Task 2) then:
- FULL CHECK: `for f in devbot ci-monitor email-triage code-reviewer document-reviewer decision-reviewer skill-reviewer skill-creation; do grep -c "project.agentic-setup" ~/.openclaw/agents/$f/TOOLS.md; done` — each should return >= 1
- GREP: `grep -l "Synapse (Mandatory)" ~/.openclaw/agents/*/TOOLS.md | wc -l` — should return 8
</verification>

<success_criteria>
All 8 execution-tier agent TOOLS.md files contain a "## Synapse (Mandatory)" section referencing project.agentic-setup, synapse-checkin.sh, and synapse-record-learning.sh. No existing TOOLS.md content was altered.
</success_criteria>

<output>
Create `.planning/phases/13-synapse-integration-org-wide-memory-and-coordination-layer-w/13-02-SUMMARY.md` when done.
</output>

---
phase: 13-synapse-integration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/synapse-checkin.sh
  - scripts/synapse-record-learning.sh
autonomous: true
requirements: [SYN-01]
must_haves:
  truths:
    - "Any agent can check in to Synapse with a single shell call"
    - "Any agent can record a low-confidence learning with a single shell call"
    - "Missing SYNAPSE_TOKEN causes a warning to stderr and exit 0 — never breaks agent execution"
  artifacts:
    - path: "scripts/synapse-checkin.sh"
      provides: "Synapse workflow check-in wrapper"
      contains: "TODO_SYNAPSE guard"
    - path: "scripts/synapse-record-learning.sh"
      provides: "Synapse low-confidence learning recorder"
      contains: "TODO_SYNAPSE guard"
  key_links:
    - from: "scripts/synapse-checkin.sh"
      to: "https://cnu.synapse-os.ai/v1/intent/synapse.checkin"
      via: "/usr/bin/curl POST"
      pattern: "synapse\\.checkin"
    - from: "scripts/synapse-record-learning.sh"
      to: "https://cnu.synapse-os.ai/v1/intent/synapse.learning.record"
      via: "/usr/bin/curl POST"
      pattern: "synapse\\.learning\\.record"
---

<objective>
Create two deterministic shell scripts that wrap the most common Synapse API calls — check-in and low-confidence learning recording — so every execution-tier agent can call Synapse in one line without copy-pasting curl.

Purpose: Eliminate boilerplate and ensure all agents use identical Synapse invocation patterns with correct error handling.
Output: scripts/synapse-checkin.sh and scripts/synapse-record-learning.sh — both executable, both exit-0 safe when token absent.
</objective>

<execution_context>
@/Users/trilogy/Documents/agentic-setup/.claude/get-shit-done/workflows/execute-plan.md
@/Users/trilogy/Documents/agentic-setup/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/trilogy/Documents/agentic-setup/.planning/PROJECT.md
@/Users/trilogy/Documents/agentic-setup/.planning/ROADMAP.md
@/Users/trilogy/Documents/agentic-setup/.planning/phases/13-synapse-integration-org-wide-memory-and-coordination-layer-w/13-CONTEXT.md

<interfaces>
From .claude/skills/synapse/SKILL.md — Synapse check-in endpoint:
```
POST $SYNAPSE_URL/v1/intent/synapse.checkin
Body: { "bd_id": "<id>", "status": "start|progress|blocked|complete|failed",
        "current_task": "<description>", "target_objective_id": "<okr_id>" }
```

From .claude/skills/synapse/SKILL.md — learning record endpoint:
```
POST $SYNAPSE_URL/v1/intent/synapse.learning.record
Body: { "project_id": "<id>", "bd_id": "<id>",
        "learnings": [{ "claim": "<text>", "applies_to": ["tag1"],
                        "confidence": "low" }] }
```

From scripts/lib/json-response.sh pattern (established):
- stdout = JSON only: { "ok": true, "data": {...} } or { "ok": false, "error": "..." }
- stderr = human-readable logs
- shebang: #!/usr/bin/env zsh
- strict mode: set -euo pipefail
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create synapse-checkin.sh</name>
  <files>scripts/synapse-checkin.sh</files>
  <action>
Create scripts/synapse-checkin.sh with shebang #!/usr/bin/env zsh and set -euo pipefail.

Positional arguments (per D-131): $1=project_id, $2=bd_id, $3=status, $4=current_task. If fewer than 4 args, print usage to stderr and exit 1.

TODO_SYNAPSE guard (per D-133): check if SYNAPSE_TOKEN is set and non-empty. If absent, print to stderr: "synapse-checkin: SYNAPSE_TOKEN not set — skipping (set openclaw.synapse-token in Keychain)" and exit 0.

Use SYNAPSE_URL defaulting to https://cnu.synapse-os.ai if unset: SYNAPSE_URL="${SYNAPSE_URL:-https://cnu.synapse-os.ai}".

Call /usr/bin/curl (explicit path) with -sS -X POST, Content-Type application/json, Authorization Bearer, to "$SYNAPSE_URL/v1/intent/synapse.checkin".

JSON body includes: bd_id, status, current_task. Do NOT include target_objective_id — it is optional and agents often lack an OKR ID at call time.

Capture HTTP response. If curl exits non-zero, print error to stderr and output {"ok":false,"error":"curl failed"} to stdout; exit 0 (never fail agent execution over Synapse).

On success, output {"ok":true,"data":<response_body>} to stdout.

Make executable: chmod +x scripts/synapse-checkin.sh.
  </action>
  <verify>
    <automated>bash -c 'cd /Users/trilogy/Documents/agentic-setup && zsh -n scripts/synapse-checkin.sh && grep -q "TODO_SYNAPSE\|SYNAPSE_TOKEN" scripts/synapse-checkin.sh && grep -q "/usr/bin/curl" scripts/synapse-checkin.sh && test -x scripts/synapse-checkin.sh && echo "PASS"'</automated>
  </verify>
  <done>synapse-checkin.sh exists, is executable, passes zsh -n syntax check, contains SYNAPSE_TOKEN guard and /usr/bin/curl, exits 0 when token absent.</done>
</task>

<task type="auto">
  <name>Task 2: Create synapse-record-learning.sh</name>
  <files>scripts/synapse-record-learning.sh</files>
  <action>
Create scripts/synapse-record-learning.sh with shebang #!/usr/bin/env zsh and set -euo pipefail.

Positional arguments: $1=project_id, $2=bd_id, $3=claim (the learning text), $4=applies_to_tags (comma-separated, e.g. "openclaw,agent-scaffolding"). If fewer than 4 args, print usage to stderr and exit 1.

TODO_SYNAPSE guard (per D-133): same pattern as synapse-checkin.sh — if SYNAPSE_TOKEN absent, warn to stderr and exit 0.

SYNAPSE_URL defaulting same as Task 1.

Convert the comma-separated applies_to_tags string into a JSON array. Use python3 to do this safely:
  TAGS_JSON=$(python3 -c "import sys,json; tags=sys.argv[1].split(','); print(json.dumps([t.strip() for t in tags]))" "$4")

Confidence is always "low" (per D-132) — no evidence_artifact_id required, no risk of Synapse trust-score penalty.

Call /usr/bin/curl to POST to "$SYNAPSE_URL/v1/intent/synapse.learning.record" with JSON body:
  {"project_id":"$1","bd_id":"$2","learnings":[{"claim":"$3","applies_to":$TAGS_JSON,"confidence":"low"}]}

Build the JSON body using printf or python3 to handle the claim string safely (avoid shell quoting issues with single quotes in claim text). Use python3 -c with json.dumps for the full body construction:
  BODY=$(python3 -c "import sys,json; p,b,c,t=sys.argv[1:5]; tags=t.split(','); body={'project_id':p,'bd_id':b,'learnings':[{'claim':c,'applies_to':[x.strip() for x in tags],'confidence':'low'}]}; print(json.dumps(body))" "$1" "$2" "$3" "$4")

Same error handling as Task 1 — curl failures go to stderr, stdout is always valid JSON, exit 0.

Make executable: chmod +x scripts/synapse-record-learning.sh.
  </action>
  <verify>
    <automated>bash -c 'cd /Users/trilogy/Documents/agentic-setup && zsh -n scripts/synapse-record-learning.sh && grep -q "TODO_SYNAPSE\|SYNAPSE_TOKEN" scripts/synapse-record-learning.sh && grep -q "/usr/bin/curl" scripts/synapse-record-learning.sh && grep -q "confidence.*low" scripts/synapse-record-learning.sh && test -x scripts/synapse-record-learning.sh && echo "PASS"'</automated>
  </verify>
  <done>synapse-record-learning.sh exists, executable, passes zsh -n, contains token guard, /usr/bin/curl, hardcoded low confidence, and python3 JSON body construction.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| script→Synapse API | Bearer token sent over HTTPS; token sourced from env var, never from args or files |
| agent→script | Scripts called with positional args; claim text passed to python3 json.dumps, not interpolated into curl |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-01 | Information Disclosure | SYNAPSE_TOKEN in shell env | accept | Token is in launchd env via openclaw-secrets.sh (existing pattern); never written to stdout or files |
| T-13-02 | Tampering | learning claim text injection | mitigate | python3 json.dumps for full body construction — shell quoting bypassed |
| T-13-03 | Denial of Service | Synapse unavailable blocks agent | mitigate | exit 0 on all curl failures; Synapse absence never kills agent execution (D-133) |
| T-13-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this plan; uses only system python3 and /usr/bin/curl |
</threat_model>

<verification>
Run both automated verify commands above. Additionally:
- UNSET_TEST: run `env -u SYNAPSE_TOKEN zsh scripts/synapse-checkin.sh project.agentic-setup fake-bd-id progress "test"` — must exit 0 with warning to stderr
- SYNTAX: `zsh -n scripts/synapse-checkin.sh && zsh -n scripts/synapse-record-learning.sh`
</verification>

<success_criteria>
Both scripts exist in scripts/, are executable, pass zsh -n, guard against missing SYNAPSE_TOKEN with exit 0, use /usr/bin/curl with explicit path, output valid JSON to stdout, and send all human-readable output to stderr.
</success_criteria>

<output>
Create `.planning/phases/13-synapse-integration-org-wide-memory-and-coordination-layer-w/13-01-SUMMARY.md` when done.
</output>
